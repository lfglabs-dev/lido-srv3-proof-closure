# ADDRESS request composition with corrected physical slot constants

This is the repaired integration of source `a3ae099cdaa185245a19a16121f9fd025e0fd806` with main `10e2b693d00cbbeab65f60cd8964ced87297336a`, union `687fd842ad05a1f64a7a9d927dfcf495e3522429`. The source writer stopped before independent review and explicitly transferred repair ownership to the integrator. Its three new Lean bodies are unchanged. The canonical AddressRecipientCallBridge now corrects exactly three inherited slot literals. There are no local shadow definitions, new execution premises or altered public domains. AllGuarantees/Trust now import the public request composition and regression tests.

## Rejection and repair

The original candidate FAILED independent Solidity correspondence: lastRequestIdPosition and lastReportTimestampPosition did not equal the pinned Solidity Keccak preimages, although its standalone model proofs and separate Solidity tests passed. The full family inventory found a third incorrect tokenApprovalsPosition in the older transfer path. The blocked source review and complete inventory are retained verbatim here. The parent dossier, its old source-inputs.json and its 44 artifact hashes are preserved historical evidence of that rejected candidate; they are not a current correspondence certificate. This repair's source-inputs.json supersedes the old dependency manifest. No CLEAN source verdict is retroactively assigned to a3ae.

check-slots.py independently compares all ten inherited physical bases with the exact pinned Git source preimages using cast keccak. It checks all four selectors with cast sig and finds the four new request bases in the actual RequestHarness assembly. slot-check.json records the exact corrected values, source paths and Lean definitions. The repair changes only the three canonical literals. Fixing tokenApprovals does not claim a newly reviewed complete NFT transfer theorem. The retained actual loaded dependency closure shows the six currently published ADDRESS307 claims consume none of the three faulty constants; this finding does not credit those claims with request enqueue behavior.

## Consumed public claim

actual_request_withdrawal_enqueue executes one admitted stETH request: amount guards and owner fallback, actual transferFrom CALL and returned World, actual getSharesByPooledEth STATICCALL on that World, decoded shares truncated to uint128, then physical enqueue on that same returned state. The successful bool is decoded but its false value is deliberately accepted by the pinned caller. Noncanonical bool and short return bytes fail. Shares cumulative arithmetic precedes stETH cumulative arithmetic and last-ID increment. Entry TIMESTAMP is captured before external calls; the report timestamp is loaded after the ID write. Packed owner/timestamps/claimed fields and retained high metadata byte follow source order. The legacy OpenZeppelin add reads the length again after the array write and allows uint256 wrapping; no incorrect modern array allocation guard or nonaliasing assumption is added. WithdrawalRequested then Transfer are the observable semantic events. Public failure restores the full pre-request World while preserving attempted-call diagnostics.

Theorem execution begins at the admitted internal request. Pause/outer arrays/batch dispatch and full error/LOG byte encodings are outside this increment. The configured token and arbitrary callees do not imply real token-balance or allowance conservation or callback framing. Keccak is the accepted primitive; correct preimages, literals and consumed locations are separately checked here. No general compiler, Verity, memory, gas or consensus boundary is broadened to excuse a Lido-specific mismatch.

## Verification and reuse

Fresh repaired normal AllGuarantees/Trust passes 1,715 jobs; fresh global axiom checking recomputes dependencies and re-evaluates native claims, yielding the same 29 disclosed axioms, not globally foundations-only. A fresh query on the rebuilt test environment yields seven foundation-only scopes. The repaired actual import closure checks 1,245 source identities and 11 package pins. Every consumed local body matches a3ae except exactly the three corrected literals; selected local olean paths are checked against the active build. receipt.json records the four normal rebuilt olean hashes without claiming byte identity across worktrees.

Fresh FFI diagnostics execute the original eight success cases plus four independent literal tests. A single state contains both correct Solidity slots and obsolete wrong-slot decoys with different ID/timestamp values. The actual request reads the correct values, writes the correct last-ID slot and leaves the old wrong slot untouched. These twelve diagnostics use the official EvmYul FFI loader and retained native runtime source identities; they are executable cross-checks, not kernel proofs. The original thirteen kernel regressions/public rollback instance remain imported by the normal build.

Twelve unchanged author-run Solidity tests, including 1,024 fuzz cases, and their exact full WithdrawalQueue fixture are reused after the original 44 artifact hashes were rechecked and independent review examined the complete relevant source/assembly. The three Lean slot fixes do not change any Solidity fixture or compiler input. Seven core and nine original OpenZeppelin dependencies plus the harness constitute 17 actual inputs. The retained solc0.8.9 optimizer200 fixture targets London, not the production Istanbul profile. RequestHarness.asm and CompilerProbe.ir preserve raw stdout EOF blank lines as two explicit diff-check exceptions. The separate experimental compiler probe does not replace the legacy production source. The full fixture's IR-generation failure is disclosed; no nonexistent successful full IR receipt is implied.

A separate independent review of this exact repaired commit is required before integration. No website change or external publication occurs in this packet.
