# Independent repaired ADDRESS integration review

Verdict: **Source/proof repair CLEAN; one documentation correction remains before final integration acceptance.** Exact frozen `f9f5639bdcc30a5f653fe679c9caba306a84df19`, tree `fccbae4d673892be3055aa3f90b2748396d61df5`, checkout `/tmp/lido-address-request-integrated`. Root writer explicitly stopped; checkout remained clean and unchanged. This reviewer authored no repair or integration source.

The two physical-slot blockers from rejected candidate a3ae are fixed, and the third historical token-approval literal is corrected coherently in the canonical bridge. There are no shadow constants, new premises or narrowed public domains. No additional source, proof, compiler-order, storage, ABI-boundary or rollback defect was found. The remaining issue is precisely the README's description of the earlier dependency probe, below.

## Required documentation correction

`audit/address-request-call/repair/README.md` says that the retained actual loaded dependency closure shows that the six currently published ADDRESS307 claims consume none of the three faulty constants. The retained query has six roots, but these are **three published live claim theorems, the new request theorem and two historical executors**. The other three published supplementary helpers were checked by reading their separate Std-only module, not by that loaded query. The no-dependency conclusion for all six published statements is correct; its evidence attribution must say three live theorems via loaded closure and three supplementary helpers via the independent Std-only source inspection. Root accepted this correction and will prepare a successor after this reviewer stops. No candidate file was edited during review.

## Exact preservation and physical repair

Complete recursive tree comparisons verify union `687fd842ad05a1f64a7a9d927dfcf495e3522429` combines rejected source `a3ae099cdaa185245a19a16121f9fd025e0fd806` with accepted main `10e2b693d00cbbeab65f60cd8964ced87297336a`. Every main object is preserved outside the canonical bridge and two public wiring files. All three new Lean sources and all 45 candidate additions are unchanged. The repaired commit changes exactly three existing files and adds 17 repair-dossier files. Relative to the original bridge, exactly three hexadecimal literals change, with every other byte preserved.

The corrected canonical values are:

- lastRequestIdPosition: `0x8ee26abbbdf533de3953ccf2204279e845eecb5ab51f8398522746e4ea068041`.
- lastReportTimestampPosition: `0x6825d6bead7081b4d1ac062bbb771f0e4ade13182688453e79955a721d58c4dd`.
- tokenApprovalsPosition: `0x528f2b9d45274be04589d1a9e644321ee1435a867d38a1359d022af391cf7d75`.

I read the complete repair scripts, diagnostics and dossier, compared the full bridge change, and reused the previous complete source/Solidity and consumer-family review only after exact body identity. The independent read-only slot checker reproduces all ten pinned source preimages and four selectors. The four request bases occur in the retained actual RequestHarness legacy assembly; they agree with the earlier independent Keccak and assembly checks. Mapping keys/base words and offset arithmetic retain uint256 normalization; owner-index hashing adds one to the set base before hashing, and dynamic-array element pointers normalize keccak(base)+index. No injectivity or nonwrap premise is introduced.

The actual new public request now consumes the corrected last-ID and report-time positions throughout execution and its effects relation. Actual transferFrom arguments contain the admitted original amount and fixed sender/queue; its successful returned world feeds a distinct static shares quote and then the physical enqueue. False canonical bool is accepted, noncanonical/short returns fail, shares truncate to uint128, and amount fit follows from admission. Checked shares addition precedes stETH addition and request-ID increment. TIMESTAMP remains captured from entry independently of arbitrary returned metadata. Report timestamp is read after the last-ID write; packed cumulative amounts precede the metadata update, which preserves the unused high byte and clears claimed. The legacy owner-set insertion retains length-before-value-before-fresh-length/index order and wrapping maximum length. WithdrawalRequested precedes Transfer. Whole-program failure restores the incoming modeled world while keeping attempted-call observations separate.

The correction also changes the historical request bridge's physical ID/report accesses and historical transferFrom's ID/approval accesses. Those historical paths still have their previously documented incomplete fidelity and do not acquire a new full transfer or request theorem through this fix. The three published live307 claim/chain/rollback consumers use none of these constants, as the retained actual dependency traversal and unchanged source show. The published supplementary ID/packed helpers remain independent Std-only results. Existing accepted claims are preserved; P-ADDRESS-1 remains open.

## Registered consumers and checks

AllGuarantees imports PAddress1RequestCalls. Trust imports the actual AddressRequestCalls regression module and adds four queries covering both public results, enqueue_success and the public late-rollback instance. Existing automatic Source/Guarantees library discovery suffices; no new Lake provider or dependency pin is added. Generated All/Trust setup files contain the public consumer and the actual test module. The four current rebuilt olean hashes match the repair receipt; setup options and plugin lists are empty, traces are ordinary nonsynthetic builds, and no skipKernelTC appears. Artifact identity across worktrees is not assumed.

All 44 historical source artifact hashes and all 16 repair artifact hashes match. The original BLOCKED review, complete constant inventory and published-consumer scope log are byte-identical to the external originals. The old source packet is expressly rejected/historical; the repaired manifest supersedes it rather than claiming the old correspondence passed.

I executed repair/validate.py with its three output writes intercepted into deterministic byte comparisons. All 1,245 actual setup-derived source identities, 11 package pins, selected local source/olean identities and runtime input identities match; the only permitted old-local-source difference is exactly the three corrected canonical literals. The controlled dependency probe freshly recomputes the seven registered theorem scopes, which agree exactly with the retained sets and contain only foundations. This review wrote none of those candidate outputs.

The retained fresh ordinary combined build completes 1,715 jobs. I parsed its complete Trust theorem output with the unchanged checker: every currently printed theorem is covered and the union is exactly the existing 29 disclosed global axioms. The retained fresh global gate explicitly recomputes dependencies and reevaluates native claims. Global trust is not described as foundations-only. The inherited thirteen kernel examples and public late-rollback instance are genuinely imported by this build; no unproved complete-success kernel instance is credited.

The new executable diagnostics run the original eight checks plus four independent-literal cases. Their state seeds correct Solidity ID/report slots and obsolete wrong-slot decoys with different values; the actual request returns ID8, consumes report99, updates the correct ID slot and retains the wrong-ID decoy22. I read the complete diagnostic body, loader and twelve-PASS receipt and checked their artifact/runtime identities. These are explicit FFI runtime diagnostics, not kernel proofs. No redundant FFI build or full Solidity test run was performed by this reviewer.

The twelve inherited Solidity tests including 1,024 fuzz runs, seven unchanged core files, nine original OZ files and seventeen compiled RequestHarness inputs retain the previous independent checks by byte identity. Actual fixture settings remain solc0.8.9 optimizer200 London, distinct from production Istanbul; full deployed bytecode equivalence is not claimed. The experimental restricted IR's different push guard is not used for the legacy execution. The original raw assembly/IR stdout EOF exceptions remain disclosed; integration-only diff-check passes. No broader compiler/gas boundary is invoked to excuse the repaired source literals.

The accepted scope remains a one-item typed request execution, with the amount admission included; full pause/batch/dispatcher, actual stETH balance/allowance behavior, token deployment authenticity, error/LOG encoding, arbitrary-callee framing, full caller renaming and contract-qualified deployment refinement remain outside. This repair changes no website or publication.

Final acceptance awaits only the stated README attribution correction and exact successor review; a later union with main must be checked separately. Reviewer stopped after this external report. No source mutation, push, merge, comments, deployment or publication was performed.
