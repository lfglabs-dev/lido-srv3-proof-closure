# ADDRESS request repair on ACCOUNT treasury main

The source/proof repair at `f9f5639bdcc30a5f653fe679c9caba306a84df19` received the retained independent review. Its only remaining issue was attribution of the earlier dependency inspection: three published live claims were checked through loaded closure, and three supplementary helpers by reading their Std-only module. The repaired README now makes that distinction, and its receipt hash is updated. The no-dependency conclusion is unchanged. No Lean source changes follow that review.

Union `a9fbafedf526d1d1eed73f491ee61098d09c5376` combines f9f5 with accepted ACCOUNT treasury main `d6bbf39ad153ee979e2357fa5df2830c17f353f2`. Its single Trust append conflict is resolved by retaining both complete query blocks. Both public/regression imports and the ACCOUNT provider roots are preserved. The complete repaired ADDRESS source/dependency closure remains identical: 1,245 sources, 11 pins, seven foundation-only scopes and four normal olean artifacts. The source review's fresh scoped checks, slot-family checks, twelve FFI diagnostics and unchanged Solidity fixture review are reused by identity.

A fresh combined normal AllGuarantees/Trust build passes 1,719 jobs. The fresh global gate recomputes dependencies and re-evaluates native claims, preserving exactly the same 29 disclosed axioms. This union does not claim a new fresh Solidity or FFI run. Its only semantic source change relative to main remains the reviewed correction of three canonical ADDRESS slot literals plus the additive request consumer; the ACCOUNT source bodies are unchanged. The rejected a3ae source packet remains explicitly historical; repair/README.md and the retained repaired-source review explain its supersession.

The useful result remains a successful one-item typed request with actual transferFrom and shares-quote responses/returned World consumed by physical enqueue, source-ordered arithmetic/packed fields/owner set/events, and complete root rollback. Neither arbitrary token effects, pause/whole batch/dispatcher, WstETH body, complete error/LOG ABI, deployment qualification nor full entry equivariance is added to the claim. Correcting the historical approval slot is not a new complete NFT-transfer proof. No site change or external publication occurs here.

A final independent exact successor review is required before merge. Raw RequestHarness.asm and CompilerProbe.ir EOF exceptions remain historical; the newly united ACCOUNT raw assembly EOF exception is likewise preserved explicitly. Source files and non-raw new packet text pass diff-check.
