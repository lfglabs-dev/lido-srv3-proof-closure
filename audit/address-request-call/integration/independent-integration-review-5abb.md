# Independent exact ADDRESS/ACCOUNT union review

Verdict: **CLEAN**, retaining the documented historical raw compiler-output EOF exceptions. Exact frozen commit `5abb7996859d53fce3db600ef31f66a40bfa6d06`, tree `023f674b8e901ea548f6495b4b2dc38b0518b44a`, checkout `/tmp/lido-address-request-integrated`. Root writer explicitly stopped; HEAD and clean status checked before and after review. No project mutation or repeated build.

The sole reservation from the independent full repaired-source review of `f9f5639bdcc30a5f653fe679c9caba306a84df19` is closed. Repair README now accurately attributes the three published live307 theorem checks to the loaded dependency traversal and the three supplementary helper checks to their Std-only source inspection. It explicitly identifies the loaded probe's remaining roots as the new request and two historical executors. The no-dependency conclusion is unchanged. The only accompanying repair receipt change is the README hash.

Complete recursive Git-tree comparisons establish that union `a9fbafedf526d1d1eed73f491ee61098d09c5376` preserves both the reviewed ADDRESS repair and accepted ACCOUNT treasury main `d6bbf39ad153ee979e2357fa5df2830c17f353f2`. Every ACCOUNT/main object is unchanged except the intended AllGuarantees/Trust additions and the previously reviewed three-literal bridge repair. Every ADDRESS object is unchanged except the two wiring files and the documented README/receipt update; lakefile is exactly the accepted ACCOUNT provider configuration. No Lean body changes after f9f5. The complete successor delta over the union is the two documentation/hash changes plus exactly five integration archives.

I inspected the full AllGuarantees/Trust changes relative to both parents. The single append conflict retains both complete four-query blocks, with both public imports and both regression imports. Actual generated All/Trust setup files include PAddress1RequestCalls and PAccount1TreasuryCall, and Trust includes AddressRequestCalls and ReportFeeTreasuryCallTest. The existing ACCOUNT provider roots and every accepted ACCOUNT source body are preserved; no duplicate or shadow provider appears. The two proof compositions remain their respective accepted executions, with no invented cross-composition premise or theorem.

All 44 historical source artifact hashes, 16 repaired-packet hashes and four integration hashes pass. The archived f9f5 report is byte-for-byte the independent external original, including its now-resolved reservation. All 1,245 ADDRESS source identities and 11 actual package heads match the repaired manifest. The four normal ADDRESS olean hashes exactly match both f9f5's checked artifacts and the integration receipt. Thus the previous independent fresh seven-scope query, canonical ten-base/four-selector check, normal provider review, twelve FFI diagnostic assessment and complete relevant source/Solidity review remain applicable by identity. The rejected a3ae packet remains explicitly historical and is not retroactively labelled CLEAN.

The new retained normal combined AllGuarantees/Trust build completes 1,719 jobs. I parsed its complete Trust output with the unchanged checker: every currently printed theorem name is covered and the global union is exactly the existing 29 disclosed axioms. The fresh retained gate explicitly recomputes dependencies and re-evaluates native claims. ADDRESS's seven reused scopes remain foundations-only; the global result is not presented as foundations-only. The receipt honestly claims no new Solidity or FFI execution for this union.

The whole reviewed ADDRESS claim remains the admitted one-item typed request: original amount/context consumed by transferFrom CALL, returned World consumed by a distinct STATIC quote, truncated share value consumed by checked cumulative shares/stETH and ID arithmetic, corrected physical last-ID/report-time slots consumed by ordered queue writes and the legacy owner-set insertion, then WithdrawalRequested/Transfer events. Entry TIMESTAMP is retained independently of arbitrary callee metadata. Canonical false bool succeeds; noncanonical/short replies fail. The metadata high byte is preserved, claimed is cleared, report time is read after ID write, and owner insertion rereads length after the array value write with legacy uint256 wrap. Success and failure are derived from the same root execution without added stage, framing, separation, share-fit or nonwrap assumptions. Failure restores its complete incoming modeled World while retaining diagnostic attempts.

Full token allowance/balance semantics, deployment qualification, outer pause/batch/dispatcher, complete error/LOG encodings, precompile/gas scheduling and full address-equivariance remain outside. Correcting the third historical approval literal does not create a complete NFT-transfer proof. ACCOUNT324's independently accepted treasury-call extension is preserved exactly; no ADDRESS statement is made to inherit its separate execution evidence. London fixture settings versus production Istanbul and the restricted experimental IR versus operative legacy assembly distinction remain disclosed. The original RequestHarness.asm/CompilerProbe.ir EOF exceptions and the incoming ACCOUNT raw assembly EOF exception are preserved as accepted raw evidence. The complete successor-only diff-check passes; no blanket historical whitespace PASS is asserted.

No source edit, build repetition, FFI/Solidity rerun, push, comments, merge, deployment or website action was performed. The frozen candidate is suitable for root's final archive flow within these exact scopes. Reviewer stopped after this external report; any archive-only successor still needs its exact delta checked.
