# Independent review — canonical SSZ state placement

Final reviewed candidate: `e6b3e15ab2985d14115b3a66e2e8e9b147fb4cd3`, exact child of initially reviewed `79868705d707985a921daadf0a772545726e8ef5` (base `8ef973fa37997e55061a95fce1a41bf3cfe968e7`).

**Final verdict: CLEAN for this bounded increment.** The sole P2 reproducibility finding in the initial candidate is resolved in the exact successor; no source-proof finding or remaining blocker. This review concerns the canonical container/validator-registry spine and typed entry consumer, not SSZ-1 closure or authenticated/deployed state.

## Resolved finding P2 — initial nonportable npm lock

`audit/ssz-state-placement/reference/package-lock.json:3,14` stores its root version as a temporary file reference and all installed packages under `../../private/tmp/lido-ssz-state-reference/node_modules/...`, rather than local `node_modules/...` package keys. The reproduction instructions in `reference/README.md` prescribe `npm ci` from that directory. In an isolated copy of the exact package manifest/lock, `npm ci --offline --dry-run --ignore-scripts --no-audit --no-fund` fails with EUSAGE and eight missing locked packages, including both direct ChainSafe dependencies. Evidence: `/tmp/lido-ssz-placement-lock-review.log`.

This does not refute the already recorded Node or Solidity checks, but prevents reproducing their declared pinned dependency installation. Resolution requested: regenerate a portable lock in a fresh working directory, run clean `npm ci` there, replay the reference, confirm deterministic outputs and update receipt hashes/commands. Existing Lean sources need no change. The integrator corrected this in exact successor `e6b3e15ab2985d14115b3a66e2e8e9b147fb4cd3`.


### Exact resolution checked

The successor changes only six reference/docs/receipt files: portable package lock, added npm-ci log, both README files and both receipts. No Lean source/test, schema, source checker, reference algorithm, generator, Solidity harness/configuration, vectors or semantic output changes. Independently verified **44 outer hashes and 13 nested artifact hashes** against successor Git objects; the seven core inputs retain the same submodule pin. The eight locked package versions and integrity hashes match the initial candidate exactly, now under portable local node_modules keys. Six packages install on this platform; the remaining two are optional platform packages.

The integrator's fresh-directory normal install followed by actual `npm ci` and Node replay succeeds. Compared `/tmp/lido-ssz-state-reference-portable` check/schema/package inputs, vectors/results and actual ci/Node logs byte-for-byte with the committed artifacts: all match. Separately reproduced a successful `npm ci --offline --dry-run --ignore-scripts --no-audit --no-fund` in another isolated copy of the fixed manifest/lock; evidence `/tmp/lido-ssz-placement-portable-review.log`. This resolves the exact earlier EUSAGE failure. The full successful Node replay remains the integrator's execution, not a reviewer claim of rerunning it.

Existing exact Lean and Solidity validation remains applicable because all corresponding inputs are unchanged. No Lean or Forge rebuild was necessary. `git diff --check` passes for the successor. The detailed initial identity checks below describe the original 43-hash dossier; the final dossier has 44 hashes because npm-ci.log was added.

## Exact identities and completed checks

The reviewed commit adds exactly 28 authorized SSZ files. All 43 top-level receipt hashes were independently compared against exact Git objects, including seven pinned core inputs at `17005714f151e5502c559932319a3f2f74ac2436`; working bytes match. All nine accepted source/configuration dependencies are byte-identical to the parent. The nested reference receipt's twelve artifact hashes and six core-source hashes also match. Receipt SHA-256 is `8ee2e5a0057a986d19dd434aa4921291c7e3bdb95e8fdf7cc39496732c717a52`. `git diff --check` passes.

Read the complete generic-tree/state-placement source and tests, final source after comment wording/spacing changes, unchanged accepted SSZ dependencies, pinned relevant Lido entry/helpers/configuration, bundled canonical schema/merkleization declarations and reference generator/checker/harness/docs/receipts. The read-only offline checker was replayed and its JSON matches the committed source-check artifact. Six Solidity fixtures were independently regenerated outside the project from committed vectors and the committed generator; the resulting harness is byte-identical.

The final exact-source log records a successful 792-job targeted build, including recompilation of final StatePlacement and tests, with accepted dependencies reused. It covers 26 theorems (nine generic, seventeen state/registry), 39 regression declarations and nine principal axiom queries. Seven queries use only `propext` and `Quot.sound`; length encoding and final entry also use `Classical.choice`. No new axiom, admitted proof, native checker or warning was found. No reviewer Lean/Forge build was performed.

## Substantive review

- **Independent tree/list correspondence:** `SszPerfectTree.lean` defines a generic ordered subtree spine, a separate list-half merkleization and interval-bisection addresses. Theorems relate tree digest to ordered chunks, prove exact bounded-list zero padding, derive index/path/subtree and construct actual sibling lists. The generic depth proof supports depth 40 symbolically; it does not claim evaluation of an enormous concrete tree. Missing bottom chunks are zero and higher zero subtrees are recursively hashed.
- **Schema and capacity:** `SszStatePlacement.lean:20–70` enumerates the full ordered Electra/Fulu schema, rather than taking a validator-field index as input. Compared with the bundled immutable consensus-specs snapshot `f96d3e7acf35125295d234da4b0c67591fdef49c`: 37/38 fields, validators at position 11, Fulu appends proposer_lookahead. Both counts derive 64-position container width. Registry capacity is the actual 2^40. This external selected schema is correctly distinguished from a Lido dependency or deployed fork identity.
- **Semantic registry and real length:** `SszStatePlacement.lean:63–131` uses actual semantic Validator values, own credentials and Bytes48 domain. Their computed eight-field trees become composite-list element roots. The independent ordered list is padded exactly to capacity; length is mixed on the right as 32 little-endian octets. Faithful uint256 embedding follows from capacity, without an extra no-wrap premise. Total constructors outside the valid capacity domain truncate; the README explicitly excludes that as valid SSZ serialization.
- **Container/path derivation:** `SszStatePlacement.lean:138–240` computes validators in the named field list and keeps other named field roots opaque. Ordered field roots and exact zero padding are proved equivalent to the container tree. Position 11 at depth six gives GI75, its list-data edge GI150, and state index `150 * 2^40 + i`. The actual length is sibling40 above the forty data edges. Structural padding is a zero leaf; no universal digest inequality is inferred.
- **Actual membership and final consumer:** `SszStatePlacement.lean:243–313` takes `i : Fin values.length` and length at most capacity, derives the source offset guard, path/index equation, semantic subtree and 47-element state branch, then supplies these proved facts to accepted `validator_entry_consumer`. The conclusion uses the same computed fifty-element header proof and actual typed ordered entry, with the selected value's witness and credentials. `hi`, `hp`, `hplace`, supplied leaf equality and root-match Boolean are absent from the final theorem's premises. The exact root oracle response and total standard SHA adapter remain explicit trust inputs.
- **Membership is stronger than capacity:** tests and README correctly distinguish first-padding index admission by the actual configured offset guard from semantic list membership. The result constructs successful proofs for actual members; it does not prove successful verification authenticates membership, nor derive collision resistance or exclude padding collisions for every hash. This distinction must remain in any site claim.

## Reference and execution evidence

`reference/check.mjs` uses an independent sparse Node SHA-256 level reduction, including recursive zero hashes, against ChainSafe SSZ 1.2.1 and persistent-merkle-tree 1.2.0 (the versions declared in the pinned Lido lock). It checks 64 registry/state cases and 104 selected member proofs, all 47 state siblings, true length sibling40 and the fifty-sibling header extension. Nonvalidator fields are represented by opaque Bytes32 roots, correctly scoped as structure rather than recursive full BeaconState encoding. Concrete changed-length, unhashed-padding and reversed-proof fixtures differ under actual SHA; no universal injectivity is claimed.

The six exported fixtures cover Electra/Fulu, list lengths one/seven and first/last members. The generated harness invokes the unmodified pinned CL validator entry. Its six successful solc 0.8.25 tests each accept the canonical proof and reject changed length and level39 padding. The only root response is mocked at the exact address/timestamp request. These logs establish finite synthetic execution evidence, not consensus authentication, runtime deployment or universal Lean-to-EVM refinement. The sole lock portability finding is resolved in the exact successor as recorded above.

## Limits to preserve

Other BeaconState fields, including state slot and fork, remain opaque roots; their internal encodings, cross-field consistency and reachable consensus state are not established. Source expected credentials are selected semantic credentials here, not proof of their deployed caller provenance. The exact oracle root response remains trusted; EIP-4788 authenticity and timestamp/storage policy are not derived. Standard SHA totality, cryptography, gas/history-dependent precompile behavior, raw calldata/memory/ABI, compiler/EVM correspondence, GIndex fls assembly and deployment/fork provenance remain open. The older registered guarantee/API and unrelated lanes are unchanged.

No project writes, commit, push or merge were performed. Only isolated temporary checks and this report were written.
