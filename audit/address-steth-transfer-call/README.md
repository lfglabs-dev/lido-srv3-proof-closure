# ADDRESS actual full-Lido stETH transfer consumer

Author candidate at parent `107e57e53ad4b1bd459a062d887bbcac85be6e41`; independent exact-source review is still required. Only three new Lean modules and this dossier are added. Existing source, package configuration, public imports and Trust entrypoints are unchanged.

## Consumed public result

`actual_wrapped_physical_steth_transfer_permit_batch` specializes the remaining `tokenTransfer : External` in the complete107e wrapped permit/batch runner to `AddressStETHTransferCalls.callee`. It retains **the entire107e success conjunction**, named `PhysicalConversionPermitEffect`: the previous `PhysicalQuotePermitEffect` (including the complete original permit JoinedEffect and enhanced physical-quote transcript) and the exact107e conversion transcript. The stronger second transcript adds the executed full-Lido transfer effect on the same per-item worlds, captured amounts, IDs and attempts. This is a specialization of the existing executor, not a separate supplied successful stage.

For each item the existing actual WstETH transferFrom and unwrap run first. The reverse conversion uses WstETH's qualified slot7 on the actual preburn world. The actual burn may change storage; the transfer target is independently reread from slot7 on **that returned postburn world**. No same-target or frame premise joins these reads. The actual generated 68-byte CALL has caller WstETH, this freshly selected target, zero value, selector `a9059cbb`, the queue caller as recipient, and the **same received amount** captured by reverse conversion. `canonical_call`, `call_effect` and `token_effect` establish that the invoked callee executes its physical body and returns32-byte true, consumed by the existing WstETH return decoder. That returned World feeds the retained queue amount guards, forward quote and physical enqueue.

The new body first executes the accepted full-Lido physical `getSharesByPooledEth` helper on the same selected contract and World; the resulting shares are used for the debit, fresh recipient read, checked credit and events. Both full prior TokenEffect/Item and this new effect remain in the public conclusion. The failure theorem retains universal full-entry rollback to before permit, including earlier items, burns, storage and logs. Journals retain the existing attempted calls. No additional stage-success, rate equality, funding, width, nonzero-total, T>=E, nonalias, Keccak-injectivity or storage-frame premise appears. Old arbitrary mutable-token interpreter theorems and their domains are untouched.

## Exact pinned body and assembly order

Core pin `17005714f151e5502c559932319a3f2f74ac2436`. Read with the complete inherited Lido implementation and the reused **full-Lido** `audit/address-steth-quote-call/Lido.asm`, not a standalone StETH storage layout:

| Consumed source | Full Lido legacy assembly | Model |
|---|---|---|
| StETH.transfer, lines196–199 | tag307 calls494, returns shared tag422's true; external boolean encoder | canonical dynamic callee / reply |
| StETH._transfer, lines428–432 | tag494→getSharesByPooledEth137→_transferShares603→events605 | `program` executes physical forward quote before all move guards |
| StETH._transferShares, lines494–505 | tag603 through1096 | `move` / `MoveEffect` |
| Pausable._whenNotStopped, lines18–20, Aragon UnstructuredStorage.getStorageBool | tag397→430 SLOAD; tag917 tests entire word via ISZERO/ISZERO | actual qualified active-slot word !=0 |
| StETH._emitTransferEvents, lines562–565 | tag605: Transfer LOG3 then TransferShares LOG3 | ordered semantic events with original amount / derived shares |
| SafeMath.sub/add | tags745/485 | ordered guarded subtraction and checked recipient addition |

Line references are navigation aids; full pinned bodies and complete assembly/artifact hashes are retained in the evidence chain.

`_transferShares` checks sender!=0, recipient!=0, recipient!=address(this), then `_whenNotStopped`, then reads the sender shares and checks shares<=balance. Source SafeMath.sub repeats the identical inequality; the preceding guard derives that it cannot fail, and subtraction is exact. The model does not discard a reachable `MATH_SUB_UNDERFLOW` path. The sender SSTORE precedes the **fresh recipient SLOAD** and checked addition. For uint256 operands the source guard `(fresh+shares)%2^256 >= fresh` is equivalent to `fresh+shares <2^256`: if it wraps, shares<2^256 makes the wrapped result strictly smaller than fresh. There is no added bound premise. Overflow returns `MATH_ADD_OVERFLOW`; the outer callee `run` restores the entire pre-call World. A same-address transfer reads its debit and restores its original balance; no address/hash distinctness is assumed.

The actual mapping base in **full Lido inheritance is0**. At tags1093/1095/1096 the compiler masks the owner to160 bits, writes that word at memory0 and base0 at memory32, then Keccak64 and SLOAD/SSTORE. `balanceSlot_keccak` proves the exact same two-word byte-array hash equation; qualified contract reads/writes use this result. It uses the backend range lemma, never its separately declared mapping-injectivity axiom. Mapping-to-mapping or mapping-to-fixed-slot collisions remain represented by the actual storage keys and update order; the result does not claim unaffected totals/active fields under a hypothetical collision.

Active slot is `keccak256("lido.Pausable.activeFlag")` = `644132c4ddd5bb6f0655d5fe2870dcec7870e6be4758890f366b83441f9fdece`. The actual raw getter/assembly tests **any nonzero256-bit word**, not a low-byte field or equality to1. Values2,256 and2^255 are deliberately exercised.

The forward conversion helper is the unchanged ee24 body, including full-Lido overriding physical reads: `I=B+V+P+D` from the low/high128 buffered and CL words; `S=(T+2^256-E)%2^256` from the packed shares word. It checks amount<2^128−1, uses raw wrapped multiplication, and fails outward empty on I=0, including amount0. Actual legacy forward evaluation reads internalEther first, then internalShares; reverse evaluation retains107e's opposite order. T<E, zero S with nonzero I, oversized/raw arithmetic and independent target/rates are not excluded. Events use the original token amount and captured derived shares; no postwrite conversion is invented.

## Validation and fixture degree

- `public-build.log` and `test-build.log`: normal Lake elaboration/kernel checking, final1280 jobs PASS. The successful public build includes compilation of the new source; the later test build uses those exact current artifacts. Fourteen kernel-checked finite regressions cover payload/selector/amount, conversion-first failure priority, zero-address/self/pause guards, canonical generated dispatcher rejection, and whole-word active flags. Two public kernel instances cover the complete empty success conclusion and paused whole-entry rollback. Nonempty positives are the diagnostics below, not claimed as kernel-evaluated batch instances.
- `validation.log`, `source-inputs.json`, `axioms.json`:1263 actual imported/new source identities,11 package pins and13 fresh ordinary scoped axiom sets. Only propext/Classical.choice/Quot.sound occur. Old local sources are byte-identical to107e; actual selected package/source providers and local oleans are compared. `normal-build-identities.json` binds the three normal source/olean/ilean/trace/setup files plus unchanged root Lake/manifest/toolchain. Setup options are empty; no kernel-skipping or native proof extension is enabled. These are scoped checks, not a new root All/Trust gate.
- `native.log`: four independent cast-keccak mapping vectors (owner0,3,99,max160; each exact64-byte preimage) cross-checked against the actual Lean FFI Keccak. Six execution groups pass: complete permit-call-model batch7/8→700/800, physical balances and ordered events; late transfer failure restores permit/earlier item/burn; same-address fresh read; checked credit overflow; qualified target; selected no-code target. `runtime-inputs.json` identifies the freshly built C/Lean diagnostic dylib inputs. These executions add no theorem axioms and are explicitly distinct from kernel proofs.
- `solidity-test.log`:11 new tests PASS, including1024 direct full-Lido transfer fuzz cases. Direct tests deploy the exact **unmodified full Lido artifact**, with no override of transfer, getters, _transferShares or events. They check exact true32 and error bytes, physical mapping balances, event topics/order/data, width/zero-divisor/guard priority, any-nonzero active word, insufficient shares, overflow rollback, alias and zero transfers.
- The composed Solidity fixture deploys two unchanged Lido instances: underlying queue targetQ and fresh WstETH slot7 targetA. It uses unchanged WstETH code, actual valid permit signatures/nonce/allowance, actual burns and actual stETH.transfer. Queue Q has rate15/1500; A has20/2000. Both items retain their actual700/800 outputs and7/8 queue shares. TargetA's physical balances move20→5 and5→20, while Q receives no share write. A late second transfer failure restores the first item and real permit nonce/allowance/supply/balances. Pause and no-code failures also restore the entry. No mutable transfer controller or ConversionHarness is deployed in this lot.
- `compiler-check.log` / `compiler-identities.json`: exact91 parent receipt inputs and111 quote packet inputs reused by hash and Git identity; all37 full Lido source inputs and metadata keccaks checked again, core bodies compared to the exact pinned Git revision; unchanged Lido/WstETH deployment bytes and three full legacy assemblies; unchanged18 queue input bodies and executable objects/metadata. Only the **new19-input Solidity0.8.9 test project** was compiled here. Full Lido0.4.24 and WstETH0.6.12 were not recompiled. New test and unchanged queue artifacts are retained, alongside the two exact deployment artifacts.

The Lido artifact profile is solc0.4.24+e67f0147, optimizer200, Byzantium. The queue/new fixture is solc0.8.9+e5eed63a, optimizer200, London, with fixture code-size limit100000. These are the retained validation profiles, not a claim of production-bytecode identity. Fixture `vm.store` seeds packed rate words, active slot, WstETH target/balances/supply and full-Lido shares explicitly. This initial-state/deployment boundary is not an initialization theorem. `vm.prank(0)` tests the sender-zero body guard without asserting that an ordinary signed transaction has that sender. The real signature tests are diagnostics, not a new formal permit-signature proof.

## Remaining scope

The callee is an explicit implementation specialization at the dynamically selected request target; the theorem does not authenticate runtime code there. The formal mutable transfer body has no arbitrary callback. Other accepted external interpreter boundaries, including permit and unmatched calls, remain unchanged. The actual generated canonical dispatcher is proved; a complete arbitrary malformed-ABI dispatcher, end-to-end memory/allocator/gas accounting, deployed proxy/initialization refinement and a unified EVM trace are not claimed. Logs are the inherited semantic event abstraction, with finite actual LOG tests rather than a new general event-ABI theorem. INVALID is outward `.bubbled []`; gas behavior and distinctions among empty failures retain their earlier boundary. Qualified address0 keeps the accepted Live storage convention.

No new raw assembly stdout is copied: the old full raw assemblies remain at their exact retained hashes, including their previously disclosed EOF whitespace exceptions. New source/dossier diff checking is expected clean. Development failures were limited to proof/test record syntax, non-Decidable equality on Reply/World (changed to kernel reflexivity), the storage field name, and a copied validator's new-public filename; each was corrected before these final successful checks. No failing compiler, Forge, native or final proof result is relabeled as PASS.
