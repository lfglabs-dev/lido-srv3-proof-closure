# Review consumption and scope

Root accepts the independent CLEAN review of source `3521761bba0ad6e1651f620ae0d2359bfc1343b0` by mission `8857d050-fb71-4c9d-9b19-585a90be0c02`, terminal event `69631aac-c7f5-4446-88cc-decf47eaeb26` (sequence 128). The complete report is retained, together with the later background-job acknowledgement (sequence 134). The writer remained stopped throughout review; the independent reviewer is acknowledged with no execution. Its earlier provider error was infrastructure failure, not a technical verdict.

The independent reviewer fetched the exact git candidate, read the complete source packet and pinned Solidity/optimized IR, verified all 78 packet files, ran the 1246-comparison identity checker and reproduced the targeted 1134-job Lean build. It inspected the eight-input Forge harness and four hash-bound PASS results but did not rerun Forge, which was unavailable in that workspace. Root's original four fresh Forge executions remain the finite Solidity evidence. The review's incidental “14-file” closure reference does not change the manifest's sixteen local import count or 1117 complete source identities.

The accepted result binds original raw calldata to the fields, key/proof slices, WC, root and raw GIndex used by the actual SHA/proof consumer. Caller environment preservation is derived from EVM.call's return rule, including arbitrary Theta account/substate outputs; no initial resource or output-size premise is added to that invariant. Signed negative relative pointers remain accepted where the inspected compiler guards accept them. The nonpayable/selector check reorder yields the same model ABI error; this is not exact revert-byte or gas equivalence.

All internal work listed in README remains required: full raw-byte/typed-tree equivalence, derivation of reachable extents/resources, the complete slot/root/GIndex entrypoint, compiler allocation/spills/gas, exact error ABI and compiled World rollback. The review's huge-calldata observation is not an added size restriction or a closure of the initial-state obligation. SHA implementation and authentic deployed/consensus context remain external trust, with no new deployment verification. All eight unfinished guarantees remain OPEN. ALLOC-1, ALLOC-2, RESERVE-1 and public identifiers are unchanged. No site merge or deployment is authorized.
