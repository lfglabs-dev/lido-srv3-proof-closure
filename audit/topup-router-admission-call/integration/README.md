# Actual router admission integration

Reviewed source ed043832023c75e04adf76ea05c3ac96f3353897 is composed with accepted main347 6e59055f72a4da135915844f6f504b02f10c6fcb. Union 74d4ca39 retains all source modules and the entire reviewed DEPOSIT347 dossier. The two aggregator conflicts were resolved by retaining every main347 import/query and adding the new TOPUP public/test imports and five ordinary queries. No prior source, public theorem, configuration or receipt was changed. The independent exact ed043 source/Solidity review is retained verbatim and CLEAN; exact integration review remains required before merge.

The runner executes the existing gateway entry, locator, credentials and root prefix once, constructs Ready internally, then performs router admission on that produced module input. The actual decoded router calls its independent locator for topUpGateway, compares the actual gateway sender, validates the produced pubkeys/limits and original keys/operators, and reads physical registration/status/type. Only a zero computed rounded target calls the supplied Lido target for canonical canDeposit. The old module/withdrawal/history continuation executes once on the same actual input and World. The public theorem retains the literal entire342 conjunction, exact old result/World projection and actual admission call origins/journals. No successful-stage, authorization equality, array alignment, fit, initialization or frame premise is supplied. All covered failures restore original entry World, including late module/callback effects.

The first normal integration build on main346 completed 1792 jobs and actively compiled four new TOPUP modules plus both aggregators. Its build and four normal module identities are retained separately. Composition with reviewed DEPOSIT347 changes the aggregators and adds its three modules: the final normal build completes 1795 jobs and reuses unchanged TOPUP artifacts. Six current normal source/setup/trace/olean identities are retained; actual import providers are checked. Fresh final default global trust recomputes the same 29 disclosed axioms. The new increment separately has 39 ordinary foundational scopes, 1361 actual source identities and 11 package pins.

The comparison wrapper changes only path-dependent build-identities output, redirecting it to an external temporary record, and excludes the two explicitly additive aggregators from the source validator's whole old344 tree comparison. All other 344 blobs, source/provider identities, the full prior public conjunction, ordinary closures and compiler comparisons remain strict. All 42 source receipt hashes compare unchanged. Current configurations match accepted347, which already contains the accepted ACCOUNT registration change. The integration review separately verifies the full main347 tree and additive wiring.

The source has 25 kernel computations plus a nonempty public consumer and public late rollback. Its 17 fresh full-router Solidity tests include 256 status fuzz runs (sampling, not asserted exhaustive enumeration). Raw endpoints enforce actual caller/selector and static behavior; tests include guard priorities, malformed replies, positive-target skip, zero-target successful module execution and late rollback. Those executions are reused, not rerun by integration. All 28 cached source inputs, 29 fresh sources, five metadata-checked artifacts and complete 5046-line production router IR retain identity. Cached and fresh compiler packets remain distinguished. The failed development EOF comparison and other failed attempts stay explicitly failed in the source dossier.

The allocation phase still omits getDepositableEther and the linked SRLib DELEGATECALL; the inherited allocation value remains a stated boundary. Actual cap/minimum/rounding determines the target, but no arbitrary library reply is granted a read-only World frame. The outer gateway-to-router CALL/raw ABI/nonpayable dispatch, immutable/getter deployment identity, whole allocator trajectory, gas and inherited cryptographic/root/consensus/log qualifications remain explicit. New errors have exact bytes, while inherited allocator Panic(0x41) remains a symbolic Fault. New admission attempts are separate from the exact prior result; no flattened whole-EVM trace is invented. Accepted ALLOC/RESERVE are unchanged. Site426 remains on reviewed main346 until its separate publication pass.

Source/receipt and unchanged compiler/Forge/native checks are reused by exact identities. This integration does not claim a fresh Solidity or FFI execution.
