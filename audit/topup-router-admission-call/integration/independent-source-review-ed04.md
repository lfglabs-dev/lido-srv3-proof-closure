# Independent exact TOPUP router-admission source review

**CLEAN for the bounded additive source/proof increment** at `ed043832023c75e04adf76ea05c3ac96f3353897`, tree `5c0bfb9e40aaf27826205f88f088f771b77c105c`, parent accepted main344 `d353e85438b3456604a76ee417f9359627751240`. No blocking source, proof, consumed-argument, correspondence or evidence finding. Integration/global wiring and publication remain subsequent work.

The author account_locator was stopped and the candidate clean. I authored none of this increment, the preceding TOPUP entry admission, router-locator, or timing/history proof. I have authored later CONSOL quota/entry and DEPOSIT admission-error increments; those are not credited as independent evidence here and are not imported by this source on main344. The older locator reviewer disclosed authorship of DepositPhysicalAdmission; that component retains its separate independent source39f4/integration7305 reviews. This provenance is not erased by reusing the locator report.

I read all four new Lean modules in full, including all proofs and regression instances, both validation/sealing scripts, the Solidity writer runner, complete new and inherited runtime fixture sources, README/source map, current receipts/identity records, retained build/test results and development diagnostics. I inspected complete relevant pinned Solidity bodies and the entire fresh production topUp IR dispatch (not only the validator's token excerpts), together with complete called admission/registry/scalar/allocator helpers and the full fixture dispatch. The retained IR files contain 5,046 production lines and 5,165 fixture lines; unrelated entrypoints were not newly given a semantic review. Previously independently reviewed remaining consumer paths are reused by exact identity as described below. This is a semantic composition review, not compile-only acceptance.

## Actual execution and full public consumption

`walk` executes the gateway's actual physical role/resume gates, typed lengths, timing gates, gateway locator lookup, physical credentials lookup/prefix guard and actual root loop exactly once. `Ready` is constructed only by that successful loop; it carries the decoded router/credential, actual Output and reached locator/credential/root journals. It is not an input certificate. Early failure branches retain the reached old result. `consume` performs the new router admission on this internally produced seam, and calls `finish` only after admission success.

The gateway identity compared with the router locator's decoded address is `i.gateway.gateway`, the actual source caller of the omitted outer gateway-to-router call. It is deliberately distinct from `i.caller`, used by the gateway role gate. The new STATICCALL caller is `selected.ctx.sender`, which is the router returned by the gateway locator. Its target is the independent `g.locator`; no equality with the gateway locator is imposed. The conditional second target is `selected.ctx.self`, the supplied Lido identity already used by the withdrawal continuation. Exact request payloads are four bytes `644862de` and `e78a5875`, with zero value.

The input validator consumes `moduleInput i ready`, built from the actual root-loop Output: produced pubkeys and wei limits plus original module ID, keys and operators. The unchanged module continuation receives precisely that same input through `TopupBatchMemory.finish`. The rounded target comes from the existing physical uint64 cap/minimum/rounding computation and the inherited arbitrary allocation value, not a newly supplied bounded target or reconstructed counter.

After authorization, checks are empty keys, parallel lengths, each pubkey length48, full nonzero physical registry position, enum byte range, active status, then credential type2. Registry preimage is moduleId followed by routerRoot+2; config is moduleId followed by routerRoot, both loaded at the selected router. Status is bits224–231, type is bits232–239. Raw position values other than one remain accepted. Status≥3 gives the enum panic before inactive/type errors. No initialization, canonical-position, collision separation or hash-injectivity hypothesis appears; existing explicit hash interpretation is unchanged.

Only a zero computed rounded target invokes canDeposit. A positive target skips that call entirely. False returns LidoDepositsPaused before module execution. Both STATICCALLs consume the same modeled pre-module World, legitimately under the existing static transport with no successful mutable reply World. Forbidden state changes reject. This statement does not extend read-only semantics to the omitted SRLib DELEGATECALL or arbitrary mutable module/withdrawal calls.

`walk_prior` and `walk_relation` are proof-time equalities over the same prefix. `run` does not invoke `prior` or run an external stage again to recover success. `run_success` derives old whole-run success, exact final World equality, `projection = some (prior i)`, actual Ready and actual admission journal. The public `actual_router_admission_complete_prior` consumes these derived facts and includes literal **entire342 conjunction** through `PriorEffects`, then adds Admitted, CallFacts and the real admission attempt equality. Its only success premise is success of the complete new runner. No separate successful lookup/module/root stage, authorization equality, fit, frame, nonzero target, aligned-array or stronger crypto premise is present.

The complete retained conjunction still binds locator reply/cursor, physical credentials, root authentication, actual module input/reply/decoded allocations, physical withdrawal/beacon continuation, memory scalar checks and intermediate-world effects. History uses actual witness-produced total limits and the actual returned intermediate World; it remains distinct from allocation total, preserving packed neighboring bits and full timestamp event. The retained prior result contains its complete original journals. New admission attempts are stored separately, and their position between Ready's reached prefix and `finish` follows the executor; this does not invent a new flattened whole-EVM trace theorem.

`failure_restores` handles every covered early, new-admission and late error and proves complete original `i.gateway.before` equality. Late module/callback effects therefore do not survive failure. The public rollback theorem consumes that result. Structural positive and late-failure instances actually execute the new runner, rather than merely specializing a vacuous implication.

## Source, errors and complete compiler path

Core pin is `17005714f151e5502c559932319a3f2f74ac2436`. Read the entire StakingRouter.topUp and _validateTopUpInputs bodies, _getTopUpGateway/_checkAppAuth/_getModuleState, full SRStorage/SRUtils and relevant complete packed SRTypes definitions. The actual ModuleStateConfig stores an enum despite the separate legacy struct's uint8 commentary. The full production and executed fixture dispatches confirm nonpayable/raw ABI head decoding first (outside the typed model), then locator STATICCALL and canonical address authorization, typed checks, physical registry/status/type, allocation views, rounded-target conditional bool STATICCALL, module CALL and retained full continuation.

The locator and bool calls have no EXTCODESIZE precheck. Failure bubbles raw returndata. Success copies min(32, returndata length), finalizes allocation before the signed head check, then applies canonical address or boolean validation. The inherited scalar decoder exactly models that order, including allocation wrap/uint64 rejection. Address zero is not rejected merely for being zero; boolean accepts only0/1, trailing bytes ignored. The inherited ordinary no-code model succeeds empty and then fails decoding; its precompile exclusion remains the established transport interpretation. This is not an assertion about all code-less Ethereum targets.

Exact new raw failures are NotAuthorized `ea8e4eb5`, EmptyKeysList `c4d88632`, ArraysLengthMismatch `fc235960`, WrongPubkeyLength `500585ad`, StakingModuleUnregistered `d41d6282`, Panic `4e487b71` plus uint256(0x21), StakingModuleNotActive `645cc9f6`, WrongWithdrawalCredentialsType `2e5c948c`, and LidoDepositsPaused `5609c247`. Except for the36-byte enum panic these are four bytes. The existing allocator Panic(0x41) remains a symbolic Fault, explicitly disclosed; this increment does not claim to close every inherited error's ABI bytes.

The entire fresh dispatch shows cap SLOAD before getDepositableEther, actual STATICCALL `f2cfa87d`, then the linked SRLib DELEGATECALL with164-byte input, before minimum/rounding and canDeposit. Those allocation calls remain omitted by the inherited allocation boundary. There is no theorem claiming their arbitrary effects preserve this World, and no supplied successful allocation/frame premise. Consequently this remains a bounded composed admission increment, not a complete source transaction across the omitted phase. The subsequent module address is a fresh physical read in the retained module execution; arbitrary returned module/withdrawal Worlds are preserved, not framed away.

Both scalar cursors are phase inputs. Their fit is derived when their actual decoder succeeds, but no whole allocator trajectory from the gateway/outer router ABI to these cursors is claimed. The outer gateway-to-router CALL/dispatch, immutable deployment/proxy initialization, actual deployed locator/Lido getter implementations, full memory/copy/alias/gas and established SHA/root/consensus/log boundaries remain explicit. Accepted ALLOC/RESERVE guarantees are untouched.

## Exact evidence and reuse

After reading scripts and the isolated ordinary-dependency probe, I ran only `PYTHONDONTWRITEBYTECODE=1 python3 audit/topup-router-admission-call/validation/validate.py` and the default `seal.py`. Both completed exit0. The validator checks **1,361 actual source identities, 11 package pins, 39 ordinary scopes and four normal module providers**, comparing old local bodies to exact main344 and actual selected source/olean/setup/trace records. All scoped dependencies are foundations only (propext, Classical.choice, Quot.sound, or empty). No source macro/axiom/native_decide/unsafe replacement was introduced. Global29/AllTrust is not this source-only result and was not rerun.

The same comparison independently verifies28 cached full compiler inputs against accepted no-code packet/core/vendor bodies;29 fresh Forge bodies; five artifact hashes/settings/source-metadata Keccaks; exact fresh production/fixture IR; and exact selectors plus literal whole prior public conclusion. Compiler profile is solc0.8.25+b61c2a91, optimizer200, viaIR, Cancun. Cached input/output and fresh artifacts are deliberately distinguished; no cached/fresh bytecode identity or deployed-bytecode identity is credited.

Final retained normal build succeeds with1,378 jobs, actively builds the regression module and replays the three source/public modules. Earlier retained logs show active Gate, Call and public builds amid later failed downstream attempts; current recorded normal provider identities are what the comparison validates. I do not label the final log four fresh builds. Twenty-five kernel computations, an actual nonempty public consumer and public late rollback are foundation-only. Their SHA/hash fixture is the accepted structural interpretation, not independent cryptographic execution.

The author's fresh runtime log reports17 selected full-router tests with256 status fuzz runs. I read all tests and the constructor-only inherited BatchRouter/BatchModule fixture: topUp itself is unmodified pinned production source. Raw endpoints enforce actual router caller and selector; cases cover malformed/short/noncanonical/bubbled/no-code responses, static-write rejection, guard priorities, zero-target denial/success, positive-target skip and actual late module rollback. These are retained actual Solidity executions, not a new execution by this reviewer. The256 fuzz runs are not asserted to exhaust all256 status values. Complete source/IR establishes the universal enum ordering independently of sampling.

Development logs retain failed proof shapes, reserved error pattern syntax, result projection/match elaboration, public conjunction parsing and test fixture issues. The failed validator extra-EOF comparison is preserved and corrected by exact comparison, not weakening identity. Those failures and temporary failed proof reports are not accepted evidence. The final records and normal closure recomputation pass. No compiler, Forge, native FFI, module build or global test was repeated.

Prior source bdeb review is retained verbatim at SHA256 `09d45120aff8fec233ed5fbf751e83d09c9a99ffcab775347d3219aaab0fdf5c`; locator6c33 report is `1bb422a614df27c343f87446afae5e25ddbb232e895729558129058c48638c03`. I reread both full reports. Entry source/public compare byte-for-byte to bdeb; timing source/public compare to independently reviewed2efb. Existing registered import bodies compare exactly to accepted base through the validator. The inherited complete timing/physical-getter/root/module correspondence is reused with that identity and its recorded authorship qualifications, rather than claiming a new full review of all1,361 modules.

All43 changed paths are additions only, consisting of the four new modules and dossier. All42 receipt hashes match current bytes; old tracked objects/configuration/All/Trust remain unchanged. `git diff --check HEAD^ HEAD` passes without exceptions. Candidate remained clean at the exact HEAD/tree after both comparison checks. Only this external report was written.

**CLEAN at exact ed043832023c75e04adf76ea05c3ac96f3353897 for the stated bounded increment. Review complete; STOP.**
