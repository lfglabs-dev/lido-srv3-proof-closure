# Independent exact TOPUP router-admission integration review

**CLEAN — exact bounded integration.** HEAD `840b7874e80b61a5ad0923642f19eb8a54c10a49`, tree `645e8639ddf530818d649ab0aaeb499605a64bea`, clean stopped checkout `/tmp/lido-topup-router-admission-call-integrated`. Parent union `74d4ca3911b2a7d2514abefc1010c7a08073a83e` incorporates accepted main347 `6e59055f72a4da135915844f6f504b02f10c6fcb` and the already reviewed TOPUP source ed043. No blocking preservation, wiring, source-consumption, hypothesis, provider or evidence finding. Publication and any final archive gate remain separate.

I authored neither the TOPUP source nor this integration. I did author the DEPOSIT admission-error source contained in accepted347; I do not present my own reinspection as independent acceptance of that component. Its retained independent a279 source and a3c0 integration reviews were read in full and reused only with complete accepted347 object preservation. The independent a3c0 report remains SHA256 `c6c7aa91a5214b056ae1dd338fb623a24150af3f2bf56da341f846442e637b2f`. This review is independent of the new TOPUP increment and verifies the union without replacing DEPOSIT's separate independent gate.

## Complete union and wiring

Compared complete Git object maps: all5,651 paths in accepted347 remain identical except `LidoSRv3/Audit/AllGuarantees.lean` and `LidoSRv3/Audit/Trust.lean`. Their complete diffs are additive: one TOPUP public import, one TOPUP regression import, and five ordinary axiom queries. The prior DEPOSIT import and all five queries remain in place, as do every historical import/query. No theorem body, premise, accepted configuration, compiler input, receipt or other main347 file changed. The union adds the43 source paths and the final candidate adds10 integration archives,53 new paths overall.

The parent-to-candidate delta is exactly those10 integration files. All42 original source/dossier receipt hashes match current bytes and the exact ed043 Git objects; the source receipt itself is also unchanged. All9 integration receipt hashes match. The retained source review is verbatim my external ed043 report, SHA256 `d3f39eec5802dbb5e2d41d454113eea650a9c9155e4a632104bbb89a123135ed`. No rewritten subset of that semantic review is substituted.

AllGuarantees imports the complete `PTopupRouterAdmissionCall`; Trust imports its actual regression module and queries the whole new public success, whole rollback, auth origin, canDeposit origin and admitted call facts. Active import providers for both TOPUP and DEPOSIT public/test modules point to the matching current artifacts. No commented-out wiring, partial replacement of347 or custom axiom allowance was added.

## Source composition and hypotheses preserved

The four source/public/test modules and entire source dossier are identical to independently reviewed ed043. That full review read all four modules, actual source/error order, complete relevant production and fixture IR dispatches, tests and transitive consumers. Its findings apply unchanged here; no compiler-only substitution is being made.

The actual gateway role/resume, lengths/timing, locator, physical credentials and root prefix executes once. Internally constructed Ready supplies actual pubkeys/limits to router input validation and the unchanged module encoder. The actual resolved router calls its independent locator, canonical decoded gateway authorization is consumed, physical membership/enum/active/type checks precede the zero-target conditional Lido canDeposit STATICCALL, and the same suffix executes once. `walk_prior` is a proof relation, not a runtime second execution. Exact old result/World projection and the literal entire342 conjunction survive in the public theorem, together with new actual call/decoder origins and admission journal. Whole original World rollback includes late quota-independent module/withdrawal effects; no final callback frame appears.

The theorem still has only whole-run success as its new propositional premise. No successful intermediate stage, authorization equality, fit, initialization, aligned-array or cryptographic strengthening was added. Existing effects remain at the actual intermediate World before history. History retains actual produced total limits, fresh packed state and the full timestamp event. Admission attempts are separately represented alongside the retained full prior journals; this is not a new flattened whole-EVM trace claim.

The allocation phase still omits getDepositableEther and SRLib DELEGATECALL. Its arbitrary allocation value does not grant arbitrary library effects a read-only frame. Outer gateway-to-router CALL/raw ABI/nonpayable dispatch, deployment/immutable/getter implementation, full allocator/memory/gas and inherited crypto/root/consensus/log boundaries remain explicit. New local error bytes are exact; inherited allocator Panic(0x41) remains symbolic. Accepted ALLOC/RESERVE and independent DEPOSIT scope are unchanged.

## Normal providers and evidence reuse

Read the full integration wrapper before deciding not to rerun it unnecessarily. It transforms only the path-dependent normal-record writer to an external temporary record and excludes the two expressly additive aggregators from the old344 whole-tree check. All other old-body/source/provider/compiler/ordinary comparisons remain strict; configuration is explicitly compared with347. The complete main347 object comparison above independently closes the wider preservation obligation that the old344 validator alone cannot establish.

Independently recomputed the six current source/setup/trace/olean identities: four TOPUP modules and both aggregators. Normal setup options/plugins are empty; traces are nonsynthetic and contain no skipKernelTC setting. The first four full records equal the premain347 records exactly. Recomputed all1,361 actual regression import source hashes, checked the actual imported source paths and selected local oleans, and verified all11 package HEAD pins. All three configuration hashes match the accepted347 configuration records. Current source imports contain no dependency on the newly added DEPOSIT admission-error theorem, so adding that disjoint consumer changes aggregates, not TOPUP's transitive premises.

Retained premain347 log actively builds all four TOPUP modules and both aggregators and completes1,792 jobs. The final log reuses those unchanged TOPUP modules, actively builds the three DEPOSIT modules and both aggregators, and completes1,795 jobs. These are the root's retained builds, not fresh reviewer builds. The final global log records actual environment recomputation and native-claim reevaluation for29 disclosed axioms:23 test/mutant native axioms, three existing production exceptions and foundations. This is distinct from the39 ordinary foundation-only scopes of the new TOPUP source. The latter current source/provider identities and archived computed records remain exact; no global or scoped axiom probe was repeated during this integration review.

Source evidence remains25 kernel computations plus nonempty public consumer and public rollback. Retained17 fresh full-router Solidity tests include256 status fuzz samples, not a claim to exhaustive status enumeration. All28 cached compiler input bodies,29 fresh runtime inputs, five metadata-checked artifacts and full5,046-line production IR remain exact ed043 bytes and retain that review's complete source correspondence. No Forge, compiler, native FFI or unchanged module build was rerun. Development failures remain failed, with final normal/provider evidence distinguished from their intermediate logs.

A preliminary per-file main347 comparison stopped on the `lido-core` Git submodule directory because it attempted `read_bytes` on a directory. This was a read-only checker-shape error, not a candidate discrepancy or failed build. It was replaced by complete Git object-map comparison, including the unchanged gitlink, which passed; all subsequent receipt/provider checks completed successfully.

`git diff --check HEAD^ HEAD` passes. Final candidate status remains clean. Only this external report was written. **CLEAN at840b7874e80b61a5ad0923642f19eb8a54c10a49; exact integration review complete. STOP.**
