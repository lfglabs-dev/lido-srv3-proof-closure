# Independent exact-candidate review — TOPUP router continuation

Verdict: **CLEAN for the declared typed post-module continuation scope**, candidate `1ef813405ac4d238fea7565c375a313022128858`, parent `38f1ceb327053a4ce54c88f8664967421a5be951`. No blocking finding or requested source change. This is not acceptance of full StakingRouter execution, deployment provenance, or the remaining TOPUP guarantee as closed.

The reviewer did not author TopupRouterCredentials, TopupRouterContinuation, their tests, or the credentials Solidity harness. The reviewer previously authored reused accepted TOPUP withdrawal/batch consumers; those remain unchanged dependencies, not the new code under review. Review was read-only: no Lean/Forge rebuild, project edit, or Git mutation. Only this report and a companion evidence JSON were written under `/tmp`.

## Exact identity and validation evidence

- The parent-to-candidate diff contains exactly15 new files: two Lean source modules, one Lean test module, and the continuation dossier. Every candidate blob matches the full working-tree file read during review. No SSZ source, prior TOPUP module, shared foundation or metadata was modified in this diff.
- Receipt SHA256: `66b5d70cc774b4645019eb0fa18cbbff4be13381b794996847eba38939305542`. Root's sole final receipt clarification expands `coordinator_base` to `4286b2de7c5359d7550a2d4b63674e9c8ea94735`; the compiled source/test hashes remain unchanged.
- `python3 audit/topup-router-continuation/check.py` executed independently and passed88 validated input hashes, six evidence hashes, nine ordinary axiom queries and two source-order token checks. All59 prior Lido dependency inputs in the receipt were additionally byte-compared against accepted base4286. The eight relevant core files were independently byte-compared with source pin `17005714f151e5502c559932319a3f2f74ac2436`.
- The exact-source build log records `lake build LidoSRv3.Tests.TopupRouterContinuationMutants`, exit0,1272 jobs, with source dependencies reused where unchanged. It contains no warning from the three new files. The reviewer inspected and hash-bound that existing log; no fresh independent compilation is claimed.
- There are15 source theorems,37 regression examples, two substantive scenario theorems and one initial-balance fixture lemma. The nine printed declarations use only `propext`, `Classical.choice`, `Quot.sound`. New sources/tests contain no `sorry`, `admit`, new axiom declaration, `native_decide` or `bv_decide`.
- Core source hashes, candidate blobs and all15 candidate-file SHA256 values are recorded in `/tmp/lido-topup-router-continuation-independent-evidence.json`.

Main source SHA256 values:

| File | SHA256 |
|---|---|
| `LidoSRv3/Audit/Source/TopupRouterCredentials.lean` | `1917509309754b11fbeba8b70a9b73334a5ad18b3e3805bfb135287a58a4d53f` |
| `LidoSRv3/Audit/Source/TopupRouterContinuation.lean` | `496ba9905f6b5b42c854a58f88f4dbe6ea5849a4de12aba6ebb776fc642104da` |
| `LidoSRv3/Tests/TopupRouterContinuationMutants.lean` | `3566148eacf3a1deb2092a8b19c594d275ccbf087f7731139ba220a4a97eddd2` |

## Source and proof review

Read the complete new Lean source/test files, README, checker, source-check, receipt, build/axiom logs and credentials harness. Relevant unchanged definitions were examined across Live, Pipeline, TopupLiveWithdrawal, TopupBeaconBatch/FundedTx, TopupWeiBounds and TopupTx's sourceDepositInput/routerWithdrawalCredentials. The delivered concrete callee and batch guarantees are consumed within their existing documented boundaries. Source comparison includes pinned StakingRouter721–758 and1046–1049, BeaconChainDepositor66–107, SRStorage, SRTypes' layout, WithdrawalCredentials.setType and ISRBase's event declaration.

**Executable guards and amount derivation.** `guardSum` checks alignment before accessing the limits list, preserves the bounds-panic position for an overlong response, compares the corresponding limit before the modular uint256 update, and stops on the first error. It has no invented up-front equality-of-length check. `guardSum_spec` and `guardSum_run` connect this actual pure calculation to the existing allocation guards/unchecked sum. `checked_fields` derives total=sum, uint256 fit, alignment and uint64-gwei width from the same allocations and evaluated gateway limits. The `hcheck` premise is a pure checked source computation, not a successful module, callee or final-program result. No final desired balance or credit is supplied as a premise.

**Program placement.** Saved target comparison precedes withdrawal; zero total emits the actual semantic event and performs no withdrawal/helper/assertion. Positive execution snapshots the post-module router balance, runs real `withdrawDepositableEther(amount,0)`, uses the resulting world for the helper, executes the balance assertion and then appends StakingRouterETHTopUp. The model's snapshot is expressed using `before.balances` before pure guard evaluation; those guards do not modify the world, so this has the source snapshot value. The empty-key helper return and nonempty length mismatch occur after withdrawal. Invalid key width precedes zero skip, and minimum/uint64 guards precede the beacon call. The documented omission of the pre-loop dummy-signature hash failure is consistent with the total opaque SHA model.

**Current physical credentials.** The actual router account's root+4 word supplies WC. The module config slot is the opaque Keccak applied to the actual two32-byte ABI words `(moduleId, routerRoot)`. Byte29/bit232 is selected from the config; `typeOf_packed` independently isolates it from lower232 and upper16 bits. The ABI encoding roundtrip and octet bounds use the source word, not an arbitrary reader. `inputAt` ultimately uses the existing `routerWithdrawalCredentials` byte rule: current type followed by the current raw WC tail. `helper` constructs all inputs from the single post-withdrawal world, matching source's once-read WC before traversal rather than re-reading on every beacon iteration. No transport of an earlier admitted type2 through arbitrary module/callback effects is inferred.

The numeric namespace, storage positions and byte construction are supported by pinned source inspection, compiler layout and actual helper tests. The Lean adapter does not claim a universal compiled SLOAD/Keccak/bitwise-codec refinement. Keeping this distinction in the README is appropriate.

**Concrete funding, batch and assertion.** `positive_success` invokes the delivered Pipeline success theorem and the concrete withdrawal balance/callback theorem. The zero-seed arithmetic condition is derived. Code/count preservation through withdrawal is derived from actual accounting writes. The same allocation list funds and drives the actual `TopupBeaconBatch.loop`; runtime-generated source inputs use matching pubkeys, dummy signature, current credentials and amount/1e9. Initial aggregate count capacity derives every per-call insertion capacity. Pointwise withdrawal and beacon balance equations compose to the independent Lido-to-beacon transfer; the router's arbitrary old holdings are restored. That derived equality discharges the executed assertion. `Postcondition` additionally fixes count growth, every independent per-deposit BranchEffect/event step, chronological attempts and the final router event. It deliberately keeps a withdrawn world and a deposited world so the callback is not misreported as the last global call and the batch event chain ends before the final router event.

**Rollback and regression quality.** `failure_restores` inherits the actual outer Live.run boundary and restores all components of the initial suffix World. The substantive `late_execution` test first derives actual source-beacon success, then reaches the helper's minimum failure on an aligned one-gwei second allocation after allocation guards passed. It proves full restored balances/storage/logs with the real withdrawal/first-deposit attempts retained. The initial ModuleEffect log survives zero success and is the rollback baseline. Other regressions check first-error priority, unchecked update, over-target before calls, withdrawal failure before helper mismatch, key-before-zero, empty helper followed by assertion failure, count/conservation/event placement and bounded mass.

`current_words_execute` uses a clearly identified synthetic effectful external interpreter that changes WC/config during withdrawal. Its concrete emitted beacon payload contains the later type1 and low byte99. This is a meaningful read-after-call ordering regression; the report does not confuse it with the delivered receiver or a reachable deployed reentry scenario.

## Solidity evidence and boundaries

Read the full root-authored harness and its frozen receipt. It executes actual SRStorage/WithdrawalCredentials helpers with solc0.8.25; four tests pass, two with1024 fuzz runs, all256 type bytes and repeated current-word reads. The separately reviewed compiler layout identifies mapping at root+0, WC at root+4 and packed type at byte29. The harness's slot-zero RouterState is only a type-layout witness; actual reads use the namespaced getter. The candidate harness SHA matches the independent preparatory review at `/tmp/lido-router-credentials-harness-prereview.md`. The earlier archive/extracted OpenZeppelin comparison is recorded in that prereview and source receipt; this reviewer did not rerun extraction or compilation.

These tests do not execute the full router continuation or mutate storage during an actual withdrawal. Event/revert full ABI, module return ABI and execution, saved target provenance, earlier gateway/module preamble, raw memory, compiler/EVM/gas/SHA behavior and physical deployed identities remain open. The theorem also retains genuine helper success conditions (matching lengths,48-byte keys, nonzero minimum), Pipeline physical admission/funds and initial beacon capacity. These are not hidden in an arbitrary callee-success/frame hypothesis.

Conservation is relative to the **post-module** world, not original transaction entry. Therefore full-parent rollback of the earlier module call and transport of earlier invariants remain separate obligations. `postcondition_bounded` preserves a supplied independent finite-support asset bound from derived conservation; it does not derive reachability or intermediate EVM balance representation from the uint256 sum bound. `A-TOPUP-BEACON-ADDRESS` remains explicitly open.

No material mismatch was found between the scoped claims, exact source order, theorem premises/conclusions, executed test evidence and documented remaining boundaries. The exact candidate is suitable for integration under those boundaries.
