# Independent exact integration review: TOPUP credentials

**CLEAN** for the bounded integration at
`f4b4c07c5af3c70e18e08ae60401d479aa75f3f8`, tree
`1c255afb379792207e01de1de300763a74f5c1ce`.
Parent union `1c1636f5509d01b030a1a74dabd812b6f90bfccd` includes accepted main
`ae30a086154dc78196034657953b9a09cc75b48b` and independently reviewed source
`f00863276fbe279c566328ed4ae90f5bf223a38e`.
No blocking finding. Writer was stopped; reviewer made no candidate mutation,
Git change, build, Forge/native execution or browser call.

## Tree, wiring and retained claim

- All 28 files of the reviewed source increment (three Lean files and complete
  original dossier) are byte/blob identical to f008. All 27 original receipt
  hashes match exact candidate Git objects and working bytes, including full
  Solidity input/output/IR and the prior source validation artifacts.
- The current integration delta is exactly two additive wiring edits plus eight
  new integration files. AllGuarantees adds the real public import at line1;
  Trust adds the real regression import at line1 and four active queries at
  lines789–792 (public success, public rollback, lookup origin, whole public
  instance). Existing imports, assertions and queries are preserved. Existing
  Lake providers already cover Source/Guarantees/Tests; no provider, package,
  toolchain, baseline or shared theorem change was needed.
- Every file on accepted main331 is preserved exactly except those two additive
  wiring edits. The reviewed f008 source and main's other guarantees coexist in
  the exact union; no older claim or proof input was replaced by an archive.
- Both public statements and the consumed source composition remain precisely
  as reviewed: actual getter request/raw reply → first Word/prefix02 → resolved
  credentials in the same witness/root loop → same actual module/memory runner
  and complete old Success/Effects/cursor conjunction. The result equality
  binds the same world and journals; no unused adapter, successful-stage/frame/
  fit/sum/funding/alignment hypothesis was introduced during integration.
- The arbitrary getter body, supplied gateway/router roles and locator binding,
  actual gateway→router topUp call/final history writes, ordinary-no-code versus
  precompile restriction, initial cursor provenance/relationship, compiled
  memory/copy/alias/gas and inherited typed-root/hash/callback boundaries remain
  explicit. This still does not prove the router storage-backed getter or
  equality with credentials reread after an arbitrary later module/callback.
  The separate physical-getter work receives no credit.

## Exact evidence and reuse

All seven integration receipt hashes match exact Git objects and current bytes.
The archived f008 independent review is byte-identical to my external report.
The 25 scoped axiom sets are byte-identical to the source archive and contain
only the ordinary foundational axioms in their recorded scopes.

I independently checked current hashes of all 1343 recorded imported/new source
bodies and the 11 package Git pins. The 145 selected local import source/artifact
pairs match the actual registered import paths. All three new normal module
source, olean, setup and trace hashes match the integration-specific identities;
setups have no nonstandard options/plugins and traces are nonsynthetic with no
skipKernelTC. AllGuarantees and Trust's current registered imports contain the
new actual public and regression modules. Path-dependent setup/trace hashes are
correctly retained separately rather than overwriting the source archive.

The complete normal integration log records **1745 jobs PASS**, including active
new Source1.9s, public1.5s, tests5.5s, AllGuarantees2.2s and Trust2.1s. The four added
queries appear in the actual Trust output. The global log records **29 exact
existing axioms** with the disclosed 23 native-test, three production and three
foundational entries; it is not mislabeled as an entirely foundation-only global
environment. The new increment's separate 25 scopes remain ordinary. Root's
identity report records the comparison-only source validator's 1343/11/25/22/3
PASS and 27 source hashes. Its initially wrong postcheck field name was fixed
only for that hash postcheck; no passing build or scope result was manufactured
from the earlier typo.

The build union `b07663267fea4b1970aebd23e85c29b3ec318872` differs from the final
union solely by these ADDRESS331 documentary changes:

- added `audit/address-wrapped-transfer-call/integration/independent-integration-review-13c6.md`;
- modified `audit/address-wrapped-transfer-call/integration/receipt.json`.

No source or build input changed in that transport. Current additive wiring is
also the wiring represented by the normal All/Trust build and query traces.
The successful build/global/scoped results are therefore reused by input
identity; repeating them merely for this documentary union would add no new
validation. No new Forge runtime or native diagnostics are claimed here.
The complete original 22-input compiler/IR source evidence and five independent
FFI diagnostics remain exactly the already reviewed evidence at their original
scope, including the historical seven-test Solidity fixture qualification.

`git diff --check` for this integration delta passes. The sole inherited raw IR
blank-EOF exception is unchanged. Checkout remained clean at the exact frozen
HEAD after all read-only identity checks.

Independent evidence: `/tmp/lido-topup-credential-f4b4-independent-evidence.json`.
CLEAN applies to this exact integration and its scoped claims, not complete TOPUP
delivery or deployed gateway execution. Reviewer STOP; no pending mutation.
