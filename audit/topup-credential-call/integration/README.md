# Integrated TOPUP credential reply consumer

Source `f00863276fbe279c566328ed4ae90f5bf223a38e` is independently reviewed CLEAN for its complete source/IR correspondence. Final union `1c1636f5509d01b030a1a74dabd812b6f90bfccd` includes that source and integrated ADDRESS331 main `ae30a086154dc78196034657953b9a09cc75b48b`. All three new Lean bodies and their complete source dossier remain unchanged. Root adds only AllGuarantees/Trust wiring and this archive.

The public successful runner derives the actual gateway-to-router credentials request, raw bytes32 reply, allocator bounds and prefix02 check. Those same credentials replace the environment word consumed by the existing actual roots, ordered witnesses, module request/return and physical continuation. Its exact result equation and entire preceding public conjunction preserve the same worlds, journals, cap/sum, zero/positive effects and both module scalar memory allocations. The public failure theorem retains the incoming World even after a successful module and late memory error. No stage-success, fit, sum, funding, alignment or frame premise is added.

The getter's storage body, role/pause/timing/locator admission, outer gateway-to-router topUp CALL and final history writes remain outside this increment. Separate scalar cursor inputs do not establish full memory provenance, copies, aliasing or gas. Initial role/configuration, typed external/SHA and physical hash boundaries retain their previous scope. This is actual reply consumption, not deployed getter authentication or complete compiled gateway execution.

Fresh normal AllGuarantees/Trust build:1745 jobs PASS. Fresh global dependency inventory:29 exact existing axioms, including the disclosed native-test and production exceptions. The source validator independently recomputed25 ordinary scopes and checked1343 imported/new source identities,11 package pins,22 exact original compiler inputs and3 normal modules. It ran in comparison-only mode except that path-dependent build-identities were recorded outside the checkout and retained here. All27 source receipt SHA256 entries were checked, and actual normal olean hashes match the recorded integration artifacts. The first post-validation receipt check used a nonexistent hashes field; correcting that to sha256 required only the hash postcheck, not repeating successful scopes/build/global validation. No source receipt was overwritten.

The build/global/validation preceded the final union's archive-only ADDRESS331 receipt additions: exact Git comparison against build union b07663267fea4b1970aebd23e85c29b3ec318872 shows only that prior independent review and its receipt changed. All source and build inputs, including the two current wiring edits, are identical, so those successful checks are reused without rerunning.

The complete source review is retained verbatim. Source17 named kernel regressions include actual successful batch and a full public consumer instance, using an explicitly structural SHA fixture. Five separate native FFI diagnostics use independently built SHA trees; they are not kernel proofs. Fresh source compiler IR contains2519 lines from22 original inputs at0.8.25 viaIR200 Cancun; no new Forge run is claimed. Historical seven Solidity tests and three1024-case fuzz properties retain only their original scope. Original IR blank EOF is retained unchanged; nonraw source and integration whitespace checks pass. Independent exact integration review follows after writers stop; later physical getter work receives no credit here.
