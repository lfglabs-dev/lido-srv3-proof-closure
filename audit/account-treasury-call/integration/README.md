# Actual ACCOUNT treasury call integration

Source `a7ea281e72298cc09239ed8a5e94f92eae2d03ce` is united with main `10e2b693d00cbbeab65f60cd8964ced87297336a` at `0e9116c415e687dd9b322549bfdcbe88fa86500d`. Four new Lean bodies and all 37 imported source identities are unchanged. The existing AccountAddressChecks provider now includes TreasuryCall, ReportFeeTreasuryCall and its regression module; AllGuarantees imports the public consumer, and Trust imports its tests and prints both public theorems, call_success and distribute_success. No existing executor or domain changes.

The actual report and fee mint feed ordered module transfers. The positive treasury branch makes the real typed readonly treasury request on the same returned ACCOUNT World, decodes its actual returned bytes and pays that actual recipient. The public success theorem preserves the accepted report/mint/distribution Success and pointwise Ledger, with the precise call World, arguments, bytes, payments, events and attempts. Its failure theorem restores the entire incoming ACCOUNT World. Zero mint and zero treasury skip the corresponding call. Caller width follows from the successful transfer rather than a new premise. Code presence, canonical return-address guards and transfer guards are executed, not assumed. The former supplied TreasuryRead is now fixed to this actual call in the projection proof; that proof does not execute an additional call.

The supplied immutable locator/code metadata and arbitrary readonly external retain explicit context boundaries. The ACCOUNT shares map remains abstract, and earlier locatorAccounting authorization is separate. Scalar returned-byte guards do not establish allocator/copy/pointer or full compiled report execution. Complete report callbacks and deployment provenance remain outside this increment; compiler, declared Verity, cryptographic, gas and consensus boundaries are unchanged.

Combined normal AllGuarantees/Trust passes 1,716 jobs. Fresh global checking recomputes dependencies and re-evaluates native claims: 29 previously disclosed axioms, not globally foundations-only. Fresh normal-environment queries reproduce all 18 candidate foundation-only axiom sets. Root verifies the 37 actual source hashes, 11 package pins and 28 candidate hashes; identities.json records four normal olean hashes without asserting cross-worktree olean byte identity. The exact source review independently covers complete relevant Solidity bodies/assembly, consumed constants/selectors and same-World composition, 12 kernel regressions, five complete pinned source bodies, 29 retained dependencies, 32 compiler inputs and four compiled artifacts.

Nine unchanged author-run Solidity tests are reused after source/artifact identity checks and independent review; no fresh root Forge run is claimed. They exercise the actual inherited StETH and exact Accounting distribution fragment. Fixture profile is solc 0.8.9 plus StETH 0.4.24, optimizer200, Byzantium; it is not production bytecode equivalence. Raw DistributionHarness.asm preserves the exact compiler stdout extra EOF newline; the single diff-check formatting exception has no source difference. The candidate packet's pending-review text is historical and superseded by the retained exact source review. A separate frozen integration review is required before merge. No site changes or publication occur here.
