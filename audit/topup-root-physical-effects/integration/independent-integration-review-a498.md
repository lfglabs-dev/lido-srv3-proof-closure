# Independent TOPUP physical-effects integration review

**CLEAN — exact frozen `a4987bacc69f9dfce7623c44667bd636aef039c8`.** No unresolved integration, source identity, provider, scope or receipt issue. Checkout `/tmp/lido-topup-effects-integrated` is clean; root integration writer stopped before review. Date 2026-09-10. Reviewer authored neither this source increment nor integration; reviewer-authored prior #315 substrate is disclosed in the retained independent source review.

## Full union and source preservation

Verified reviewed source `c0e2fe625f423c3f9b804b64e65d635c5af6d896` and accepted main `70301660264f2f710e7160e190aaa3af330e84fa` are both ancestors. Merge union is `a34d355da0b78b7add2ee62dbb05aef59b5c4188`.

Compared the entire accepted-main Git tree. Every existing object is preserved except the two intended additive wiring files AllGuarantees.lean and Trust.lean. All ACCOUNT #317, TOPUP #316 and earlier executables, public statements, tests, manifests, package/Lake wiring, documentation and prior evidence remain unchanged. All three new Lean bodies are exactly those independently reviewed at c0e2. The source review archived in this integration is byte-for-byte the original external report.

The complete integration delta adds only the new public PTopup1RootCalls import to AllGuarantees, the new regression import plus six active axiom queries to Trust, and the integration dossier. No executor or proof premise changes in integration.

## Actual providers and consumer

Inspected actual setup import artifacts for the source composition, public theorem and regression. All imported source bodies match the independently reviewed three-target source packet. AllGuarantees and Trust resolve PTopup1RootCalls, TopupRootCallEffects, TopupBatchRootCalls, TopupModuleCall and TopupRouterCommitted to this local integration build. No stale alternate provider or duplicate package is introduced. Setup options are empty; facade/Trust build traces use normal compilation without a kernel bypass.

Trust's new regression import transitively imports TopupModuleCallMutants as well as the existing actual-root regression. The README accurately distinguishes this new transitive consumption from its previously separate validation. The build log also shows the module-call and physical-continuation mutant modules built on this combined graph.

The public theorem remains the exact reviewed necessary-success statement. It relates real root-authenticated rows to the exact module input and reply, derives the mathematical decoded sum from actual guards and evaluated-limit bounds, and ties that same post-module World to the actual zero or PositiveEffects suffix. No count, width, sum, array, stage-success, module/withdrawal frame or locator-preservation premise is introduced.

Physical helper balances remain relative to the actual post-withdrawal World; router restoration is relative to the post-module World. Slot32 is the actual modeled deposit-contract count; helper amount/count preserve empty-key behavior. The integration does not expand these into conservation from an arbitrary pre-module World or complete deployed execution.

## Receipts and checks

Independently checked all 13 source-packet hashes, all four integration hashes, all 1,288 reviewed source-body hashes and all 11 actual package HEAD pins. Verified the three new source bodies against c0e2 and the archived review against the external original. Rechecked all 45 historical Solidity compiler/source/fixture identities and the three historical receipt hashes at the retained core pin; no Solidity execution was performed or credited.

Reused the retained normal `lake build LidoSRv3.Audit.AllGuarantees LidoSRv3.Audit.Trust` result: PASS, 1,694 jobs. The log contains successful new composition/public/regression/facade/Trust builds and the two now-transitive physical/module mutant builds. No error or kernel-bypass evidence appears. The receipt records a fresh normal trust gate with environment dependencies recomputed and disclosed native claims re-evaluated.

Independently parsed and reconciled the full retained Trust reports with the unchanged checker, allowlist and active source queries: exactly 29 global disclosed axioms; all active printed names represented; exactly six new active queries, each foundation-only. Global 29 still consists of the existing 23 test/mutant native-decision dependencies, three production exceptions and three foundations. It is not described as globally foundations-only. The independently checked c0e2 packet remains 14 scoped foundation-only closures and 12 named regressions including two public instances.

No redundant source build, kernel replay, full trust reevaluation or Solidity run was made during integration review. The earlier independent c0e2 source checks remain valid by exact source/dependency identity; the root's combined build and fresh trust receipts supply the integration evidence. `git diff --check` passed. Final HEAD is exactly a4987bac and the worktree is clean.

## Scope and disposition

Read the complete integration README and receipt. Their claims preserve the source-review boundaries: typed covered phase, ordinary non-precompile call interpretation, explicit arbitrary module/withdrawal interpreters, omitted admission/locator/full ABI/gas/compiler/consensus refinements, retained opaque SHA/crypto scope and diagnostic attempt types. Historical Solidity identities are reused; no new compiled composition or runtime run is claimed. The source dossier's pending status is correctly identified as historical to c0e2, while this integration has its separate review gate.

No candidate changes requested. Root owns archival, PR/merge, any site publication and later modified bytes. This review does not approve adding this unmerged lot to the separately pinned site candidate. Reviewer made no candidate edits and is stopped after this report.
