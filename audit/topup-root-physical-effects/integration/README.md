# Actual root batch physical-effects integration

The reviewed c0e2 source is combined with accepted main70301660 (including ACCOUNT317). Its three new Lean bodies and 11 pins are unchanged; all inherited executables and public propositions are preserved. AllGuarantees now imports PTopup1RootCalls; Trust imports the actual regression module and queries the public consumer, source composition and four zero/positive/rollback results. That import also now consumes TopupModuleCallMutants, previously separately validated.

The combined normal AllGuarantees/Trust build passes 1694 jobs. The fresh trust gate recomputes dependencies and re-evaluates disclosed native claims: exactly the existing 29 global axioms. All six new queried closures are foundational only; the global gate retains its existing disclosed production/test exceptions. The unchanged source review independently checked all three modules with normal kernel, all 12 named regressions (including two public instances), 1288 source identities, 11 pins, 14 scoped axiom closures, 13 artifact hashes and 45 inherited Solidity identities. No new Solidity runtime execution is credited.

The same actual root-authenticated rows generate the module arguments. Its actual decoded allocations have an exact sum derived from guards and limit bounds, and its actual returned world feeds the zero or physical positive suffix. The physical deposit count is slot32; helper amounts/counts retain their actual empty-key behavior. Arbitrary module/withdrawal effects remain explicit: no pre-module ledger or locator frame is asserted. Existing admission/full ABI/compiled-entry boundaries retain their scope. The candidate dossier's pending integration status is historical to c0e2; this directory supplies the combined evidence. Independent review of the frozen integration is a separate gate.
