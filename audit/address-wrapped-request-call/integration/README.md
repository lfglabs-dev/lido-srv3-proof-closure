# Wrapped withdrawal request integration

Source `19c52ff56f40e374b568eba38658c15067f76abe` is united with accepted DEPOSIT326 main `b77355c3245910348f8eb46f46ba47d88e225612` at union `751500a2f558a63bd8905bd4b8db79ddad71ba5b`. The source’s three new Lean files, all 46 candidate hashes and 1,246 actual source identities are unchanged. The union preserves the complete corrected ADDRESS325 evidence, ACCOUNT324 and DEPOSIT326. AllGuarantees/Trust add only the new public/regression imports and four queries. No executor, old domain, provider or dependency pin changes.

The public consumer executes the same WSTETH transferFrom and unwrap CALL on their actual returned worlds, then applies the amount guards to the actual decoded unwrap word, calls STETH getSharesByPooledEth with that amount and passes the same world/values into the existing physical enqueue. It makes no extra STETH transferFrom. Canonical false bool and trailing return bytes are accepted as the pinned caller permits. Amount fit is derived; shares truncate modulo2^128. Actual nested attempts and ordered physical queue writes/events are retained, and root failure restores both callees’ effects. The caller adds no nonzero wrapped-input guard. No supplied successful stage, returned payload, frame, share-fit or nonalias premise is introduced.

The declared result concerns WithdrawalQueue calling arbitrary token implementations through its existing typed interfaces. Actual WstETH burn/balances/allowances, token deployment authenticity, full outer pause/permit/batch/dispatcher, memory allocation/copy, raw custom-error/LOG encoding and whole EVM static semantics remain outside. The existing queue storage lens and crypto/gas/runtime boundaries are unchanged. No unused standalone unwrap adapter substitutes for the real caller path.

Normal combined AllGuarantees/Trust passes 1,725 jobs. Fresh global checking recomputes dependencies and re-evaluates native claims, retaining the same 29 disclosed axioms; the global environment is not foundations-only. Five fresh normal-environment scopes are foundations-only. Root verifies 1,246 imported source hashes, eleven package pins and 46 candidate hashes, and records three normal olean hashes without assuming cross-worktree byte identity. The exact independent source review freshly re-elaborates all three modules and reviews 18 kernel regressions/public rollback; four full positive/late-rejection checks are explicitly FFI diagnostics rather than kernel success proofs.

Nine unchanged source-run Solidity tests including 1,024 fuzz cases are reused by identity after independent complete source/legacy-assembly review. Four source preimages, three selectors, sixteen original dependencies and seventeen actual compiler inputs were independently checked. The complete WithdrawalQueue harness executes the actual wrapped internal request; token doubles make no real-token proof claim. Fixture solc0.8.9 optimizer200 London is distinct from production Istanbul. Raw WrappedRequestHarness.asm retains its byte-exact compiler-stdout blank EOF as the sole new formatting exception; inherited raw-output exceptions remain. No new integration Forge or FFI run is credited.

The source writer stopped before the retained independent source review; root stops before a separate exact integration review. No site, deployment or external publication changes are included. The hourly site snapshot frozen at DEPOSIT326 intentionally precedes this increment and does not credit it.
