# Independent ADDRESS wrapped-request source review

Verdict: **CLEAN**, with the explicitly documented raw assembly stdout EOF whitespace exception. No proof/source-correspondence blocker found.

Exact frozen candidate: `19c52ff56f40e374b568eba38658c15067f76abe`, tree `dbadf5822146392b1dbf27734612b294d981f607`, checkout `/tmp/lido-address-wrapped-request`. Parent: `f9f5639bdcc30a5f653fe679c9caba306a84df19`. The writer was stopped. I authored none of this increment and made no checkout changes. This is source-candidate review, not global integration, deployed-bytecode certification, publication or merge approval for some different tree.

## Exact scope and preservation

The complete recursive Git tree comparison preserves every one of the parent's 4,344 tracked objects, including mode and blob identity; exactly 47 files are added: three Lean modules and the new audit packet. All 46 receipt hashes independently match current bytes. Existing `AddressRequestCalls`, its public consumer and regressions, and the canonically repaired `AddressRecipientCallBridge` are byte-identical to the previously reviewed f9f5 sources. The prior f9f5 documentation attribution reservation was resolved in the later accepted request integration; this new source packet does not rely on the rejected pre-repair constants or claim that reservation established more proof coverage.

I read all three new Lean files, the complete new harness and token doubles/tests, packet README and validation/diagnostic/slot scripts, compiler settings and receipts, and inspected the actual wrapped caller's complete legacy assembly path, decoders, admission helper, enqueue writes, checked arithmetic and owner-set insertion. The inherited request/enqueue and call/quote definitions were revisited; the broader source and physical helper review is reused only after exact identity to f9f5. The seven pinned core and nine OpenZeppelin source bodies are unchanged from the already reviewed original queue closure and independently verified against their origins again.

## Actual consumer and execution semantics

`actual_wrapped_request_withdrawal_enqueue` assumes only success of the complete new `runRequest`. Its conclusion consumes `AddressWrappedRequestCalls.RequestEffect`, whose witnesses are extracted from actual execution by bind inversion, not supplied stage successes. It joins the same first transfer CALL world, second unwrap CALL world, STATIC quote reply, and physical enqueue result, and derives chronological `ta ++ ua ++ qa` attempts. Both mutable CALLs retain actual nested attempts. Enqueue adds no external call.

The first request has caller `ctx.self`, target `wstETH`, zero value, and transferFrom selector plus `ctx.sender`, `ctx.self`, original uint256 wrapped amount. Its decoder checks minimum 32 bytes and canonical 0/1; false is accepted and trailing bytes are allowed. The second CALL targets the same explicit WSTETH address on the first returned world with `unwrap(uint256)` and the same original amount. Its first uint256 word is decoded from actual returndata of at least 32 bytes. There is no second STETH transferFrom or use of the older standalone unwrap/burn adapter.

Only the decoded unwrap amount is admitted by the lower 100 and upper 1000*10^18 guards, in that order and after both CALLs. That same amount feeds STETH `getSharesByPooledEth`, the uint128 cast, physical enqueue and event. Amount fit is derived from the admitted upper bound; returned shares are explicitly truncated modulo 2^128. The wrapped input is not silently narrowed or given an invented nonzero guard. Code existence is checked separately at each call on its then-current world, so arbitrary previous call effects are actually consumed. Rejected CALL bytes and decoder errors remain distinguishable modeled faults.

The inherited physical suffix reads the actual post-unwrap queue state: checked cumulative shares first, cumulative stETH second, then checked uint256 ID increment. It writes the last ID before reading the report timestamp, writes packed amounts before reading/updating metadata, preserves the high unused metadata byte, and uses the existing exact wrapping mapping/array helpers. Owner resolution uses the stable typed caller; entry block TIMESTAMP is captured before either arbitrary returned model environment can change it. This is consistent with the transaction-constant EVM opcode, not a callee-frame premise. OZ insertion checks absence, writes the wrapped length, writes the cell at the old length, then freshly reads the length for the index value. It has no invented 2^64 limit, no nonwrapping premise, and no assumption excluding storage collisions. The conclusion is the ordered state expression rather than unjustified final re-reads of potentially aliased slots. WithdrawalRequested then Transfer follow insertion on the same world. Root failure restores the complete entry world while retaining attempts as observations.

## Pinned Solidity and actual compiler evidence

Core pin `17005714f151e5502c559932319a3f2f74ac2436` was checked via local Git objects. WithdrawalQueue.sol 383–392 is exactly WSTETH transferFrom, WSTETH unwrap, decoded-amount admission, STETH quote, uint128 casts/enqueue, Transfer. The outer wrapped wrapper supplies owner fallback but no wrapped-input amount guard. Actual WstETH.sol 69–75 separately checks nonzero, obtains a conversion, burns and transfers; those implementation effects are intentionally outside this caller theorem.

The fresh fixture subclasses unchanged WithdrawalQueue and invokes `_requestWithdrawalWstETH`; setup/seed/read helpers and both token doubles are explicit test machinery. I independently checked the npm tarball SHA512 integrity and all nine vendored files, all seven core files against pinned Git, all 17 actual compiler metadata source Keccak values against bytes, and the complete archived compiler artifact against the existing Forge output artifact. Archived assembly is exactly `artifact.assembly + newline`, including its raw EOF.

The actual legacy assembly `tag_291` uses CALL/CALL/STATICCALL with the consumed addresses and payloads, code checks and failure returndata bubbling. `tag_654` validates minimum length and canonical bool through `tag_983`, and its caller pops the bool. `tag_660` decodes the first uint256 word without an exact-length demand. The signed buffer-distance checks and returndata allocation belong to the explicitly excluded raw-memory execution boundary; the Lean byte-list decoder is not advertised as a proof of arbitrary EVM pointer arithmetic or gas. `tag_533` checks low then high. The enqueue path `tag_668` and add helper `tag_883` match the guards, packed masks, write/read order and wrapping insertion described above.

Four consumed physical bases and all three selectors were independently recomputed by the inspected read-only slot script and matched the retained JSON plus actual assembly. In particular the canonical repaired last-ID/report bases remain in use. Fixture metadata is solc 0.8.9+e5eed63a, optimizer 200, legacy, London. The pinned repository's production 0.8.9 profile is Istanbul; that difference is explicitly disclosed and no production artifact identity is credited.

## Validation reviewed and independently checked

- All current new source/artifact SHA256 identities in `build-identities.json` match the actual cache; ordinary setup options are empty, plugins empty, traces nonsynthetic. No skipKernelTC or admitted proof is used by the accepted new source.
- Independently ran ordinary `lake env lean` on each of the three new modules without output-artifact flags. All passed, including 18 kernel examples and the public rollback instance. No full Lake rebuild was repeated.
- Inspected `validate.py` before execution, then intercepted its three audit output writes and compared proposed bytes with existing files. The completed read-only run checked 1,246 actual setup-import sources, 11 package pins, actual selected old local artifacts and unchanged old sources, and freshly computed all five named ordinary axiom closures. Outputs were byte-identical. Only propext/Classical.choice/Quot.sound occur; unwrap_success uses propext/Quot.sound. The first interception attempt stopped because it also intercepted the checker’s temporary names file; it made no checkout change and was superseded by the completed run allowing only external temporary files.
- Retained normal source build 1,261 jobs and final targeted build 1,263 jobs agree with traces and logs. Development error logs are explicitly historical failed attempts, including their temporary sorryAx report; they are not accepted validation. Final source checks, fresh ordinary compiles and fresh scoped queries pass.
- Reviewed the four complete native diagnostics and their recorded fresh-dylib commands/runtime identities. They cover successful physical execution/event consumption, entry TIMESTAMP, successful zero wrapped input under an arbitrary callee, and late insertion rollback. These are executable diagnostics, not positive kernel theorems. I did not rerun or newly credit FFI execution.
- Reviewed the nine retained actual Solidity tests including one 1,024-run fuzz test, their full source and final passing log. They exercise both post-call mutations, exact original/decoded amounts, canonical false/trailing replies, decoder and amount failure ordering, bubbling, STATIC write failure, late owner failure, fallback and shares truncation. State counters observed after revert establish rollback, not independently that earlier calls were never attempted. I reused this expensive validated evidence and did not claim a fresh Forge run.

Unqualified `git diff --check parent HEAD` exits 2 solely for `solidity/WrappedRequestHarness.asm:13609`, a new blank line at EOF in preserved raw stdout. Excluding exactly that file, the complete source/nonraw delta passes. This is disclosed by README and receipt and confirmed by exact assembly/artifact identity, not a blanket diff-check PASS.

## Boundaries and final state

The theorem covers a typed one-item queue caller with arbitrary external interpreters, designated addresses and the existing queue storage lens. It does not establish real WstETH balances, allowances, burn, token conservation, immutable provenance, full pause/permit/batch/dispatcher/ABI allocation, raw custom-error arguments/LOG encoding, arbitrary EVM static execution, gas or compiler/runtime equivalence. StaticExternal has no returned mutable world. Existing Keccak/model trust limits remain; no new frame, collision-exclusion, funded-caller, successful-stage or shares-fit hypothesis is introduced. Global All/Trust wiring and full repository scope remain for subsequent integration review.

Final checkout remained clean at the exact candidate. No source, package, receipt, site, branch publication or GitHub mutation was performed. **Reviewer STOP.**
