# Independent review: concrete funded TOPUP beacon suffix

Verdict: **CLEAN** for exact candidate `c806f641b25db6605ad7b18d596c9e102689cd66`, parent `3ee6957cc845fb8886227f3dc62fca1ef033078d`. No blocking or nonblocking findings were identified within the stated typed source-suffix scope. This is an independent review; the reviewer did not author the TOPUP changes. No build, project edit, commit, push or merge was performed during review.

## Exact scope and evidence

The candidate adds exactly nine files: `LidoSRv3/Audit/Source/TopupBeaconBatch.lean`, `LidoSRv3/Audit/Verity/TopupBeaconFundedTx.lean`, `LidoSRv3/Tests/TopupBeaconFundedMutants.lean`, and six files under `audit/topup-beacon-funded/` (README, receipt, source check, input hashes and two logs). No existing source, tests, common metadata, DEPOSIT file, registered TopupTx or SSZ file is modified.

I read the three new Lean files completely, their principal concrete dependencies (TopupBeaconCallee/Effects, TopupLiveWithdrawal, TopupFundedSource, Pipeline, Live, CallData, BalanceSpec, and the relevant TopupTx input construction), and the actual pinned Solidity paths. All nine reviewed workspace files were then checked byte-for-byte against the exact candidate Git blobs. The 57 imported local Lido dependency files listed by the source check are byte-identical to accepted callee merge `8ef973fa37997e55061a95fce1a41bf3cfe968e7`.

All **78 hashes** in the receipt verify: 73 validated inputs and five documentary evidence files. Verification used 72 candidate Git blobs, four Lido source blobs at `17005714f151e5502c559932319a3f2f74ac2436`, and two Verity blobs at `e977aaad6e1a9e92e0132d41b3d33a14135a4d46`. The validated-inputs text list equals the receipt map. The candidate's Lido gitlink and lake manifest agree with those pins. Static source token ordering was compared with the actual Solidity, rather than treated as a semantic proof.

The recorded targeted command is `lake build LidoSRv3.Tests.TopupBeaconFundedMutants`. Its exact committed log ends with successful completion of 1270 jobs and has no warning from the three new files; old dependency warnings are visibly replayed. There are 11 source theorems, 22 regression examples, the substantive `late_execution` theorem, four fixture lemmas and nine axiom queries. The axiom log records only `propext`, `Classical.choice` and `Quot.sound` (gateway arithmetic needs only the first and third). No new axiom declaration, sorry/admit or native checker shortcut is introduced. No heavy build was repeated for this review. No fresh Forge validation of this composed suffix is claimed; inherited PR271 Solidity evidence is explicitly limited to the callee.

## Semantic checks

The executable program starts with the actual wrapped allocation sum and uses the same `call.moduleReturndata` for the Lido pull and every later deposit. Source deposit inputs derive the actual public keys, typed withdrawal credentials, zero signature, amount in gwei and computed root; payloads are serialized through the accepted canonical ABI implementation. The new batch invokes the concrete beacon dispatcher through `CallData.invoke`. It does not execute the earlier beacon stub loop or obtain balances from a caller projection.

`loop_success` proves all calls succeed from paired source admission, initial funds and one global capacity condition, `initialCount + nonzeroCount allocations <= 2^32 - 1`. The induction derives each current CALL's funds and strict count guard, invokes the real deposit insertion result, and carries the remaining funds/capacity into the next call. It derives code preservation from the concrete storage writes. It does not take per-call success, root agreement, final balances or a callee frame as assumptions. The independent `Step`/`Effects` specification retains count increments, semantic event appends and the concrete first-odd-quotient branch effect for every intermediate world.

`positive_conservation` obtains the actual zero-seed withdrawal through Pipeline and TopupLiveWithdrawal. Physical accounting writes preserve the beacon count and code. The resulting router balance funds the batch in the very same `Live.World`; it may contain arbitrary old router holdings. The postcondition derives exact Lido debit, restoration of the router's old balance, beacon credit, the other-account frame and pointwise aggregate `CallSpec.Balances`. These are not supplied as desired outputs. The independent initial `BalanceSpec.Bounded` premise yields final finite-support mass/bound preservation. The separate `gateway_amount_exact` lemma derives a uint256-fitting allocation sum from actual gateway/cardinality/allocation bounds; it neither invents an initial asset invariant nor bounds arbitrary old balances.

The journal is chronological: physical withdrawal calls and its value callback precede actual source-byte beacon calls, with zero allocations skipped. The callback predicate is attached to the withdrawn intermediate world, not incorrectly asserted to be the final global call. The late-failure regression derives one actual successful beacon insertion and then the actual alignment failure of a second call. Outer `Live.run` restores the complete initial world, including balances/storage/logs, while retaining the callback, accepted first attempt and rejected second attempt. Failure is not injected by a caller-supplied success/failure oracle.

Regression checks also distinguish missing/doubled beacon credit, arbitrary old router and beacon balances, actual key bytes in calldata, key-width-before-zero ordering, minimum and uint64 bounds, wrapped-zero behavior, Lido underfunding, malformed internal length pairs and final total mass.

## Limits preserved in the candidate

The theorem covers a new source suffix after the gateway/module preamble, under `SourceTopupCallWellFormed`; it does not establish module ownership, dispatch, target acceptance, a complete external topUp transaction or replacement of the registered implementations. Paired-list well-formedness is stronger than arbitrary malformed helper inputs: Solidity returns early for empty pubkeys before checking amount-list length, whereas the internal traversal has a defensive mismatch error. The README explicitly confines correspondence accordingly.

The late alignment failure deliberately bypasses the earlier StakingRouter alignment guard and is not presented as a reachable full-entry transaction. The positive theorem retains source admission, checked withdrawal arithmetic, actual funding, distinct physical addresses and global capacity as explicit premises. Mathematical sum fit is distinct from initial finite-support balance bounds; Nat ledger conservation does not certify every intermediate EVM balance representation. Wrapped-zero execution remains represented.

The source router balance assertion is discharged as a property, while execution of the final assertion and StakingRouterETHTopUp emission remain outside the program. Opaque SHA, precompile failure/gas/compiler refinement, exhaustive malformed ABI behavior, exact revert/log ABI, initial beacon-tree reachability and deployed beacon identity remain open. In particular A-TOPUP-BEACON-ADDRESS is not closed. No hash collision resistance, runtime authentication or full transaction equivalence follows from this review.
