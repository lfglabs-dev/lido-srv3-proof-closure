# Independent exact ADDRESS transferFrom review — a9c6

**Verdict: CLEAN for this bounded source increment.** No blocking implementation, public-composition, physical-storage, source-correspondence or evidence mismatch found. This verdict does not establish a full WithdrawalQueue transaction/EVM theorem or authorize wider delivery claims.

Exact checkout `/tmp/lido-address-wrapped-transfer-call`, clean HEAD `a9c6e2b1f7d3edcf5764082d7e266bb1df8fb42a`, sole parent `1d0709e937934bd9d8eaaaa84dd59504d9810255`. Author/site_audit was explicitly stopped. This reviewer did not author the increment. Independently verified the complete delta: exactly 51 additions, consisting of the three new Lean files and `audit/address-wrapped-transfer-call/**`. No existing tracked object changes. Fifty receipt hashes cover every added file except the receipt itself; every hash and the three configuration hashes passed. Final Git status remains clean.

## Full-source and composition review

Read all of `Audit/Source/AddressWrappedTransferCalls.lean`, `Audit/Guarantees/PAddress1WrappedTransferCalls.lean` and `Tests/AddressWrappedTransferCalls.lean`, including all proof bodies and executable diagnostics. Reviewed the actual consumed mapping/storage, Live execution, ABI/reply, previous token adapter and request-effect composition definitions. The complete prior token/queue source review at 1d07 is reused only after exact dependency identities: no old implementation or old public domain has been edited. This is not a claim of manually reading every proof body among the 1,252 imported/new files.

The new runner supplies its actual transferFrom callee as `otherCalls` to the old unwrap runner. The old token dispatcher passes the transferFrom selector to that callee. `canonical_outer_call`, actually rewritten into the success proof’s first-call equality, ties the queue’s canonical payload to `transferReply` with token self, queue caller, requesting sender as source, queue as recipient and the same original Word amount. The new adapter is therefore consumed by the execution; it is not a separate successful-call assumption or an unused encoder theorem. Nonpayable value and minimum calldata length are checked. Address arguments use low-160-bit cleanup and the amount uses the uint256 word decoder. Canonical generated calls are the explicit correspondence domain.

`JoinedEffect` retains the **entire previous `AddressWrappedTokenCalls.JoinedEffect`** as its first conjunct, instantiated with this new callee. The added facts bind physical transferFrom to the exact first CALL preworld (including the inherited zero-value transfer operation) and the same transferred world subsequently consumed by unwrap. The same unwrapped world, returned conversion amount, token effect, amount bounds, share quote, truncating cast, physical enqueue and complete attempts remain connected. The first reply is actual ABI-encoded true with no nested attempts, consistent with the empty-hook implementation’s lack of external calls. The proof excludes the impossible traced-success branch and obtains the physical effect from the actual reply.

Both public theorems retain only whole-run outcome premises: success derives the joined effect; failure restores the whole entry world. There is no allowance-fit, balance-fit, nonalias, frame, callback restriction or successful-stage hypothesis. Necessary arithmetic bounds are derived from executed guards. The earlier arbitrary-call and actual-unwrap consumers remain available unchanged; the additional implementation specialization does not reduce their domains.

## Pinned Solidity, assembly and storage

Read the complete relevant OZ3.4.0 `ERC20.transferFrom`, `_transfer`, `_approve`, empty `_beforeTokenTransfer`, Context sender and SafeMath addition/subtraction definitions, actual WstETH source/inheritance, and the queue0.8.9 caller. Core source pin is `17005714f151e5502c559932319a3f2f74ac2436`; token dependencies are the integrity-checked npm3.4.0 package and queue dependencies npm4.4.1. Reviewed the full relevant legacy assembly function bodies, including the transferFrom dispatcher/argument decoding, transferFrom body (around lines 1935–2047), `_approve`, `_transfer`, SafeMath add/sub and `_msgSender`. This source/assembly review goes beyond compilation success.

Source and assembly agree on the executed order: sender-zero guard, recipient-zero guard, concrete empty hook, checked sender subtraction and balance write, **fresh recipient balance read after that write**, checked recipient addition and write, Transfer event; only then fresh nested allowance read, checked subtraction, `_approve` owner-zero and spender-zero guards, allowance write, Approval event, return true. The allowance SafeMath argument expression executes before `_approve` guards. Supply is not written by transferFrom. The model uses Nat addition bounded by 2^256; for bounded Word operands this matches the generated wrapped-add/c>=a SafeMath test.

All new storage reads/writes are qualified by the token address. Balances mapping is slot 0. Allowances nested mapping is slot 1 with owner in the inner preimage and spender in the outer preimage, both 32-byte words; slot output is normalized to 256 bits. Rechecked the actual helper definition and compiler storage layout, not just a slot comment. The correspondence checker independently recomputes owner1 balance `0xada5013122d395ba3c54772283fb069b10426056ef8ca54750cb9bb552a59e7d` and owner1/spender99 allowance `0xff67a398ecff03b66263bf0fe462d7e9454f0ea652ad311ddbbbff1dae964b28`; diagnostics consume these actual helpers. The imported mapping-injectivity axiom is not in any of the nine checked theorem closures. Alias handling comes from sequential reads/writes, not collision-freedom assumptions.

Self-transfer reads the debited sender slot again as recipient, restores the transferred balance and still decreases allowance/emits both events. A physical alias between balance and allowance locations would likewise make the allowance read observe the actual post-transfer world. A late allowance failure may leave dirty transfer writes/logs inside the intermediate Exec result, but `transferReply` runs rollback and exposes no rejected committed world; outer request rollback restores its entire entry world. Recipient-overflow failure similarly occurs after the debit but before Transfer; no committed debit survives a returned failure. Zero-amount transferFrom is permitted when source guards allow it; the later actual unwrap zero guard retains its own semantics.

The unchanged actual unwrap still reads physical stETH, performs conversion, burns on the returned transfer world, rereads stETH after burn and executes its transfer. Its solc0.6.12 ignored bool minimum-length semantics remain distinct from the queue0.8.9 canonical bool decoder. Arbitrary subsequent STETH callbacks preserve their returned worlds. The new TransferFromEffect describes the first call’s intermediate state; it asserts no final conservation across unwrap/callbacks. Attempt journals remain abstract diagnostic calls, separate from committed event logs.

## Validation and evidence

Read `validate.py`, `check-correspondence.py`, `run-diagnostics.py`, receipt/README and all new fixture/diagnostic source. Executed the first two validators through a read-only interception: candidate output writes became exact existing-byte assertions, other candidate writes were prohibited, Python bytecode caching was disabled, and external scratch queries remained allowed. No source/evidence regeneration or package mutation occurred.

Fresh independent results: **1,252** imported/new source identities, **11** Git package pins, unchanged old local sources, selected imported local olean identities, final normal source/olean/ilean/trace hashes and runtime/FFI source/repository identities all pass. All **nine active ordinary axiom queries** were freshly evaluated on the current normal environment and contain only `propext`, `Classical.choice`, `Quot.sound`. Recomputed manifests match archived bytes. New files contain no admitted proof, new axiom or native_decide escape. Configuration and all fifty receipt hashes also pass.

Fresh correspondence checks pass **37 exact reused input entries**, **27 actual Git/npm source bodies**, and **11/17/19** token/queue/test compiler-metadata source sets. Thirty-seven is an input-entry count, not 37 distinct vendor Solidity bodies. The complete deployed token artifact is byte-identical to the prior actual WstETH artifact; all relevant old assembly/layout/source bytes also match the reviewed base. Selectors and nested preimages are independently recomputed. New queue sources and inherited test fixture are exact copies of reviewed inputs, not substituted doubles of WSTETH.

After these identities, reused the author’s normal targeted compilation evidence: source 1,267 jobs and public/tests 1,269 jobs, 13 kernel regression examples plus one public zero-sender rollback instance, nine source theorems/two public theorems. Public compilation originally succeeded within a test build that subsequently failed on two rewrite instances; final unchanged public/source artifacts and corrected tests pass. That history is correctly separated in the receipt. Warm unchanged dependencies are not a full fresh repository build.

Inspected the native log and full diagnostic bodies: ten execution/order/rollback checks plus two independent Keccak slot checks pass. These are FFI-backed executable diagnostics, not positive kernel theorem instances. No native diagnostic rerun was needed here. Inspected the fresh Solidity compilation/test log: 19 files compiled with0.8.9, thirteen tests pass, including one 1,024-run fuzz test. Five added tests cover self-transfer/events/nested allowance, late allowance rollback, balance-error priority, recipient overflow, and zero guards/zero amount; eight inherited real queue/token tests are executed again. The suite deploys the actual archived0.6.12 token creation artifact through its constructor. STETH remains an explicitly configured double, including real callback into WstETH.wrap. Token0.6.12/optimizer200/Istanbul is reused, not recompiled; queue/tests0.8.9/optimizer200/London are freshly compiled by the author. This reviewer did not rerun Forge or a heavy Lean build.

Archived failed source/test/validator/import attempts are historical diagnostics, not final-success evidence. The corrected fixture resolves imports through exact local copies, and final validator NEW paths are correct. Parent-to-candidate `git diff --check` passes. The inherited raw `WstETH.asm` EOF blank is unchanged from1d07 and remains the explicit prior byte-preservation exception; this fresh-delta pass is not a whole-history whitespace claim.

## Acceptance boundary

The result establishes the typed World/Exec implementation composition of actual pinned WSTETH transferFrom and unwrap with one already admitted queue request. STETH conversion/transfer/quote and subsequent callbacks remain external. Initial deployment/storage provenance and layout assumptions, configured-address relations, queue root lens, outer pause/permit/array/batch setup, arbitrary malformed dispatcher behavior, general EVM/compiler/gas/memory/LOG equivalence and cryptographic implementation correctness remain outside the theorem. No global All/Trust wiring or broad ADDRESS completion claim is included.

Full Lean reading, pinned source/assembly checks and real finite Solidity evidence support CLEAN at this exact bounded increment; model-only validation has not been promoted to universal Solidity correctness. No candidate edit, merge, site update, deployment or external publication occurred. **CLEAN; reviewer STOP.**
