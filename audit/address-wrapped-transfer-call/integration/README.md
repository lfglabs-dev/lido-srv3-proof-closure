# Integrated actual wrapped transferFrom composition

Source `a9c6e2b1f7d3edcf5764082d7e266bb1df8fb42a` is independently source/Solidity reviewed CLEAN. Union `0dbc9f9d5af8e31aac03ea66c889bbee34fa7401` merges that source with proof main `cefc3c5452861e252cc17201d264734209251fcb`; all source/public/test and prior dossier bodies remain byte-identical. Only AllGuarantees/Trust wiring and this receipt directory are added by integration.

The public successful runner retains the entire preceding WstETH unwrap/caller/physical enqueue JoinedEffect and identifies the actual first transferFrom CALL's canonical arguments, qualified sequential balance/allowance writes and Transfer/Approval events on the same worlds. Its failure theorem retains whole entry-world rollback. No initial allowance, successful-stage, nonalias or callback-frame premise is introduced. Configured storage/deployment, outer admissions and external STETH implementations remain scoped; no final balance/conservation statement across later callbacks is claimed.

Fresh normal AllGuarantees/Trust build:1742 jobs PASS. Fresh global inventory:29 exact existing axioms, including the existing production and native-test exceptions. Fresh scoped inspection:9 ordinary closures;1252 actual imported/new source identities and11 package pins checked. All50 source receipt hashes and all three configuration hashes match. Actual three normal olean hashes are recorded separately; author build traces and source receipts are preserved. The read-only integration runner compares would-be manifest writes and permits only the dependency probe's external temporary files.

The first read-only interception attempt correctly left the dossier unchanged but also rejected the probe's temporary names.txt. Allowing writes outside this checkout fixed that validation-wrapper error; no Lean build or global check was rerun for it. No failed proof attempt is reclassified as successful.

Source13 Solidity tests (one fuzz ×1024),10 executable diagnostics plus2 Keccak checks,13 kernel examples and public rollback instance retain their exact reviewed scope. The actual0.6.12 WstETH artifact, full reviewed source/assembly and0.8.9 queue/test inputs remain identical. No fresh Forge, FFI, compiler or whole-positive kernel result is claimed by this integration. The complete source review is retained verbatim. Exact integration review follows after writers stop; no site update is implied by this proof merge.
