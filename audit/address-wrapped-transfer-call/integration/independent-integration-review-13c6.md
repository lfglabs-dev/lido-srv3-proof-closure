# Independent exact ADDRESS integration review — 13c6

**CLEAN for the bounded integration.** Exact clean HEAD `13c6935186bebbe9e38dde066a6b89d18a20af91` at `/tmp/lido-address-wrapped-transfer-integrated`. Root/source writers were stopped. No source/evidence mutation, build, Forge/FFI execution, merge, site action or deployment was performed during this review. Only this external report is written.

## Union, provider identities and wiring

Verified sole parent `0dbc9f9d5af8e31aac03ea66c889bbee34fa7401`. That union’s exact parents are reviewed source `a9c6e2b1f7d3edcf5764082d7e266bb1df8fb42a` and proof main `cefc3c5452861e252cc17201d264734209251fcb`, with common base `1d0709e937934bd9d8eaaaa84dd59504d9810255`. An independent recursive tree comparison, including object modes/IDs, reconstructs precisely the union by applying source changes to main. There are no conflicting overlapping edits, missing provider changes or extra union objects. Main SSZ330 and earlier claims/evidence remain verbatim.

The complete union-to-HEAD delta is two wiring edits plus nine added integration files. AllGuarantees adds the actual transfer public import. Trust adds its test import and exactly four queries: actual_wrapped_transfer_request_enqueue, actual_wrapped_transfer_request_failure_restores, canonical_outer_call and joined_success. No prior import, query, theorem, domain or implementation changes. All other tracked objects match the union.

Read the full integration README, receipt, identity/scoped manifests, wrapper, trust result and retained full source review, and inspected the complete integration delta/build evidence. The source review is byte-identical to my accepted a9c6 report, SHA-256 `6535339b817cc11b67f0af64a7b77ae37b6bce3a89c36dbe7874cd95ff7db08b`. All eight integration receipt hashes, fifty source hashes and three configuration hashes independently pass.

## Actual consumer preserved

The entire three-module implementation and all consumed old local sources remain exactly as reviewed. The registered public runner still executes the first canonical queue transferFrom through the new callee passed to the old unwrap adapter. canonical_outer_call is consumed in the success proof, binding actual owner, recipient, Word amount and first-call world to physical debit/fresh recipient credit/fresh nested allowance update and Transfer/Approval. The entire previous JoinedEffect remains the first conjunct, with the same transferred/unwrapped worlds, actual token conversion/return, quote, amount checks, physical enqueue and journal. Whole entry-world rollback remains the public failure conclusion.

There is no introduced allowance-fit, frame, nonalias or successful-stage premise. Existing source domains and prior public theorems remain intact. The transferred and burned states remain intermediate observations; arbitrary subsequent STETH callbacks do not acquire a final conservation guarantee. Integration descriptions correctly preserve deployment/storage/configured-address, outer-admission, queue-lens, STETH external, malformed ABI, gas/memory/compiler/cryptographic correspondence boundaries. Import registration does not turn the slice into complete ADDRESS delivery or universal Solidity equivalence.

## Independent evidence at final HEAD

Read the writing behavior of the archived wrapper and source validator before running checks. Used a separate nonmutating interception: any attempted write under the source dossier must equal existing bytes; other checkout writes are prohibited; external temporary dependency-query files are allowed; Python bytecode caching is disabled. This freshly passed 1,252 actual imported/new source identities, eleven package pins, unchanged prior sources, selected consumed local artifacts and nine active ordinary axiom closures. Recomputed source/runtime/axiom manifests equal their archived bytes. All nine closures contain only propext, Classical.choice and Quot.sound. The recorded three normal olean hashes were independently checked against their actual current files, without guessing cross-worktree object identity.

Independently rechecked the SHA-256 of every preserved correspondence input: 37 prior entries, 27 Git/npm source bodies and all three compiler artifacts. Together with the unchanged full source dossier/tree, this preserves the exact pinned source, complete relevant assembly, slot/preimage, actual token artifact and compiler-source correspondence reviewed at a9c6. Token/queue/test metadata sets remain 11/17/19. No new source or bytecode provider substitutes for those previously reviewed inputs.

The hash-bound root build log shows successful normal AllGuarantees/Trust completion at 1,742 jobs; all three new modules and both aggregators actively compiled. Root’s global check reports the same 29 exact axioms, with dependencies recomputed and native-decision claims re-evaluated. This is explicitly not a foundations-only global environment: 23 test/mutant native exceptions, three production exceptions and foundations remain. The global check and build are reused after identity/log inspection; I independently refreshed only the nine ordinary scopes, not a redundant full build/global run.

The integration’s initial wrapper failure rejected an external temporary names.txt, rather than a proof or source check. The corrected wrapper permits dependency-probe temporary writes outside the checkout and keeps dossier writes as equality assertions. Its implementation and successful archived identity output support the stated correction. That first failure is described in the README; no separate raw failed-attempt transcript is included in these nine archives. Accordingly this review credits the successful fresh validation and explicit diagnostic account, not a nonexistent archived failure transcript or a second build/global check. My corrected nonmutating run passes without changing the dossier.

Reused source evidence retains its exact scope: thirteen real Solidity tests including 1,024 fuzz cases, ten native complete-execution diagnostics plus two Keccak checks, thirteen kernel regressions/public rollback instance. Actual WstETH0.6.12 creation artifact is reused; queue/tests0.8.9 compilation is author evidence. No fresh integration Forge/FFI/compiler run or positive whole-execution kernel theorem is credited. Source/manual correspondence, finite Solidity execution and model proofs remain distinguished.

Both union-to-final and main-to-final `git diff --check` pass. The inherited raw WstETH.asm EOF exception remains byte-identical outside this delta; this is not an unconditional whole-history whitespace pass. Git status remained clean after verification.

**Verdict: CLEAN at exact 13c6 with the preserved a9c6 limits. Reviewer STOP.**
