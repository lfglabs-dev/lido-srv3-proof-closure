# Failed experiment, not accepted evidence

The initial fixture/test bodies are retained beside this note. They are reconstructed byte-for-byte from the authoring script and the exact removal transformation; the temporary Forge project was already removed when the runner raised on failure. No original failed build-info/artifact was saved, so no independent failed artifact identity is claimed. The accompanying original forge/run logs are untouched. All other compiler input bodies were identical to the final 25-input build (substitute these two bodies for test/PhysicalEntryHarness.sol and test/PhysicalEntry.t.sol).

The artificial external charged() used the Foundry deal cheatcode to zero its own balance after receipt of nonzero msg.value, then entered an internal function with the actual inherited role/balance/pause modifiers. The test intended to distinguish missing-role rejection from arithmetic Panic11 with pause also pending. The observed result is only the generic `exact competing guard` assertion failure; the log reports eight other successes and this one failure. It does not identify which subcase, observed returndata or cause. In particular, the available evidence does not establish a Foundry rollback bug or a Solidity/model mismatch. The large gas figure is not a diagnosis. No further rerun was justified for this unreachable credited-entry instrumentation.

The final fixture removed only BalanceInstrumentation, charged/_charged and this artificial test. It retains all actual inherited modifiers and all eight useful public/suffix tests. The normal Lean public theorem still ranges over arbitrary incoming Worlds; the kernel/native role-before-underflow-before-pause cases remain intact, with no new reachability or sufficient-balance premise. A correct payable credit already makes entry SELF balance at least msg.value. The compiler's complete actual checked subtraction and its position between role and pause remain retained separately.
