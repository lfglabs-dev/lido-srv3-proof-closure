# CONSOL-ETH1 physical modifier admission

Source candidate on `82051ab0a552734712b8f33e3f96f0074bf9ee39`. This author also wrote that quota source; neither this dossier nor reuse of its checks is an independent review. A separate source review and root integration are required. Only the three new Lean modules and this new dossier are authored here.

`PConsolidationEth1.actual_physical_entry_quota_settlement_requests` consumes only success of the new complete modeled executor. It derives actual caller role admission, the checked entry-balance condition and physical resumed status, then retains the **entire** prior physical-quota existential conjunction, including exact prior result equality, `QuotaEffects`, full `GatewaySettlement.Success` and full `SettlementRequests.Effects` on the actual post-quota World. The prior conclusion is preserved literally inside parentheses (only surrounding indentation/newline normalized in `validation/prior-conclusion.txt`). `ofPrior` retains the actual result's outcome, World and attempt trace and records that result, rather than rerunning a detached counter. The public failure theorem restores the whole original incoming credited World, including quota and any callback changes. It does not assert a final quota, role or resume frame when callbacks mutate storage.

The prefix uses actual `ctx.sender` and physical storage at actual `ctx.self`; the configured vault-authorized gateway remains a distinct argument until the inherited vault authorization. No extra role, pause, timestamp, fit, nonzero, canonical-boolean, hash-injectivity, nonaliasing, frame or successful-stage premise is introduced. Imported MappingSlot's injectivity axiom is not in any of the 22 ordinary theorem closures.

## Exact source and compiled correspondence

Core is `17005714f151e5502c559932319a3f2f74ac2436`. Full bodies of the 22 pinned core/OpenZeppelin inputs remain in the unchanged quota dossier; fresh `solidity/build-info.json` retains all 25 actual input bodies and full compiler output. The pinned OpenZeppelin **non-upgradeable** AccessControlEnumerable5.2.0 inherits AccessControl and Context. Its layout is `_roles` at slot0, `_roleMembers` at slot1, and `RoleData.hasRole` at offset0. This is not TOPUP's ERC7201 layout. The actual check hashes the role and base0 as 64 bytes, then actual caller and that digest as 64 bytes; it accepts any nonzero low byte. The pure Lean mapping definition uses these exact UInt256 big-endian preimages without an injectivity assumption.

Gateway185 modifier order is `onlyRole(ADD_CONSOLIDATION_REQUEST_ROLE)`, **then** `preservesEthBalance` entry checked subtraction, **then** `whenResumed`. Source118–122 retains its final balance assertion through the inherited settlement success. Unauthorized encoding is68 bytes (`e2517d3f`, caller32, role32); balance underflow is36-byte Panic11 through the prior exact arithmetic encoder; paused encoding is4-byte `14378398`. PausableUntil reads the entire word at `keccak256("lido.PausableUntil.resumeSinceTimestamp")` and checks timestamp < resume; equality is allowed, including the raw maximum boundary. No `resume != max` approximation is used.

Fresh full `PhysicalEntryHarness.ir` is2610 lines. The actual inherited Gateway selector is704–728 for admission: typed ABI checks/decoding before role711, selfbalance713, checked subtraction715, pause717, then zero-value719. The complete actual role helper is1771–1799: base0, both64-byte hashes, `and(...,0xff)`, caller and68-byte revert. Resume is2156–2168: full SLOAD/timestamp and4-byte revert. The fixture's typed suffix selector653–669 uses those same actual unmodified modifier helpers. The full actual Gateway body and every helper are retained, as is the unchanged2456-line prior compiler IR. Source hashes, artifact metadata source Keccaks, bytecode versus full output, compiler0.8.25+commit.b61c2a91, optimizer200, viaIR and Cancun are checked.

## Boundary and execution

The World entering this theorem is already payable-credited. Outer ABI decoding, entry CALL/value-credit mechanics, deployment/proxy initialization and full memory/gas behavior remain outside. Actual source pure guards/count189–199 precede omitted DSM preconditions201, locator203 and witness205–207, then quota209, quote211, vault220 and refund222. Adding physical entry modifiers does **not** close those omitted stateful preconditions and does not prove full deployed transaction success against them.

The executor performs role → checked balance guard → pause, then the unchanged quota runner. That runner explicitly replays pure balance/zero/count guards before quota; its settlement suffix again replays accepted pure guards. These replays act on the same unchanged incoming balances/groups and have no calls or writes. The intervening omitted stateful prefix is not silently moved, absorbed or asserted successful. The actual quote and all later callbacks consume the physically quota-modified World. The inherited full fee/actual vaultCALL/decoded 48-byte request/refund behavior and late rollback remain intact.

## Checks and their degree

`validation/test-build.log` is a normal successful1272-job build; all three new modules were actively built. The existing All/Trust/lakefile wiring is untouched. Current normal setup, trace and olean identities are retained for each module: empty options/plugins, nonsynthetic traces, no skipped kernel checks/errors/sorry. Actual imports yield1255 source identities with11 exact package pins. `validation/axioms.json` records22 ordinary transitive scopes, foundational axioms only. This is scoped source validation, not a new global All/Trust count.

Fourteen kernel regressions and two public instances cover role/underflow/pause priorities, high-only denial, raw2 admission, full-word/equality/max pause, exact unauthorized bytes, zero/empty ordering, nonempty complete settlement and late failure restoring the original World. The nonempty kernel fixture deliberately uses constant gateway storage and a large raw timestamp so the kernel does not evaluate Keccak; it still performs actual quota packing/uint32 frame casting, quote, two inbox requests and refund. It does not represent a realistic current block timestamp or establish concrete Keccak evaluation.

Separate native diagnostics evaluate actual nested Keccak at the selected slots and normal timestamp100: six execution groups include complete nonempty success, competing guard failures, original rollback and actual caller lookup. Four vectors for callers0,17,999 and max160 are recomputed independently with Python Crypto Keccak; both64-byte preimages are retained and compared with the Lean evaluator. The Lean mapping definition itself has no `implemented_by` replacement. The EvmYul native byte-array support library is loaded for its required runtime primitives; this runtime evidence is not a kernel theorem and is not treated as a fresh theorem axiom. The runner compiles only those support primitives in a temporary directory; no package source/cache is modified.

Eight fresh Solidity tests pass, including1024 arbitrary uint256 role/resume fuzz runs. Actual inherited Gateway rejection cases test the real public selector. Positive and late-failure cases use the explicitly named typed suffix fixture with actual unmodified inherited modifiers/rate/fee/refund/pair logic and the cached actual vault artifact. Cached QuotaInbox/QuotaRefund observe the new quota during quote, both requests and refund. Successful callback mutation demonstrates why no final quota frame is claimed. The three cached support artifacts are exact8205 bytes with metadata/input comparisons; the vault is the prior two-source0.8.9+e5eed63a optimizer200 London harness. No legacy compiler rerun or production bytecode identity is claimed.

Development failures are retained and excluded from PASS: a reflexive-True proof error; native missing byte-array support and record-layout syntax; surrounding-whitespace comparison in the validator; and a test-only forced-balance-damage Solidity experiment (8 cases passed, that artificial case failed). The exact initial helper/test bodies are retained with an explicit reconstruction/diagnostic note; failed compiled artifacts were not saved. The generic assertion failure does not establish its cause or a Foundry defect. The forced-damage helper/test was removed from the final fixture. A naturally credited CALL does not reach entry subtraction underflow; arbitrary modeled-entry underflow order is covered by kernel/native checks and the actual compiled modifier path, not by the failed synthetic runtime experiment. Final eight-test output is distinct from that failed nine-test run.

## Reproduction and review

Read wrappers before executing. `validation/validate.py` defaults to comparisons plus ordinary temporary Lean axiom queries; `--write` records only this new dossier's identity files. It does not build or invoke Solidity/native compilers. `solidity/run.py` is author-only fresh compilation in an isolated temporary Forge project and writes this dossier. `validation/run-diagnostics.py` likewise reruns native diagnostics only when needed. A reviewer can verify retained identities without redundant build/Forge/native execution. `write-receipt.py` is author-only and rewrites this dossier receipt.

The receipt lists every new file hash and all three unchanged configuration hashes. The only new raw whitespace exception is the compiler's blank final line at `solidity/PhysicalEntryHarness.ir:2610`; retain it verbatim. No old proof, quota dossier, All/Trust configuration, site, merge, push or deployment is changed by this source candidate.
