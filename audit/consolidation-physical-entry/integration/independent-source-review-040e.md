# Independent physical consolidation entry source review

Verdict: **CLEAN for exact `040e3903f13cd5819c5be207004cad1f824b4a11`; STOP.** No blocking source, composition, compiler-correspondence or evidence finding. This accepts the bounded source increment, not full gateway admission or subsequent aggregate/site integration.

Clean reviewed checkout `/tmp/lido-consolidation-physical-entry`, tree `5498e5697f6a4a5800ea167a5556d2e706ddd5da`, parent `82051ab0a552734712b8f33e3f96f0074bf9ee39`. Core pin remains `17005714f151e5502c559932319a3f2f74ac2436`. Exactly40 additions contain the three new Lean modules and dossier; all39 receipt hashes and three unchanged configuration hashes match working bytes and exact committed blobs. Every old tracked object, including All/Trust/Lake and the full quota dossier, remains unchanged.

## Independence and review coverage

I am `/root/direct_steth_exact_review`, not the source author or root integrator. The source writer was stopped. I read all three new source/public/test Lean modules in full, every proof and regression, the complete README, validator, receipt/recording scripts, correspondence and prior-conclusion records, full new Solidity harness/tests/runner, native diagnostics/runner, relevant successful and failed logs, and the reconstructed failed instrumentation packet. I inspected actual pinned Gateway modifier/entry source, complete relevant OpenZeppelin non-upgradeable AccessControl/Enumerable/Context and PausableUntil bodies, compiled layout and complete relevant helper/entry IR paths. This was not compile-only review.

I reuse my full independent8205 physical-quota source review and its exact f1da/afbe integration/archive reviews under unchanged body/evidence identity. The retained prior Success and SettlementRequests.Effects, source compiler checks and e7ea/27f2 independent provenance remain authoritative with their original boundaries. The source author explicitly does not label their own quota reuse as independent review.

Only this external report was written. The inspected default validator creates its ordinary temporary Lean dependency probe; it does not build or compile. I made no candidate/cache/Git/site/browser mutation and ran no compiler, Forge, native suite, aggregate build, publication or external contact.

## Actual physical admission and error order

The new role lookup is the non-upgradeable OZ5.2 layout: `_roles` base0, RoleData.hasRole offset0; `_roleMembers` base1 is not consulted. It is not the separate TOPUP ERC7201 layout. The first exact64-byte Keccak preimage is role32 followed by base0; the second is actual caller32 followed by that inner digest. The physical slot is read at actual ctx.self, using ctx.sender for authorization. The compiler accepts any nonzero low byte, including raw2/high-bit-plus2, and rejects high-only words. It does not require a canonical boolean or infer initialization.

The role constant is `2893ea78cf4b35bcb6ca1f49c79c387fe7f3d8fd18fe56f2424c792c6a8207d2`; resume slot is `e8b012900cb200ee5dfc3b895a32791b67d12891b09f117814f167a237783a02`. Their Keccak preimages and both error selectors are independently recomputed by the comparison validator. Unauthorized is exact68 bytes: e2517d3f, caller32, role32.

Actual source order is onlyRole, then preservesEthBalance's checked incoming-balance subtraction, then whenResumed. The Lean gates follow precisely role denial → balance-underflow Panic11 → paused error14378398. Resume compares the whole stored256-bit word to the Word timestamp; equality is admitted, including maximum at maximum. No `resume != max` shortcut or narrow-time premise appears. The gate proof derives role, balance and pause facts solely by inversion.

Full compiled selector7328fa92 has outer ABI checks/decoding before role711, SELF balance713, checked subtraction715, pause717 and zero-value719. Role helper1771–1799 contains both64-byte hashes, caller, low-byte SLOAD test and68-byte error. Pause helper2156–2168 has full SLOAD/timestamp LT and4-byte error. checked_sub_uint2561488–1497 yields exact36-byte Panic11; the final assertion helper yields Panic01 and remains executed after refund. The typed entry selector653–669 uses these same unmodified inherited modifier helpers and final assertion. I inspected these complete paths, including the fixture's actual count-to-quota-to-quote continuation.

## Entire consumer preserved

The public theorem supplies only whole new execution success. It derives Admitted and exact new-result equality to `ofPrior` of the actual unchanged quota executor with identical arguments and incoming World. The entire old physical-quota existential conclusion is retained literally, including preparation/count bound, physical transition, QuotaEffects, exact prior result equality and both complete GatewaySettlement.Success and SettlementRequests.Effects on the actual postQuota World. The wrapper preserves outcome, World, trace and the actual prior result in its suffix field. No detached counter or reduced certificate is substituted.

The new gates neither write storage nor call externally. The old balance/value/group/count replays therefore occur on unchanged incoming data; the earlier quota-only write does not alter balances/groups either. These pure replays do not execute the omitted DSM/Lido preconditions, locator resolution or witness validation. The source still places those stateful operations between count and quota; they are explicitly excluded, not assumed successful or silently reordered into the modeled executor.

Quota, fee reads, actual vault CALL, decoded width-valid inbox loop, refund and final balance assertion remain on their actual sequential Worlds. Configured vault gateway identity stays distinct from executing self until actual successful authorization derives equality. Hypothetical storage aliases are not excluded: there is no role/resume/quota nonalias or final frame premise. Callbacks may change the final quota, role or pause storage. Every covered failure restores the complete original already-payable-credited entry World, including quota and late callback effects; diagnostic traces retain their inherited meaning.

## Independent validation and normal/kernel evidence

I read `validation/validate.py` completely before running `PYTHONDONTWRITEBYTECODE=1 python3 audit/consolidation-physical-entry/validation/validate.py` in default comparison mode. Observed exit0:1255 actual source identities,11 package pins,22 ordinary foundation-only scopes,three normal modules, unchanged full8205 conjunction and42 old receipt files,25 compiler inputs/two artifacts/full IR,three cached support artifacts,eight Solidity cases/1024 raw fuzz,six native groups/four independent vectors.

Actual import source/provider comparisons, Git package pins, normal source/setup/trace/olean identities and retained deterministic records pass. Normal setups have empty options/plugins; traces are nonsynthetic with no errors, sorryAx or skipped kernel check. All22 scoped closures contain only propext, Classical.choice and Quot.sound. The imported MappingSlot injectivity axiom is not consumed by them. The new mappingSlot calls pure KeccakEngine directly, bypassing the imported solidityMappingSlot's implemented_by backend.

The retained1272-job normal build actively compiles all three new modules and passes. No source-stage global All/Trust claim is made. Fourteen finite kernel regressions plus two public instances cover competing guard order, high-only denial/noncanonical-low-byte acceptance, full-word/equality/max pause, exact unauthorized bytes, zero/empty order, actual nonempty quota/quote/two-request/refund success and original-World late rollback.

The kernel nonempty fixture uses constant gateway storage and a large synthetic timestamp so symbolic Keccak slots need not reduce. It still executes actual quota packing/frame truncation and the complete continuation; it is not a concrete nested-Keccak computation or realistic current-time fixture. The separate runtime suite evaluates actual caller slots at time100, six execution groups and four independently recomputed caller vectors0/17/999/max160. I read its complete code and retained successful log and did not rerun it.

## Compiler/runtime evidence and failed experiment

All25 fresh compiler inputs and full output match the retained build-info. Twenty-two pinned core/cached OZ bodies and the unchanged quota harness plus two new files are accounted for. Two artifacts, metadata source Keccaks, bytecode/output identity, full2610-line optimized IR and actual storage layout agree. Profile is solc0.8.25+b61c2a91, viaIR, optimizer200, Cancun. Cached QuotaInbox/QuotaRefund and the actual two-source solc0.8.9+e5eed63a London200 vault artifact are exact8205 inputs, with metadata and compiler output checked; no legacy compile is credited.

Eight final Solidity cases pass with1024 arbitrary uint256 role/resume fuzz inputs. Rejection tests exercise the actual inherited public Gateway selector, including actual caller mapping and max timestamp equality. Nonempty success and late rollback use the explicitly bounded typed suffix with unmodified inherited modifiers/quota/fee/pair/refund logic. Cached inbox/refund require actual updated quota; two exact48-byte keys form each96-byte payload. Tests observe final balances and late restoration, and accepted callback mutation disproves any final quota frame. These are not full omitted-prefix success tests.

The failed nine-test forced-balance experiment is not passing evidence. Its untouched logs report only a generic competing-guard assertion failure, without identifying the subcase, returndata or cause. Reconstructed initial fixture/test bodies are clearly labeled; no failed artifact/build-info provenance is claimed. I independently verified that removing only the named BalanceInstrumentation/charged/_charged code and artificial test yields the final files exactly. The eight final tests are separately compiled/run and separately recorded. No Foundry defect, Solidity/model discrepancy or causality is inferred. Arbitrary-entry balance-underflow priority rests on kernel/native cases and the actual compiled subtraction path, not this failed experiment. Natural payable credit ordinarily prevents that underflow; no reachability premise is added to the abstract theorem.

Native-support provenance precision: the EvmYul ffi Lean/C bodies match package pin f7e4ee0dc8f8d5265ce822a937ab5be771f182e9. The separately cached SHA support repositories linked by the diagnostic runner are outside the11 Lake pins. Current clean SHA2 HEAD is565f65009bdd98267361b17d50cddd7c9beb3e6c (sha-256.c SHA2567a74437adc78576b8faff060f9573ba88a6da798914f45d263c75b149add3d27; header c2173d83813a0c29fcc3345ce489766efeadee6a52ca927ecd0f917a120df9fb). SHA3 HEAD isfc8504750a5c2174a1874094dd05e6a0d8797753 (sha3.c9499df301d240fdd296fb53aae554d2615f875156e39174390dc0b7dd2a25fea; header ba0f5cb513018ae84959293f82a2f1b2c027457c6564e8e1b8397fa439bcce6c). I verified those current bytes against their own Git commits. These current observations do not establish saved failed-build or historical support-object identities. The new mapping uses pure KeccakEngine, while runtime requires the pinned ByteArray.zeroes support primitive. This is a nonblocking runtime-evidence distinction, not a theorem axiom or reason to claim all native C dependencies are covered by11 Lean pins.

## Remaining scope and final disposition

Outer ABI decoding, entry CALL/value-credit mechanics and pre-payable caller rollback, omitted DSM/locator/witness stateful admission, deployment/initialization provenance, complete memory/copy/gas, LOG ABI, deployed bytecode equivalence and consensus remain outside. Prior semantic balance/address/component boundaries remain. Imported hash injectivity is not an assumption of this increment; no crypto collision-freedom or final callback frame is newly asserted.

The sole new raw whitespace exception is the compiler's blank EOF at PhysicalEntryHarness.ir2610. Excluding only that artifact makes the complete source delta pass diff-check; all prior quota exceptions remain unchanged in their prior scope.

**CLEAN at exact040e3903f13cd5819c5be207004cad1f824b4a11 / tree5498e5697f6a4a5800ea167a5556d2e706ddd5da. STOP. Separate root integration and exact publication review remain outstanding gates.**
