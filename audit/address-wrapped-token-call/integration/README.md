# Actual wrapped token request integration

Source `1d0709e937934bd9d8eaaaa84dd59504d9810255` is united with integrated DSM328 main `03727cb5fd25694aaa8d38ab9bd54c8420e7a214` at `fef1020189db6faf33672a037778e8155736cb00`. All three new Lean files and all 66 source-receipt hashes are unchanged. AllGuarantees adds the public import; Trust adds the regression import and four dependency queries. Existing source implementations, domains, evidence and package pins remain intact, including DSM328 and the earlier wrapped caller327.

The public theorem actually executes WstETH unwrap through the caller's canonical calldata and retains the entire prior RequestEffect. The same original amount enters conversion and burn; the actual conversion result returned after STETH transfer enters the caller's guards, quote and physical enqueue on the same returned worlds. Token mapping/supply/address accesses are WSTETH-qualified, with the actual balance-write then supply-read order and the actual post-burn stETH reread. No internal success-stage, frame, fit or nonalias premise is introduced. Complete root rollback includes the preceding transfer and burn. Arbitrary STETH callbacks retain their returned-world effects, so the intermediate BurnEffect is not a final token-conservation claim.

First WSTETH transferFrom and STETH conversion/transfer/quote remain explicit external implementations. Deployment, initial balances/allowances/layout provenance, configured-address relations, outer pause/permit/batch setup, complete malformed ABI dispatch, the queue's designated storage lens and general memory/bytecode/gas/cryptographic correspondence retain their stated scope. The new actual-token consumer rejects zero through its source guard; the older arbitrary-unwrap theorem is preserved unchanged. Raw queue event/custom-error equivalence is not added.

Normal combined AllGuarantees/Trust passes 1,732 jobs, including newly imported existing ReplyABI plus the three new modules. Root freshly recomputes global dependencies and re-evaluates native claims: the same 29 disclosed axioms remain. The global environment is not foundations-only. Seven fresh normal scoped checks use only ordinary foundations. Root verifies all 1,250 imported/new source identities, eleven package pins, 66 source-receipt hashes and three actual normal olean hashes. Recomputed source and runtime manifests are byte-identical to the retained candidate manifests. No guessed cross-worktree artifact identity substitutes for these checks.

The retained independent exact source review reads complete new Lean/source composition and actual pinned WstETH/OZ3.4/queue/OZ4.4.1 code and consumed assembly. It independently verifies physical slots/preimages, 27 dependency sources, 11 token and 17 queue compiler metadata sources, actual normal artifacts and seven scoped closures. By these identities, 17 kernel examples/public rollback, seven native diagnostic groups and eleven real Solidity tests (three token, eight queue including 1,024 fuzz cases) are reused. The queue deploys the exact archived token creation artifact and exercises a real WstETH.wrap callback after burn. No fresh integration Forge/FFI run or positive kernel success instance is credited. Profiles are token0.6.12/200/Istanbul and queue0.8.9/200/London, not deployed-bytecode identity.

The full token assembly and actual fixtures justify minimum-length-only decoding of the ignored solc0.6.12 STETH transfer bool, including word2; the caller's distinct solc0.8.9 initial transferFrom decoder remains canonical. The source delta's only formatting exception is preserved raw WstETH.asm EOF at4692. Source diff-check excluding that exact file passes; this does not erase earlier main raw-output exceptions or assert unconditional whole-history cleanliness.

The source author stopped before independent review. Root freezes this integration before its separate exact review. No site edit, deployment or external publication is included; validated site79ce does not yet credit this increment.
