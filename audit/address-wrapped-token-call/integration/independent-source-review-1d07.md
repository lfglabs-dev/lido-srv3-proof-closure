# Independent exact ADDRESS wrapped-token review — 1d07

**Verdict: CLEAN for the explicitly bounded source increment.** No blocking proof, composition, physical-storage, pinned-source or evidence mismatch found. This is not a full WithdrawalQueue transaction/EVM equivalence certification or permission to upgrade broader ADDRESS coverage.

Reviewed commit `1d0709e937934bd9d8eaaaa84dd59504d9810255`, sole parent `9383467fe19c621cc5c37ea54e1d5585b9ba49ee`, checkout `/tmp/lido-address-wrapped-token-call`. Author was stopped before review. The reviewer did not author this lot. Git status was clean at entry and completion. Delta is exactly 67 added files: three Lean modules and the audit directory. Every addition other than the receipt itself is covered by its 66 independently verified SHA-256 entries; no existing tracked source changed. No candidate writes, rebuild, Forge execution, merge, site action or deployment were performed in this review.

## Reading and proof conclusion

Read the complete new `Audit/Source/AddressWrappedTokenCalls.lean`, `Audit/Guarantees/PAddress1WrappedTokenCalls.lean`, and `Tests/AddressWrappedTokenCalls.lean`, their proof bodies and public conclusions. Read the complete inherited wrapped-request consumer, the pertinent request/call/static/ABI/Live definitions and physical queue helpers consumed by the composition. Inspected the complete evidence delta, scripts, fixture sources, metadata/layout and relevant full compiler assembly functions. This is not a claim of manually reviewing every body in the 1,250-file import closure.

The implementation genuinely replaces the selected unwrap external with `tokenReply`: `calleeFor` selects the token address and unwrap selector, checks value and minimum argument length, and decodes the caller's amount. `canonical_call` binds the actual existing queue-generated calldata to that implementation, with token self and queue sender. Other calls, including the initial WSTETH transferFrom, remain an explicit external parameter.

`tokenProgram` executes its zero guard, physical stETH read and conversion STATICCALL, actual qualified burn, second physical stETH read and transfer CALL, then returns the captured conversion amount. Its return carries the body’s actual resulting world and nested attempts. A rejected token execution produces no committed callee world. The joined proof uses the existing request success theorem, obtains its actual unwrap callee equality, rewrites it using `canonical_call`, derives the token effect from this actual reply, and proves the decoded caller result is the captured token conversion. This is not an unused adapter or a supplied successful unwrap-stage premise.

`JoinedEffect` retains the entire accepted `AddressWrappedRequestCalls.RequestEffect` as a conjunct. Additional token facts bind to the same transferred world, unwrapped world, wrapped amount, received amount, downstream quote, physical enqueue and attempts. The public success theorem has only whole-run success as its premise. No frame, nonalias, balance-fit, callback restriction or independent success-stage hypothesis has been introduced. The failure theorem applies to the whole joined runner and restores the entry world, including the initial mutable transfer, token burn/transfer and later queue effects.

The old arbitrary-unwrap consumer and its zero-input domain are unchanged. The new actual-token specialization rejects zero through its executed source guard. This is an additional implementation-specific theorem, not a restriction retrofitted onto the old theorem.

## Physical and Solidity correspondence

Checked against exact core pin `17005714f151e5502c559932319a3f2f74ac2436`, the complete WstETH source and IStETH interface, concrete OpenZeppelin 3.4.0 inheritance/storage and consumed ERC20/SafeMath definitions, plus the pertinent full WithdrawalQueue/WithdrawalQueueBase request/enqueue code and OpenZeppelin 4.4.1 set operations. The vendor/source checker independently matched 27 dependencies to exact Git objects or integrity-checked npm archive bytes. Token metadata binds 11 compilation sources; queue metadata binds 17.

WSTETH storage accesses are qualified by the token address. `_balances` mapping base is 0, `_totalSupply` is 2, and stored `stETH` is 7; nonce storage is 6. Inherited layout and compiler storage layout agree. `balanceSlot` consumes the actual mapping helper. Independently recomputed the Keccak preimage for owner 99/base 0 and matched `0x01c4951e729acbc05299798279cd10e5be143681a3d883a027edec470c12b1a9`. Independently recomputed and matched all four consumed inherited queue namespace literals: queue, requestsByOwner, lastRequestId and lastReportTimestamp. Mapping input widths/order, low-160-bit address decoding, qualified write/read order and queue packed fields were inspected; no trust in old slot literals was substituted for these checks.

The concrete token hook is empty. Burn executes the nonzero-account guard, balance read/check/write, then rereads supply from the debited world, checks/subtracts and writes supply, then logs. It does not pre-read both words. Transfer target rereads slot 7 from the burned world, preserving alias-sensitive sequencing. The compiler assembly’s full unwrap and `_burn` sections confirm both slot-7 reads, balance SSTORE before supply SLOAD, and supply SSTORE before the burn log. No injectivity/nonalias premise is used by the checked theorem scopes.

For solc 0.6.12, the generated conversion and transfer calls both check target code existence, bubble failure and reject return data shorter than 32 bytes. The conversion consumes the first word. The ignored transfer bool is not checked for canonicality: false, word 2 and trailing bytes are admitted. The source model's discarded word observation adds no acceptance condition beyond minimum length. This differs correctly from the inherited solc 0.8.9 transferFrom bool decoder, which admits only 0 or 1. The assembly, complete source and actual Solidity tests support this distinction; it was not inferred from Lean alone.

Callbacks may return changed worlds. `BurnEffect` describes the intermediate burn state, not final postcallback conservation. The real Solidity fixture calls WstETH.wrap from STETH transfer and leaves supply 5 after the burn while the request succeeds. The model likewise preserves a changed returned world. The nested journal records the conversion as static and transfer as mutable, preserving child attempts and depth; it is an abstract call journal, not a full instruction trace.

## Independent validation and reused evidence

The validators were read before use. They normally write evidence files. This review executed them through a Python read-only interception: candidate-output `Path.write_text` calls became byte-equality assertions; other candidate writes were prohibited; temporary external scratch queries were allowed, and bytecode-cache writing was disabled. Neither candidate evidence nor source was regenerated.

Fresh results: all 1,250 imported/new source identities, 11 package pins, unchanged old local sources, recorded runtime/FFI identities and seven ordinary active axiom scopes passed. Each queried scope contains only `propext`, `Classical.choice`, `Quot.sound`. In particular the consumed mapping implementation does not pull the mapping-injectivity axiom into these claims. Recorded normal Lean source/object/ilean/trace identities were also independently checked against current bytes. Source-correspondence validation passed all 27 vendor dependencies, compiler source sets, layout and selectors. The archived WstETH artifact deployed by the queue fixture is byte-identical to the token's present compiled artifact.

Reused, after these identities: author's targeted normal builds (1,265 source jobs / 1,267 public-test jobs), 17 kernel regression examples and public zero rollback instance; seven native FFI diagnostic groups; three real token Solidity tests; eight real cross-compiler queue tests including one 1,024-run fuzz test. Inspected the successful active-build and replay logs. The active queue build compiles 18 files with 0.8.9; the token build compiles 12 with 0.6.12, including their tests. Final queue replay explicitly skips unchanged compilation and executes all eight tests successfully. Warm dependency cache reuse is disclosed; this is not an independent full rebuild. Native positive checks remain diagnostics, not positive kernel or universal EVM proofs. Archived failed Lean development attempts remain historical diagnostics and do not replace final successful receipts.

Token compiler profile is 0.6.12/optimizer 200/Istanbul, queue is 0.8.9/optimizer 200/London. Real archived token creation bytecode is deployed through its actual constructor. STETH is an explicit configured double. These fixtures substantiate the covered behavior, not deployed-bytecode identity or all external executions. No redundant compilation/test run was needed after exact source/artifact checks and fresh scoped queries.

`git diff --check` is **not an unconditional PASS**: it exits 2 solely for `audit/address-wrapped-token-call/solidity/token/WstETH.asm:4692: new blank line at EOF`. This is the declared raw compiler-output byte-preservation exception. It is retained explicitly, not cleaned or hidden.

## Acceptance limits

The accepted result is the actual unwrap-body implementation composed with one already admitted wrapped request. Initial WSTETH transferFrom, conversion and STETH transfer/quote behavior remain explicit external interfaces. Deployment and storage provenance, allowances, configured token/queue address relation, outer pause/permit/batch setup and complete malformed-dispatch semantics are not proved. The old designated queue storage lens remains; token storage is qualified. The model assumes no equality between the token's pre-burn/post-burn stETH values or the queue's configured stETH. Arbitrary external callbacks remain admissible.

General compiler/EVM correspondence, memory/gas/exhaustion behavior, exact raw queue custom-error/event encodings, cryptographic implementation correctness and universal token conservation are outside scope. Kernel theorems establish the stated model execution implications; source/assembly checks and finite real Solidity tests provide additional bounded correspondence evidence. Nothing in this review promotes model-only checks to full Solidity equivalence or upgrades global guarantees. No global All/Trust wiring is included in this candidate.

**Disposition: CLEAN at the exact commit and scope above. Reviewer STOP.**
