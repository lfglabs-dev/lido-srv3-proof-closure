# Independent exact ADDRESS integration review — 09a3

**Verdict: CLEAN for this bounded integration.** No source, provider, public-composition, wiring or evidence discrepancy found. Reviewer STOP after this report.

Exact clean checkout: `/tmp/lido-address-wrapped-token-integrated`, HEAD `09a3ccc0f49e271774c14e02e85e3179476ae9f7`, sole parent `fef1020189db6faf33672a037778e8155736cb00`. That union has exactly the two parents `1d0709e937934bd9d8eaaaa84dd59504d9810255` (reviewed ADDRESS source) and `03727cb5fd25694aaa8d38ab9bd54c8420e7a214` (integrated DSM328 main). Author/root were stopped. Review performed read-only with no source/evidence mutations, Lean rebuild, Forge/FFI rerun, merge or site action.

## Exact union and final delta

Independently compared complete recursive Git trees, including object modes and IDs, against common base `9383467fe19c621cc5c37ea54e1d5585b9ba49ee`. Applying every ADDRESS source change to DSM main produces exactly the union tree; there are no divergent overlapping edits, missing files or extra provider changes. Thus DSM328 and all unchanged older implementations/evidence survive verbatim, while the ADDRESS source provider is exactly the previously reviewed one.

From the union to final HEAD there are exactly nine changed paths: two wiring files and seven integration archives. AllGuarantees adds one public import. Trust adds the test import and precisely four axiom queries: the two actual-token public theorems, canonical_call and joined_success. No existing import/query is removed. All other tracked objects are unchanged. The source receipt’s 66 hashes pass independently at final HEAD, including all three new Lean modules. The retained source review is byte-identical to my original complete review (SHA-256 `41d6b3283c8277a048a3f9cf9251c3ad29d4ea6806c4812861f63db2d29936bf`). All six hashed integration archives also match their receipt.

## Consumer and boundaries retained

The full-source verdict from 1d07 remains valid by these exact identities. The registered public consumer still executes the canonical unwrap call through calleeFor/tokenReply, with the actual queue caller, token target and same input amount. Its public JoinedEffect retains the entire previous RequestEffect and connects actual token conversion, qualified burn, post-burn stETH reread/transfer and encoded reply to the same caller result, returned worlds, quote, enqueue and journal. No supplied internal-stage success or new nonalias/frame premise appears. Whole-run failure restores the complete entry world. The imported theorem is the real implementation composition already reviewed, not an unused adapter or replacement weaker conclusion.

Integration README and receipt correctly retain the original limits: first WSTETH transferFrom and STETH implementations remain external; arbitrary callback worlds preclude final token-conservation claims; initial deployment/storage/address relations and outer pause/permit/batch setup are not proved. Queue designated storage lens, malformed-dispatch, memory/gas/compiler/cryptographic correspondence and raw queue event/custom-error limits remain. The actual-token zero guard does not restrict the older arbitrary-unwrap theorem. No broader ADDRESS coverage upgrade follows merely from global registration.

## Validation checked at this exact integration

Read the source validator before executing it through a nonmutating interception: candidate output writes became byte-equality assertions, other candidate writes were prohibited, and Python bytecode caching was disabled. It freshly checked 1,250 source identities, eleven exact package pins, unchanged old local sources, selected consumed artifacts, runtime/FFI identities and seven active ordinary axiom closures against the current normal environment. Recomputed source, runtime and scoped manifests were byte-identical to the preserved files. Each of the seven closures contains only propext, Classical.choice and Quot.sound. Independently checked the three actual normal olean files against the integration receipt’s recorded hashes; no guessed cross-worktree equality was used.

Inspected the hash-bound combined build log: normal AllGuarantees/Trust completes successfully at 1,732 jobs. Existing ReplyABI is newly included in the global build; the three ADDRESS modules and the two aggregators actively compile successfully. This is compatible with the source identities and does not denote a new ReplyABI implementation. The global trust-check log reports 29 exact disclosed axioms, recomputed built-environment dependencies and re-evaluated native-decision claims. The global environment is expressly not foundations-only: 23 test/mutant native axioms, three production exceptions and foundations remain. I reused this root validation after exact source/receipt inspection rather than rerunning the heavy global check; the seven scoped queries above were independently fresh.

Previously reviewed source evidence is reused: 17 kernel examples/public zero rollback, seven native diagnostic groups and eleven real Solidity tests (three token, eight queue, including 1,024 fuzz cases). There is no fresh integration Forge/FFI run or positive kernel success instance credited. The pinned Solidity source/compiler/slot/alias/ignored-bool checks and exact archived deployed token artifact from the source review remain byte-bound. They establish the previously stated bounded correspondence, not universal deployed EVM equivalence.

`git diff --check` passes for union-to-integration. Main-to-final source diff excluding exactly `audit/address-wrapped-token-call/solidity/token/WstETH.asm` also passes. The known raw assembly EOF blank at line 4692 remains an explicit source-byte preservation exception; this is not an unconditional whole-source or whole-history whitespace PASS and does not erase older raw-output exceptions.

Git status remained clean after the checks. No further work is required for this exact integration review. **CLEAN; STOP.**
