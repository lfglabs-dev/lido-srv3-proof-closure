# Independent exact ADDRESS transferFrom integration review

**CLEAN at `66915b62ecedae0d3f4364dcc092a7bb4cdf62c5`**, tree `9bad79e31c8b2b9043a760a1ef5b08cd383986a6`, sole parent final union `a4629328e26b3768be5ab280717c7cf07732df83`. Reviewed clean frozen `/tmp/lido-address-steth-transfer-from-integrated` with root integrator stopped. No blocking source-consumption, integration, provider, domain or evidence finding. This is bounded integration acceptance, not full ADDRESS/EVM closure or publication approval. STOP.

## Independence and exact union

I authored neither the new transferFrom source nor this integration. My complete independent713c source/Solidity/assembly/public-consumption report is retained verbatim in `integration/independent-source-review-713c.md`; its bytes equal the external report. The complete new source and dossier remain exact713c. Reuse of that review is justified by exact identity, including its explicit inherited-review provenance: f1d9's reviewer authored earlier1d07/a9c6, whose separate retained independent reviews remain their correctness provenance. This integration does not silently convert author reinspection into independent review.

Read complete integration README, receipt, wrapper, identities, changed wiring and retained source report; inspected both complete build logs for active providers, results and failure markers, and the failed and corrected trust records. Read complete TOPUP bdeb source, ab02 integration and6e2d final archive reports. Their bounded provenance and earlier authorship qualifications remain intact. This review reuses the complete source/compiler inspections under identity rather than claiming another compiler-path review or execution.

Complete recursive Git path/mode/object comparison shows current tree is exactly accepted main342 `1cd11a4719ce9dec72f0c7dcffde5bcd7848f28d`, the51 exact713c source additions, two wiring edits and10 integration archive additions. Every other accepted-main entry is unchanged. Main342 has exactly the independently reviewed6e2d tree. Final union differs from checked combined union `decd74ec1a54e9159538d8016f09928b19fcf132` only by the two TOPUP final-review archive files; every checked source, configuration and aggregator is identical. HEAD adds only the ten transferFrom archive files to that final union.

All50 source receipt hashes, nine integration hashes,18 normal artifact records and three configuration hashes match current bytes. Also checked45 inherited transfer,91 conversion and111 quote hashes,32 TOPUP source and seven TOPUP integration hashes, and both TOPUP final receipt hashes. Source archive identity is exact. The three configurations equal accepted main342; lakefile's difference from source713c is the inherited accepted ACCOUNT registration, not a new integration exception. New integration diff-check passes. Earlier raw-assembly EOF exceptions remain unchanged historical artifacts.

## Full consumer and preserved scope

The integrated AllGuarantees adds exactly the public transferFrom import. Trust adds exactly its test import and nine queries: both public successes, both whole-entry rollback theorems, allowanceSlot_keccak, spend_success, transfer_effect and the two public kernel instances. Every previous import and query, including the reviewed TOPUP entries, remains. The final wiring bytes equal the successful combined build's wiring. No changed hypothesis, narrower arbitrary-callee domain or replacement consumer is introduced.

The unchanged source actually specializes direct requestWithdrawals and requestWithdrawalsWithPermit, not WstETH. canonical_call identifies the generated100-byte23b872dd CALL with owner=queue caller, recipient=queue self, spender=actual CALLER and original token amount. transfer_effect consumes that equality from the executed TransferEffect, derives exactly true32 and ProgramEffect, and item_effect strengthens the same StItem. The public permit theorem retains the entire ee24 direct physical-quote conclusion and maps the same transcript to enhanced Items. No-permit likewise retains its full batch effect. Worlds, IDs, captured amounts, quote/casts, enqueue and ordered attempts are the same executed values.

Physical allowance at full-Lido nested mapping base1 executes before accepted f1d9 transfer on its actual returned World. Full uint256 infinity skips _approve entirely. Finite allowance bound precedes owner/spender zero checks, raw guarded subtraction, storage write and Approval. Conversion and physical movement then read that actual World, and their returned World supplies the later queue quote/enqueue. Sequential reads preserve hypothetical allowance/rate/balance aliases; no final frame or same-rate claim is introduced. The old queue bool decoder consumes and discards the concrete true result without a new true-only acceptance guard for arbitrary tokens.

The complete previous source review directly checked pinned StETH and actual full-Lido entry, nested allowance, approval, return and queue call/decoder paths. It verified clean160 keys, both64-byte preimages, source error order, full-word infinity, allowance before conversion, inherited debit before fresh recipient read, checked credit, original token/derived-share events and true32 return. That exact correspondence is retained here, not inferred merely from compilation.

Both failure theorems apply to their actual specialized runners. Permit failure restores the complete pre-permit World including earlier items; direct failure restores entry World. Diagnostic attempts remain observations. No successful-stage, allowance, fit, frame, nonalias, equal-rate or cryptographic premise is added. Complete old arbitrary-callee statements remain unchanged.

## Current providers and validation

The retained initial normal build at `f28c8beb4cc482a9e3146f4631b8cc2d534712e8` completes1775 jobs, actively compiling the three transferFrom modules and both aggregators as1771–1775. The combined build completes1778 jobs, actively compiling the three added TOPUP modules and both aggregators as1774–1778 while reusing unchanged transferFrom artifacts. Both logs have no error/failure marker; inherited warnings are not represented as a warning-free fresh dependency build. Final union changes no build input.

Current normal records match every source/olean/ilean/trace/setup/config hash in integration identities. AllGuarantees' actual setup selects the exact local new source/public providers; Trust selects all three. All five new/aggregate setups have empty options/plugins, nonsynthetic traces and no skipKernelTC option. Thus the new declarations are actually consumed by normal aggregators, not merely named in an unattached source file. Path-dependent integration trace/setup records are compared with integration identities, not asserted identical to source-checkout traces.

After reading the original validator and wrapper, executed a comparison-only in-memory version with PYTHONDONTWRITEBYTECODE=1. The two proposed external temporary output writes were replaced with exact comparisons to archived integration identities; no candidate/cache or wrapper temporary output was written. The wrapper's sole semantic exception remains exact inherited main341 lakefile identity. Every other source/dependency comparison is unchanged. The ordinary temporary Lean dependency probe remains the existing checker mechanism, not a source rebuild.

Fresh result exited0:1265 actual imported/new source identities,11 exact package pins,14 ordinary transitive scopes,50 source hashes and18 normal records PASS. Scopes contain only propext, Classical.choice and Quot.sound; old local source and selected normal olean comparisons pass. No mapping-injectivity, sorryAx or new native theorem assumption enters these scopes.

The separate retained corrected global check reports29 exact disclosed axioms:23 test/mutant native-decision exceptions, three production exceptions and foundations, with dependencies recomputed and native claims reevaluated. Its failed predecessor supplied prefixed Lake output and correctly reported no named reports; that failed invocation is explicitly retained and not credited. The corrected checker obtains plain Lean output itself. Global29 and new ordinary14 are separate claims. I reused the hash-bound global result under unchanged final build inputs; no global/native reexecution was performed here.

## Evidence degree and disposition

Unchanged source evidence remains normal source/public1281 and test1282 builds;15 finite kernel examples; two public kernel instances consisting of empty permit success and nonempty allowance-failure rollback. Nonempty positive success remains executable evidence: four independent nested Keccak vectors, eight native groups, and12 Solidity tests with1024 bounded uint64 fuzz cases. No kernel nonempty-success claim is invented by integration.

The compiler chain retains37 exact original full-Lido inputs,18 unchanged queue inputs and19 new fixture inputs, with unmodified full-Lido code and reviewed profiles. Full Lido uses0.4.24 Byzantium optimizer200; queue/new fixture0.8.9 London optimizer200; unchanged WstETH is a constructor fixture. Actual Solidity permit nonce/signature/_approve executes, but FixtureDomain explicitly supplies the external EIP712 hash service; formal permit remains External. Seeded initial storage and finite tests do not prove deployment or general authentication. No compiler, Forge, native or module build was repeated.

Runtime code identity, initial physical-state/immutable/context provenance, general malformed legacy dispatch, full ABI/memory/allocator/gas/EVM trace and generic raw event/error refinement remain outside. The adapter's short-payload rejection is not claimed to match all legacy CALLDATALOAD behavior. Old address0 and qualified World conventions remain. The reviewed TOPUP contribution retains its own gateway/router/allocation/environment boundaries and is not enlarged by this ADDRESS integration. No new site or full-guarantee closure is asserted.

Only this external report was written. No candidate, dossier, cache, Git, site or external-service mutation occurred. Final checked status remained clean. **CLEAN exact6691; reviewer STOP for root's separate archive gate.**
