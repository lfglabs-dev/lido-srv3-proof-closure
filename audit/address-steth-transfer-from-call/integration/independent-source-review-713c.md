# Independent exact ADDRESS full-Lido transferFrom source review

**CLEAN for exact `713c8bf70df2125258b27e6d7b8baa76455f8192` within the stated typed-call, physical-storage and outward-result scope.** No blocking source, composition, provider, identity or correspondence finding. This does not approve future integration or publication and is not whole ADDRESS/EVM closure. Reviewer STOP.

Reviewed frozen clean `/tmp/lido-address-steth-transfer-from`, tree `571e5dcdf2eecb2f0a18d8cbe6a41c223399600f`, sole parent `f1d91e3a8ad9829e92ab2e8ff443e391449a36bc`. Source author was stopped. Complete delta has 51 additions only: the three new Lean modules and dossier. All 50 receipt hashes match both working bytes and exact candidate Git blobs; they cover precisely all additions except receipt.json. Every previous tracked object is unchanged. Parent-to-candidate diff-check passes without exclusions. Existing raw-assembly EOF exceptions are inherited, not new or silently waived.

## Independence and complete reading

I authored none of this candidate or its antecedents. Read all 191 new source lines, 66 public lines and 97 test lines, including every proof, all fifteen finite kernel examples, both public kernel instances and the complete native execution program. Read the full README, receipt, both validators before execution, diagnostic driver/program, complete Solidity fixture and harnesses, build/test logs and relevant artifact metadata. Read the complete independent f1d9 source, bc12 integration, ee24 quote and 107e conversion reports supplied in campaign local-goals. Revisited their actual consumed source/public definitions and the physical transfer, request TransferEffect, StItem, public quote conjunction, and Live execution/rollback/zero-value call definitions.

The f1d9 reviewer disclosed authorship of earlier wrapped-token 1d07 and wrapped-transferFrom a9c6. Their retained separate independent reviews remain the provenance for those older bodies; f1d9 is not silently treated as independent re-certification of its author's antecedents. Its physical-transfer increment, and the separate ee24/107e physical-rate reviews, are reused under exact unchanged source/dossier/compiler identities. This review does not claim fresh manual inspection of all 1,265 imported source bodies or repeat unrelated ACCOUNT acceptance from bc12.

## Actual consumer and preserved results

The consumer is direct WithdrawalQueue.requestWithdrawals and requestWithdrawalsWithPermit. WstETH is not the newly specialized consumer. Both public runners execute the concrete transferFrom callee and physical quote. The permit theorem's first conjunct, DirectQuotePermitEffect, is definitionally the entire ee24 direct physical-quote permit conclusion: complete JoinedEffect, old batch facts, actual permit effect, Resumed, old StItem transcript, Worlds, IDs and chronological attempts. It then maps that same transcript to stronger Items. The no-permit theorem similarly retains StETHEffect and derives the enhanced transcript from the actual batch transcript. No detached run or user-supplied primitive success certificate replaces execution.

The exact chain is canonical_call → transfer_effect → item_effect → public transcript. canonical_call proves the actual generated 100-byte payload: selector 23b872dd, sender owner=ctx.sender, recipient=ctx.self, full original token Word, zero value and actual spender/caller=ctx.self. transfer_effect rewrites the actual old TransferEffect's invoked callee on `transfer before ctx.self target 0`; reply_success extracts ProgramEffect and exactly encoded true32 from that call. The concrete body cannot return successWithTrace. Item retains the whole old StItem and uses its same called World, shares, amount, enqueue/cast, ID and journals. The old bool decoder consumes the actual reply and discards its decoded boolean. No true-only guard is added to old arbitrary-token claims.

program binds spend to the accepted f1d9 physical transfer on the actual allowance-returned World. ProgramEffect includes the executed successful spend equality, independent SpendEffect and complete inherited transfer ProgramEffect. Thus allowance writes can affect later physical rate, pause and balance reads under hypothetical key collisions. The later queue quote executes on the actual transfer-returned World. Neither same-rate nor unchanged-slot assertions are smuggled in. The allowance subtracts original token amount, not internal or external quoted shares.

Both failure theorems apply to the same specialized runners. Permit failure restores the entire World from before permit, including permit effects and all earlier items; direct failure restores its original entry World. Attempts remain observations. The internal Exec can contain provisional allowance/debit changes on late failure, but callee reply uses Live.run and exports no rejected post-World, and outer run restores the whole entry. There are no extra allowance, fit, frame, nonalias, rate-equality, successful-stage or mapping-injectivity premises. Old arbitrary mutable-callee domains and theorem statements are unchanged.

## Pinned source and actual full assembly

Core source pin is `17005714f151e5502c559932319a3f2f74ac2436`. Directly read StETH.transferFrom252–256, _spendAllowance462–468, _approve447–453 and the consumed _transfer body, plus actual queue125–136,171–176,373–381 and StETHPermit99–111. Examined complete pertinent full-Lido assembly entry/allowance/approve/return paths and queue CALL/decoder/quote continuation, rather than inferring storage from standalone StETH or compiler success.

Full-Lido entry tag19 rejects nonzero CALLVALUE, tag147 loads canonical arguments at offsets4/36/68 and masks addresses to160 bits, then tag149 obtains actual CALLER and calls492 before494. Tag493 produces1; shared tag113 normalizes and returns exactly32 bytes. The adapter's short-payload rejection is deliberately outside the claimed general legacy dispatcher equivalence: legacy CALLDATALOAD behavior is not incorrectly claimed identical. Canonical generated requests are the proved scope, with trailing suffix acceptance tested separately.

Tag492 cleans owner and spender and hashes two64-byte preimages at full-inheritance allowance base1: ownerWord||base1Word, then spenderWord||innerHashWord. It SLOADs the full Word, compares to NOT(0), and branches to shared return453 on equality. Thus UINT256_MAX skips _approve completely, including both zero-address guards, write and Approval. All other values, including maximum-minus-one, take the finite path. Guard current>=amount precedes ALLOWANCE_EXCEEDED; successful tag1033 performs raw SUB then calls423. Source and assembly do not have a special spender=owner exemption.

Tag423 checks owner nonzero before spender nonzero, with the exact APPROVE_FROM_ZERO_ADDR and APPROVE_TO_ZERO_ADDR strings, then tag942 recomputes the same two nested hashes, performs one SSTORE and Approval LOG3. The model's natural subtraction is sound under the executed bound and Word range; there is no invented SafeMath subtraction error. Finite zero amount still checks, writes and logs; infinite zero skips approval. The real Approval topic was independently recomputed by the evidence checker, as were Transfer and TransferShares.

Only afterward does unchanged f1d9 tag494 call the physical forward quote137, movement603 and events605. The exact accepted physical transfer review supplies full rate arithmetic and movement correspondence: strict conversion width/error priority, entire nonzero pause Word, base0 balance mapping, sender debit before fresh recipient SLOAD, checked recipient addition, original token amount and captured shares in events. New checks also confirm these retained assembly orders and identities. Sequential accesses preserve address aliases and hypothetical allowance/balance/fixed-slot collisions; no final unaffected allowance or balance frame is claimed.

The actual direct queue tag543 encodes owner CALLER and recipient ADDRESS, amount and selector, uses its immutable target with EXTCODESIZE and zero-value CALL, bubbles errors, invokes bool decoder660, and tag821 POPs the result before its physical quote STATICCALL. This agrees with the old canonical bool decoder, including absence of a true-only requirement. The source then enqueues the actual uint128 casts and emits its request Transfer. Permit executes before this same resumed batch path.

## Independent checks and evidence degree

Executed only the two read-only validators with PYTHONDONTWRITEBYTECODE=1 after reading them. validate.py --readonly uses an external temporary ordinary dependency probe and compares deterministic outputs without writing the candidate. check-evidence.py --readonly compares retained artifacts, recomputes cast hashes/selectors/topics and reads tool versions; it does not compile. Both exited0.

Fresh checks establish 1,265 actual imported/new source identities, 11 exact package Git pins and 14 transitive ordinary scopes containing only propext, Classical.choice and Quot.sound. Every consumed old local source equals f1d9; selected normal local olean bytes match actual providers. No sorryAx, native theorem axiom, mapping injectivity or new escape appears. All18 normal source/olean/ilean/trace/setup/config records match. All three setups have empty options/plugins; traces are nonsynthetic, invoke the correct local candidate sources and outputs, and contain no skipKernelTC option. Parent configurations remain unchanged.

Retained successful normal builds actively compile source/public in1281 jobs and tests in1282 jobs. Earlier failed development logs are disclosed and not credited, including the temporary syntax-recovery sorry warning. Successful new source contains no proof escape. Prior cached dependency warnings are not represented as a clean rebuild of every dependency.

Compiler comparison freshly passes parent45, conversion91 and quote111 hashes against base Git objects; all37 original full-Lido inputs and metadata Keccaks; exact pinned core bodies; retained full-Lido deployment objects and three full assemblies;18 unchanged queue inputs/objects; and19 new fixture inputs. Full Lido remains solc0.4.24+e67f0147, optimizer200, Byzantium. Queue/new fixture use0.8.9+e5eed63a, optimizer200, London. Unchanged WstETH0.6.12 is only the constructor fixture providing stETH address. These are evidence profiles, not production bytecode identity.

Fifteen finite kernel regressions exercise canonical bytes, allowance/infinity/zero-guard ordering, maximum-minus-one, value/short dispatch and suffix behavior. The two public kernel instances are empty permit success and nonempty allowance-failure rollback. Positive nonempty success is expressly native/runtime evidence, not a kernel-evaluated successful public instance.

Four nested mapping vectors retain and independently recompute both exact64-byte preimages; retained native execution checks the actual Lean FFI results. Eight native groups cover two-item permit and direct success, exact physical balances/allowances and ordered events, second-item allowance rollback, owner=recipient fresh reads, true infinity, late overflow rollback, zero-spender infinity and qualified target. Native source/C/runtime identities are recorded separately and add no theorem axioms.

Read all12 Solidity tests and their1024-case fixed-seed0x20260911 fuzz evidence. They deploy the exact unmodified full Lido artifact. Primitive tests check true32, real nested mapping orientation/base, error priority, infinity, spender=owner, finite zero, aliases, pause/overflow rollback and token-unit allowance. Actual direct and signed-permit queue successes check IDs and physical/cumulative state. Second-item allowance failure restores actual nonce, allowance, earlier request ID and balances. Fuzz inputs are bounded uint64; these are finite correspondence checks, not exhaustive uint256 executions.

FixtureDomain explicitly supplies the external EIP712 domain/hash service. The actual Lido permit deadline, nonce, signature validation and _approve execute unchanged; fixture storage seeds initial packed totals, pause, balances, allowances and helper pointer. This is neither an EIP712StETH runtime identity theorem nor general formal permit authentication. Formal permit remains External. Queue harness overrides only its abstract request Transfer emitter and exposes fixture seed/resume/read hooks; the direct queue bodies are unchanged.

No kernel/source rebuild, compiler run, Forge replay or native diagnostic replay was needed after exact evidence identity. No source, dossier, cache, Git, site or external service mutation was performed. Only this external report was written.

## Boundaries and disposition

Initial deployment/storage/immutable/context provenance, runtime code authentication, general malformed ABI dispatch, full EVM trace, memory/allocator/gas and general raw event/error encoding remain outside. Typed qualified World and address0's inherited storage convention remain explicit. No new All/Trust integration, ALLOC, unrelated guarantee or publication acceptance is asserted. Existing independent reviews supply their own bounded antecedent provenance.

**CLEAN exact713c source review; STOP.** Root owns separate integration and archive gates. Candidate remained clean at the end of read-only checks.
