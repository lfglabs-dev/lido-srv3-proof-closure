# ADDRESS direct full-Lido transferFrom request consumer

Author candidate on `f1d91e3a8ad9829e92ab2e8ff443e391449a36bc`. Only three new Lean modules and this dossier are added. This packet requires independent exact-source review and later root integration; it does not claim a new All/Trust gate or publication acceptance.

## Public consumer and preserved conclusion

`actual_steth_transfer_from_quote_permit_batch` specializes the direct `requestWithdrawalsWithPermit` runner to the real full-Lido transferFrom implementation. `DirectQuotePermitEffect` is definitionally the **entire direct ee24 `actual_steth_physical_quote_permit_batch` conclusion**, including the old JoinedEffect, batch pause/length/IDs, permit effect, physical quote transcript and chronological attempts. The new second transcript adds `Item`, which itself retains the entire old `StItem`, and derives physical allowance and transfer effects from the same actual first CALL. `actual_steth_transfer_from_quote_batch` provides the corresponding no-permit public entry. Both entries have universal whole-entry rollback theorems; permit failure restoration goes back to the original World **before permit**, including earlier items.

For every admitted item, the old outer amount check runs first. The generated100-byte calldata has selector23b872dd, owner=`ctx.sender`, recipient=`ctx.self`, original token amount and caller/spender=`ctx.self`. `canonical_call` is actually consumed by `transfer_effect`, then `item_effect`, then the public transcript. It identifies the invoked callee with its actual allowance-first program. The successful reply is exactly32-byte true; the inherited queue boolean decoder consumes it and ignores the boolean value in the same way as the pinned caller. No new requirement that an arbitrary token return true is inserted into any old theorem.

The physical allowance write and Approval event produce an intermediate `allowed` World which is the **executed input** to the already accepted f1d9 full-Lido transfer program. Its internal forward quote, sender debit, fresh recipient read, checked credit, Transfer and TransferShares occur on that World. The returned World is then the input to the queue's second, external physical forward quote and to the old physical enqueue/Transfer event. The allowance amount is the original **token amount**, not either computed share amount. The internal and later quote are not assumed equal: all actual storage updates remain visible, even under a hypothetical storage-key collision. `ProgramEffect` connects the executed spend equality, independently stated allowance effect and entire inherited movement effect; `Item` binds the same actual called World, quote, amount/cast, ID and ordered journal.

No successful-stage, initial-allowance, rate-equality, balance-fit, nonalias, frame, Keccak-injectivity or final-slot-value premise is added. Old arbitrary mutable-token interpreter statements remain unchanged. The specialization executes this implementation at the selected target; authenticating the deployed target's runtime code is outside the theorem.

## Source and full-inheritance compiler correspondence

Core pin `17005714f151e5502c559932319a3f2f74ac2436`, with exact source bytes and inherited full-Lido assembly checked by `check-evidence.py`:

| Pinned source | Actual full-Lido legacy assembly | New or consumed model |
|---|---|---|
| WithdrawalQueue.sol125–136,171–176,373–381 | inherited complete BatchHarness assembly | existing amount admission, permit→batch, transferFrom→quote→enqueue→Transfer |
| StETH.sol252–256 transferFrom | tag149→allowance492→transfer494→true493 | `callee`, `program`, canonical CALL theorem |
| StETH.sol462–468 _spendAllowance | tag492 through1033 | `spend`, `SpendEffect` |
| StETH.sol447–453 _approve | tag423 through942 | finite owner/spender guards, nested physical write, Approval |
| StETH.sol428–432 _transfer | tag494→forward137→move603→events605 | unchanged f1d9 `program` / `ProgramEffect` |
| StETH.sol494–505 and562–565 | full movement603 and events605 | unchanged physical balance order and events |

The actual full-inheritance allowance mapping base is **1**. Assembly masks owner and spender to160 bits and hashes the two exact64-byte preimages: `ownerWord ++ base1Word`, then `spenderWord ++ innerHashWord`. `allowanceSlot_keccak` establishes this same equation using the existing range lemma. Qualified contract storage is used throughout. The separately declared mapping-injectivity axiom is not in any new theorem's transitive axiom set.

StETH's `INFINITE_ALLOWANCE` is literally full uint256 maximum. The assembly compares the SLOAD to `not(0)` before all finite checks. This real branch skips `_approve` completely, including owner/spender checks, storage write and Approval. The finite branch checks `current >= originalAmount` first (`ALLOWANCE_EXCEEDED`), then owner nonzero (`APPROVE_FROM_ZERO_ADDR`), then spender nonzero (`APPROVE_TO_ZERO_ADDR`), then writes `current-originalAmount` and emits Approval. This is raw subtraction after its guard, not a new SafeMath error branch: the successful finite bound and Word range imply the mathematical difference is in uint256. An owner spending their own tokens has no special allowance exemption. Zero finite amount still runs both approve guards, writes and logs; infinite zero skips them.

Only after this stage does the accepted physical transfer conversion run. Thus an insufficient allowance may fail before ETH_TOO_LARGE; finite zero-address approve errors may precede conversion errors; infinite allowance instead reaches conversion and transfer guards. The f1d9 whole-word pause rule (any nonzero256-bit active word), balance mapping base0, sender debit before fresh recipient SLOAD, checked addition, original token/share event quantities and own callee rollback are unchanged. Alias cases are handled by actual sequential reads, not a separation assumption. Mapping-to-fixed-slot collisions can also affect rate or pause reads; the theorem makes no unjustified final allowance/balance conservation assertion after all later writes.

The preserved generated-call scope is canonical ABI. The decoder accepts a trailing suffix, but no complete arbitrary malformed-calldata equivalence or general legacy short-calldata dispatcher theorem is claimed. Error strings are the pinned body strings. The outer framework retains its prior empty-failure/gas distinction and typed semantic logs.

## Validation evidence

- `public-build-2.log`: normal Lake build,1281 jobs, actively compiled new source and public module. `test-build-2.log`:1282 jobs, actively compiled tests with the exact same source/public artifacts. Fifteen finite kernel regressions cover canonical bytes, finite/infinite guard ordering, zero amount, maximum-minus-one, canonical dispatcher and trailing suffix. Two kernel instances use the public theorems: complete empty permit success and **nonempty allowance-failure rollback**. They are not presented as kernel-evaluated nonempty success instances.
- `validation-2.log`, `source-inputs.json`, `axioms.json`:1265 actual imported/new source identities,11 package pins,14 fresh scoped ordinary axiom sets. Every consumed old local source is byte-compared against f1d9 and every actual selected package source against its exact Git pin. Normal local provider oleans are compared. Only propext, Classical.choice and Quot.sound occur. Eighteen records in `normal-build-identities.json` bind the three source/olean/ilean/trace/setup sets and the three unchanged root config files. `validate.py --readonly` computes the same checks and compares outputs without writing this dossier; its scoped Lean query uses temporary support files, not a rebuild of sources.
- `native-1.log`: four independent cast-Keccak nested vectors, with **both64-byte preimages** retained in `nested-vectors.json`, pass against the actual Lean FFI. Eight native execution groups pass: nonempty permit batch700/800→IDs1/2 with exact physical allowances/balances and ordered Approval/Transfer/TransferShares/WithdrawalRequested; no-permit batch; second-item allowance failure restoring the prepermit world; owner=recipient fresh-read behavior; real infinity; checked recipient overflow restoring allowance/debit; zero spender with infinite allowance; qualified target. These are executable diagnostics, not kernel proofs or native theorem axioms. Fresh diagnostic dylib source/C inputs are identified in `runtime-inputs.json`.
- `solidity-test-1.log`:12 tests PASS, including1024 fuzz cases with fixed seed0x20260911. They deploy the exact unmodified full Lido artifact; no override of transferFrom, allowance, _spendAllowance, _approve, conversion, _transferShares, events or permit exists. They verify real true32, exact error priority, full-word infinity, finite maximum-minus-one, spender=owner, finite zero amount, nested mapping orientation/base, alias, late pause/overflow rollback and token-unit allowance. Two actual queue batch success tests cover direct and signed-permit paths. The second-item failure test restores the real permit nonce/allowance, first queue ID and token balances.
- Permit fixture degree: actual full-Lido permit/signature check/nonce/_approve execute; **FixtureDomain is an explicit external domain/hash provider** using the standard domain formula, not a deployed EIP712StETH identity claim. Initial packed totals, pause, balances, allowance and helper pointer are seeded through fixture storage hooks. The formal permit still has the inherited External boundary; these signature tests do not introduce a general cryptographic or initialization theorem.
- `compiler-check-1.log`, `compiler-identities.json`:45 f1d9 receipt hashes +91 conversion +111 quote hashes matched the base Git objects. All37 unchanged full-Lido source inputs and metadata were checked; all exact pinned core bodies and unchanged Lido/WstETH deployment objects, three full assemblies and18 queue fixture inputs/objects were reused by identity. Only the new19-input test project was compiled (solc0.8.9+e5eed63a, London, optimizer200). Full Lido remains the retained solc0.4.24+e67f0147/Byzantium/optimizer200 artifact; WstETH0.6.12 is only an unchanged constructor fixture that supplies the queue token address. These profiles are evidence profiles, not production-bytecode identity. No old compiler or old Forge suite was rerun.

Reproduction commands are recorded in receipt.json. `check-evidence.py --readonly` compares artifacts and writes nothing; default mode creates the new compiler evidence. Runtime scripts compile only a temporary native diagnostic library. No All/Trust or Lake provider config is edited here.

## Boundaries and development failures

The existing typed World/storage qualification, raw frame/caller provenance, returned-memory/allocator/gas bounds, canonical outer ABI admission, initial storage/deployment and semantic event abstractions remain. The public result covers the existing whole direct batch and permit sequencing, but not a new unified deployed EVM trace or cryptographic permit proof. The specialized actual transferFrom body contains no arbitrary callback; the older arbitrary External theorems remain available unchanged. Address0 retains the old Live storage convention. No new ALLOC or unrelated guarantee claim is made.

Development logs are preserved without credit: source-build-1 failed record syntax (Lean's recovery printed a temporary `sorry` warning); public-build-1 failed using reflexivity where simp had already reduced the goal to True; test-build-1 lacked a Decidable instance for log equality and was corrected to kernel reflexivity; validation-1 had a copied old public filename in the new-file allowlist. None is evidence of final proof success. The successful builds contain inherited cached linter/deprecation warnings, with no new-source warning or escape. No source changed after the final successful builds and scoped checks. No new raw assembly stdout was copied; inherited EOF whitespace exceptions remain only in their existing artifacts.
