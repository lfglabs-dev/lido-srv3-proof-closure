# Independent exact TOPUP router-locator review

**CLEAN for the bounded additive source/proof increment**, exact `6c332904a894c51f1cc57e23c9ec8cdc969453a8`, tree `8948d89fb9004fa05c78ce8642b5ff8c9a3a72c0`, sole parent accepted main336 `295e6c676b61de1815b7027a49ed8bc5fe5b8b3f`. No blocking source, proof, public-composition, correspondence or evidence finding. This is not acceptance of whole deployed TOPUP execution or later integration/publication.

Reviewed clean `/tmp/lido-topup-router-locator` with author stopped. I authored none of this locator increment or the inherited timing/history increment. I previously authored DepositPhysicalAdmission, reused further down the physical-getter chain. Its independent correctness rests on the unchanged source39f4/integration7305 reviews, not on presenting my own reinspection as independent review. Those retained reports have SHA256 `c37efb46ff225791363c6f3275591418d7937ee039a1b06555beb31b5f3099af` and `e40e6599c894916fa30cf6a43801eae2f28c42aa892ad07fa064b6f257530779`. My earlier independent f008/3674/44d5 reviews and the distinct timing reviewer’s complete 2efb/6fd1 reviews supply inherited provenance; all remain unchanged.

## Full source and actual public composition

Read all three new Lean modules, all proofs, nineteen kernel computations, the additional public rollback theorem and complete success instance, README, both receipts, validators, diagnostic and Solidity fixture/runner sources. Revisited the complete consumed credential decoder/caller, low-level static-call branches, timing execute/finish/run and public predicates, physical getter and module-context/physical-input selection. The complete independent timing source review and retained source/IR evidence were read and reused only after identity checks. This is not a claim to reread every theorem among 1,355 imports.

| Obligation | Assessment |
|---|---|
| Source order | New `run` (Source:113–130) executes typed lengths and physical count first, then the existing temporal gates, then locator lookup. Early failures have no locator/suffix attempts. Locator failure bubbles before allocation/decoding and retains its actual attempt. No extra authorization, nonzero locator/router, future-time or stage-success guard is inserted. |
| Actual request | `request` uses gateway caller, supplied immutable locator target, zero value and exactly four bytes `ef6c064c`. `lookup` calls the existing actual low-level STATICCALL interpreter on the entry World. It does not use an EXTCODESIZE precheck; ordinary no-code succeeds empty and reaches the decoder. Static rejection and forbidden state change retain the inherited actual error behavior. |
| Canonical actual reply | `decodeRouter` first executes the inherited min32 scalar allocator and signed-head-equivalent guard, then loads the first word and rejects noncanonical high160 bits. Zero and maximum uint160 are accepted by this decoder; no invented nonzero-address policy appears. Trailing bytes are ignored. Success derives actual raw response, canonical router, exact allocator equation, next=cursor+32 and next<2^64. |
| Consumed next cursor | The locator’s returned `next` is passed directly to `TopupTimingHistory.execute` as its credential cursor. It is not a separate supplied equality. The boundary test at cursor=2^64−64 witnesses locator success followed by getter allocation failure, demonstrating that the second allocator really uses the new pointer. Module returnBuffer remains a separately supplied later cursor. |
| Consumed router | `resolved` changes only Context.sender to the decoded router; Context.self remains supplied Lido. The actual getter target and qualified registration/config/raw-WC reads use that selected router. Through existing routerContext/moduleInput, the same selection determines the module caller/address, packed cap and physical continuation context. The original supplied router999 is deliberately replaced by reply2 in the successful regression. |
| Complete old public result | `TimingEffects` is byte-for-byte the entire 335 public conclusion, checked by the validator. It retains the preceding full IntermediateEffects, temporal admission, actual produced root-loop Output, exact intermediate result, three journals and HistoryEffects on the actual intermediate/final Worlds. The new public proof applies the existing theorem to success derived from the actual suffix, not to a supplied successful stage. |
| Exact result and observations | Public:46–55 binds the new result to `ofTiming` of that exact selected-router run, including world equality, suffix equality and chronological locator-before-credential-before-root-before-module observations. The full same-word/witness/module/continuation history chain is retained. |
| No duplicated actual prefix/loop | Executable code calls finish/execute after the already executed temporal guards. `suffix_eq_run` derives equality to the old public runner from those actual pure guard results. The existing pure length check can be repeated on identical data; no second temporal execution, root interpreter or module call reconstructs a result. |
| Failure | The public theorem restores the complete original World for any covered error. Later failures retain the locator and reached suffix journals. It does not erase diagnostic attempts or assume successful lookup. |

The new public success theorem has only complete new-run success as its propositional premise. The locator identity, hash/external interpretations, typed data, prior router allocation and scalar phase cursors are explicit parameters. No successful lookup, address equality, canonical-width, frame, nonalias, balance, sum or fit premise is added. The original public domains remain unchanged.

## Exact pinned source and complete relevant IR

Core remains `17005714f151e5502c559932319a3f2f74ac2436`. Read the complete relevant TopUpGateway topUp body and the complete retained IR segment for lengths/count, timing, locator, scalar return handling and credentials target/prefix, including the allocator helper. The unchanged timing reviewer already followed the complete remaining root/module/history path; those exact source and IR bodies are reused with that review.

At source185 and IR480–511, `LOCATOR.stakingRouter()` emits the exact selector, STATICCALLs the immutable target without a code precheck, bubbles failure returndata, takes min32 bytes, finalizes scalar allocation, applies signed wrapped ADD/SUB head checking, then rejects unless the loaded word equals its low160 bits. The inherited `copied_signed_guard` proves the signed guard’s natural comparison for this bounded copied size. The allocator helper rejects wrap or a next pointer above uint64 and writes the free pointer. Actual memory-copy/alias execution remains outside the scalar model.

IR512–538 uses the decoded `expr_7` as the credentials target and `_24=mload(64)` as the next buffer, not the initial locator pointer. The word passes prefix02 and reaches the existing validator leaf. The same selected router is the outer source topUp target; the Lean composition deliberately starts the existing inner router phase without claiming to execute that outer CALL/dispatch. This distinction is explicit in both source/public documentation and README.

All twenty-two retained compiler source bodies match core Git, accepted OZ5.2 vendoring and the inherited harness, their original SHA256s and metadata Keccak. Complete input/output/2,519-line optimized IR identities are unchanged at main336. Selector Keccak was recomputed by the comparison validator. Profile is solc0.8.25+b61c2a91, viaIR, optimizer200, Cancun, not a deployed production-bytecode identity statement.

The complete existing 335 result still locates physical effects at the actual intermediate World, before final packed history. History branches on witness-produced total limits, not decoded allocation sum; positive limits with zero allocations update it. It preserves the other packed bits and appends the full intermediate-world timestamp. There is no newly asserted equality between initial and returned metadata or between initial selected credentials and later post-callback reads.

## Independent identity and ordinary checks

The exact parent-to-candidate diff contains **25 additions only**: three Lean modules plus the new audit dossier. No old object changes. All **24 receipt hashes** match exact Git objects and current bytes. The source/configuration/provider/AllGuarantees/Trust inputs remain unchanged. `git diff --check` passes and final status is clean. The three timing source/public/test bodies remain identical to accepted2efb. The retained timing source and integration reviews have SHA256 `fbb6a1dc1417ff7e98994086fc8f1cf7a466aa88f54f7c528201c7c248fe0945` and `c5b1df705dd0921f71f8f87f6f63229391155cad19a2a00773464901cf9a3876`.

After fully reading its implementation, I ran `PYTHONDONTWRITEBYTECODE=1 python3 audit/topup-router-locator-call/validation/validate.py` in default comparison-only mode. Captured completion exited 0: **PASS 1355 sources, 11 pins, 27 ordinary foundation-only scopes, 22 exact reused compiler inputs, 3 normal modules**. The checker resolves actual registered regression imports, compares old local bodies to base and package bodies to exact Git pins, compares selected local oleans and all three current source/setup/trace/olean records, and recomputes the named scoped axiom dependencies without rebuilding modules. New declarations contain no admitted proof, native theorem shortcut, unsafe substitution or new axiom. Scopes use only propext, Classical.choice and Quot.sound; normal setup options/plugins and nonsynthetic traces are checked.

The author’s archived normal named build completes **1,372 jobs**. Precisely, its final log **replays source/public** and **actively builds tests in 5.3 s**; I do not restate it as three fresh builds. Source line40 carries only an unnecessarySimpa linter suggestion. No warning-free build is claimed, and this harmless style diagnostic does not impair kernel acceptance. Nineteen named kernel computations cover the listed boundaries; the additional named rollback theorem applies the public proof. Their full positive instance uses the acknowledged structural hash interpretation, not independent cryptographic execution.

No module rebuild, full global check, compiler or native/Forge execution was repeated. Global All/Trust wiring and validation remain subsequent integration work.

## Native and Solidity evidence scope

Eight author FFI IO diagnostic groups are archived with complete code, commands and runtime/vector identities. The three-row independent f008 SHA roots/50-word branches remain byte-identical; the actual SHA FFI and explicit mapping-hash fixture are distinguished. Positive `[1 ETH,0,2 ETH]` physical continuation/history, zero allocations with positive limits, changed WC/root, temporal-before-lookup rejection, late rollback, age overflow and changed selected physical router are exercised. These are executable diagnostics rather than kernel axioms, consensus evidence or full EVM execution.

Read the full fresh Solidity test, runner and successful eight-test log, plus the consumed inherited fixture setup/tree construction and recorder definitions. The runtime suite compiles23 exact source inputs with the stated profile and executes **eight selected locator tests**, with no fresh fuzz count or reexecution of the seven old tests claimed. Its fallback checks actual gateway caller and exact four-byte payload, then returns/reverts raw configured bytes. The selected-router test verifies a new recorder is called and the old recorder untouched. Short/noncanonical/trailing/reverted/no-code cases and temporal/length priority are checked.

The Gateway and verifier are actual unmodified inherited source operations. Router is an explicit recorder and locator is a transport fixture; the source does not claim those fixtures prove the physical-router implementation or deployed locator body. Independent sparse SSZ roots use actual SHA but opaque other-field roots and mocked anchoring remain fixture boundaries. The validator checks all23 fresh input hashes, the log hash, three exact compiled artifact hashes and their actual compiler settings/metadata source identities. Author Forge success is not converted into a formal theorem or production-bytecode claim. Reverted counters alone are not used here to infer absence of earlier attempts; the source/IR guard ordering supplies that correspondence.

## Retained limits and disposition

The supplied immutable locator identity and arbitrary read-only locator body remain explicit. Role/pause, initial deployment/storage/configured-role provenance, outer gateway ABI and gateway-to-router CALL/dispatch/allocator, preceding router allocation, full memory/copy/alias/gas and complete LOG/error bytes remain outside this phase. The ordinary no-code transport excludes precompiles by interpretation; positive code derived under that interpretation does not classify all Ethereum addresses. External/hash/SHA/root trust, arbitrary module/callback effects, the separate conditional ledger and transaction-wide environment continuity retain their established scope.

The useful new result is the actual locator request/reply and canonical router selecting the entire previously accepted typed phase, with its actual consecutive scalar cursor, unchanged complete effects, exact returned World/history and chronological journals. No broader runtime or whole-guarantee closure is claimed. **CLEAN at exact6c33; review complete and STOP.** Only this external report was written.
