# Integrated TOPUP actual router locator

Source `6c332904a894c51f1cc57e23c9ec8cdc969453a8` passed independent full-source/Solidity/IR review. The union includes accepted main337 `88dc7ae70ceffd463a29cc44175c58e82e2155a9`. Root adds AllGuarantees/Trust imports, six queries and this archive. All old source/public bodies and domains remain unchanged.

The actual gateway STATICCALL to LOCATOR supplies the canonical router consumed by the physical credentials getter and complete root/module/physical/history suffix. Its returned scalar next pointer is used by the credentials getter. Length and temporal guards run first. The entire335 public conjunction, exact intermediate and final Worlds, loop-produced history total and all observations remain connected to the same execution. Failure restores the entry World without erasing diagnostic attempts. No supplied lookup/stage success, canonical-width, role equality, nonalias, funding, fit or frame premise is added.

Fresh normal AllGuarantees/Trust passes1763 jobs; fresh global trust retains29 disclosed existing axioms. Read-only comparison passes1355 source identities,11 pins,27 ordinary foundation-only scopes,24 source receipt hashes and three configuration identities. Actual normal artifacts are recorded separately because path-dependent setup/trace bytes differ in the integration checkout. The wrapper redirects only build-identities output to external scratch; every other dossier output remains byte-comparison-only. Root read the full validator before execution.

Build/global/scoped checks ran on union `a95f5e2f2b2e5987b7324307ccbe5b60f466278f` with exactly the current new locator wiring. The later main337 merge adds only the accepted quote integration review and its receipt update; all source, dependencies and actual wiring are unchanged. Those validations are reused by identity, not rerun or described as freshly repeated on a different source tree.

The full independent source review is retained verbatim. Source1372-job final log replays source/public and actively builds tests, with an unnecessarySimpa suggestion. Nineteen kernel computations plus the public rollback and concrete success, eight actual-SHA native diagnostics and eight selected actual inherited Gateway Forge tests retain their stated scope. Complete2519-line Gateway IR and22 compiler inputs are reused exactly; fresh runtime evidence binds23 inputs and three artifacts. Structural hash fixtures, real SHA diagnostics, locator fallback and router recorder boundaries are distinct. Root did not rerun compiler/Forge/native checks.

The immutable locator identity/body, role/pause, outer gateway-to-router CALL/dispatch and prior allocation remain explicit phase boundaries. Changing the consumed Context.sender does not establish that omitted outer call. Scalar locator-next and credential-next are linked; module returnBuffer remains separate. Full memory/copy/alias/gas, deployment/storage/context provenance, complete LOG/error bytes, precompiles in the ordinary no-code arm and external hash/root semantics retain scope. No whole-TOPUP or whole-guarantee closure is claimed. New source/integration diff-check passes; inherited unrelated compiler-output exceptions are unchanged. Writer stops for exact integration review.
