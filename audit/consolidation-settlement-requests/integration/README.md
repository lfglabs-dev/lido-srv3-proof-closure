# Settlement request integration

The independently reviewed 27f2394 source is united with accepted main241c2ce1 (ACCOUNT319 and TOPUP318). The three candidate source bodies, their imported executors and all pinned dependencies are unchanged. AllGuarantees now imports the public joint consumer. Trust imports the seven kernel regressions and actively queries the public result, its source proof, the two public instances and the concrete decoded-loop/refund effects. Existing Lake providers cover all three paths; no provider or executable is changed.

The normal combined AllGuarantees/Trust build passes1700 jobs using unchanged caches and builds the new source, public facade and test. The fresh global trust gate recomputes dependencies and re-evaluates native claims: exactly29 existing disclosed axioms, not a globally foundations-only claim. The independently reviewed source packet checks nine ordinary foundations-only scoped sets,1249 source identities,11 pins and13 hashes. Its three source files were also checked independently through normal Lean. Seven regressions include the two public theorem instances.

The same successful settlement now jointly yields the full earlier Success certificate and the exact decoded request payloads, widths, per-call fees, nested attempts and returned loop world actually consumed by refund. Outer and inner fees are separately read on incoming and credited worlds. No fee-equality, array-roundtrip, successful-stage or callee-frame premise is added. The starting world remains already payable-credited; full gateway admission, producer/decoder memory equivalence, LOG ABI and net recipient credit under arbitrary callbacks remain outside this bounded composition.

Forty-one inherited Solidity artifact/fixture identities and three complete pinned Solidity bodies were independently checked. Historical9+5 tests are reused by identity; no fresh Solidity runtime execution or new compiled composition refinement is claimed. The original candidate dossier is preserved as history; this directory supplies combined integration validation. A separate independent review of the frozen integration remains required before merge.
