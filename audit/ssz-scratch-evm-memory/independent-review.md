# Independent review — SSZ scratch block on existing EvmYul primitives

**CLEAN for the bounded pre-SHA primitive-memory increment**, exact candidate `38f1ceb327053a4ce54c88f8664967421a5be951`, parent `4286b2de7c5359d7550a2d4b63674e9c8ea94735`. No blocking or nonblocking finding. This verdict does not establish full BLS/SHA execution, gas-charged EVM semantics or closure of SSZ-1.

Also verified that all 15 SSZ candidate paths are byte-identical in its immediate successor `1ef813405ac4d238fea7565c375a313022128858`. That successor adds a disjoint TOPUP lot, which is outside this source review and requires its own independent verdict.

## Exact objects and validation

The reviewed commit adds exactly the 15 authorized SSZ source/test/audit files. Independently compared the 35 outer input/evidence hashes and 1,080 recorded source-closure entries to exact Git objects, using the committed Lake manifest revisions for external packages and Lido submodule pin `17005714f151e5502c559932319a3f2f74ac2436`. All match, as do corresponding working bytes. This yields 1,100 distinct Git file identities after removing overlaps; the recorded closure includes local imported modules as well as external packages. Eight accepted SSZ dependency/configuration inputs are byte-identical to the parent.

Ran the read-only `check_receipt.py`: **1,128 file/package-pin/source comparisons, no error**. This count includes repeated source identities and pin comparisons; it is not a count of 1,128 independent proofs or distinct files. Seven selected Lean core byte/array/UInt/platform files match their recorded hashes. Verity remains `e977aaad6e1a9e92e0132d41b3d33a14135a4d46`, EvmYul remains `f7e4ee0dc8f8d5265ce822a937ab5be771f182e9`; the committed manifest/toolchain/configuration are unchanged. This is source-identity evidence, not a rebuild or audit of every transitive dependency. `git diff --check` passes.

Read both complete new source files and tests, final additions (`context_preserved`, offsets0/257 and typed consumer regression), the existing EvmYul write/read/mstore/calldatacopy/FFI definitions and their opcode routing, the accepted typed pubkey interface, pinned Solidity helpers, and complete audit/harness/receipt/checker material. Existing targeted log attests **1,097 jobs**, 18 public source theorems, one private USize lemma, 25 regression examples, one fixture lemma and eight axiom queries. Only `propext`, `Classical.choice` and `Quot.sound` occur in the queried dependencies. No new axiom, admitted proof or native checker shortcut was found. Existing Conform.Wheels/EvmYul.Pretty deprecation warnings are replayed; no new-file warning is present. No reviewer Lean/Forge build or axiom query was run.

## Source and proof substance

- `SszScratchEvmMemory.lean:17–21` calls the existing `MachineState.mstore(32,0)` and then `SharedState.calldatacopy(0,offset,48)` on the same actual shared state. The copy reads `executionEnv.calldata`. These are precisely the pre-hash operations at pinned `BLS.sol:538–550`, after its length48 guard. There is no new memory interpreter, ghost calldata result or substitution of the weaker word-memory abstraction.
- `SszScratchByteArray.lean` derives write/clear/copy normal forms by unfolding existing `ByteArray.write` and core copy/extract operations. The source extent premise discharges the in-bounds branch; small destination/padding bounds justify USize conversion on both supported32/64-bit platforms. `ffi.ByteArray.zeroes` has the inspected Lean `Array.replicate` body, and the real UInt256 zero-word codec is reduced in the kernel. No memory/SHA oracle axiom is introduced.
- `memory_exact` gives the complete memory array: the original raw48-byte slice, then sixteen zero bytes, then the arbitrary original tail starting at64. Initial memory may be empty, short, long or dirty. The second write correctly overwrites bytes32–47 from the earlier clearing word, retaining zeros48–63. This is stronger than merely equality of a final hash or one selected byte.
- `read_exact`, `frame` and `memory_size` derive the actual padded64-byte read, all zero-extended byte observations at addresses at least64, and final array length `max 64 old.size`. The out-of-array frame observes zero; it does not claim preservation of inaccessible hidden storage. These results assume no final read/copy equation and no clean scratch memory.
- `context_preserved` derives preservation of world/environment, gas field and existing return buffers under these direct primitives. Unchanged primitive gas is correctly distinguished from gas charging by the enclosing execution engine; the theorem does not claim gas-free actual EVM opcodes.
- `typed_pubkeyBlock` derives the exact accepted typed input from an actual raw layout `prefix ++ key ++ suffix`, a real UInt256 offset equal to prefix length and key width48. The layout implies the raw extent condition. The UInt8-to-BitVec conversion is lossless. No typed encoded key, copied output, leaf digest or final-root predicate is supplied as a premise. Executing Solidity's ABI decoder to establish this layout remains an explicit boundary.

## Regression and Solidity evidence

The 25 Lean examples cover short/boundary/long dirty arrays, offsets0/7/257, all48 key bytes, all16 padding bytes, frame64/96/beyond allocation and the typed consumer. Seven negative examples execute actual primitive variants for six mutation classes: omitted zero store, reversed operation order, copy32, copy64, shifted offset in both directions, and destination32. They disagree with concrete independent byte observations after proven normalization; these are meaningful mutation witnesses, not just assertions of the implementation's own definition.

The frozen root-authored Solidity receipt/log attests four solc0.8.25 tests, including two fuzz properties of1,024 runs each, all48 one-hot key positions and six mutation classes. The actual helper imports unmodified pinned BLS.pubkeyRoot, dirties scratch and raw ABI padding, varies dynamic key offset and compares its root to independent SHA256(key++zero16). A separately identified duplicate of only the two assembly operations exposes the pre-SHA64 bytes. The dossier correctly distinguishes that observation fixture from the genuine helper execution.

Dirty ABI padding makes copy64 observably wrong. The destination32 mutant restores the free-memory pointer only after capturing its incorrect slice, permitting return without changing that observation. The actual tests frame words64/96; universal framing is provided by Lean. Solidity function prologues do not test short/empty initial byte arrays, and neither Solidity test claims universal precompile failure/returndata behavior. The existing final successful run and exact harness/source/log hashes are sufficient; no redundant Forge run was needed.

## Remaining obligations and downstream wording

This increment proves the input scratch block through existing primitive memory operations. It does not execute the Solidity length guard or ABI preamble, malformed/out-of-bounds calldata semantics, public opcode dispatch, compiler code, gas-charged EVM.X, stack safety, native host allocation resources or deployed runtime. `hfit` is an honest raw calldata-extent condition; the typed consumer derives it only from its explicit layout.

The later SHA staticcall, transfer of this memory input into precompile execution, SHA mathematics, gas/failure schedule, returndata length/output-buffer copying and final mload remain open. In particular the typed ShaReply.output is not established as an EVM call result by this theorem. BLS's success-and-exact32-size checks remain distinct from SSZ.verifyProof's flag-only check. The other SSZ memory/mcopy overload, proof-sibling loads/loop scratch, generalized-index/fls interpretation, root lookup and runtime/consensus provenance are not closed. The accepted leaf, entry and canonical placement programs remain unchanged.

No project writes, Git mutation, commit, push or merge were performed. Only read-only comparisons and this external report were produced. The separately requested small TOPUP credential harness prereview is recorded at `/tmp/lido-router-credentials-harness-prereview.md`; it is not a verdict on the new TOPUP continuation source.
