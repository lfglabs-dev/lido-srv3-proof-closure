# Integration response and scope precision

The original independent review of source commit `2741475f298bfda8d20a778dd9f407ded0517e25` is preserved in full. Root read the source, tests, full review and original 13,782-byte build log, and compared all 1,294 actual imported source bodies with the frozen candidate or the existing local package cache. The 98 unchanged local imports match current main `06d52d72f370dbed09926af11a79cc16079b95b1`; three imported production sources are the candidate additions/changes. The final test module itself is covered by the frozen source identities. No new compilation or Forge execution is attributed to this transport/integration.

`LinksSource` is derived from the executed `prepareDepositABI`, then consumed by `execute_ok_conservation_derived` and the live suffix's `positive_success`. The latter composes the existing physical RESERVE withdrawal and actual beacon callee in one live ledger. It supplies concrete success under explicit admission conditions; it is not a proof that every source input succeeds. Its funding, signature width, seed arithmetic, capacity, configuration binding and address conditions still need to be derived from the full executed path or classified against the useful public promise. They are not waived as accepted new boundaries.

The review's wording “source-ordered root transition” is too broad for the remaining metadata: `LiveBeacon.recordDeposit` still lives in a separate metadata structure and is assembled after the live suffix, while Solidity updates physical module metadata and emits its deposit event before the withdrawal. This candidate proves live ledger/callee composition, not the visibility or order of that physical event to callbacks. The allocation transcript and `obtainDepositData` interface also still require the actual physical/ABI call connection. Error reason tags and wrapper rollback do not by themselves establish every announced source error byte and observable rollback property.

The reviewed README lists general compiled-EVM correspondence and SHA under historical “remaining composition obligations”. Under the current accepted assurance scope, general solc correctness, declared Verity semantics, cryptographic primitives, general gas and consensus are retained boundaries, not new prerequisite campaigns. Lido-specific payload/decoder/physical state/public-consumer correspondence remains necessary. Deployed identity assumptions already recorded by the baseline are preserved without claiming they have been verified anew. The `maxEBType1 = 32 ether` identity is explicit; the mismatch fixture reaches the balance assertion. No domain is silently narrowed to call that identity universally true.

The public registered `PDeposit1.source_deposit_conserves_and_rolls_back` and `PDeposit1.NFrame.verity_tx_composes_nframe_deposit` still need to consume this actual-callee result with their source inputs. This increment closes the supplied `LinksSource` dependency in the supplementary executor and adds conditional live-callee conservation; it does not complete DEPOSIT-1 or any other open guarantee.

The original fixture `prepared_eq` uses `native_decide`, and its dependent fixture theorems retain that axiom. The six `#eval` executions are finite native checks. The separate `LiveBeaconKernelProbe.lean` remains excluded from the delivered target and fails when selected; it is preserved as diagnostic evidence only. A kernel fixture is not asserted and is not a substitute for the necessary public composition. Production queries use only propext, Quot.sound and, where printed, Classical.choice.
