# Consumed TOPUP module-return scalar guards integration

Source `2063fb762cda58e6824f64cb8a395ebace58f24c` is united with accepted main `eee5d6700e5d99cc35eab7bf3e1e2b51a2465857` at union `ad0ffffc13d12b40db5592b12967900c7bd938cb`. Four new Lean bodies and all 1,340 imported source identities remain unchanged. AllGuarantees now imports PTopupMemoryCalls; Trust imports its regression module and prints the two public consumers plus raw_bounds and decode_success. Existing definitions, domains, provider configuration and package pins are unchanged.

Normal combined AllGuarantees/Trust passes 1,712 jobs. Fresh global control recomputes dependencies and re-evaluates native claims: 29 existing disclosed axioms, not globally foundations-only. A separate fresh query on the normal imported test environment matches all 21 candidate axiom sets and uses only foundations. The retained file includes the four normal olean hashes; source identity and fresh normal elaboration are the basis of reuse, not an assertion of byte-identical olean files across worktrees.

The public consumer executes the actual root loop, module CALL, scalar return allocator/uint256-array decoder and existing continuation once. The same actual reply and returned World feed its allocations, bounded sum, authenticated rows, physical zero/positive effects, ordered attempts and failure restoration. Scalar allocation and signed/unsigned decoder guards occur in the reviewed compiler order; raw allocation plus the signed head guard derive nonwrapping size bounds. There is no new size, stage-success, decoder, alignment, sum or no-wrap hypothesis.

The explicit phase returnBuffer input and immutable copied-byte view remain boundaries: origin/alignment, actual returndata copying, mutable-memory aliasing and full bytecode memory simulation are not established. This additive theorem does not imply every old memory-abstract success passes the new guards, nor change the old claim's domain. Actual post-module/post-withdrawal physical relations do not assert a new pre-module ledger frame for arbitrary callees. Typed external calls and accepted compiler/Verity/crypto/gas/consensus boundaries retain their earlier scope; semantic Fault labels do not establish complete revert-byte ABI.

Independent source review covers all four complete sources, the consumed public composition and full pertinent pinned Solidity/IR functions. Its four fresh kernel rechecks, 1,340 source identities, 11 pins, 15 candidate hashes, 17 inherited artifacts and 28 compiler input bodies are retained. Root rechecks source identities, pins and all 15 candidate hashes at this union. Twelve normal-kernel/transport regressions and one full public instance are imported. No fresh Solidity runtime tests were run for this increment; inherited runtime evidence keeps its historical scope. The candidate packet's pending-review wording is historical and superseded by the retained exact source review; integration requires a separate frozen-commit review before merge.

No website changes occur here. The current site publication includes ACCOUNT322 and excludes this unintegrated TOPUP increment until a later reviewed publication pass.
