# Independent frozen TOPUP integration review — CLEAN

Exact reviewed head: `fba7b72443f0abe07f673f5a579f1cf84e0330fa`.
Worktree: `/tmp/lido-topup-memory-integrated`.
Source candidate: `2063fb762cda58e6824f64cb8a395ebace58f24c`.
Accepted main: `eee5d6700e5d99cc35eab7bf3e1e2b51a2465857`.
Union: `ad0ffffc13d12b40db5592b12967900c7bd938cb`.
Independent reviewer: `/root/topup_memory_exact_review`; I authored neither candidate nor integration. Root stopped before review. Review used read-only local checks; this report is the only durable write.

**CLEAN at the exact frozen integration head. No blocking finding.** The previously reviewed source and pertinent pinned Solidity/IR correspondence remain valid by verified body/import identity, and canonical AllGuarantees/Trust wiring correctly exposes the additive theorem without strengthening its boundary.

## Union and actual source identity

Verified the union's two actual Git parents are precisely source2063 and accepted main eee5. Every one of the sixteen source-candidate files, including all four Lean bodies and the complete original dossier, is byte-identical to source2063. Relative to accepted main, all pre-existing tracked files are unchanged except the two reviewed wiring files. This preserves accepted ACCOUNT322 and earlier claims; the account union did not silently alter a covered TOPUP dependency.

Independently reconstructed all four integrated target setup import closures and checked all 1,340 resolved source paths and hashes against the source-reviewed manifest. The source set and actual bodies agree exactly. All eleven manifest/package HEAD pins agree with the source-reviewed pins. These checks cover the actual shared Mac package cache and local source paths rather than assuming reuse is valid merely because a directory exists.

Verified every one of the fifteen original candidate artifact hashes and all five integration receipt hashes. The retained `integration/independent-source-review-2063.md` is byte-identical to my original independent campaign report. Thus its complete four-source review, expanded public conclusion, scalar guard/error/width/order analysis, pinned Solidity/full pertinent IR review, 17 inherited artifact/28 compiler-input verification, independent 21-scope axiom probe and four focused normal-kernel re-elaborations are reusable here. I did not rerun a build or Solidity test absent changed covered dependencies.

## Canonical wiring and evidence

The integration-only diff is exactly one AllGuarantees import, six added Trust lines (regression import, blank line and four queries), and six new integration packet files. `AllGuarantees.lean` imports `LidoSRv3.Audit.Guarantees.PTopupMemoryCalls`. `Trust.lean` imports `LidoSRv3.Tests.TopupModuleMemoryRegression` and queries the two public theorems plus `raw_bounds` and `decode_success`. No definition, registry domain, provider setting, package pin or other proof source changes accompany these imports.

The actual AllGuarantees and Trust `.setup.json` files resolve the new covered modules to this integrated worktree's canonical olean paths; Trust includes the regression module and AllGuarantees includes the public module. Normal build traces name the canonical integrated source files. All four actual normal olean SHA256 values match the integration scoped packet. Source2063 versus integration olean byte identity is not required or asserted: cross-worktree compiled artifacts can differ, while source/dependency identity and their normal elaboration evidence are preserved.

The hash-verified combined build log records `Build completed successfully (1712 jobs).` It contains the four intended Trust axiom queries. The retained fresh global trust-check receipt reports 29 exact existing disclosed axioms: 23 test/mutant native-decision exceptions, three production exceptions (Phase-3 capacity, consolidation flow, SSZ digest), and foundations, with dependencies recomputed and native claims re-evaluated. This is deliberately **not** a globally foundations-only claim. The integration's separate 21 scoped axiom sets exactly equal the source-reviewed sets and contain only `propext`, `Classical.choice`, and `Quot.sound`. These fresh root integration receipts were reused by identity; this review does not relabel them as new independent build runs.

## Meaning and limits preserved

The public theorem still consumes success of actual root loop → module CALL → guarded raw-return allocator/uint256[] decoder → same existing continuation. Produced witnesses/limits are used in the actual module payload; its actual bytes and returned World feed allocations and the continuation. Equality transport supplies inherited Success and Effects on the same result, with scalar allocator/pointer conclusions. There is no second external execution in the program and no newly supplied stage, decoder, size, sum, alignment or no-wrap premise.

The same zero branch/event, positive physical helper/withdrawal Worlds and balance/count relations, cap and exact sum, ordered diagnostic attempts and whole-root failure restoration remain. Arbitrary module effects are not replaced by an assumed pre-module conservation frame. Old memory-abstract success is not asserted to imply success of the stricter scalar execution.

The integration README explicitly preserves phase returnBuffer provenance/alignment, immutable copied-byte-view versus actual mutable-memory copy/aliasing, earlier admission, compiled bytecode/memory linkage, semantic Fault versus complete revert ABI, and accepted compiler/Verity/crypto/gas/consensus boundaries. No general Ethereum or full memory simulation claim is introduced. There are zero fresh Solidity runtime tests; historical runtime evidence retains its original CALL/rejection scope. No website/publication change is part of this head.

Final checked HEAD is `fba7b72443f0abe07f673f5a579f1cf84e0330fa`; worktree status is clean. This exact integration is clear for the root's authorized next step. Later edits/publication require their own applicable verification. Reviewer stopped with no source, artifact, receipt, log or external-service mutation.
