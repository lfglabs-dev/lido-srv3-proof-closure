# Integrated ADDRESS physical reverse conversion

Source `107e57e53ad4b1bd459a062d887bbcac85be6e41` passed independent full-source/Solidity/assembly review. Final union `92e8fb45347e861ab22c671a5cae8c34eb93d7de` includes main338 `494bfc734ecded66ed10a37461711f203f932e4c`. Root adds AllGuarantees/Trust imports, six public/source/test queries and this archive. No old source, theorem domain or configuration changes.

The actual WstETH unwrap now executes the physical reverse-conversion interpreter at its real preburn slot7-selected target. Its captured returned amount is consumed by the actual burn/transfer/queue-guard/forward-quote/enqueue chain. The public result retains the entire preceding physical-forward-quote permit effect and enhanced transcript, then strengthens each same-world item with its reverse formula. No equal-target, equal-rate, frame, nonzero divisor, result-fit or stage-success premise appears. Failure restores the entire pre-permit World; journals remain observations.

Strict SHARES_TOO_LARGE, raw256-bit subtraction/multiplication and zero-divisor INVALID outward empty failure follow the full original Lido and derived assembly. Reverse denominator/internalShares is evaluated before numerator/internalEther, unlike the forward getter. The shared internalEther bound derives all three SafeMath addition bounds from packed Word fields. Actual canonical36-byte calldata and32-byte result are executed and consumed. Generic malformed dispatch, runtime bytecode recognition, internal fault/gas equivalence and arbitrary mutable stETH/permit bodies remain outside this specialization.

Fresh normal AllGuarantees/Trust build passes1766 jobs and actively compiles the three new modules and both aggregators. Fresh global check retains29 existing disclosed axioms. Fresh read-only validation covers1261 source identities,11 pins,nine ordinary foundation-only scopes,91 source receipt hashes and three configuration identities. The integration identities record actual three normal oleans. The full source validator was read before execution; its candidate manifest writes are intercepted as byte-equality checks, permitting only external temporary ordinary dependency probes.

Those checks ran on build union `f4c1d5c5185cfb4edb778270880ddd0a89aee319` with exactly the current wiring. Subsequent main338 merge adds only the independent locator integration report and its receipt update. Every covered source, dependency, configuration and actual wiring byte is unchanged; existing validations are reused by identity rather than rerun or relabeled as executions after that archival merge.

The complete source review is retained verbatim. Source1278 normal jobs,16 kernel examples/two public instances, four native diagnostics and11 Solidity tests including1024 full-Lido fuzz inputs retain scope. Public kernel instances are empty success and paused rollback; nonempty successful compositions are native/Solidity evidence. Complete37-input original Lido and38-input derived fixture compiler identities/assemblies are checked. Real WstETH permits and controller/Foundry storage hooks test distinct A/B/Q targets and rates, captured amounts and rollback; they do not prove signature authentication or that actual Lido.transfer implements those hooks. Root does not claim a new compiler, Forge or FFI run.

Typed lists/calls, outer ABI/memory/allocator/gas, complete low-level faults/LOG encoding, initial deployment/context/storage and immutable-address provenance, arbitrary mutable transfer/permit bodies and their nested observations remain explicit. No stronger assumption is introduced under an accepted global label. The source raw ConversionHarness.asm has the sole new blank-EOF exception at17140; inherited raw artifacts retain their exact original exceptions. New integration files pass diff-check. Writers stop for independent exact integration review.
