# Independent exact ADDRESS reverse-conversion integration review

**CLEAN**, exact commit `778f6aae58391c545bc802b34b5e3747ca973ebb`, tree `9b9013abd341a7532f43707ed9fa3e74993bf789`, sole parent union `92e8fb45347e861ab22c671a5cae8c34eb93d7de`. Reviewed clean frozen `/tmp/lido-address-steth-conversion-integrated`, with the root writer stopped. No blocking integration, public-consumption, source-domain or evidence-identity finding. This accepts the bounded physical reverse-conversion integration, not universal deployed execution. STOP.

## Independent provenance and union

I authored none of the new ADDRESS conversion implementation or integration. Read all three new Lean modules in full, including every proof, sixteen kernel examples, two public instances and the complete native diagnostic program. Read the entire archived independent source107e review and the integration dossier/validator. The source review is byte-identical to the reviewer's external continuous-address-conversion-107e-independent-review.md. Its full Solidity/assembly/fixture review is reused after identity checks below; it is not restated as fresh line-by-line inspection of every compiler path by this integration reviewer.

That source reviewer explicitly authored separate TOPUP work, including locator, and did not independently approve that part of a later union. I therefore read the separate full independent locator6c33 source review, locator961f integration review and locator7134 final archive review. They disclose the locator reviewer's inherited DepositPhysicalAdmission authorship and the distinct39f4/7305 reviews on which independent provenance rests. Those qualifications are preserved. My earlier complete ee24/5e51/1a31 quote reviews apply to the unchanged inherited quote implementation.

A complete recursive Git path/mode/object comparison confirms final union92e8 is exactly the source107e/main338 merge: every source-side change remains source107e, all remaining main entries remain main338, and the path set is their union. Main338 `494bfc734ecded66ed10a37461711f203f932e4c` has exactly the tree of independently accepted locator7134 `7134b9112e1baf13febba36883cb192ce94fd377`. Build union `f4c1d5c5185cfb4edb778270880ddd0a89aee319` differs from final union92e8 only by the locator961f integration report and its receipt update. No covered source, dependency, configuration or wiring input changed across that documentary merge.

The final integration adds only two wiring edits and nine archive files. AllGuarantees imports PAddress1StETHConversionCalls in its import block. Trust imports the regression module and adds six queries covering successful public consumption, failure restoration, canonical_call, conversion_effect, item_effect and paused rollback. Old declarations/imports/queries are preserved.

Independently checked91 conversion source hashes plus8 integration hashes;111 inherited quote hashes plus9 integration hashes;24 locator source hashes plus9 integration hashes. All match. All three configuration files equal source107e and accepted main338. The complete full-source review, archived compiler artifacts and evidence remain bound to the reviewed source commit. No theorem body or old domain was adjusted during integration.

## Complete public result and consumed values

The new PhysicalQuotePermitEffect definition is the entire accepted ee24 successful public conclusion, with only the reverse conversion interpreter specialized: complete permit JoinedEffect and prior WrappedEffect, plus the enhanced forward-quote Transcript, same final World/IDs/attempts and batch length facts. The public proof obtains that actual previous theorem on the new executable, retains it as the first conjunct, and maps its exact transcript to Items. No independent successful stage or detached arithmetic result is supplied.

The actual runner passes AddressStETHConversionCalls.conversion into wrappedStep and through the existing unwrap interpreter. I also revisited the inherited physical slot7 reader, staticWord, tokenProgram and full TokenEffect connection. The canonical36-byte selector0x7a28fb88 request executes body at req.target on the actual preburn World; its32-byte returned Word is decoded by the same StaticEffect. The concrete target is low160 of qualified WstETH slot7 at that World. It is not assumed equal to the later slot7 reread used for mutable transfer or to the queue's supplied STETH target.

Each new Item preserves the complete old enhanced WrappedItem, including all previous transfer/unwrap/forward-quote/enqueue effects and journals. Its additional TokenEffect binds the original wrapped amount to the real burn and the derived received Word to the real mutable transfer payload. The same captured received Word is returned by unwrap and consumed by queue amount guards, the actual forward physical quote at the postcallback World, uint128 share truncation and physical enqueue. Actual IDs, timestamps, owner-set/cumulative fields, events, intermediate/final Worlds and nested/direct attempts remain in the complete retained conjuncts. The old conjunction binds actual attempts even though a subsidiary existential alone would be weaker.

The sole successful public premise is success of the entire concretely specialized permit/batch runner. Strict width, nonzero divisor, bounds and formula derive from that execution. There is no new rate equality, target equality, frame, nonalias, accounting-total, fit, nonzero-divisor, successful-stage or authentication hypothesis. Arbitrary-conversion predecessor statements are unchanged. Failure applies to the same new runner and restores the entire original pre-permit World, including permit effects and earlier items. Attempts remain observations. Empty batches still execute permit and the physical pause check.

## Arithmetic and correspondence reuse

The complete source review establishes the pinned Solidity0.4.24 reverse getter's strict amount<2^128−1 SHARES_TOO_LARGE check, S=(T−E) mod2^256, I=B+V+P+D, raw product modulo2^256 and division by S. InternalEther_bound derives the four packed-field and SafeMath prefix bounds. The reverse assembly evaluates denominator/internalShares before numerator/internalEther, unlike the forward quote; the new executable preserves that binding order. S=0 gives legacy INVALID's outward empty failure after the strict-width check. I=0 with S nonzero returns zero, which the wrapped consumer passes to its later guard. WstETH's own zero-amount rejection precedes conversion. The error abstraction does not identify INVALID with REVERT or prove gas/fault equivalence.

The source review followed complete relevant full Lido, WstETH and derived ConversionHarness paths, including slot7/codeguard, canonical encoding/decoding, captured result, burn and fresh target reread. I reuse those exact compiler correspondence conclusions after checking archived metadata/source/object/assembly identity. Independently recomputed38 derived-harness and18 queue metadata source Keccaks, compiler/settings/source counts and executable objects from retained artifacts; the derived full assembly equals its archived compiler assembly plus newline. Lido and WstETH deployments and the inherited full Lido/WstETH/queue assembly identities also match. The91 source receipt and111 inherited quote receipt bind the original37-input Lido closure and prior reviewed provenance. No production-bytecode identity or generic malformed-dispatch theorem is inferred.

## Normal providers and validation

The retained normal AllGuarantees/Trust log reports1766 successful jobs and actively builds the new source/public/test modules followed by both aggregators as jobs1762–1766. The actual final AllGuarantees setup selects the local conversion source/public oleans; Trust selects all three. Every selected conversion provider source and olean equals the frozen candidate/current normal artifact. All five relevant new/aggregator setups have empty options/plugins; their normal traces are nonsynthetic and have no kernel-skip option. The new source/public/test source, olean and ilean identities match source107e. All three integrated olean hashes match the integration manifest.

The source-checkout trace hashes are deliberately not claimed equal to new integration traces: the latter contain the actual new local source/setup paths and normal build commands. An initial broad comparison exposed that expected path dependence; narrowing identity claims to the recorded integrated oleans and independently inspected local normal traces resolves it without changing or recreating any artifact.

After reading the writer, I reran only the source validator with Python bytecode disabled and candidate write_text calls intercepted as exact existing-byte comparisons. External temporary ordinary dependency probes remained permitted. PASS:1261 actual imported/new source identities,11 pinned packages and nine fresh ordinary transitive closures containing only propext/Classical.choice/Quot.sound. They match both archived manifests; selected old local bodies remain unchanged. No module rebuild or cache write occurred.

An optional attempt to run the full compiler checker unchanged stopped at the absent ignored `solidity/lido/out/ConversionHarness.sol/ConversionHarness.json` in this integration checkout. The source validator had already passed. I did not rebuild or copy a compiler cache, and do not claim that attempted checker passed. Instead, the read-only archived-artifact metadata/object checks described above passed, and the complete source review binds those artifacts to its original actual compiler cache. This is sufficient evidence reuse for an integration with byte-identical source/artifacts. All91+8 receipt hashes remained unchanged after checks.

The retained fresh global gate reports the same29 exact disclosed axioms:23 test/mutant native-decision exceptions, three production exceptions and foundations. It is distinct from the nine foundation-only new scopes. These global/build checks ran at build unionf4c1 with the current wiring; the two-file documentary successor is verified by identity, not described as a later fresh execution.

Source validation retains1278 normal jobs,16 kernel examples/two public instances, four native groups and11 Solidity tests including1024 full-Lido fuzz inputs. Kernel public instances are empty success and paused rollback; nonempty successful composition is native/Solidity evidence. Distinct A/B/Q targets, captured outputs and late rollback are exercised using real WstETH permits and explicitly external controller/storage hooks. This does not prove signature authentication or actual Lido.transfer implements those hooks. No new Forge, FFI or compiler execution was performed here.

## Boundaries and final disposition

The README/receipt retain typed lists/calls, outer ABI/memory/allocator/gas, complete low-level faults/LOG encoding, initial deployment/context/storage and immutable-address provenance, generic malformed dispatch/runtime bytecode recognition, and arbitrary mutable stETH/permit bodies as explicit boundaries. No new stronger assumption is hidden under an accepted label. No site, unrelated continuation or whole-protocol closure is approved by this report.

The exact integration delta passes diff-check. The new source's ConversionHarness.asm blank-EOF exception at17140 remains explicitly documented; inherited compiler artifacts retain their original exceptions. No unconditional whole-source whitespace PASS is asserted.

Candidate remains clean and frozen. Only this external report was written; no candidate/source/cache changes, rebuild, compiler/Forge/FFI execution, merge, push, site action or deployment. **CLEAN exact778f; STOP for root's separate archive-only step.**
