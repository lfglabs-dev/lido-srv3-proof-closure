# Independent exact ADDRESS reverse-conversion source review

**CLEAN within the stated typed-call, physical-storage and outward-result scope**, exact commit `107e57e53ad4b1bd459a062d887bbcac85be6e41`, tree `8bfb8515f4be711beeca4095d13c63176eb10d66`, sole parent `ee24f9dc158ed25cec118a8ab8cd3a4fc59942f1`. Reviewed clean frozen `/tmp/lido-address-steth-conversion` with its author stopped. No blocking source, public-consumption, arithmetic/domain, target/world, rollback or source/compiler-evidence discrepancy found. The sole raw assembly EOF exception is disclosed below. STOP after this external report.

I authored none of this ADDRESS increment, its antecedent quote source, tests or fixtures. I previously independently reviewed the permit increment6e87. I have authored separate TOPUP source increments, including timing and locator work; this report does not constitute independent review of my TOPUP implementation in any later union. The complete retained independent ee24 quote report was read and reused only after unchanged tree/source/evidence identity. It independently covers the inherited forward quote, physical word providers, old wrapped/permit consumers and their stated boundaries.

## Exact tree, normal artifacts and read-only checks

The recursive Git path/mode/object comparison preserves every ee24 entry and adds exactly 92 files: three new Lean modules and `audit/address-steth-conversion-call`. All 91 receipt hashes cover exactly those additions except the receipt and match actual bytes. All 12 recorded new source/olean/ilean/trace identities match current normal artifacts. All three actual setup files have empty options/plugins; trace records are nonsynthetic and contain no skipKernelTC. The source/public/test logs report normal successful builds of 1276/1277/1278 jobs respectively. No old body, dependency, provider, All/Trust, site or configuration was edited by the candidate.

I read both writing validators before executing them. `validate.py` and `check-evidence.py` ran with Python bytecode generation disabled and Path.write_text intercepted: every proposed candidate output had to equal the existing bytes and was not written. Only the existing checker’s external temporary ordinary Lean dependency probe was allowed. Both passed. This independently rechecked 1261 actual imported/new source identities, 11 pinned packages, the selected old local source/olean providers, and nine fresh ordinary axiom closures containing only propext, Classical.choice and Quot.sound. The proposed source-input, axiom, runtime-input, compiler-artifact and compiler-identity files all equaled the retained bytes.

The compiler/evidence comparison also passed all 111 parent receipt hashes, 37 unchanged original full-Lido input bodies, 38 derived ConversionHarness bodies and compiler metadata Keccaks, unchanged queue executable objects/metadata, and exact retained Lido/WstETH deployment artifacts. Independently compared all 12 copied core sources and the actual WstETH source to Git objects at `17005714f151e5502c559932319a3f2f74ac2436`; the 25 vendor bodies retain the exact ee24 provenance. Recomputed both conversion selectors by Keccak. The new complete ConversionHarness assembly equals the actual archived compiler assembly plus its documented newline. No build, compiler, Forge or native diagnostic was rerun; no candidate/source/cache file was written. Final Git status remained clean.

Unqualified `git diff --check ee24 107e` returns2 solely for `audit/address-steth-conversion-call/ConversionHarness.asm:17140: new blank line at EOF`. Excluding exactly that verbatim raw compiler output passes. Its bytes equal the actual assembly artifact; this is the disclosed evidence-format exception, not a source-correspondence defect. There is no unconditional raw diff-check PASS claim.

## Complete new proof and actual consumer

Read all three new Lean files in full, including every proof, the sixteen kernel computations, both public instances and the entire native diagnostic program. Read the complete new Solidity fixture/harnesses, runner/check scripts, manifests, receipts and logs. Followed the accepted actual token interpreter, static-word call, burn/transfer, wrapped transfer/request, batch/permit runners and full public effects relevant to the new connection; reused the independently reviewed unchanged antecedents for their remaining scope.

The public runner actually supplies `AddressStETHConversionCalls.conversion` to `wrappedStep`, through the existing physical transferFrom and canonical unwrap callee. This replaces the executed inverse-conversion interpreter. The proof is not an unrelated arithmetic wrapper around a still-arbitrary reply. `canonical_call` proves the real generated selector0x7a28fb88/36-byte request decodes the actual Word argument, executes `body req.target amount w`, and encodes that body's returned Word. `conversion_effect` extracts its formula from the exact executed StaticEffect and returned bytes.

`PhysicalQuotePermitEffect` retains the entire ee24 successful public conclusion specialized by that concrete interpreter: complete permit JoinedEffect, old WrappedEffect including its length/physical batch facts, plus enhanced forward-quote Transcript on the same permitted/after Worlds, IDs and attempts. The new theorem then maps that exact transcript to stronger Items. Each Item first retains the entire old enhanced WrappedItem and additionally connects its same transferred/unwrapped Worlds, received Word, actual token effect, forward formula/quote, enqueue and ordered journal. The old arbitrary-conversion theorem remains unchanged.

For each actual item, physical transferFrom completes first. The outer zero-value unwrap transfer supplies the actual preburn World. `stETHAddress wstETH before` reads qualified physical slot7 and truncates to low160. The concrete reverse interpreter reads its three physical words at this actual target on that same World. This is distinct from the queue's supplied immutable STETH target and from the fresh slot7 target read after burn for the mutable transfer. No equal-target, same-rate, nonalias or callback-frame assumption identifies them.

The old TokenEffect retained inside the new effect binds the original wstETH amount to the actual burn and the derived received amount to the actual stETH transfer payload. The captured received Word is returned by unwrap even if the mutable callee changes rates or slot7. The queue consumes that same captured value in its 100..1000 ETH guards, derives its uint128 fit, executes the actual physical forward quote on the returned unwrapped World at its own STETH target, then truncates the returned shares to128 bits for the same physical enqueue. Existing timestamps, cumulative fields, request ID/owner-set operations, events, Worlds and direct/nested attempts remain in the preserved complete effects.

The sole public premise is success of this entire specialized permit/batch execution. Primitive success lemmas derive strict input width, nonzero divisor and the formula; none is an extra public prerequisite. The failure theorem applies to the same executable and restores the complete pre-permit World, including permit writes/logs and earlier successful items. Attempts remain observations. Empty batches still execute the actual permit and physical pause check.

## Pinned Solidity, arithmetic and actual assembly order

Read the actual full Lido/StETH/WstETH source bodies, relevant packed storage/Aragon SLOAD/SafeMath providers, and complete relevant paths in the full retained Lido, WstETH and new ConversionHarness assemblies, including shared arithmetic continuations and data constants. This is source/assembly correspondence review, not a universal compiler-correctness theorem.

StETH329–334 first requires `_sharesAmount < UINT128_MAX` with Error("SHARES_TOO_LARGE"). Lido overrides the rate helpers: low/high128 of the shares slot are T/E; buffered slot B/D; CL slot V/P. The consumed helpers implement I=B+V+P+D and S=(T+2^256−E) mod2^256. The successful reverse result is `((amount*I) mod2^256)/S`. The actual packed Word bounds imply each of the four fields is below 2^128, so I<2^130 and every source SafeMath prefix addition fits256 bits. No accounting-total invariant is needed. The raw subtraction and multiplication wrap; T<E and multiplication overflow are deliberately admitted, and the output need not fit128 bits.

The strict maximum is not weakened to <=. With S=0, even amount0 reaches INVALID in the primitive getter after its width check. With I=0 and S nonzero it returns0, later consumed by the queue's amount guard in a wrapped execution. WstETH itself rejects unwrap amount0 before calling the conversion. The model distinguishes the required source error ordering and represents INVALID as outward empty failed subcall `.bubbled []`; consumed subcall gas and internal exceptional-versus-REVERT classification remain explicitly outside that abstraction.

The **reverse** getter evaluates the denominator first, unlike the forward quote. In full Lido.asm, the width-success path tag651 calls denominator380, then tag652 calls numerator378; shared tag461 executes raw MUL, divisor zero check/INVALID and DIV. The new derived assembly independently agrees: tags660→388,661→386, then469 MUL/zero INVALID/DIV. Denominator reads total/external shares then raw SUB. Numerator reads buffered/D first and CL V/P second, followed by the three SafeMath additions. Shared packed getter1058 in original/1068 in derived assembly performs actual SLOAD, low128 mask and high128 division. Derived tags900/1213 and their data constants are the exact shares/buffered slots, while536 carries the exact CL literal. No silent reversal of the compiler's evaluation profile is hidden behind the general compiler boundary.

The actual WstETH0.6.12 unwrap path tag114 first rejects zero, reads SLOAD7 with uint160 cleanup, encodes the canonical request, executes EXTCODESIZE then STATICCALL, bubbles failed bytes and requires at least 32 returned bytes before capturing the Word. It burns the original wstETH amount next. It then rereads slot7, encodes transfer(recipient,captured amount), executes mutable CALL and its minimum-word return decoder, discards that decoded return, and returns the captured amount. This is precisely why the new formula belongs on the preburn target/world while the forward formula belongs after the mutable callback.

## Finite validation and retained boundaries

The sixteen normal `decide +kernel` examples test selectors/length, packed fields, strict maximum priority, zero divisor/zero numerator, wrapped SUB/MUL, large output, target qualification and slot7 cleanup, and conversion-before-burn errors. Both public instances are correctly described: empty successful complete effect and paused pre-permit rollback. Nonempty successful batch execution is runtime evidence, not mislabeled kernel nonvacuity.

Read all four native diagnostic groups and their actual PASS log. They execute real Keccak-backed physical token operations and the new conversion interpreter: item1 uses targetA/rate20:2000 to obtain 700; callback changes slot7 toB and queue targetQ's rate; item2 obtains 1600 atB/rate20:4000. Forward queueQ quotes 3 and 4 shares from its distinct postcallback rates. Actual IDs/events, attempts, allowance, captured outputs and late reverse-invalid rollback are checked. The other groups verify zero captured result reaches the queue guard and actual selected-target no-code failure. FFI identities are bound separately and add no theorem axiom.

Read all 11 Solidity tests and the 1024-run fuzz property. Primitive arithmetic cases deploy the exact unmodified full Lido artifact, not a rate override. Composed cases deploy actual WstETH and signed permits, with three Lido-derived targets A/B/Q. ConversionHarness overrides only mutable transfer and calls an explicit controller hook; its getters/rate helpers remain inherited. Foundry storage hooks model the arbitrary mutable-callee boundary by changing slot7 and physical rates. The fixture does not claim actual Lido.transfer performs those writes. Successful amounts/IDs/cumulative shares are checked; later INVALID restores real permit nonce/allowance, supply/balance, earlier IDs, targets and rates. These retained fresh-author executions are reused by exact inputs/artifact identities, not rerun by the reviewer.

The recorded profiles are 0.4.24+e67f0147 optimizer200 Byzantium for full Lido/ConversionHarness and 0.8.9+e5eed63a optimizer200 London for queue tests, with the unchanged 0.6.12 WstETH deployment artifact. They do not assert production bytecode identity. Unrelated inherited compiler warnings are retained. Initial deployment/context/storage provenance, typed-list/typed-call outer ABI, complete memory/allocator/gas, generic malformed dispatch, full LOG/error encoding, arbitrary permit/mutable-transfer implementations and their nested observations remain explicit. The concrete body interpreter specializes the actual selected request; runtime bytecode recognition/authentication of arbitrary addresses is not added. Signature authenticity is not proved merely because finite signed-permit fixtures run.

**Disposition: CLEAN at exact 107e within those boundaries.** Independent full-source/assembly review and read-only exact validation are complete. Root owns later integration/global provider checks; this source report does not approve a future union or independently review my TOPUP work. Only this external report was written. No project/cache mutation, rebuild, Forge/native/compiler rerun, merge, push, site change or deployment. STOP.
