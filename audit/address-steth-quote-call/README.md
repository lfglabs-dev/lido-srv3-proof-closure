# ADDRESS physical StETH quote consumer

Candidate parent: `6e87eb6e7d6465936d3fab76c3fc7fab5ec51194`. Additive three-module lot. No old source, public wiring, shared definitions, site or roadmap changes. Await independent exact-source review; this is author evidence.

The current permit runners now instantiate the actual queue quote interpreter. Both public success theorems retain the **entire** accepted permit JoinedEffect and additionally derive the per-item physical quote formula in a Transcript on the same permit-returned world, actual callback-returned item worlds, IDs, final world and attempts. The old public theorems remain unchanged. Root failure theorems restore the pre-permit world, including successful permit and earlier items. The new transcript is extracted from the accepted actual effects, not a separately supplied successful quote or totals interpretation.

## Exact physical body

Core pin `17005714f151e5502c559932319a3f2f74ac2436`:

- `contracts/0.4.24/StETH.sol:317–322`: strict amount `< UINT128_MAX`, Error("ETH_TOO_LARGE"), raw uint256 multiply and divide.
- `contracts/0.4.24/Lido.sol:1271–1277,1298–1307`: actual overriding numerator is internal ether; actual denominator is raw totalShares minus externalShares. `_getTotalPooledEther` and its external-ether division are **not** called.
- `Lido.sol:1481–1483,1495–1497,1539–1541`, `utils/UnstructuredStorageExt.sol:38–42`, Aragon `UnstructuredStorage.getStorageUint256`: actual SLOAD then low128 mask/high128 division.

All reads are qualified by the actual request target in the same Live.World. The low/high physical fields are:

| Slot | Low 128 | High 128 |
|---|---|---|
| `6038150aecaa250d524370a0fdcdec13f2690e0723eaf277f41d7cae26b359e6` | T total shares | E external shares |
| `81a11fa1111afa59b50051f60ccf604a39d96acb484dc467ad8eadb4a63f0a5f` | B buffered ether | D deposited post report |
| `096e465397f38e659238ccd5d5a2c434ced54a63fd8d694045bfb058ab9d8112` | V CL validators balance | P CL pending balance |

The concrete result is `((amount * ((T + 2^256 - E) % 2^256)) % 2^256) / (B + V + P + D)`. Every field comes from the actual Word; `internalEther_bound` derives `I < 2^130` without storage assumptions and this bound is part of each derived QuoteFormula. Every prefix sum is at most this final nonnegative sum, so the three actual SafeMath additions cannot overflow. No T>=E, I>0, output-fit, correct-totals, frame, nonalias or successful-stage premise is introduced. Successful execution itself derives I!=0. T<E wraps; zero shares returns zero with positive I; output may exceed uint128 and the unchanged queue cast is consumed.

## Compiler/body and ABI boundary

`Lido.asm` is the complete unmodified Lido 0.4.24 legacy assembly. `QuoteHarness.asm` and `BatchHarness.asm` are complete inherited fixture assemblies. `compiler-identities.json` and `check-evidence.py` bind the actual full metadata source closure, executable objects, optimizer200, compiler builds and profiles. All 37 full Lido compiler inputs were copied offline and checked against the exact core Git pin or accepted original npm hashes; no selective rate harness replaces Lido helpers. The Lido-derived callback fixture adds one source (38), overriding only mutable permit/transfer/transferFrom boundary implementations. Its quote and both rate helper bodies are inherited unchanged. Direct arithmetic tests deploy unmodified full Lido. Queue sources and actual WstETH0.6.12 artifact are byte-identical to the accepted permit lot.

Legacy full Lido assembly tags: quote137/459–462 evaluates numerator378→internalEther825 **before** denominator380→shares890; internalEther825 reads buffered1203 then CL528 and performs three SafeMath485 adds. Packed getter1058 calls storage430/SLOAD, uses `0xffff...ffff`128 mask and division by2^128. Shares/buffered literals are compiler data constants; CL is a literal push. Denominator uses SUB, quote uses MUL and checks zero before DIV. I=0 executes **INVALID**, not Panic nor Error(string). The model represents this outward empty failure as `.bubbled []`; it does not model consumed subcall gas or distinguish INVALID from all other empty failures. Strict amount rejection precedes physical arithmetic. Full compiler warning about SHR in unrelated ECDSA code under the Byzantium target is preserved; this does not occur on the quote path and no Lido signature body correctness is claimed.

The accepted caller emits STATICCALL from queue to actual stETH target with zero value and selector19208451 plus one 32-byte uint256 (36 bytes total), performs the 0.8.9 EXTCODESIZE guard, bubbles failure, and decodes uint256 returndata before uint128 enqueue casting. BatchHarness assembly includes this exact encoding/guard/call/returndata decoder. `canonical_call` proves the concrete request adapter actually decodes that canonical amount and returns actual32byte output. There is no generic malformed-dispatch equivalence claim. The adapter's broader rejected inputs are outside the generated canonical request theorem.

This remains the accepted typed-array/typed-call, ABI/memory/gas scope: arbitrary external mutable worlds are preserved; quote itself has no callbacks/writes. Caller constructor identities and reverse conversion interpreter remain explicit context/external implementation boundaries. Actual permit signature/domain/nonce body remains external in Lean. The Solidity wrapped consumer additionally exercises real signed WstETH permits and actual reverse Lido quotes; this is diagnostic evidence, not a new Lean reverse-conversion or signature theorem. No unified low-level EVM memory or instruction trace theorem is claimed. Address0 retains the accepted Live storage convention. Old arbitrary-callee public domains are untouched; the new theorems specialize only queueQuote.

## Validation and actual attempts

- `final-build-with-nonempty.log`: normal `lake build LidoSRv3.Tests.AddressStETHQuoteCalls`, 1276 jobs PASS; 16 `decide +kernel` regressions; three public instances, including nonempty actual quote failure after permit+transfer and root rollback. Empty success is the public kernel success instance; positive nonempty composed runs are native/Solidity diagnostics, not mislabeled kernel instances.
- `validation-2.log`: 1259 imported/new actual source identities, 11 package pins, 11 fresh ordinary scoped axiom sets, only propext/Classical.choice/Quot.sound. Old actual imported local sources and selected normal oleans checked byte-identical. No hidden axioms/admit/native_decide.
- `native-2.log`: four fresh FFI diagnostics PASS, checking actual stETH/wrapped permit→callback→physical quote IDs/event amounts/shares and later-zero-quote rollback of permit and earlier item. Wrapped conversion remains the declared external function in these Lean diagnostics. Keccak runtime input receipt is separate from proof evidence.
- `solidity-final-test.log`: 13 actual Solidity tests PASS, including 1024 full-Lido packed-input fuzz runs, all physical slot keccaks, strictmax/zeroI/zeroS/rawunderflow/muloverflow/all128max, exact oversized-result queue cast, both postcallback permit consumers, and later INVALID rollback including real WstETH nonce/allowance/balance/supply. Fixed permissive mutable boundary doubles are clearly labeled; they are not claims about actual Lido permit/transfer bodies.
- `compiler-final-check.log`: full metadata/source-byte and executable object checks PASS. `prepare-solidity.py` prepares original Lido closure; queue compiler/source files and archived deployment objects are retained for reproduction.

Failed development commands are preserved, not counted as passing validation: initial source/public proofs had selector/typed existential/let reduction/nested conjunction issues (`development-source.log`, `public-build.log`, `public-build-2.log`); corrected public `public-build-3.log` PASS. Initial test build had an omitted theorem type and unqualified namespace names (`tests-build.log`), corrected `tests-build-2.log` PASS. First Solidity compilation lacked the copied OpenZeppelin remapping (`solidity-test.log`); `solidity-test-2.log` PASS12, then adding the meaningful oversized-cast regression gave final PASS13. First native diagnostic rejected a stETH double returning noncanonical bool2 (`native.log`); double corrected to canonical1, proof rebuilt, final native PASS4. First validator retained the old public filename in NEW and stopped (`validation.log`); corrected final PASS. First compiler receipt lookup assumed queue output `out` but actual Foundry output is `artifacts` (`compiler-check.log`); corrected lookup passed. The first cached QuoteHarness assembly inspection returned literal null (`QuoteHarness-inspect-null.log`). A justified fresh Lido compiler run requesting evm.assembly (`lido-assembly-build.log`) produced the complete 16,353-line fixture assembly; all executable objects and metadata remained identical, and all three archived assemblies equal actual compiler assembly strings plus the CLI newline. No property/premise was weakened to conceal these failures.

Raw compiler assembly EOF blank lines, and any original source whitespace, are preserved byte-for-byte. The final receipt explicitly records raw `git diff --check` status and any exact artifact exceptions; filtered checking is not an unconditional raw PASS.
