# Independent exact ADDRESS physical stETH quote source review

Verdict: **CLEAN within the stated typed-call and outward-result boundary**, exact commit `ee24f9dc158ed25cec118a8ab8cd3a4fc59942f1`, tree `3a1617ad85083a72aec75db45b2922d97b0e7df2`, sole parent `6e87eb6e7d6465936d3fab76c3fc7fab5ec51194`. No blocking source, physical-state, consumed-argument, public-composition or transitive-premise discrepancy found. Reviewed clean frozen `/tmp/lido-address-steth-quote`; author was stopped. STOP after this external report.

This review is independent of the new ADDRESS quote implementation. I did not author its source, tests, fixture or dossier. Inherited ADDRESS evidence is reused explicitly after identity, including the complete prior independent permit6e87 review and the previously read full permit/batch/transfer sources and public effects. The retained permit review discloses its reviewer's TOPUP authorship; it does not claim authorship of ADDRESS. Earlier source reviews are evidence of their bounded scopes, not a replacement for inspecting this increment.

## Exact scope and evidence identity

The complete recursive Git path/mode/object comparison preserves every parent entry and adds exactly112 files: three Lean modules and `audit/address-steth-quote-call`. The111 receipt hashes cover every addition except the receipt itself and all match. No old source, public theorem, dependency, wiring, site or configuration changed. All12 recorded new source/olean/ilean/trace identities match current normal artifacts. New source/public setup files have empty options and plugins. The final test's actual importArts are checked by the validator against selected normal local artifacts and pinned external sources.

I read the writing validators before execution. I ran `validate.py` and `check-evidence.py` with Python bytecode disabled and Path.write_text intercepted: each proposed dossier output had to equal the existing bytes, and candidate writes were blocked. External temporary ordinary Lean dependency probes remained allowed. Both passed. This freshly establishes1,259 imported/new source identities,11 exact package pins,11 active transitive axiom scopes containing only propext/Classical.choice/Quot.sound, and the full compiler/source/object identities. The111 receipt hashes were unchanged after the checks. No build, Forge test, FFI diagnostic, compiler or source/cache write was run. The compiler checker only used retained artifacts, cast keccak and tool version queries.

The original full Lido source closure contains37 inputs. Independently checked all12 core bodies against Git objects at `17005714f151e5502c559932319a3f2f74ac2436`, and all25 vendor bodies and package provenance against the accepted account-fee-distribution source identities. Compiler metadata keccak checks cover all37 original inputs, all38 derived QuoteHarness inputs, and all18 queue inputs. Compiler0.4.24+e67f0147, optimizer200 and Byzantium apply to full Lido and QuoteHarness; queue uses0.8.9+e5eed63a. Archived and cached bytecode/deployed objects and metadata match. All three complete assembly strings equal the actual cached compiler assembly plus the retained newline. Every queue source and the actual WstETH artifact equal the accepted permit fixture. Independently checked all50 permit,52 batch and50 wrapped-transfer inherited receipt hashes. The complete unchanged parent Git tree also binds the reused proof/source review context.

## Full new proof and public consumption

Read the complete AddressStETHQuoteCalls source, PAddress1StETHQuoteCalls public module and test module, including every proof and instance, and the full native program, runner, Solidity test, harnesses, preparation/check scripts and dossier. The new successful theorems use the actual existing runPermit and runBatch runners with `quote target` substituted for queueQuote in stETHStep or wrappedStep. They do not prove facts about a detached computation while executing the old arbitrary quote interpreter.

Each successful conclusion retains the entire prior permit JoinedEffect as its first conjunct. This includes the previous batch length equality, actual permit call, physical pause check at the permit-returned World, ordered same-item Transcript, final World and full attempts. Each wrapped item still contains the entire earlier transfer/unwrap JoinedEffect, including its physical token/allowance behavior and continuation. The additional transcript is mapped from that exact old transcript and uses the same amounts, IDs, intermediate worlds, final world and journal. It is not rerun from an unrelated or pre-callback state.

StItem derives its physical formula on `called`, the actual stETH transfer-returned World. WrappedItem derives it on `unwrapped`, after the actual wrapped transfer/unwrap and arbitrary mutable token callback. Both retain the actual QuoteEffect and actual physical EnqueueEffect using the quote's returned shares modulo2^128, with the actual amount, timestamp, ID and attempts. The original complete effects continue to carry event/world/journal facts. The helper's amount<2^256 premise is discharged by the actual Word's bound in both public paths; it is not a new public assumption. No supplied successful-stage, frame, nonalias, total-accounting, T>=E, result-fit or cryptographic premise was introduced.

Both failure theorems apply to the same concretely specialized runners and restore the entire original World from before permit. This includes permit writes/logs and all earlier successful batch-item effects, even if a later physical quote fails. Attempt journals remain observations and are not claimed erased. Empty batches still execute permit and the inherited physical pause check.

## Physical formula and pinned Solidity fidelity

Read pinned StETH.getSharesByPooledEth, complete relevant Lido overrides/internal-ether/packed-getter bodies, UnstructuredStorageExt, Aragon SLOAD and SafeMath source, and followed the complete relevant retained legacy assembly paths and their shared continuations. This is source/assembly correspondence review with bounded execution evidence, not a new universal compiler-correctness theorem.

The three qualified physical words are exactly:

- `6038150aecaa250d524370a0fdcdec13f2690e0723eaf277f41d7cae26b359e6`: low128 total shares T, high128 external shares E.
- `81a11fa1111afa59b50051f60ccf604a39d96acb484dc467ad8eadb4a63f0a5f`: low128 buffered ether B, high128 deposited-post-report D.
- `096e465397f38e659238ccd5d5a2c434ced54a63fd8d694045bfb058ab9d8112`: low128 CL validators V, high128 CL pending P.

The source first rejects amount>=2^128−1 with Error("ETH_TOO_LARGE"). With I=B+V+P+D and S=(T+2^256−E) mod2^256, the successful quote is `((amount*S) mod2^256)/I`. InternalEther_bound derives I<2^130 from the actual packed Words. Every nonnegative prefix sum is below that final bound, so the three Solidity SafeMath additions cannot overflow; no assumption of correct accounting totals is necessary. Raw Solidity0.4.24 subtraction and multiplication wrap. T<E is intentionally allowed, zero S with positive I returns zero, and the result need not fit128 bits.

The full unmodified Lido legacy path confirms strict rejection before arithmetic; tag137/459–462 evaluates numerator378→internalEther825 before denominator380→shares890. InternalEther825 reads buffered1203 and CL528, then follows the shared SafeMath485 additions/continuations. Packed getter1058 calls storage430/SLOAD, masks low128 and divides by2^128 for high128. Compiler data constants resolve to the shares/buffered slots above; CL is a direct literal. Denominator uses SUB, quote uses MUL, then zero-check and DIV. I=0 executes INVALID even when amount or shares is zero. The adapter's `.bubbled []` represents the resulting outward empty failed subcall, not an assertion that INVALID is a REVERT or that gas/fault classes are equivalent. That distinction is explicitly documented.

The derived QuoteHarness inherits these quote/rate/getter paths unchanged and overrides only mutable permit/transfer/transferFrom boundaries. Its full corresponding quote/internal-helper paths agree. Direct arithmetic tests deploy the unmodified full Lido artifact, not this mutable harness.

The actual queue paths encode selector0x19208451 plus the consumed32-byte argument, total36 bytes, perform EXTCODESIZE then STATICCALL to the actual immutable stETH target, bubble failed returndata and decode a complete uint256 word. I followed both request paths in BatchHarness and the shared decoder/enqueue continuation. Enqueue applies the actual uint128 truncation and inherited checked cumulative arithmetic/storage/event logic. The canonical_call proof establishes the exact adapter decoding for the generated request and actual32-byte return. Broader malformed calls rejected by the adapter are not claimed equivalent to the full legacy dispatcher; that boundary is explicit.

## Validation strength and limits

The retained final normal log reports1,276 jobs PASS with16 kernel regressions and three public instances. The nonempty public failure instance includes permit and transfer before a zero-I quote, and proves root rollback; the empty instance supplies kernel successful public consumption. Positive nonempty composed successes are native/Solidity evidence, not mislabeled kernel instances. Fresh ordinary dependency validation above checked all11 scopes from the final test source.

Read the full four-group native diagnostic program and final log. Wrapped7/8 uses actual allowance15 and changes I after each callback1500→3000→6000; the actual quote yields item shares3 and2. Direct stETH600/1200 yields3 and3. IDs, event values, attempts and allowance effects are checked, and late zero-I restores the pre-permit state. Its reverse conversion remains an external interpreter. FFI source/runtime hashes are separately bound and do not become theorem evidence.

Read all13 Solidity tests and final log, including1,024 packed-input fuzz cases on the full unmodified Lido artifact. They cover strict maximum priority, zeroI/zeroS, raw underflow/multiply overflow, maximal packed fields, actual oversized-result uint128 queue truncation, both postcallback permit consumers, and late INVALID rollback. Wrapped cases use the real WstETH artifact and signed fixture permits, actual inverse quote and token behavior; direct mutable stETH boundaries remain explicit doubles. Their nonce/allowance/balance/supply and earlier-item rollback assertions are substantive finite diagnostics. They do not prove universal permit authentication or remove the external inverse-conversion/permit/STETH-transfer boundaries in Lean.

Failed development proofs, test syntax, remapping, noncanonical bool2 diagnostic, stale validator filename, artifact-directory lookup and null assembly inspection are retained and not credited as successful validation. Corrected final artifacts/logs are identity-bound. The unrelated ECDSA SHR warning under Byzantium is preserved; the reviewed quote path has no such operation. Reviewer-side receipt schema probing initially used the wrong inherited key, then was corrected to `hashes`; all inherited checks passed. No candidate adjustment was needed.

Raw diff-check has exactly three documented blank-EOF exceptions: BatchHarness.asm13650, Lido.asm16951, QuoteHarness.asm16353. Excluding only those three verbatim compiler outputs passes; no unconditional raw diff-check PASS is claimed.

## Disposition

CLEAN at exactee24 for physical stETH quote specialization of both existing permit/batch consumers, complete retained effects, actual postcallback formulas and whole pre-permit rollback. Initial deployment/configuration/storage provenance, typed outer arrays/calls, full ABI/memory/allocator/gas, general low-level fault/LOG equivalence, external permit/inverse-conversion/STETH transfer bodies and general transaction context continuity remain the stated boundaries. Global normal All/Trust integration is root's separate task. No new site, locator or unrelated quote-continuation claim is made.

Candidate remains clean and frozen. Only this external report is written. No source/cache/project mutation, build, native/compiler/Forge rerun, merge, push, site change, deployment or contact. STOP.
