# Integrated ADDRESS physical stETH quote

Source `ee24f9dc158ed25cec118a8ab8cd3a4fc59942f1` passed independent complete source/Solidity review. Union `f7cf6fc528471919b6b5b43c9f375fa9972edf7e` includes main336 `295e6c676b61de1815b7027a49ed8bc5fe5b8b3f`. Root adds AllGuarantees/Trust imports, seven public/source/test queries and this archive. No source body or old theorem domain changes.

Both public permit/batch runners actually use the physical stETH quote interpreter. The complete prior permit JoinedEffect and all per-item effects remain conjuncts, on the same amounts, IDs, intermediate Worlds and attempts. The additional formula reads target-qualified packed Lido state after the real transfer or unwrap callback, and its returned shares feed actual uint128 queue truncation and enqueue. Failure restores the whole World from before permit; journals remain observations.

For amount below the strict ETH_TOO_LARGE threshold, physical I=B+V+P+D and S=(T+2^256-E) mod2^256 yield ((amount*S) mod2^256)/I. The SafeMath bounds are derived from the packed Word widths. No accounting-order, nonzero denominator, result-fit, stage-success or callback-frame premise is added. Zero I follows the legacy INVALID outward empty failed-call result, without equating INVALID with REVERT or proving gas/fault equivalence. The canonical36-byte request and actual32-byte response are proved and consumed. Broader malformed legacy dispatcher inputs are outside this specialization.

Fresh normal AllGuarantees/Trust build passes1760 jobs. Fresh global checking retains29 existing disclosed axioms, distinct from the11 new ordinary foundation-only scoped closures. Fresh read-only comparison covers1259 source identities,11 pins,111 source receipt hashes and three configuration identities. identities.json records the actual three normal oleans. The adapter compares the candidate manifests without mutation, allowing only external temporary dependency probes.

The full independent source review is retained verbatim. Source1276 normal jobs,16 kernel regressions/three public instances, four native groups and13 Solidity tests including1024 full-Lido fuzz cases retain their documented scope. Positive nonempty composed successes are native/Solidity evidence; kernel instances include empty success and nonempty rollback. Complete37-input unmodified Lido and38-input derived QuoteHarness compiler metadata/objects/assembly are bound to the pinned sources. The derived harness changes mutable permit/transfer boundaries only; direct arithmetic tests use full unmodified Lido. Real WstETH fixture permits do not establish universal authentication. Root reuses these exact validations and does not claim a fresh compiler, Forge or FFI run.

External permit, inverse conversion and mutable stETH bodies, deployment/configuration/storage provenance, typed outer arrays and full ABI/memory/allocator/gas, complete low-level faults/LOG encoding and transaction context continuity remain the stated boundaries. No newly stronger claim is placed under a global boundary. Raw source diff-check has exactly three retained compiler EOF exceptions: BatchHarness.asm13650, Lido.asm16951 and QuoteHarness.asm16353. Newly authored integration files pass diff-check. Writers stop for exact integration review.
