# Historical word-copy component input

These are the original 59-file identity and preparation/configuration inputs
from ALLOC-2 commit `188488bc7f19017519d04669eb942918d46d600c`, archived under a
distinct path. Their original component working directory was `byte-abi/`;
the copied preparation script is an historical input, not a standalone replay
entrypoint at this archive location. They accompany receipt `bf503ac6`.

The original 58-file `byte-abi/source-identity.json` remains intact for replay of
the earlier `b0faae30` byte-copy receipt and its Solidity harness. Do not overwrite
one identity with the other. The final root `RunByteABI.lean` directly builds and
elaborates both registered targets against their actual integrated dependencies.

The new word-copy theorem has explicit source coverage, aligned memory size and
fresh destination premises as stated. No new executed word-copy differential
vectors are claimed; the worker's unpublished harness remains outside this input.
