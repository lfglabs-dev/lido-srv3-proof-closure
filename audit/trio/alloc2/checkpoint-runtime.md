# Parent bounds and pinned Verity runtime

ParentArraySafety.lean proves both conversion loops preserve the two array
lengths on success and can fail only with arithmetic panic when their ranges
fit the input arrays. Parent.producer_positive_arrays_safe and
producer_zero_arrays_safe derive those bounds from actual producer success and
actual consumer length preservation. No new count cap or arithmetic-success
premise is assumed. The universal scalar values and total multiplication bound
remain in ParentConversion. Full pointwise conversion correspondence is still
required.

Consumer job cb531c88-1eee-4af2-9325-b73c3fe47756 succeeded with 28 jobs;
composition job a0f555d7-81b0-4478-8b1a-b880b7131b6e succeeded with 34 jobs.
The new array-safety axioms are only propext and Quot.sound. Earlier failed
receipts preserve the tactic and missing-import errors.

runtime/Library.lean adds an explicit adapter to pinned Verity Contract.run.
It proves exact byte outcome and complete ContractState preservation for this
pure library, retaining canonical ABI extent bounds. runtime/Vectors.lean emits
eleven actual Contract.run executions. This is EDSL runtime execution of the
source interpreter, not compiler-generated bytecode or physical-memory proof.
The Solidity runner's fourth argument binds a successful runtime receipt and
all source hashes, checks the same ABI inputs/outcomes, and executes eleven
additional direct-library comparisons.

The first runtime job failed because the Lake target omitted its composition
imports; the second checked the runtime theorems but failed a vector syntax
error. Neither failure is accepted as successful execution evidence. A retry
was rejected before job creation with HTTP 422: ashur had 83 GiB available,
less than 16 GiB estimate plus 80 GiB emergency floor. The estimate was retained
and the corrected build routed to old-agent.

UX2 passed at 1dfd283. The chained provenance command then exited 75 at the
local Lake shim because its environment assignment covered only the UX2
command. A separate pinned-toolchain provenance run passed all guards at the
same fixed source tree. No check was weakened. Producer coordination still
returns writer_identity_stale for its existing PR #245; no retag was attempted.

Still open: producer byte extent/reachable memory allocation; compiler dispatch,
copying and noncanonical ABI refinement; universal parent conversion and parent
frame/rollback; parent mutants; producer agreement/integration; final current
make prove/test/trust and independent review. This remains an active task.

Corrected runtime job 693addeb-cdb6-44fc-a196-3499e5e5dac9 on old-agent
succeeded with 43 jobs and all eleven runtime vectors. The verified overlay has
37 source/config files, including the pinned transitive dependency manifest.
The three runtime theorem axiom reports contain only propext, Classical.choice
and Quot.sound; no sorryAx occurs in the successful receipt.

Differential job 131d08f7-4a2f-4eba-8f71-cd37e3767a2d exited 0: 127 exact
return/revert byte comparisons, comprising 32 decoded, 11 byte-model and 11
Verity runtime vectors plus the existing arithmetic/malformed checks. Parent
rerun 612a2eac-7b4a-441c-8fee-f1fbf5e753db exited 0 with all eight cases
against the current 34-job composition receipt. Metadata generation/checks,
proof-escape and source-annotation checks passed. Main remains bcfbb5f027a5.
