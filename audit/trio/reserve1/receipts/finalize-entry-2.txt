LidoSRv3/Audit/Source/TrioReserve1/FinalizeEntry.lean:47:12: error: Tactic `rfl` failed: The left-hand side
  {
    outcome :=
      { outcome := Except.error e✝, world := (QueueFinalize.finalize keccak ctx last amount rate before).world,
          attempts := (QueueFinalize.finalize keccak ctx last amount rate before).attempts }.outcome,
    world :=
      { outcome := Except.error e✝, world := (QueueFinalize.finalize keccak ctx last amount rate before).world,
          attempts := (QueueFinalize.finalize keccak ctx last amount rate before).attempts }.world,
    attempts :=
      { outcome := Except.error e✝, world := (QueueFinalize.finalize keccak ctx last amount rate before).world,
          attempts := (QueueFinalize.finalize keccak ctx last amount rate before).attempts }.attempts }
is not definitionally equal to the right-hand side
  match (QueueFinalize.finalize keccak ctx last amount rate before).outcome with
  | Except.error e =>
    { outcome := Except.error e, world := (QueueFinalize.finalize keccak ctx last amount rate before).world,
      attempts := (QueueFinalize.finalize keccak ctx last amount rate before).attempts }
  | Except.ok a =>
    { outcome := Except.ok (),
      world :=
        { core := (QueueFinalize.finalize keccak ctx last amount rate before).world.core,
          balances := (QueueFinalize.finalize keccak ctx last amount rate before).world.balances,
          logs :=
            (QueueFinalize.finalize keccak ctx last amount rate before).world.logs ++
              [{ emitter := ctx.self, name := "BatchMetadataUpdate",
                  values :=
                    [word ((before.core.readContractSlot ctx.self.val Queue.finalizedSlot).val + 1),
                      (QueueFinalize.finalize keccak ctx last amount rate before).world.core.readContractSlot
                        ctx.self.val Queue.lastSlot] }] },
      attempts := (QueueFinalize.finalize keccak ctx last amount rate before).attempts }

case h_1
keccak : Queue.Keccak
ctx : Context
last amount rate : Word
before : World
ha : (before.core.readContractSlot ctx.self.val QueueFinalize.resumeSlot).val ≤ before.core.blockTimestamp.val
hu : (before.core.readContractSlot ctx.self.val (QueueFinalize.memberSlot keccak ctx.sender)).val % 256 ≠ 0
hf : (before.core.readContractSlot ctx.self.val Queue.finalizedSlot).val + 1 < Verity.Core.UINT256_MODULUS
x✝ : Except Fault Unit
e✝ : Fault
heq✝ : (QueueFinalize.finalize keccak ctx last amount rate before).outcome = Except.error e✝
⊢ {
      outcome :=
        { outcome := Except.error e✝, world := (QueueFinalize.finalize keccak ctx last amount rate before).world,
            attempts := (QueueFinalize.finalize keccak ctx last amount rate before).attempts }.outcome,
      world :=
        { outcome := Except.error e✝, world := (QueueFinalize.finalize keccak ctx last amount rate before).world,
            attempts := (QueueFinalize.finalize keccak ctx last amount rate before).attempts }.world,
      attempts :=
        { outcome := Except.error e✝, world := (QueueFinalize.finalize keccak ctx last amount rate before).world,
            attempts := (QueueFinalize.finalize keccak ctx last amount rate before).attempts }.attempts } =
    match (QueueFinalize.finalize keccak ctx last amount rate before).outcome with
    | Except.error e =>
      { outcome := Except.error e, world := (QueueFinalize.finalize keccak ctx last amount rate before).world,
        attempts := (QueueFinalize.finalize keccak ctx last amount rate before).attempts }
    | Except.ok a =>
      { outcome := Except.ok (),
        world :=
          { core := (QueueFinalize.finalize keccak ctx last amount rate before).world.core,
            balances := (QueueFinalize.finalize keccak ctx last amount rate before).world.balances,
            logs :=
              (QueueFinalize.finalize keccak ctx last amount rate before).world.logs ++
                [{ emitter := ctx.self, name := "BatchMetadataUpdate",
                    values :=
                      [word ((before.core.readContractSlot ctx.self.val Queue.finalizedSlot).val + 1),
                        (QueueFinalize.finalize keccak ctx last amount rate before).world.core.readContractSlot
                          ctx.self.val Queue.lastSlot] }] },
        attempts := (QueueFinalize.finalize keccak ctx last amount rate before).attempts }
LidoSRv3/Audit/Source/TrioReserve1/FinalizeEntry.lean:47:12: error: Tactic `rfl` failed: The left-hand side
  {
    outcome :=
      { outcome := Except.ok (),
          world :=
            { core := (QueueFinalize.finalize keccak ctx last amount rate before).world.core,
              balances := (QueueFinalize.finalize keccak ctx last amount rate before).world.balances,
              logs :=
                (QueueFinalize.finalize keccak ctx last amount rate before).world.logs ++
                  [{ emitter := ctx.self, name := "BatchMetadataUpdate",
                      values :=
                        [word ((before.core.readContractSlot ctx.self.val Queue.finalizedSlot).val + 1),
                          (QueueFinalize.finalize keccak ctx last amount rate before).world.core.readContractSlot
                            ctx.self.val Queue.lastSlot] }] },
          attempts := (QueueFinalize.finalize keccak ctx last amount rate before).attempts }.outcome,
    world :=
      { outcome := Except.ok (),
          world :=
            { core := (QueueFinalize.finalize keccak ctx last amount rate before).world.core,
              balances := (QueueFinalize.finalize keccak ctx last amount rate before).world.balances,
              logs :=
                (QueueFinalize.finalize keccak ctx last amount rate before).world.logs ++
                  [{ emitter := ctx.self, name := "BatchMetadataUpdate",
                      values :=
                        [word ((before.core.readContractSlot ctx.self.val Queue.finalizedSlot).val + 1),
                          (QueueFinalize.finalize keccak ctx last amount rate before).world.core.readContractSlot
                            ctx.self.val Queue.lastSlot] }] },
          attempts := (QueueFinalize.finalize keccak ctx last amount rate before).attempts }.world,
    attempts :=
      { outcome := Except.ok (),
          world :=
            { core := (QueueFinalize.finalize keccak ctx last amount rate before).world.core,
              balances := (QueueFinalize.finalize keccak ctx last amount rate before).world.balances,
              logs :=
                (QueueFinalize.finalize keccak ctx last amount rate before).world.logs ++
                  [{ emitter := ctx.self, name := "BatchMetadataUpdate",
                      values :=
                        [word ((before.core.readContractSlot ctx.self.val Queue.finalizedSlot).val + 1),
                          (QueueFinalize.finalize keccak ctx last amount rate before).world.core.readContractSlot
                            ctx.self.val Queue.lastSlot] }] },
          attempts := (QueueFinalize.finalize keccak ctx last amount rate before).attempts }.attempts }
is not definitionally equal to the right-hand side
  match (QueueFinalize.finalize keccak ctx last amount rate before).outcome with
  | Except.error e =>
    { outcome := Except.error e, world := (QueueFinalize.finalize keccak ctx last amount rate before).world,
      attempts := (QueueFinalize.finalize keccak ctx last amount rate before).attempts }
  | Except.ok a =>
    { outcome := Except.ok (),
      world :=
        { core := (QueueFinalize.finalize keccak ctx last amount rate before).world.core,
          balances := (QueueFinalize.finalize keccak ctx last amount rate before).world.balances,
          logs :=
            (QueueFinalize.finalize keccak ctx last amount rate before).world.logs ++
              [{ emitter := ctx.self, name := "BatchMetadataUpdate",
                  values :=
                    [word ((before.core.readContractSlot ctx.self.val Queue.finalizedSlot).val + 1),
                      (QueueFinalize.finalize keccak ctx last amount rate before).world.core.readContractSlot
                        ctx.self.val Queue.lastSlot] }] },
      attempts := (QueueFinalize.finalize keccak ctx last amount rate before).attempts }

case h_2
keccak : Queue.Keccak
ctx : Context
last amount rate : Word
before : World
ha : (before.core.readContractSlot ctx.self.val QueueFinalize.resumeSlot).val ≤ before.core.blockTimestamp.val
hu : (before.core.readContractSlot ctx.self.val (QueueFinalize.memberSlot keccak ctx.sender)).val % 256 ≠ 0
hf : (before.core.readContractSlot ctx.self.val Queue.finalizedSlot).val + 1 < Verity.Core.UINT256_MODULUS
x✝ : Except Fault Unit
a✝ : Unit
heq✝ : (QueueFinalize.finalize keccak ctx last amount rate before).outcome = Except.ok a✝
⊢ {
      outcome :=
        { outcome := Except.ok (),
            world :=
              { core := (QueueFinalize.finalize keccak ctx last amount rate before).world.core,
                balances := (QueueFinalize.finalize keccak ctx last amount rate before).world.balances,
                logs :=
                  (QueueFinalize.finalize keccak ctx last amount rate before).world.logs ++
                    [{ emitter := ctx.self, name := "BatchMetadataUpdate",
                        values :=
                          [word ((before.core.readContractSlot ctx.self.val Queue.finalizedSlot).val + 1),
                            (QueueFinalize.finalize keccak ctx last amount rate before).world.core.readContractSlot
                              ctx.self.val Queue.lastSlot] }] },
            attempts := (QueueFinalize.finalize keccak ctx last amount rate before).attempts }.outcome,
      world :=
        { outcome := Except.ok (),
            world :=
              { core := (QueueFinalize.finalize keccak ctx last amount rate before).world.core,
                balances := (QueueFinalize.finalize keccak ctx last amount rate before).world.balances,
                logs :=
                  (QueueFinalize.finalize keccak ctx last amount rate before).world.logs ++
                    [{ emitter := ctx.self, name := "BatchMetadataUpdate",
                        values :=
                          [word ((before.core.readContractSlot ctx.self.val Queue.finalizedSlot).val + 1),
                            (QueueFinalize.finalize keccak ctx last amount rate before).world.core.readContractSlot
                              ctx.self.val Queue.lastSlot] }] },
            attempts := (QueueFinalize.finalize keccak ctx last amount rate before).attempts }.world,
      attempts :=
        { outcome := Except.ok (),
            world :=
              { core := (QueueFinalize.finalize keccak ctx last amount rate before).world.core,
                balances := (QueueFinalize.finalize keccak ctx last amount rate before).world.balances,
                logs :=
                  (QueueFinalize.finalize keccak ctx last amount rate before).world.logs ++
                    [{ emitter := ctx.self, name := "BatchMetadataUpdate",
                        values :=
                          [word ((before.core.readContractSlot ctx.self.val Queue.finalizedSlot).val + 1),
                            (QueueFinalize.finalize keccak ctx last amount rate before).world.core.readContractSlot
                              ctx.self.val Queue.lastSlot] }] },
            attempts := (QueueFinalize.finalize keccak ctx last amount rate before).attempts }.attempts } =
    match (QueueFinalize.finalize keccak ctx last amount rate before).outcome with
    | Except.error e =>
      { outcome := Except.error e, world := (QueueFinalize.finalize keccak ctx last amount rate before).world,
        attempts := (QueueFinalize.finalize keccak ctx last amount rate before).attempts }
    | Except.ok a =>
      { outcome := Except.ok (),
        world :=
          { core := (QueueFinalize.finalize keccak ctx last amount rate before).world.core,
            balances := (QueueFinalize.finalize keccak ctx last amount rate before).world.balances,
            logs :=
              (QueueFinalize.finalize keccak ctx last amount rate before).world.logs ++
                [{ emitter := ctx.self, name := "BatchMetadataUpdate",
                    values :=
                      [word ((before.core.readContractSlot ctx.self.val Queue.finalizedSlot).val + 1),
                        (QueueFinalize.finalize keccak ctx last amount rate before).world.core.readContractSlot
                          ctx.self.val Queue.lastSlot] }] },
        attempts := (QueueFinalize.finalize keccak ctx last amount rate before).attempts }
LidoSRv3/Audit/Source/TrioReserve1/FinalizeEntry.lean:78:6: warning: This simp argument is unused:
  pure

Hint: Omit it from the simp argument list.
  simp [Describes, ha, QueueFinalize.entry, Live.read, require, bind, bindExec,
  ̵  ̵ ̵ ̵ ̵ ̵pure,̵ ̵p̵u̵r̵e̵Exec, fail,
  ̲  ̲ ̲ ̲ ̲ ̲Active] at *

Note: This linter can be disabled with `set_option linter.unusedSimpArgs false`
LidoSRv3/Audit/Source/TrioReserve1/FinalizeEntry.lean:78:12: warning: This simp argument is unused:
  pureExec

Hint: Omit it from the simp argument list.
  simp [Describes, ha, QueueFinalize.entry, Live.read, require, bind, bindExec, pure, p̵u̵r̵e̵E̵x̵e̵c̵,̵ ̵fail,
  ̲  ̲ ̲ ̲ ̲ ̲Active] at *

Note: This linter can be disabled with `set_option linter.unusedSimpArgs false`
