The registered RESERVE-1 model uses cached queue demand and supplied authorization. This draft implements live calls, physical packed accounting, actual ETH transfer and transaction rollback, with independent specifications and source composition proofs. **Full parent specification coverage and integration readiness remain open.**

Changes are confined to the four RESERVE-1 owned directories. The branch preserves recovered head `fa377ac1372733b8781255a5e47cfde80f92e9d6`; Solidity is pinned at `17005714f151e5502c559932319a3f2f74ac2436` and Verity at `e977aaad6e1a9e92e0132d41b3d33a14135a4d46`.

- Independent ACL logic-control rules are total and unique. Source correspondence preserves the selected child, bool negation and ordered oracle trace, with no assumptions about unselected children. Separate proofs cover out-of-bounds indices, invalid logic operators and first-child failure. Whole-tree and independent leaf/resource correspondence remain open.
- ACL depth bounds now have proved stability: increasing capacity preserves any completed result and its oracle trace, and any two completed capacities agree. This extends through permission selection and dispatch. It does not establish termination for cyclic storage or EVM gas/stack correspondence.
- ACL now evaluates physical specific/wildcard permissions and packed parameter arrays, including comparisons, short-circuit logic, invalid enums and raw oracle STATICCALL. Independent comparison/grant rules and source composition through Kernel/Aragon are proved; unconditional permission derives admission from storage. Recursion exhaustion remains an explicit host limit requiring termination/resource correspondence. A 28-case suite executes inherited ACL/Kernel/Aragon/Lido and checks layout, state/events and ordered call results.
- Kernel permission forwarding now resolves the physical nested ACL mapping, short-circuits zero ACL, checks code and forwards exact role bytes. Composition derives the kernel response through Lido admission and the target writer, preserving nested calls and callee world effects. ACL evaluation remains an explicit boundary. A pinned inherited Kernel fixture checks the actual mapping and forwarding behavior.
- External target-writer admission now executes Aragon initialization/kernel checks and the exact role-query calldata/decoder before writes. Proofs retain callee world changes and transaction rollback. Ten pinned-bytecode comparisons cover denial, no code, malformed replies, rejection and success. Kernel replies in these historical ten cases are explicit boundary fixtures; the separate source ACL suite supplies broader implementation coverage.
- Allocation maximality/conservation is related to actual queue CALL bytes and saved buffer/reserve locals. Admission derives from bunker bytes, the post-call pause word, router lookup and caller equality, with ordered early failures.
- Pipeline.success/rejected/shortage derive full concrete source executions through accounting, seeds and the ETH receiver, with exact world/log/trace effects. Prefix calls are derived from physical/configuration inputs rather than assumed successful. Complete unmutated source configurations in the differential runner now use Pipeline.external and report that coverage.
- PhysicalReserve.success_preserves relates the concrete successful withdrawal to physical buffer/reserve subtraction and the independent reserve-protection invariant. Every cell of a distinct queue contract is unchanged, so its next live demand read agrees. No reserve <= buffer or hash-injectivity premise is added; queue/Lido namespace separation is explicit.
- Independent sequence rules distinguish immediate target reduction, deferred reserve increase at rebalance, and admitted spending. Physical internal-writer/committed-withdrawal sequences refine those rules and preserve all distinct-contract storage, including live queue demand. Concrete successful withdrawal instantiates the spend rule. External sequence reachability, ACL/report admission and queue writers remain open.
- Consensus composition connects actual frame computation and physical pointers through STATICCALL, checked timestamp arithmetic and both caller ABI decoders. It preserves consensus rejection, missing-code and timestamp-overflow traces, and relates the same inputs to independent frame/timestamp specifications. Constructor/layout/primitive binding remains explicit.
- Concrete queue-call proofs derive status and allocation from physical state, including exact underflow panic and unchanged worlds. All immutable lookups are bound through caller decoding. Oracle-call proofs compose the locator/queue/oracle dispatch chain and retain nested attempts on success or rejection; concrete consensus/deployment obligations remain open.
- ABI proofs cover arbitrary-width round trips, truncation and word/tuple decoding. Getter composition retains actual call worlds/traces and the physical locator pointer. Oracle/consensus execution includes nested STATICCALL, physical frame configuration, checked arithmetic and uint64 projections.
- SpendingSpec independently relates the accounting quantities. Source proofs cover packed writes/events around the frame call, exact failure/rollback traces, and bounds derived from actual allocation and physical reads. Accounting additions fit uint256; uint128 storage truncation remains explicit.
- Withdrawal composition substitutes actual allocation/frame executions for a successful-spending assumption, then connects to seed outcomes and the actual router receiver's authorization, ETH credit and event. Late failure restores the original transaction world while retaining attempted calls.
- The fresh Cancun suite passes 51 comparisons and seven executed mutant kills. Nine cases use Pipeline.external, including full-source receiver rejection and ETH shortage after accounting/seed writes, consensus failures and a frame boundary. Original 49-case receipts are retained.

At `bd6f7edb93070e808bda4f45fda54a86c93b8db5`, ACLLogicSpec, ACLLogic, updated LiveTrust and all seven baseline/import checks pass. The other 48 modules' source and olean hashes match prior receipts; inspected axioms are standard. `acl-logic-summary.json` links exact evidence. Runtime sources are unchanged, so prior execution receipts retain their original commit attribution.

At `0e632f4a369f23b918b832be62821cd9a86a8ce5`, ACLBounds, updated LiveTrust and all seven baseline/import checks pass. The other 47 modules' source and olean hashes match prior receipts; inspected axioms are standard. `acl-bounds-summary.json` links the evidence. Runtime sources are unchanged, so the 28-case execution remains attributed to its original commit.

At `c5858d1f0af9e2132982cce5067f4eceed42857c`, six selected Lean checks, seven baseline/import checks, pinned ACL compilation and 28 fresh full-source ACL admission comparisons pass. Physical mapping/parameter layout, state/events and ordered direct/nested call results are checked; host recursion exhaustion fails the runner. The other 42 modules' source and olean hashes match prior receipts; inspected axioms are standard. `acl-summary.json` links exact evidence, including lossless trace archives with verified decompressed hashes. Independent parameter-tree/resource correspondence and deployment/callback/mutation obligations remain open.

At `4917545c7aad3093bfdd6ddfa1dda4ff2cae2618`, three selected Kernel/runner/trust checks, seven baseline/import checks, pinned Kernel compilation and ten fresh admission comparisons pass. Runtime comparison includes the physical ACL mapping, storage/events, and direct/nested call targets, values, bytes, acceptance and depths. The other 41 modules' source and olean hashes match prior receipts; inspected axioms are standard. `kernel-summary.json` links exact evidence. ACL policy evaluation remains outside this boundary-fixture coverage.

At `5ff24074eb374d5b79287991ece3e5206da95bfc`, four selected Aragon/runner/trust checks, seven baseline/import checks and a fresh ten-case admission execution pass. The other 38 modules' source and olean hashes match prior receipts; inspected axioms are standard. `aragon-summary.json` links exact evidence and verified compiler/artifact provenance. Failed drafts are retained. Kernel/ACL evaluation remains outside this boundary-fixture coverage.

At `e47b7d1720dffeb0d09a2aa52e0fd0a5335e0ecf`, SequenceSpec, PhysicalSequence and updated LiveTrust pass, as do all seven baseline/import checks. The other 36 modules' source and olean hashes match prior receipts; only standard axioms appear. `physical-sequence-summary.json` links this evidence. Runtime sources remain unchanged.

At `de68e309d6cef918e7153c971f2396aaa1ec8b2a`, PhysicalReserve and updated LiveTrust pass, all seven baseline/import checks pass, and only standard axioms appear. The other 35 modules' source and olean hashes match their earlier receipts. `physical-reserve-summary.json` records exact checks and reuse. Runtime sources are unchanged; the 51-case execution remains evidence from its original commit.

All 36 Lean component checks passed at `2e4b6a50f68077d65a3d716522f9818fafd50073`. Test-only source `4b82879f28d0e1d58a9972a3db92aa5ea225d898` passed the fresh 51-case execution, seven mutant kills, six baseline checks and import-DAG validation. Every checked Lean source hash is unchanged. `pipeline-summary.json` links exact terminal evidence. The following evidence-only commit does not claim new exact-head full gates.

Validation is bounded component elaboration/axiom inspection plus finite execution, not a clean/full build or independent certification. Exact source/toolchain/command/exit receipts are retained under `audit/trio/reserve1/receipts/`. The installed remote wrapper rejects the lido-core gitlink before submission (exit 2), rejects .lake paths, and offers no dependency-bundle transport. No new remote bundle/node/job identity or full-gate receipt exists; no credentials were accessed.

Still required: complete independent parent return/revert/state/balance/call/event coverage; full source-callee/input/deployment/primitive binding and recursive callbacks; Aragon ACL, initialization/migration and all accounting/queue writer and sequence proofs; bounded EVM world relation; coordinated canonical registration; full remote gates and independent review. No merge or self-certification.
