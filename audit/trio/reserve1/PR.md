P-RESERVE-1's current registered model uses cached queue demand and supplied authorization. This additive draft implements a live-call replacement and checks component proofs and matching Solidity executions. **The full P-RESERVE-1 correspondence is still open; this is not ready for integration.**

Immutable implementation checkpoint: `b06cf0dc0861b93592f1de80820b68ff10b6905f`, descended directly from requested base `c7adae04416704a839d56333efad003f0a0f46b7`. Solidity remains `17005714f151e5502c559932319a3f2f74ac2436`; Verity remains `e977aaad6e1a9e92e0132d41b3d33a14135a4d46`. Changes are restricted to the four owned paths; old declarations and shared Trust, metadata, build configuration and site files remain unchanged.

Implemented and checked:

- `Live` preserves the withdrawal/helper boundaries, real caller equality against locator-returned router, live bunker/pause and queue calls, saved locals across calls, packed accounting, actual balances, ETH transfer, arbitrary returned/rejected bytes and successful external world effects.
- Independent `QueueSpec` and `Queue.StateRel`: `unfinalized_corresponds` proves the numeric live demand or uint128-underflow panic for all related physical states. No monotonicity/freshness or hash-injectivity premise.
- `PhysicalPacking.pack_matches_bitwise` equates numeric packing with the source mask/shift/OR expression, including truncation. Physical low/high projection bounds are checked.
- `Erasure.erase_bind`, `erase_run` and `failure_restores_world` establish compositional instrumentation erasure and interpreter rollback after arbitrary intermediate effects.
- Independent `WriterSpec`: `Writers.target_corresponds` and `rebalance_corresponds` prove scalar physical-state relations for internal helpers; separate observation theorems prove returns, no calls, unchanged balances and exact event order. External ACL/report-parent closure remains open.
- 29 matching executions using inherited pinned Lido (solc 0.4.24), inherited queue demand/bunker (solc 0.8.9), and the handwritten Lean executor with pinned Verity words/storage. Compare exact return/revert bytes, relevant physical slots/balances, directly Lido-issued ordered calls and ABI logs. Cases cover malformed data, word limits/truncation, unauthorized/pause/bunker, ETH rejection, failure after effects, queue finalization/underflow, rebalance and second spending after queue growth. Two executed mutants (cached demand, omitted rollback) disagree with Solidity.

Evidence: `audit/trio/reserve1/STATUS.md`, `SOURCE-MAP.md`, and `receipts/`. Component Lean and owned trust inspection exit 0; only standard propext/Classical.choice/Quot.sound occur. The repository dependency probe confirms all 16 new axiom reports against the built environment (exit 0). Proof-escape, source annotation and baseline inventory/provenance/pin/metadata checks exit 0, but do not constitute new canonical registration. Production/test/trust and make prove/test gates are currently running in an owned disposable worktree against the immutable checkpoint. The earlier remote job terminated failed during private dependency fetch; its terminal receipt is retained. No duplicate remote build or credential changes.

Open: complete independent withdrawal return/revert/state/balance/call/event correspondence; actual immutable locator, oracle/consensus and router implementations; callback/nested-call composition; Aragon ACL; full report/accounting/queue writer composition and sequential invariant proofs. The generic external interpreter and finite fixtures do not discharge these obligations. Full canonical Trust/guarantee/source-map/provenance registration is MISSING under the additive-only boundary.

Assumptions/exclusions: compiler, execution backend and primitive correctness are explicit; full bytecode, gas/deployment-size and full consensus-state truth are excluded. No new parent guarantee is registered or claimed closed. Draft only; no merge, self-certification, deployment, publication, or Lido contact.
