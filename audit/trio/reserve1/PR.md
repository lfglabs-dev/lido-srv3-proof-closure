The registered RESERVE-1 model uses cached queue demand and supplied authorization. This draft implements the live-call path with physical packed accounting, actual ETH transfer and transaction rollback, and adds independent specifications and component correspondence proofs. **Full withdrawal correspondence and integration readiness remain open.**

Changes are confined to the four RESERVE-1 owned directories. The branch preserves recovered head `fa377ac1372733b8781255a5e47cfde80f92e9d6`; Solidity remains pinned at `17005714f151e5502c559932319a3f2f74ac2436` and Verity at `e977aaad6e1a9e92e0132d41b3d33a14135a4d46`.

- Allocation maximality/conservation is independently specified. The executor theorem connects its successful result to the actual locator lookup, queue CALL bytes and ABI decoding while preserving pre-call buffer/reserve locals across arbitrary callee effects.
- The source receiver now enforces immutable LIDO authorization, rejects with NotAuthorized(), emits DepositableEthReceived, and has checked CALL success/rejection effects. Pinned Solidity 0.8.25 with viaIR/Cancun compiles the inherited receiver and linked source libraries. Immutable LidoLocator getters and the live queue are also executed in composed cases.
- Existing physical packing, queue return/panic, internal writer and erasure/rollback proofs remain. Transfer proofs cover debit/credit, self-call balance identity and conservation. The aggregate bound needed for EVM credit bounds remains an explicit upstream obligation.
- The local Cancun suite contains 35 matching cases comparing exact return/revert bytes, physical cells, account balances, ordered direct Lido calls and committed ABI logs. Negative controls cover cached demand, omitted rollback, receiver authorization and receiver event. See actual immutable-source receipts and exit codes in `audit/trio/reserve1/receipts/`.

Validation is component elaboration/axiom inspection and finite matching execution, not a clean/full build or independent certification. The installed remote wrapper is unchanged: supported full-source mode rejects the `lido-core` gitlink before submission (exit 2), rejects `.lake` paths, and offers no dependency-bundle transport. The earlier remote EVMYulLean authentication failure remains infrastructure evidence; no credentials were accessed. Full production/test/trust and make prove/test receipts remain missing.

Still required: complete independent withdrawal return/revert/state/balance/call/event correspondence; actual oracle/consensus and nested/static-call behavior; full deployment/primitive binding; Aragon ACL and all report/accounting/queue writer and sequence proofs; bounded EVM world relation; coordinated canonical Trust/guarantee/provenance registration; full remote gates and independent review. No merge, self-certification, public deployment, unrelated PR changes or Lido contact.
