The registered RESERVE-1 model uses cached queue demand and supplied authorization. This draft implements live source calls, physical packed accounting, ETH transfer and transaction rollback, with independent specification components. **Full implementation, parent coverage and integration readiness remain incomplete.**

Changes stay within the four RESERVE-1 owned directories. The branch preserves recovered head `fa377ac1372733b8781255a5e47cfde80f92e9d6`; Solidity is pinned at `17005714f151e5502c559932319a3f2f74ac2436` and Verity at `e977aaad6e1a9e92e0132d41b3d33a14135a4d46`.

- An independent eight-case withdrawal parent relation now has bidirectional exact return/world/trace correspondence to actual stage observations, covering every early exit and tail outcome without demanding unvisited stages. Complete independent status rules now replace the status interface, including lookup/CALL failures, malformed ABI data and bunker/post-call pause decisions. Queue and router lookup now use complete physical locator/CALL/ABI/address rules, including failures and low-160 narrowing. Independent checked-seed and ordered receiver-tail rules now replace the tail interface, including overflow before the receiver and every receiver outcome. Complete independent spending control now covers allocation failure, insufficient funds, frame failure and success, with saved locals, physical writes and derived arithmetic bounds. Complete oracle lookup and frame tuple rules now substitute the current-frame interface through spending, including lookup/CALL failure and validation of both words before decoding. Allocation and CALL/deployed binding remain explicit lower interfaces. Withdrawal composition derives status, router authorization, live queue allocation, frame calls, packed writes, seeds and receiver behavior from physical/configuration inputs. Successful execution returns the exact world/log/trace effects; receiver rejection and ETH shortage restore the original transaction world.
- Locator, Queue, AccountingOracle, HashConsensus and Router source components retain direct/nested calls, ABI decoding, checked arithmetic and source integer narrowing. Physical reserve proofs connect successful withdrawal to the independent protection invariant and unchanged queue storage under distinct contract namespaces.
- The independent target continuation now covers every authorization outcome, including arbitrary callback worlds, denial and fault rollback, with bidirectional exact result correspondence. Concrete prefix-denial and missing-kernel-code failures are derived from physical inputs. The external target setter has an exact successful return/world/event/trace theorem derived from independent permission rules, plus queue and other-storage preservation. Its closed post-state instantiates the physical sequence relation. Internal target/rebalance writers and committed withdrawal sequences refine independent accounting rules. Target lowering cannot reduce protection; rebalance establishes a recomputed partition. External sequence reachability and enclosing report/queue writers remain open.
- Aragon, Kernel and ACL components execute physical initialization and permission state, nested ACL mapping lookup, packed parameter evaluation, short-circuit logic and raw oracle STATICCALL. Oracle failures, malformed replies, static writes and invalid enums retain their source distinctions. The recursive evaluator reports host exhaustion separately from denial; it is not an EVM gas model.
- Independent ACL comparison, grant and logic-control rules are related to source execution. A fuel-free finite-graph specification has bidirectional recursive correspondence, stable sufficient depths and outcome/trace agreement, with atomic-leaf soundness/completeness discharged by independent input, operator and oracle rules. Ordered permission correspondence now covers absent/unconditional permissions, specific short-circuiting and wildcard fallback with ANY_ENTITY. Independent derivations supply stable traced Lido/Kernel/ACL admission and the target accounting/denial rules. The physical projection uses explicit raw static-call replies; deployed primitive and EVM resource correspondence remain open.

At source `107c6310d6d917b14e564a33181e630b5521f2d0`, the three selected frame-read-spec/frame-read/trust checks and seven baseline/import checks pass. The other 68 modules’ source and olean hashes match prior successful receipts; inspected axioms are only propext, Classical.choice and Quot.sound. [frame-read-rules-summary.json](audit/trio/reserve1/receipts/frame-read-rules-summary.json) records exact checks, dependency revisions and reuse. Draft failures and prior checks are retained. This is bounded component elaboration using existing dependency oleans, not a clean/full build or canonical registration.

Runtime evidence remains attributed to its tested source commits:

- `4b82879f28d0e1d58a9972a3db92aa5ea225d898`: 51 withdrawal comparisons and seven executed mutant kills; nine cases explicitly use Pipeline.external. [Pipeline evidence](audit/trio/reserve1/receipts/pipeline-summary.json).
- `c5858d1f0af9e2132982cce5067f4eceed42857c`: 28 inherited ACL/Kernel/Aragon/Lido comparisons, including physical layout, state/events and ordered direct/nested call results. The runner rejects host-depth exhaustion. [ACL evidence](audit/trio/reserve1/receipts/acl-summary.json).
- Earlier ten-case Aragon and ten-case Kernel boundary suites, draft failures and original runs are retained. Large ACL traces use lossless archives with verified decompressed hashes. Proof-only additions after these executions do not claim new runtime runs.

Still required: deployed ACL oracle/primitive and EVM resource correspondence; full deployment/proxy/primitive and callback binding; discharge every parent stage interface with independent return/revert/state/balance/call/event semantics and concrete callee binding; ACL mutation admission, initialization/migration, enclosing report/submission/finalization and all accounting/queue writer sequences; bounded EVM world relation; coordinated canonical registration; full remote gates and independent review.

The installed remote wrapper rejects the lido-core gitlink before submission (exit 2), rejects .lake paths and offers no supported dependency-bundle transport. The prior EVMYulLean fetch authentication failure is an infrastructure result. No new remote bundle/node/job or full-gate receipt is claimed, and no credentials were accessed. No merge or self-certification.
