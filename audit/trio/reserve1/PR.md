The registered RESERVE-1 model uses cached queue demand and supplied authorization. This draft implements live source calls, physical packed accounting, ETH transfer and transaction rollback, with independent specification components. **Full implementation and integration readiness remain incomplete.**

Changes stay within the four RESERVE-1 owned directories. The branch preserves recovered head `fa377ac1372733b8781255a5e47cfde80f92e9d6`; Solidity is pinned at `17005714f151e5502c559932319a3f2f74ac2436` and Verity at `e977aaad6e1a9e92e0132d41b3d33a14135a4d46`.

- Withdrawal has bidirectional exact return/world/trace correspondence with independent ordered rules for every parent path. Status, queue/router lookup, allocation, spending, frame decoding and seed/receiver tail are expanded. Allocation uses pre-call buffer/reserve observations and independent maximal-priority arithmetic. Independent high-level CALL rules now have bidirectional source correspondence for code/funds guards, provisional transfer, success/rejection and nested traces. These CALL rules are now substituted throughout every expanded stage predicate, preserving exact direct/nested traces and worlds. The raw primitive/deployed-callee interpreter remains the external boundary.
- An independent finite aggregate balance invariant now proves exact total conservation and per-account uint256 bounds for every funded provisional transfer, including self-transfers and fresh recipients. It preserves source credit semantics. Concrete Locator/Queue/Oracle/Router paths now preserve provisional balances; every CALL outcome conserves total balance and preserves aggregate/uint256 bounds under that property. The aggregate invariant now propagates through the complete withdrawal program, failed intermediate stages and root rollback; internal target/rebalance writers also conserve balances. Delegated selectors retain a premise. Establishing the incoming aggregate invariant from EVM state and general callback/deployed-callee closure remain open.
- Packed accounting retains uint128 narrowing and full-count events. Physical bounds discharge intermediate uint256 arithmetic failures on admitted spending paths. Saved locals, post-call worlds, receiver rejection and original-transaction rollback remain distinct.
- Concrete Locator, Queue, AccountingOracle, HashConsensus and Router components retain ABI failures and direct/nested calls. Successful composed withdrawals have exact state, logs and traces; physical reserve proofs connect them to the independent protection invariant and unchanged queue storage under distinct namespaces.
- Aragon, Kernel and ACL execute physical initialization, permission mappings, packed parameter graphs and raw oracle STATICCALL. Independent leaf, graph and ordered permission rules have bidirectional correspondence with exact traces and stable sufficient host depths. Host exhaustion is distinct from denial and is not an EVM gas claim.
- The target setter has exact successful state/events/traces from independent permission derivations and an exhaustive continuation after every authorization outcome. Denial/fault restores the original world; allowed writes retain the authorization-returned world. Internal target/rebalance and committed-withdrawal sequences refine independent accounting rules; general external sequence reachability remains open. Balance conservation now also covers completed ACL/Kernel dispatch, the physical authorization prefix and every external target-setter outcome with rollback; unhandled requests and host-exhaustion fallback retain explicit premises.

- The enclosing collectRewardsAndProcessWithdrawals body now executes saved-locator/accounting authorization, conditional argument-complete vault and queue calls, post-call buffer arithmetic, reserve rebalance and ETHDistributed. Seven Lean source-model cases cover callback mutations, ABI/order/value, call skipping, rejection rollback, authorization/decoding and arithmetic failures. Sixteen comparisons now match the inherited pinned Solidity report body with explicit callee fixtures, including exact CALL observations, storage/logs and ETH rollback. The first comparison exposed and fixed missing reward-vault uint256 return decoding. The report accounting tail now has independent ordered arithmetic rules and bidirectional exact return/world/event correspondence, including every pre-write failure. Independent parent rules now have exact bidirectional whole-report outcome/world/attempt correspondence, including every early exit and root rollback with the captured locator retained. Conditional reward/withdrawal/finalization stages now use independent zero-skip, lookup/CALL failure and return-validation rules with exact correspondence substituted into the parent. Saved-locator decoding and complete-ABI CALL rules are now substituted throughout the report relation, which has no source stage executor or source CALL in its expanded predicate. Raw primitive/deployed vault/queue/callback and resource binding remain open.

Source reward/withdrawal vault models now implement reward balance capping, withdrawal zero/funds/custom-error guards and nested value-bearing Lido callbacks. Callback admission reads the current physical locator; reward collection checks and writes the cumulative counter. ABI failure bytes and nested traces propagate through report rollback. Nine composed Lean cases pass. The production vault/report execution suite below now validates twelve paths; independent callee correspondence remains open.

At source `86ec8bf257ad8244f1a7ef4ae6c194c22f3ba4b5`, the new VaultDifferential component and seven baseline checks pass. The other 99 component source/olean pairs and eleven dependency revisions match retained evidence. Twelve EVM comparisons now match the composed model against unmodified production vaults and inherited Lido report/callback bodies: exact raw root failure bytes, four balances, storage, events and ordered direct/nested CALL payload/value/results. [Vault execution evidence](audit/trio/reserve1/receipts/vault-execution-summary.json) records compiler/source hashes, constructor arguments, deployed code and compressed trace hashes. Locator and queue remain fixtures; this is finite execution coverage, not independent callee correspondence or full integration. Component checks use existing dependency oleans and are not a clean/full build or canonical registration.

Runtime evidence remains attributed to its tested commits:

- `86ec8bf257ad8244f1a7ef4ae6c194c22f3ba4b5`: 12 production-vault/inherited-Lido report and callback comparisons, including nested transfers and late rollback. Locator and queue use fixtures. [Vault evidence](audit/trio/reserve1/receipts/vault-execution-summary.json).

- `4f2b6867518337bc12acd8806fb1b463ff19c516`: 16 inherited report-body comparisons with explicit callee fixtures. [Report execution evidence](audit/trio/reserve1/receipts/report-execution-summary.json). Production vault/queue implementations remain outside this suite.
- `4b82879f28d0e1d58a9972a3db92aa5ea225d898`: 51 withdrawal comparisons and seven executed mutant kills; nine cases use Pipeline.external. [Pipeline evidence](audit/trio/reserve1/receipts/pipeline-summary.json).
- `c5858d1f0af9e2132982cce5067f4eceed42857c`: 28 inherited ACL/Kernel/Aragon/Lido comparisons, including physical layout, state/events and ordered direct/nested call results. The runner rejects host-depth exhaustion. [ACL evidence](audit/trio/reserve1/receipts/acl-summary.json).
- Earlier ten-case Aragon and Kernel suites, draft failures and historical receipts are retained. Large ACL traces are losslessly archived with verified decompressed hashes. Historical proof-only extensions do not claim new runtime runs. The report has its separate 16-case EVM suite above; the older suites do not cover it.

Still required: independent CALL/primitive and deployed-callee/proxy/callback binding across all paths; EVM resource and bounded-world closure; ACL mutation admission, initialization/migration, enclosing report/submission/finalization and all accounting/queue writer sequences; coordinated canonical registration, full remote gates and independent review.

The installed remote wrapper rejects the lido-core gitlink before submission (exit 2), rejects .lake paths and offers no supported dependency-bundle transport. The prior EVMYulLean fetch authentication failure is an infrastructure result. No new remote bundle/node/job or full-gate receipt is claimed; no credentials were accessed. No merge or self-certification.
