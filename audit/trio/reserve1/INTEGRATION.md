# Shared integration proposals — not applied

`proposed-trust.patch` is a concrete component-only Trust import/axiom-inspection
patch against the requested base. It does not replace registered P-RESERVE-1
parents or modify their status. Refresh its context against coordinated shared
integration before applying; no shared file has been edited here.

The build globs already include these production/source and test modules. No
lakefile or Makefile patch is necessary for their compilation. Explicit canonical
Trust inspection is separate from recursive production/test compilation.

Do not prepare an unconditional replacement guarantee or regenerate canonical
metadata/manifests/site claims yet. The complete withdrawal correspondence,
production callee and writer composition, sequential proofs, and relevant
physical/balance/ABI relations are open. When those close, migrate consumers only
after the replacement declarations compile and retain old declarations during
validation. The final registration must list the scoped component statements,
actual source spans, hashes, remaining assumptions and omissions, and actual
execution/mutation evidence; none of the existing statuses is strengthened by
this draft.

The canonical validation receipt is tied to its existing tracked tree. A future
coordinated update must compute its new non-self-referential tree using the real
checker against the staged integration, not copy this draft's SHA into a claimed
green receipt. Missing receipt/Trust/metadata registration is reported missing,
not worked around by altering canonical files inside this additive checkout.

Independent implementation and component checking do not depend on any other
closure/site PR. This delivery neither modifies nor reviews those branches.
