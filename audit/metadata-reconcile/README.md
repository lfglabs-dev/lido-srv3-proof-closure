# Reconcile metadata with integrated main 02b53a15

This increment repairs stale guard inventories and generated evidence for already-integrated sources. It changes no Lean source, theorem registration, guarantee ID, assumption registry, Solidity pin, or guarantee status. All eight complete guarantees remain OPEN. The structured sandboxed.sh tasks remain the roadmap; the existing repository coverage files are evidence only.

The proof-escape inventory had 218 records. The integrated tree adds 73 records exclusively in the five `LidoSRv3/Tests/TrioConsolidation` files listed, hashed and attributed to source commits in `native-decide-inventory.json`. Removing exactly those records recovers both the original count and original digest. The tests are native-backed regressions, not kernel-only proof evidence. No new native use is introduced by this increment; the production axiom allowlist is unchanged, and the strict inventory still rejects future additions, removals and moves. The existing negative escape tests pass.

The public TOPUP guard now enumerates the six declarations already present in the accepted `PTopup1` source: constructor input/admission, the wrong-beacon witness and two obstruction theorems, plus `SourceTopupCallCorresponds`, consumed by the registered parent. The exact declaration/import guard remains enforced; no wildcard is added. The constructor proves only a nonzero argument check and assignment, so the inability to derive the canonical beacon address from that source remains explicit.

Source-span validation preserves the existing `provenance_status` annotation, requires nonempty text, rejects unknown or missing fields, and detects duplicate source identities even when their annotations differ. The UX2 consumer projection preserves that disclosure; a mutation deleting it from the generated record is rejected. The former report basis is retained in `report-basis.json`. Its three structured inputs differ from current integrated main only by the already-accepted TOPUP constructor span and its explicit OPEN annotation. The report binds the new exact input family without inheriting or broadening the historical independent review. The legacy registry prose is not a fresh correspondence certification.

Regeneration changes only the TOPUP UX2 record, its source-tree index, and the R1 report's exact basis and source-span count. The other ten UX2 records and the repository roadmap/status/reproduce files are unchanged. The TOPUP record now quotes the actual registered parent and its explicit call correspondence premise, rather than the obsolete allocation-only parent statement.

`validation.json` records nine successful relevant checks and 577 source input hashes. The metadata and UX2 mutation suites cover invalid annotations, missing/unknown fields, annotation-only duplicate spans, and loss of the consumer disclosure, alongside their existing adversarial cases. Shared span handling and test cases are extracted into small modules so the Python quality ceilings only decrease; no debt ceiling is raised. Python 3.12 runs the AST quality suite (the Mac's default Python 3.9 lacks `ast.Match`). No Lean build, Solidity execution, new axiom audit, or final campaign review is claimed by this documentary increment.

After integration of PR296 at `58c1cea8`, all 577 original validation inputs remain byte-identical. `integration-validation.json` binds its two additional unchanged main sources, the regenerated UX2 source-tree index and three repeated current guards. The original nine checks are reused as recorded, not presented as newly rerun mutation suites.

Before independent review, root found that the optional annotation projection could silently omit mandatory consumer fields. The source writer was reopened to retain strict reads of all six mandatory fields and add six omission checks. The two affected mutation suites and the quality guard were rerun successfully. `strict-projection-validation.json` records the two changed helper inputs and 575 unchanged original inputs; earlier validation stages remain historical, with no generated-output change.
