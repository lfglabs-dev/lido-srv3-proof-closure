# Integrated ACCOUNT physical pause invariant

Source `62669fad69b4bae620458eff7130b2d5e0017720` passed independent complete source/Solidity review. Final union `ee6e49ecd77017fcbe796e579d5e13e2dad3a16b` includes main339 `66c699faabb5d9a248108f6cf6f7032c1e4da58b`. Root registers ReportFeePhysicalPause in the existing AccountAddressChecks provider, imports the public/test modules in AllGuarantees/Trust, adds seven active queries and archives the exact evidence. No old source/public domain is changed.

The new executor obligatorily interprets the legacy activeFlag from the physical Pausable word before running the full324 report/mint/module/treasury executor. The entire324 success and ledger/read effect remains on this explicitly projected input. A derived invariant follows the actual mint total-word write, all actual payment intermediate states and the precise module state consumed by treasury STATICCALL. No supplied flag/storage equality, active input, nonalias, fee-positive or successful-stage premise is added. Whole failure restores the original complete World including its legacy flag metadata.

The pure projection is not an extra early EVM SLOAD trace. Zero fee may normalize only nonphysical metadata without requiring active state or performing mint/distribution. No equality between that successful metadata-bearing output and an unprojected old World is claimed. Literal total-share/pause-slot inequality is kernel-derived, while the accepted abstract shares map and separate modeled router/StETH components remain unchanged. Existing supplied accounting authorization and report prefix/suffix boundaries remain explicit.

Fresh normal AllGuarantees/Trust passes1769 jobs with the flat source now registered and all three new modules and both aggregators actively built. Fresh global checking retains29 existing disclosed axioms. Fresh read-only source validation compares40 recorded source identities,11 pins,six pinned core bodies,nine ordinary foundation-only scopes and all39 source receipt hashes. Integration identities record three actual normal oleans. The manifest/toolchain are unchanged; the lakefile has exactly the new provider-root entry and its integrated hash is recorded. No claim that source-side manual artifact paths/trace bytes equal registered integration paths is made.

Checks ran on build union `3b72bb5096634caca8f1473902a18ed8e927fd40` with the current registration/wiring. The subsequent merge of main339 has the identical Git tree; no source, dependency, configuration or current wiring input changed. Build/global/scoped evidence is reused by these identities, without a repeated build. The comparison wrapper intercepts all three candidate manifest writes, allowing only external temporary ordinary dependency probes; root read the full validator first.

The independent source report is retained verbatim. Source validation covers39 registered prerequisites followed by three manual ordinary module builds,16 kernel computations including literal-slot distinctness, one nonempty complete public success and one late public rollback, and13 actual full-Lido Solidity tests including1024 arbitrary bool-word fuzz inputs. Word2/highbit interpretation, guard priority, module/treasury state and rollback are tested. Full37-input original Lido artifacts and111 inherited quote evidence hashes are reused; fresh fixture compiler metadata and assembly remain checked. Root did not rerun compiler/Forge tests or claim the large forbidden-static gas number as a gas result.

The physical pause interpretation, full prior324 effect and derived invariant are the increment. Abstract share mapping, deployed identities/separation, accounting locator authorization CALL, complete report prefix/reward/rebase suffix, full ABI/memory/gas and universal instruction/bytecode refinement remain outside it. Failed development logs and their temporary sorryAx output are retained as failures; current normal and scoped checks contain only the stated foundations in these results. Raw source EOF exceptions are DistributionHarness.asm1609 and Lido.asm16951. New integration files pass diff-check. Writer stops for independent exact integration review.
