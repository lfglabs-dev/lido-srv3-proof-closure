# Committed module-to-router beacon effects

Core17005714 StakingRouter.sol717-758 calls the staking module, decodes its allocation array, checks alignment/per-key limits and the wrapped sum against the rounded target, then returns on zero or withdraws, calls the beacon helper and checks/restores the router balance before its final event. BeaconChainDepositor.sol72-106 supplies the helper early return and per-key loop; deposit_contract.sol101-159 is the actual accepted modeled beacon callee.

TopupRouterCommitted.continuation_success proves necessary properties of that existing program's successful result: the executed guard total and target bound, then either the actual zero-event return or the actual withdrawal followed by helper and final state. For the positive case, the helper's mathematical ledger and physical slot32 increment/capacity are derived from its executed body through TopupBeaconCommitted. Core/balances are the same state carried into the final router output; logs append the actual router event and chronological attempts are concatenated. No new executor or callee-result input is introduced.

module_execute_success consumes the existing full TopupModuleCall.execute success. The real module request, raw return, decoded allocation list and module-produced World flow into the very suffix covered by the new theorem. Arbitrary module effects are preserved. execute_success_program connects the continuation's actual rollback wrapper to its successful raw result.

Three source theorems; three execution/consumer regression theorems and seven kernel examples. The positive module case uses raw ABI bytes and a module-produced world; a separate actual 3ETH continuation witness forces the positive branch (not the zero disjunction). Only inherited execution success is used to obtain the new effects, not inherited balance/count postconditions. Empty keys move/count zero, as the source early return requires. The retained arbitrary-module witness changes the router balance7->12 with zero allocations; no false pre-module frame is asserted. A malformed reply's root rollback is retained. Ordinary axiom queries only: propext, Classical.choice and Quot.sound; no sorry/admit/native_decide.

## Necessary internal work and external trust

This increment does not deliver TOPUP-1 or TOPUP-2. The count and balance facts are relative to the World after the executed withdrawal; that interpreter's physical preservation is NOT derived here. The arbitrary module can change pre-suffix state. Actual withdrawal/module bodies, router/gateway preamble and input validation, gateway witness/limit admission, target/share computation, physical initialization and history/reentry still need composition. A supplied InitialBound or successful-frame premise is not substituted for those obligations.

The guard sum here is the actual unchecked wrapped total; the helper consumes the mathematical allocation sum only on a reached positive path. This theorem alone does not prove these sums equal. Empty-key behavior is retained, not ruled out by a new premise. A positive capacity bound applies only if a nonzero deposit actually executes; empty/zero loops do not bound arbitrary initial storage.

The accepted Live model's code-size guard differs from the compiled typed-call empty-return failure trace. Decoder allocation/memory/gas limits, exact revert/log ABI and complete compiled execution remain internal correspondence work. SHA implementation/cryptography and authentic deployed/root context remain separate external trust boundaries. The inherited actual Solidity tests exercise the already accepted module ABI and beacon body; they are not new full-router EVM execution or an audit of every dependency/toolchain source.

All eight complete guarantees remain OPEN. ALLOC-1/ALLOC-2/RESERVE-1 and public identifiers are unchanged. Pending independent review of a frozen exact commit and complete source packet; no site or integration credit before that gate.
