def stringForm := s!"{(sorry : Nat)}"
def messageForm := m!"{(sorry : Nat)}"
def formatForm := f!"{(sorry : Nat)}"
def versionForm := v!"{(sorry : Nat)}"
def printForm := println! "{(sorry : Nat)}"
def throwForm := throwError "{(sorry : Nat)}"
def throwAtForm := throwErrorAt ref "{(sorry : Nat)}"
def macroTraceForm := Macro.trace[Demo] "{(sorry : Nat)}"
def traceForm := trace[Demo] "{(sorry : Nat)}"
def traceGoalForm := trace_goal[Demo] "{(sorry : Nat)}"
def reportForm := reportIssue! "{(sorry : Nat)}"
def reportDbgForm := reportDbgIssue! "{(sorry : Nat)}"
def reportEMatchForm := reportEMatchIssue! "{(sorry : Nat)}"
