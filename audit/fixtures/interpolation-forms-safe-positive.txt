def ordinary := "ordinary {sorry} text"
def stringForm := s!"ordinary sorry text {1 + 1} and escaped \{admit}"
def messageForm := m!"ordinary admit text {1 + 1} and escaped \{axiom}"
def formatForm := f!"ordinary axiom text {1 + 1} and escaped \{unsafe}"
def versionForm := v!"ordinary constant text {1 + 1} and escaped \{sorry}"
def printForm := println! "ordinary sorry text {1 + 1}"
def throwForm := throwError "ordinary admit text {1 + 1}"
def throwAtForm := throwErrorAt ref "ordinary axiom text {1 + 1}"
def macroTraceForm := Macro.trace[Demo] "ordinary unsafe text {1 + 1}"
def traceForm := trace[Demo] "ordinary constant text {1 + 1}"
def traceGoalForm := trace_goal[Demo] "ordinary sorry text {1 + 1}"
def reportForm := reportIssue! "ordinary admit text {1 + 1}"
def reportDbgForm := reportDbgIssue! "ordinary axiom text {1 + 1}"
def reportEMatchForm := reportEMatchIssue! "ordinary unsafe text {1 + 1}"
