# Independent exact integration review — TOPUP physical credentials getter

**CLEAN for the bounded integration**, exact commit `0d8c29b4793cb8b7424856949d9722287103db10`, tree `b8f0564663d6361da55692fa695619e5252bc944`. The checkout was clean and the writer stopped. No source, Git, cache or dossier mutation, compilation, Forge, compiler or native diagnostic execution was performed during this review.

## Exact union and archive identity

Parent union `af92a5e8426eb9ff001445c24d9e52f7730f3241` preserves every existing tracked object from accepted main `6e74b316a1cb54f709c67f919d7df252a57dcf97`. Its only additions relative to that main are the seventeen source/audit paths of the corrected source candidate `44d5e9dd54ffe10b7e9be55937810533e228385a`. Every one of those seventeen paths is byte-identical at source, union and final integration. The three Lean bodies remain the exact original 3674 bodies.

The integration commit adds only the new public import in `LidoSRv3/Audit/AllGuarantees.lean`, the regression import and four active axiom queries in `LidoSRv3/Audit/Trust.lean`, and nine files under `audit/topup-physical-credential-getter/integration/`. All existing imports and queries are retained. The new queries cover public success, public rollback, the canonical dispatch/request connection and the concrete public success instance.

Both complete independent reviews are copied verbatim from the external 3674 and 44d5 reports. The archive retains the original P3 finding and its exact documentary resolution, with no retrospective removal of the finding. The integration README accurately distinguishes representative type-byte regression coverage from the universal selection theorem.

I independently checked all **24 receipt hashes**: sixteen in the source receipt and eight in the integration receipt, against exact Git objects and current bytes. The receipt itself is not self-hashed. The only `git diff --check` exceptions are precisely the original review's Markdown hard-break spaces at lines 3 and 4, preserved verbatim and disclosed by the README; there are no other whitespace findings.

## Public utility and correspondence retained

The independent complete source/Solidity/IR review at 3674 and its CLEAN documentation successor at 44d5 carry forward by exact identity. The new public consumer executes the real selected getter interpretation on the actual generated STATICCALL request. Its dispatcher derives the module ID from canonical calldata, qualifies storage by the request target, checks nonzero membership at the root+2 nested mapping before reading the selected type and raw credentials, and returns the canonical bytes32 actually decoded by the inherited caller. Status/Active and type-validity guards are not invented for a getter that does not execute them.

Whole-run success derives the same selected credential word, nonzero physical membership, high byte 2 and low248 storage provenance. That same existential word enters `resolved e wc` and therefore the actual root/witness batch consumer. The public statement retains every preceding Success/Effects, exact result/world/journal, module call, decoder and cursor conjunct; the proof reuses the preceding full conjunction. The error theorem restores the complete initial World. There is no extra premise asserting a successful getter, membership, type, bounds, frame, collision freedom or equality to a later reread after arbitrary module effects.

The retained complete router IR and its twenty-eight source/compiler metadata inputs are unchanged. Their source identity and the exact source paths for selected dispatch, registration, config extraction and WC masking were already independently checked in the source review. Integration does not alter their interpretation or add a fresh solc/Forge claim.

Independence is explicit: I authored none of the new physical-getter source or this integration. I previously authored the inherited DepositPhysicalAdmission substrate. Its independent acceptance rests on the unchanged source-39f4 and integration-7305 reviews retained in `audit/deposit-physical-admission/integration/`, identified and hashed in the full 3674 review. My reinspection of those identical helper operands is not presented as a new independent review of my own prior bodies.

## Build, scoped checks and providers

The archived normal AllGuarantees/Trust build succeeds at **1,748 jobs** and actively builds Source (1.8 s), public guarantee (1.5 s), tests (5.9 s), AllGuarantees (2.2 s) and Trust (2.1 s). The Trust log contains the four new active queries with ordinary foundational dependencies. Its fresh global check retains exactly **29 previously disclosed axioms**, including the existing native-test and production exceptions; this is not described as an all-foundational global inventory. The new increment's **24 scoped sets** contain only `propext`, `Classical.choice` and `Quot.sound` and match the reviewed source scoped records exactly.

Build/global/scoped checks were performed on union `88d7fa2e904e1d11876478e7e2a460763dbe6378`. I verified its complete tracked delta to final union af92 is only the README correction and corresponding receipt hash. Proof, dependency and provider inputs are unchanged; the final integration merely commits the checked wiring and evidence. Reusing these checks is justified without repeating them.

Independent current identity checks covered all **1,349 imported source identities**, all **eleven package pins**, all **151 selected local import olean identities**, and all three new normal source/olean/setup/trace records against the integration-specific archive. Shared import paths were resolved to their actual source bytes and compared. Setup options/plugins are ordinary and empty, traces are nonsynthetic and contain no skipped kernel checking. The source and public/test olean bytes also retain their source-candidate identities; the path-dependent setup/trace hashes are correctly recorded separately for integration.

The existing Lake source/guarantee/test providers cover the three paths without changes. `lakefile.lean`, `lake-manifest.json`, `lean-toolchain` and the trust checker are identical at accepted main, corrected source and this integration. Both aggregator normal setups include the required new public/regression modules. No additional Lake target or dormant unregistered module substitutes for the actual consumer.

The source candidate's seventeen kernel tests, concrete full public instance and four author native SHA diagnostics retain exactly their previous scope. Integration did not rerun the diagnostics or turn their mapping-hash fixture into a native Keccak/storage-layout proof.

## Limits of acceptance

This accepts the physical getter and its existing typed public composition, plus correct repository integration. It does not certify deployment, the complete TOPUP guarantee, registry reachability, compiled memory/copy/gas, outer role/pause/timing/locator/root-age/ABI admission, gateway-to-router entry CALL, final gateway history write, callback framing, or equality between this initial selected credential and an arbitrary later physical reread. All these boundaries are retained explicitly. Separate timing/history work receives no credit here.

Evidence: `/tmp/lido-topup-physical-0d8c-independent-evidence.json`. Review is complete and stopped; an eventual archive-only successor requires only its exact documentary delta check.
