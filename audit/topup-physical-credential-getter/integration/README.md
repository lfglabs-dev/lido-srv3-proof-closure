# Integrated physical TOPUP credentials getter

Corrected source `44d5e9dd54ffe10b7e9be55937810533e228385a` is independently source/Solidity reviewed CLEAN, following full source3674 review and its minor coverage-wording correction. Union `af92a5e8426eb9ff001445c24d9e52f7730f3241` includes proof main332 `6e74b316a1cb54f709c67f919d7df252a57dcf97` and that corrected source. All proof bodies and source inputs remain identical to3674. Root adds AllGuarantees/Trust imports, four public/join queries and this archive.

The actual canonical getter request decodes moduleId, reads source registration and the selected packed type/raw credentials from the request target's physical storage, and produces the bytes32 consumed by the complete preceding TOPUP public result. Membership, exact selected word, type2 and low248 provenance are derived from whole-run success; every previous Success/Effects/cursor/result/world/journal conjunct is retained. No Active/enum guard is added to this getter and no new membership, fit, type, frame, hash-injectivity or successful-stage premise appears. Whole modeled failure restores the input World.

Fresh normal AllGuarantees/Trust build passes1748 jobs and actively compiles the three new modules and aggregators. Fresh global inventory retains29 exact existing axioms, including disclosed native-test and production exceptions. Fresh scope/input validation passes1349 source identities,11 pins,24 ordinary foundational scopes,28 retained compiler inputs and3 normal modules. Path-dependent normal build identities are recorded separately here; all other source-validator manifests were compared without mutation. All16 source receipt hashes were checked before correction and checked again after the two documentation files changed.

The build/global/scoped checks ran on union88d7fa2e904e1d11876478e7e2a460763dbe6378. Its exact diff to finalunionaf92 is solely the source README correction and matching receipt hash. Source, dependencies, normal artifacts and current wiring are identical, so no repeated build/global/scoped check is credited. Source tests cover representative type bytes including255; they are not an exhaustive256-case sweep. The universal selection formula is a separate theorem.

Both complete source reviews are retained verbatim, including the resolved P3 and reviewer independence disclosure. The new getter author and reviewer differ. The reviewer previously authored the reused DepositPhysicalAdmission substrate; its independent acceptance rests on unchanged source39f4/integration7305 reviews cited and hashed in the3674 report. Present inspection of that substrate is not counted as independent review of its author's own work.

Source1366 normal build/regressions,17 named kernel tests/public success instance and4 native diagnostics retain their reviewed scope. Native positives use actual SHA with an explicit mapping-hash fixture; they are not deployed EVM or kernel executions. The complete existing28-input router IR is reused byte-identically and its source/metadata identities verified; no fresh compiler, Forge or integration-native run is claimed. Physical registry reachability, deployment/code identity, full callee/caller memory/gas, outer role/pause/timing/locator/router-call/history, typed external/hash and later callback reread boundaries remain explicit. The getter does not prove equality with credentials reread after arbitrary later module effects.

All newly authored source/integration files pass diff-check. The verbatim independent-source-review-3674.md retains its two Markdown hard-break spaces at lines3–4; these are the only integration diff-check exceptions. Previously retained history is unchanged. Exact integration review follows after writers stop. Separate timing/history work receives no integration or publication credit here.
