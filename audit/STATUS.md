<!-- GENERATED by scripts/audit_metadata.py; edit structured metadata, not this view. Lean is theorem authority. -->

# STATUS

| ID | Abstract Lean | Verity Executable Contract | Fidelity gap | Classification | Assumptions |
| --- | --- | --- | --- | --- | --- |
| `P-ALLOC-1` | CHECKED | CHECKED | live getStakingModuleSummary and getTotalModuleStake calls and returndata; packed ModuleStateConfig slot; unique moduleAddress on addModule; getDepositAllocations / MinFirst fill (see P-ALLOC-2); reachable-router CheckedBounds; modules count is a harness argument: allocate count walks List.range count rather than SRStorage.getModulesCount(), and count is not capped by MAX_STAKING_MODULES_COUNT = 32 (report issues 13/15) | IMPLEMENTATION_PENDING | `A-SOURCE-SHAPED`, `A-VERITY-SCAFFOLD` |
| `P-ALLOC-2` | CHECKED | CHECKED | Solidity allocate does not test active (filtered upstream); +1 Strategy and proportional Source are different algorithms; the registered step parent does not equate w to the independent Model.amount expression, so ceilDiv and the next-level cap are not load-bearing conclusion content; no registered full allocate-loop conservation/correspondence theorem; Verity sourceView calls the same allocateLoop implementation as the transaction instead of an independent interpreter; keccak memory-array oracle; cross-call mutation of the decoded arrays | IMPLEMENTATION_PENDING | `A-HANDWRITTEN-MINFIRST`, `A-VERITY-SCAFFOLD` |
| `P-DEPOSIT-1` | CHECKED | CHECKED | LinksSource is a caller-supplied hypothesis: Wave 4 kill-line alloc_derived_linkssource_kill_line_refutes_bridge shows P-ALLOC-1 CheckedBounds and P-ALLOC-2 step premises plus key-count composition still do not imply LinksSource, because ALLOC does not constrain per-batch wei (firstAmount) and does not constrain publicKeysBatchLength; DEPOSIT_CONTRACT address provenance is assumed rather than pinned to the deployed beacon deposit contract; MAX_EFFECTIVE_BALANCE_WC_TYPE_01 = literal 32 ether is a deployment/constructor fact, not derived by the parent; the executable transaction is an exactly-two-batch shape with no counterpart in the pinned one-module deposit entrypoint and no proved n-frame loop relation; the registered composed parent still carries LinksSource and success Preconditions, making its internal revert conjunct vacuous even though the separate public hypothesis-free rollback theorem is checked | IMPLEMENTATION_PENDING | `A-ABSTRACT-TX`, `A-SOURCE-SHAPED`, `A-VERITY-SCAFFOLD` |
| `P-TOPUP-1` | CHECKED | CHECKED | Verity parent verity_tx_simulates_source still assumes (run cfg inp).reverts = false, so a nonzero wrap is not an executed Verity observation: execute fail-closes on the underfunded push while sourceObservables describes the wrapped success schedule. Abstract conjunct 3 talks wrap unconditionally. Wrap-to-zero is included. NoUncheckedWrap is not a Verity parent hypothesis; beacon-address provenance is a named assumption, not a parent conjunct | IMPLEMENTATION_PENDING | `A-ABSTRACT-TX`, `A-SOURCE-SHAPED`, `A-TOPUP-NOWRAP`, `A-VERITY-SCAFFOLD` |
| `P-ACCOUNT-1` | CHECKED | CHECKED | SRStorage membership and unique uint24 module ids; accountingOracle caller and REPORT_EXITED_VALIDATORS_ROLE; submitReportData / _handleConsensusReportData; fee computation (sharesToMintAsFees is an argument); packed uint64 accounting words; re-read of the written router snapshot for rewards; fullReportSucceeds remains caller-supplied evidence for the demoted sourceTrace child; no later Accounting/mint/reportRewardsMinted execution discharges it | IMPLEMENTATION_PENDING | `A-SOURCE-SHAPED`, `A-VERITY-SCAFFOLD` |
| `P-RESERVE-1` | CHECKED | CHECKED | live WithdrawalQueue.unfinalizedStETH call (freshness is now an explicit freshQueueCache hypothesis with a stale-cache kill-line, not an implicit assumption); canDeposit / bunker / router authorization (now proved as scopedWithdrawGuards on any committed call, but the two booleans are still free inputs, not live bunker/pause/msg.sender checks); packed uint128 buffered ether and ETH transfer; frame-nonce reset and report-time rebalance; _seedDepositsCount; buffer is a declared oracle word, not the contract balance; the live zero-amount guard is outside scopedWithdrawGuards; the model permits amount = 0; reserve-target writer surface setDepositsReserveTarget and its report-time rebalance interaction | IMPLEMENTATION_PENDING | `A-SOURCE-SHAPED`, `A-VERITY-SCAFFOLD` |
| `P-CONSOLIDATION-ETH-1` | CHECKED | CHECKED | VaultHub / Dashboard / TriggerableWithdrawalsGateway / StakingVault withdraw sites (named out of scope); Bus/Gateway/Vault intermediate hops and live executeConsolidation ABI; reverting refund or Lido sinks; refinement between abstract parent and the Verity ensemble; getConsolidationRequestFee versus stored fee slot; composition into P-CONSOLIDATION-1 (FunctionSpec is not yet ConsolidationGateway.addConsolidationRequests); universal Verity-plane characterization of the revert arms: the registered parent quantifies over the funded, guard-passing, non-wrapping, fuel-fit success branch only; premise-necessity kill-lines witness non-success runs outside each premise, but no ∀ revert-shape theorem is registered on the Verity plane; vault→Lido/WithdrawalQueue protocol-return confinement is out of this parent (former P-CONSOLIDATION-ETH-1a retired; not the consolidation fee/refund happy path and not P-RESERVE-1 buffer accounting); canonical consolidation-request address provenance (0x00…7251), named A-CANONICAL-REQUEST-ADDRESS; fee-leg evidence uses the configured immutable/slot only; fee-leg evidence is not yet a registered parent conjunct of eth_flow_parent / verity_tx_universal_success_shape (demoted from sibling row; strengthen into the parent when the canonical address is discharged); renamed public id P-CONSOLIDATION-ETH-1 (was P-ETH-1): not a general SRv3 ETH guarantee | IMPLEMENTATION_PENDING | `A-ABSTRACT-TX`, `A-SOURCE-SHAPED`, `A-VERITY-SCAFFOLD`, `A-CANONICAL-REQUEST-ADDRESS` |
| `P-ADDRESS-1` | CHECKED | CHECKED | singleton-actor exclusion is by omission: singletonActorEntryPoint is False for every modeled tag, so the parent carries no live exclusion proof; live WithdrawalQueue.claimWithdrawalsTo batch iteration, request reads, and payout CALL; packed WithdrawalQueue ERC-721 and WstETH/ERC-20 storage, approvals, balances, and allowances; transferFrom is a wrapper-level single-item projection rather than a proved implementation-body correspondence; external-call success and most environment checks are caller-supplied Booleans; pinned implementation-body extraction and machine-level storage/execution correspondence | IMPLEMENTATION_PENDING | `A-SOURCE-SHAPED`, `A-VERITY-SCAFFOLD` |
| `P-TOPUP-2` | CHECKED | CHECKED | _verifyValidator / 0x02 module WC / block-distance / RootPrecedesLastTopUp; live independent per-key limits then allocateDeposits; Lido withdrawDepositableEther and beacon makeBeaconChainTopUp; gwei versus wei units and 48-byte pubkeys; keccak memory-array oracle; pendingBalanceGwei is trusted operator calldata | IMPLEMENTATION_PENDING | `A-SOURCE-SHAPED`, `A-VERITY-SCAFFOLD` |
| `P-CONSOLIDATION-1` | CHECKED | CHECKED | beacon eligibility, quota, witness, and gateway grouping; 96-byte packed pubkey calldata and real request-contract calls; preservesEthBalance forwarding invariant: the vault must forward exactly msg.value across request CALLs; current success stubs move no wei; Bus publisher/executor path; keccak memory-array oracle; gateway-admitted-nonzero-value premise is caller-supplied and belongs to the outer gateway msg.value surface, not the vault's forwarded totalFee call; observe reconstructs payloads from the call journal rather than rereading sourceMapSlot/targetMapSlot payload storage; composition with P-CONSOLIDATION-ETH-1 requires ABI/interpreter bridge (groups, fee fetch, failing refund, value-bearing CALLs); fence remains until then | IMPLEMENTATION_PENDING | `A-SOURCE-SHAPED`, `A-VERITY-SCAFFOLD` |
| `P-SSZ-1` | CHECKED | CHECKED | SSZ.verifyProof on production gindices; SHA-256 functional correctness of the opaque model/precompile symbol; refinement equating the opaque sha256 symbol with Sha256Engine.sha256 used by the abstract digest child; imported-to-deployed helper/wrapper Yul fragment binding; the state-root anchor (lhs), the concat operand's pow byte (rhsPow), and the tx child's fork version / claimed root remain independently supplied, non-deposit chain-level values; the Verity EncodingInput plane keeps deposit, rhs, witness, and expectedWitnessRoot independent (e.g. Tests/SszEncodingTxMutants.matching pairs a witness at index 2 with rhs := gindex 3 11): the one-object coupling holds at the abstract ComposedSszInput type level only and is not inherited by the executable transaction | IMPLEMENTATION_PENDING | `A-SHA256-FFI`, `A-MULTI-NODE-TRANSPORT`, `A-SOLC-TRUSTED`, `A-YUL-INTERFACE` |
