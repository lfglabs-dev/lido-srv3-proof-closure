# Independent ACCOUNT accounting-CALL integration review

Verdict: **CLEAN for exact `1ed9add5aa439e57e1cea1077849fe8df64678b9`**, tree `1ef1794db302b794a62fbc8d7f163f52ef7c1f2f`, in `/tmp/lido-account-accounting-call-integrated`. No blocking correctness, wiring, evidence-identity or claim-scope finding. Root integration writer was stopped; exact HEAD and clean status were checked. This accepts the registered integration within the source-specialized scope of the retained review, not whole ACCOUNT closure.

Parent union is `4e53291614459778c968ed5e8eda9204b11cb0a3`, combining accepted main `21caf4911ff14ec6a36742eb789eb08e391f1c59` with reviewed source `a4abd69d7acbcae9ff927d45057337029b21eb93`. I authored neither the source nor integration. I authored the source review now archived, so its archival check authenticates that exact report; it is not an independent second review of my own prose.

## Exact source and wiring

I independently checked all68 source-contribution paths against exact a4ab Git objects. Every source/dossier path remains unchanged, including all four Lean modules, their tests, Solidity inputs/artifacts, validators and historical source receipt. Only three existing union files change: lakefile, AllGuarantees and Trust. Exactly ten integration evidence/archive files are added. Every other union tree entry retains its prior object identity.

The complete wiring delta adds exactly `AccountingCall` and `ReportFeeAccountingCall` to the existing AccountAddressChecks flat roots, the new public import to AllGuarantees, the new test import to Trust, and eleven intended axiom queries. Existing roots, imports and queries remain intact. Manifest and toolchain remain unchanged. The integrated lakefile SHA256 is `68a2eeea808e5fe3ada64e9a89f7da8698c67893a021bdde219043fc99110411`.

The full archived independent source/Solidity report is byte-identical to my external a4ab report, SHA256 `99ba1b56d801da95d7df9267792ce91420c3800ee7837ad9d787518831251cda`. Its complete source/compiler review, independent checks, evidence reuse and boundaries remain authoritative for unchanged inputs. I read the integration README, complete comparison wrapper, receipt, identity records, both failed diagnostic logs, passing identity/global-check output, build completion and active-module entries, and actual normal setups/traces/providers.

## Composition and source scope retained

The unchanged runner prepares the actual report/getter/checked fee result before lookup. Zero fees skip lookup and distribution while retaining the established pause metadata interpretation. Positive fees read the physical low160 locator target, execute the legacy EXTCODESIZE/zero-value CALL with selector9624e83e, run the selected immutable getter branch, decode its ABI address, and consume the actual returned World for authorization and mint. Actual module payments then precede the actual treasury STATICCALL on their intermediate World and the transfer to its decoded recipient.

Getter World preservation is derived from that source-specialized getter body, not a supplied frame premise for arbitrary CALL. The successful public theorem still has only whole-runner success as its execution premise. Its explicit derived entry retains the exact old340 execution equality, the entire324 OldEffect and the entire physical-pause PhysicalEffect: report/mint equations, checked split/funding, pointwise abstract ledger, payments/events, treasury ReadEffects and intermediate physical invariants are not weakened. All failures restore the complete original World, including both incoming metadata fields. No supplied authorization equality or successful stage is introduced by wiring.

The a4ab review's full caller/callee examination is reused on byte-identical source/compiler artifacts. Legacy solc0.4.24 CALL remains distinct from treasury STATICCALL: unsigned minimum32-byte decoding and low160 cleanup, with dirty high bits/trailing bytes accepted by the legacy caller. The actual real-proxy fixture and mutable raw-callee test retain exactly their previous evidence scope. The latter demonstrates successful SSTORE under CALL outside the specialized formal getter case; there is no generalized readonly-CALL claim.

The integration README correctly repairs the historical source-map citation by recording `scripts/scratch/steps/0083-deploy-core.ts` and `scripts/scratch/steps/0090-upgrade-locator.ts`. It leaves the hash-bound source packet intact. The original typo was nonblocking and is now explicitly corrected in integration documentation.

## Independent integration verification

The complete wrapper makes precisely two source-validator substitutions: current registered normal artifact records replace source-stage manual records, and the exact integrated lakefile hash replaces its old-base equality. All other deterministic source/dossier checks remain intact. I inspected it before execution. I intercepted its two persistent temporary-output paths: the normal-record output was compared to the archived current record and final identity output to archived identities.json. Candidate dossier writes already compare only; temporary isolated probe files were allowed. Python bytecode writes were disabled, local Lean execution selected, and no candidate/cache output was generated.

Checks passed:

- All67 source receipt hashes and all nine integration receipt hashes/configuration hashes match current files and relevant exact Git blobs. Integration receipt SHA256 is `54b7fa0743838187f4dde4fe61efcfba1aab7cb35f94316b9a1b43bca7c415b1`.
- All45 source identities,11 package pins and10 complete core Solidity identities match. Actual prerequisite setup closures select matching current sources and oleans, including previously cached cross-worktree paths.
- All four current registered source/olean/ilean/setup/trace identities match integration records. All four normal olean hashes differ from source-stage manual hashes, as disclosed; no equality across these build modes is asserted.
- The six actual setups/traces, including AllGuarantees and Trust, have empty options/plugins/dynlibs, nonsynthetic traces and no skipKernelTC. Their traces name the correct source paths, providers and ordinary Lean invocations. Every selected new-module import artifact resolves to the exact current local olean with its recorded hash, including aggregator selection and the helper→composition→public→test chain.
- The hash-verified normal build log completes1782 jobs and actively builds both flat modules, public consumer, test module, AllGuarantees and Trust. This is registered integration evidence, unlike the earlier source-stage manual compilation. No fresh build was needed during this review.
- I independently reran the unchanged isolated axiom query through the comparison wrapper. All eleven actual-environment scopes exactly match source evidence and contain only propext, Classical.choice and Quot.sound, including public success/rollback and both public test instances. The probe imports only Lean and loads the audited environment as data outside its macro syntax.
- The retained fresh global check reports29 exact existing disclosed axioms:23 test/mutant native-decision dependencies, three production exceptions and foundations, with actual-environment dependency recomputation and native claims re-evaluated. I reuse this hash-bound global result; I did not rerun a global check or represent the whole aggregate as foundation-only.

The two wrapper diagnostic failures remain failures: first an overstrict guard rejected external temporary probe writes, then a receipt-key mismatch used `files` instead of `sha256`. The corrected wrapper and independent replay pass without source/build changes. Neither failed attempt receives passing proof credit.

The unchanged source evidence still supports19 test kernel computations plus one helper computation, one nonempty public success and one late whole-entry rollback. The source review already checked all111 inherited provider hashes, complete37-input full Lido assembly/deployment, nine fresh getter/proxy bodies, all2/7/11-input metadata, and15 Solidity tests including two256-run fuzz cases. Identity-preserving reuse is explicit; no Solidity compiler or Forge test was repeated here. Diagnostic profiles and memory/gas/production-equivalence limits remain unchanged.

`git diff --check 4e53291614459778c968ed5e8eda9204b11cb0a3 HEAD` passes. The larger source delta retains exactly the disclosed raw EOF blank lines in LidoLocator.asm1773 and OssifiableProxy.asm3093; this integration check does not claim those historical exceptions disappeared.

## Disposition

Abstract account-share mapping, supplied component/self identities and separation, effective deployed proxy/current implementation/configuration, constructor provenance, full report prefix and reward/rebase suffix, Nat-address admission, full ABI/memory/copy/gas behavior and universal instruction/bytecode refinement remain boundaries. Pure metadata interpretation is not an extra early EVM SLOAD trace. These are the unchanged source-review limits, not newly added public hypotheses.

**CLEAN for exact1ed9add5aa439e57e1cea1077849fe8df64678b9.** Root may archive this report and perform the final archive-only delta check. Only this external report was durably written; no candidate/cache/Git/site mutation, build, compiler/Forge rerun, merge, publication or external contact occurred. Reviewer STOP.
