# Actual accounting CALL registered integration

Sourcea4abd69d7acbcae9ff927d45057337029b21eb93 passed the retained complete independent source/Solidity/consumer review. Union4e53291614459778c968ed5e8eda9204b11cb0a3 combines it with accepted main34321caf4911ff14ec6a36742eb789eb08e391f1c59. The four new Lean modules and entire source dossier remain unchanged. Two flat roots AccountingCall/ReportFeeAccountingCall are now registered in AccountAddressChecks; All/Trust prepend public/test imports and Trust adds eleven queries. Every old provider, query and proof is preserved.

The actual report/getter/checked-fee result determines whether accounting lookup executes. Positive fees read the physical low160 locator target, perform the legacy code guard and zero-value CALL, execute the selected immutable getter, and consume its decoded address and actual returned World for authorization, mint, module payments and treasury. The entire340 executor equality/physical pause chain and324 OldEffect/pointwise abstract ledger remain on an explicit derived entry. Getter-body execution derives its World preservation; no arbitrary mutable CALL is assumed readonly. Zero fee skips the lookup. Failure restores the complete original entry including both untrusted metadata fields. No supplied auth equality, frame, successful stage, fit or nonalias premise is added.

Fresh normal AllGuarantees/Trust passes1782 jobs, actively compiling all four newly registered modules and both aggregators. These replace the source-stage four manual ordinary compilation artifacts; their olean hashes differ and are explicitly recorded as fresh normal artifacts, not falsely asserted unchanged. Actual six normal setups/traces have empty options/plugins, nonsynthetic traces and no skipKernelTC. Both aggregators select the exact new providers. Fresh global check reports29 existing disclosed axioms, separate from11 fresh ordinary scopes containing only foundations.

The comparison wrapper rechecks45 actual source identities,11 pins,10 full core bodies and67 source hashes, preserving every old-body and deterministic dossier-output check. It substitutes only fresh registered normal records and the exact new lakefile hash; manifest/toolchain are unchanged. The isolated dependency probe imports Lean and loads the audited module as data, outside its macro syntax. External temporary probe writes are allowed; candidate dossier writes are compared only. Two failed wrapper attempts are retained: an overstrict temporary-write guard rejected the probe, then the receipt reader used files instead of this source receipt's sha256 key. Both are diagnosed failures, not PASS; the corrected comparison completes without source or build changes.

Retained source evidence remains19 kernel test computations plus one helper selector computation, a nonempty public success and late rollback instance, and15 Solidity tests including two fuzz cases of256 runs each. Complete unmodified37-input Lido/111 reused hashes and fresh getter/proxy/test2/7/11-input artifacts retain exact identity and source-review validation. No additional compiler, Forge or native run is claimed by this integration. Actual proxy/immutable getter runtime tests remain distinct from the effective-code specialization and its deployed implementation/configuration boundary.

Citation correction to the unchanged historical SOURCE-MAP: pinned deployment paths are scripts/scratch/steps/0083-deploy-core.ts and scripts/scratch/steps/0090-upgrade-locator.ts. The independent reviewer checked those complete paths; the missing steps segment is a nonblocking historical citation typo, not a missing code input. This note corrects it without rewriting the hash-bound accepted source packet.

Abstract account keys, supplied component/self context, proxy implementation/deployment identity, full report prefix/reward-rebase suffix, malformed general ABI, memory/gas and universal instruction refinement retain their scope. New raw-source EOF exceptions remain exactly LidoLocator.asm1773/OssifiableProxy.asm3093; this integration's own diff passes. Separate exact integration review is required before merge. Site426 remains pinned to343 and does not credit this candidate.
