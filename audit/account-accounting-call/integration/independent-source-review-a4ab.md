# Independent ACCOUNT accounting-CALL source review

Verdict: **CLEAN for exact `a4abd69d7acbcae9ff927d45057337029b21eb93`**, within the source-specialized scope below. No blocking correctness, composition, compiler-correspondence or evidence-identity finding. One nonblocking source-map path typo and two disclosed raw-assembly EOF whitespace exceptions are recorded below. This is source-candidate acceptance; registered aggregate integration remains separate work.

Frozen checkout `/tmp/lido-account-accounting-call`; tree `c783b05e8c8c9bd7df753ccb0c913bd8c32a830d`; base `53871e66833bd45190eb83dffc15439ceafa9c42` (main340). Independently checked all 5,276 old tracked leaf entries preserve mode, object type and Git object identity, with exactly 68 new paths. HEAD and clean status were checked before and after review. No old domain, source body, dependency, provider, AllGuarantees or Trust configuration changes are present.

## Independence and method

I authored none of this candidate. I read all four new Lean modules completely, including every executable definition, proof, both macros and all tests; the complete new validators/build script, README, SOURCE-MAP, receipt, source/compiler identity records and passing build/test evidence; the real getter/proxy sources and dependency closure; and the complete relevant caller/getter/proxy assembly paths. I followed the consumed ReportFeePhysicalPause, ReportFeeTreasuryCall, TreasuryCall, ReportFeeMint, StETHMintShares, ReportFeeDistribution and FeeDistribution definitions, effects and relevant proofs.

Prior source review6266 is retained verbatim at `audit/account-physical-pause/integration/independent-source-review-6266.md`. I read it, the campaign d954 integration and7f36 archive reviews, and retained324 source reviewa7ea. Their acceptance is reused for inherited obligations, with old input preservation independently checked; it is not substituted for examining the newly composed executor. The earlier reviewers' personal authorship disclosures are theirs, not a claim of authorship by this reviewer.

All checks ran on existing local resources. I read both validators before running them. Candidate-root `Path.write_text` operations were intercepted to assert exact existing byte equality; all13 intended outputs matched. Python bytecode writes were disabled and local Lean execution was selected. The only new Lean execution was the unchanged checker's isolated temporary axiom probe, without candidate output options. No module build, Solidity compilation or Forge test was repeated. Only this external report was durably written.

## Executed composition and complete retained effect

`prepare` fixes `collect` to the actual original physical router report, post-report getter and checked fee calculation, in that order. A report/getter/arithmetic error stops before accounting lookup. `execute` obtains the actual router and fee from these stages; the public caller supplies neither a prepared stage nor a successful-call certificate.

Zero fee bypasses accounting lookup and distribution, retains incoming authorization metadata, and uses the existing physical pause projection. This can normalize nonphysical activeFlag metadata even on successful zero-fee execution. Equality with the original unprojected metadata-bearing World is deliberately not claimed.

For positive fees, `AccountingCall.call` receives `{ReportFeePhysicalPause.project before with router}`. Its request reads the low160 field of the actual StETH physical locator word, uses the StETH self address as CALL caller, zero value and exactly four bytes `96 24 e8 3e`. The effective code table is indexed by that actual target. Missing code rejects before logging a CALL attempt.

With identified getter code, `getter` executes the nonpayable/selector branch and creates its own canonical ABI word from the typed immutable. The environment supplies code/configuration, not raw bytes or a frame certificate. `call_success` derives returned World equality, immutable origin, decoder result, uint160 bound and exact nonstatic successful attempt. `execute` actually passes `withAccounting returned resolved` to `finish`; it does not discard a mutable post-World and substitute an entry snapshot. Preservation is justified by the specialized getter body. Actual mint authorization derives `x.accountingAddress = resolved` from mint success.

`finish` consumes that ready World for actual mint, actual sequential module transfers, actual treasury STATICCALL on the post-module World, and actual transfer to its decoded treasury recipient. No runtime report is rerun after the accounting CALL. The generic `legacyStages`/`mint_replay` code is proof-only reconstruction of the already executed operations. Its two macros expand solely to pinned existing private `ReportFeeMint.runRoot` and `runStages` names; they install no evaluator, axiom or command hook and cannot intercept the isolated axiom probe.

`Retained` contains the exact old340 executor equality, the entire `OldEffect` and the entire `PhysicalEffect`. I expanded these against prior324/340: actual report/getter/checked-fee/mint equations, checked casts/split and funding, pointwise Ledger and paid-share total, complete ordered payments and events, actual module/treasury ReadEffects, intermediate physical consistency, preserved pause word, and positive physical admission are all retained. The positive entry is explicitly `withAccounting before resolved`; zero uses exactly `before`. Their physical projection remains obligatory. Internal stage/frame hypotheses in helper proofs are discharged in the public runner proof, not added to its statement.

All failure branches restore the entire original incoming World, including original locatorAccounting and activeFlag metadata, physical storage, abstract shares and router. Diagnostic accounting and treasury attempt lists remain separate. The sole public execution premise is success (or failure for rollback) of the whole new runner.

## Pinned Solidity and compiler path

Core pin remains `17005714f151e5502c559932319a3f2f74ac2436`. I examined relevant report/getter/fee, mint/distribution, Lido authorization/locator, StETH mint/transfer/rate/event, unstructured-storage and pause source bodies, plus the new complete getter/proxy closure.

The actual locator literal is `0xd92bc31601d11a10411d08f59b7146d8a5915af253cde25f8e66b67beb4be223`. The independently recomputed Keccak, pinned source literal and complete Lido assembly data segment agree. `_getLidoLocator` follows tag538→tag1069→shared SLOAD tag430→low160 AND tag1238. High96 bits are not part of the selected address.

The full original Lido artifact's mint path is tag193→accounting tag466→tag725, then shared return/auth tag393→tag395 before pause tag397. Tag725 constructs selector9624e83e, four input bytes, output size32, zero CALL value, masked target and EXTCODESIZE guard. Shared tag501 executes GAS/CALL, not STATICCALL. Failure copies and bubbles all return bytes. Tags502/503 use unsigned RETURNDATASIZE minimum32 and load the first word; tag395 masks low160 before comparing CALLER. There is no canonical high-bit rejection or newer signed-size rejection in this legacy path. The Lean decoder's trailing-byte acceptance and low160 cleanup match it.

The real LidoLocator runtime checks nonpayability and minimum selector length, dispatches9624e83e to tag19, loads the accounting immutable, and uses shared tag34 mask/MSTORE and tag36 exact32-byte return. The constructor assignment and immutable identifier are consistent. The Lean getter is a specialization of this branch, not an interpreter for all LidoLocator selectors or constructor execution.

The actual proxy has no admin-selector collision with9624e83e. Its fallback follows tag14→implementation getter tag57/tag104 (ERC1967 implementation slot)→tag58 calldata copy/DELEGATECALL/returndata copy and exact return or revert. Real proxy forwarding is exercised by the fixture. The Lean table still assumes effective deployed implementation/configuration identity; it does not formally execute implementation-slot loading, proxy dispatch, constructor checks or upgrades. `Getter.accounting` can be zero at the type level; it is not a proof that the constructor's nonzero check passed.

**Nonblocking citation correction:** SOURCE-MAP names `scripts/scratch/0083-deploy-core.ts` and `0090-upgrade-locator.ts`; the actual pinned paths are `scripts/scratch/steps/0083-deploy-core.ts` and `scripts/scratch/steps/0090-upgrade-locator.ts`. I located and checked the relevant deployment/upgrade code at those paths. It supports the stated proxy boundary. The typo does not alter proof or compiler inputs and does not block exact source acceptance.

## Independent checks and reused execution evidence

- All67 receipt hashes and configuration hashes match current files and exact HEAD blobs. Receipt SHA256 is `27427f3a16612be57f907d63945f3ac8f84f1de858e189df7c3c70e8f90a7607`.
- All45 recorded actual source identities,11 package pins and10 complete core-source identities match. The three prerequisite setup closures select byte-identical current local sources/oleans even when cached setup paths refer to another worktree. All four new current source/olean/ilean records match.
- Independently recomputed all11 axiom scopes through the unchanged repository checker. Its source and probe-template identities match; it imports only Lean and loads the audited module as data outside syntactic scope. Exact sets match the retained evidence and contain only propext, Classical.choice and Quot.sound. This includes the full public success/rollback and both public test instances. No current sorryAx or native-decision dependency is credited.
- The passing normal log and checked build script establish a43-job registered prerequisite closure followed by four sequential ordinary manual `lake env lean -o ... -i ...` compilations. These new modules are not yet registered Lake roots, and no new aggregate or global Trust pass is claimed. Existing prerequisite warnings remain; the final four new compilations have no new warnings.
- Formal test credit is19 kernel computations (14 explicit `decide +kernel`, five literal decoder `rfl` reductions) plus the helper's selector computation. The public tests contain one nonempty success and one late whole-entry rollback. They exercise contradictory metadata, physical target/high bits, decoded authorization, guard priority, zero skip, early errors and actual treasury observation of derived context and module balances.
- All111 borrowed receipt hashes match exact provider `ee24f9dc158ed25cec118a8ab8cd3a4fc59942f1`; all37 full-Lido compiler-input Keccaks match. Complete old assembly and unmodified deployment artifact match that provider. DistributionHarness is byte-identical to accepted340, and its distribution body matches pinned Accounting.
- Nine complete fresh getter/proxy source bodies match pinned core or exact OpenZeppelin4.4.1 tarball bodies. Tarball SHA256, SHA1 and SHA512 integrity match. All metadata inputs, five snapshots and two complete assembly snapshots match actual artifacts; getter/proxy/test input counts are2/7/11.
- The hash-verified author-run final Solidity log passes15 tests, with two fuzz cases of256 runs each. I inspected every fixture test. Real complete Lido, real LidoLocator and real OssifiableProxy execute positive mint/distribution, implementation changes consumed by auth, high96 target cleanup, competing auth/pause/no-code failures and late treasury-guard rollback. Raw auxiliary callees establish dirty-address/trailing acceptance, short/empty rejection, exact failure bubbling and retained SSTORE on successful CALL. That mutable case is outside the specialized formal guarantee and directly refutes a general readonly-CALL interpretation.

Failed and incomplete development logs receive no proof credit, including temporary sorryAx output and the initial nonpayable-fallback compilation failure. The final payable auxiliary fallback checks zero msg.value. The successful Solidity run retains its payable-fallback/no-receive warning. Profiles remain reused solc0.4.24+e67f0147 and fresh0.8.9+e5eed63a, optimizer200, Byzantium. These diagnostic fixtures do not prove production compiler-profile/bytecode equivalence or a gas bound, nor do they run the entire Accounting oracle-report transaction.

Unqualified base-to-HEAD diff-check exits2 solely for exact raw compiler EOF blank lines at `LidoLocator.asm:1773` and `OssifiableProxy.asm:3093`. Excluding exactly those two preserved snapshots passes. No blanket whitespace PASS is claimed.

## Scope and disposition

This removes independently supplied accounting authorization metadata for the new source-specialized executed path, while preserving the complete prior340/324 effect on an explicit derived entry. It does not assert that arbitrary mutable CALLs preserve state. Effective proxy/current implementation/deployed identity, constructor provenance, modeled component separation, abstract account-share mapping, Nat-address admission, full report prefix and reward/rebase suffix, complete ABI/memory/copy/gas behavior and universal instruction/bytecode refinement remain boundaries. The inherited distinct literal total/pause slots do not prove universal share-mapping Keccak noncollision. Pure metadata projections are not additional early EVM SLOAD traces.

**CLEAN for exact a4abd69d7acbcae9ff927d45057337029b21eb93 within this scope.** Root may proceed with separate registered integration and its ordinary aggregate/Trust validation. No candidate/cache/pinned-source mutation, merge, push, publication or external contact occurred. Reviewer STOP.
