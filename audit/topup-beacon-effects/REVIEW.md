# Independent review — TOPUP concrete beacon callee effects

Verdict: **CLEAN for the bounded per-deposit source-model increment**, exact candidate `93d1881ef7fad8875b09522af16e535d0e471396`, parent `8349143d511c8e89b2f0596b16e85a651bf65a98`. No blocking or editorial finding. This is not closure of TOPUP-1/TOPUP-2 or acceptance of the deployed beacon-address assumption.

## Exact candidate and receipts

The commit adds exactly the 13 expected files; concurrent untracked batch/funded work is excluded. Independently checked all **21 validated input hashes and seven evidence hashes against immutable Git objects**, resolving the three core inputs at submodule `17005714f151e5502c559932319a3f2f74ac2436` and the two Verity inputs at `e977aaad6e1a9e92e0132d41b3d33a14135a4d46`. Corresponding working bytes match. The nine foundation/funded paths in `source-check.json` are unchanged from `9f1bc6dce3a76e31c31a398d72ea67ebfecfd965`; toolchain/configuration/artifact lock inputs also match that baseline. The unrelated Hermes ADDRESS changes in the parent do not modify these dependencies. `git diff --check` passes.

Verified the documented callee comment-only reuse independently: replacing introductory `101–159` with `111–172` in the committed callee reproduces compiled SHA-256 `cf997d7f212838e786ef8a5d23704de90979c87d1adc2ecfe854c9ed13a7472f`. Current SHA-256 is `c9ef3fa8e99966b336cb0dd3dd6092e4ca246f2e7eaa2b58b7a136c07c348568`. Definitions, statements and proofs are byte-identical. The targeted build can be reused on this basis; no reviewer rebuild was needed or performed.

Read the complete two new sources, tests, isolated Solidity harness, actual pinned deposit callee and dossier, plus the imported transaction/call/ABI/hash definitions used by the result. The committed build log ends successfully at 1,252 jobs, with 24 new theorems, 27 kernel regression examples and eight principal axiom queries. Queries use only `propext`, `Classical.choice`, `Quot.sound`; no native axiom, added axiom or admitted proof was found. New files have no warning; the log replays disclosed pre-existing dependency warnings. This is targeted compilation, not a full-repository build.

The committed Solidity log attests solc 0.6.11 and four passing tests, including one 1,024-run fuzz property. Receipt records the integrator's actual seed `0x20260909` and compiler layout command; the log's earlier reproduction header omits that seed and is not itself an exact execution-command record. The isolated harness imports the unmodified core callee. Compiler layout identifies branch slots 0–31, count 32 and zero hashes beginning at 33. Independently matched all 11 recorded guard/effect anchors against pinned source lines.

## Substance and source fidelity

1. **Canonical full payload:** `TopupBeaconEffects.lean:10–15,86–229` serializes the existing journal with a four-byte selector and 32-byte argument words. Independent canonical layout uses offsets 128/224/288, preserving all 48/32/96 field bytes and the computed root in 420 bytes. Encoding and decoding theorems discharge these equations universally under source field widths/octet constraints. `TopupBeaconCallee.lean:31–45` reads the actual complete byte payload. This is useful source-output correspondence; exhaustive malformed solc ABI equivalence is explicitly excluded.

2. **Actual ordered body:** `TopupBeaconCallee.lean:47–109` matches `deposit_contract.sol:101–159`: field lengths, minimum, exact gwei alignment, uint64 gwei limit, semantic event, recomputed root check, tree capacity, increment, binary carry. Amount bytes derive from the same call value divided by 1 gwei. The independently transcribed callee SHA chain is related to the existing caller helper at lines 191–229; the successful consumer supplies no root-match predicate. The body preserves credited balances by examination of its writes, rather than by a user-provided callee-frame premise.

3. **Independent branch property:** `TopupBeaconCallee.lean:128–158,232–344` derives insertion termination from positive incremented count below 2^32. `insert_selection` identifies the first odd quotient, proves all lower quotients even, and identifies the unique written branch slot. The digest specification is a separate range/fold over original lower branch words. `BranchEffect` combines this with the physical count write; selected height below 32 excludes collision with count slot 32. The positive theorem derives success, count, event, balance frame and this effect from source widths/value/capacity. It neither assumes a successful insertion nor substitutes synthetic counters for physical storage.

4. **Real call transfer in the model:** `TopupBeaconEffects.lean:19–81,232–274` invokes the existing complete-payload `CallData.invoke`, whose provisional sender debit/target credit occurs before dispatch. Successful callee balance preservation is derived from its body and yields the independent all-account ledger relation, including sender=target. The positive theorem requires code, sufficient funds, actual field widths, value bounds/alignment/amount correspondence and capacity. It has no supplied acceptance, credit, frame or final-world premise.

5. **Rollback and regression evidence:** the new arbitrary payload/value loop runs under existing whole-world root rollback. `failure_restores` specializes that existing framework guarantee; it should not be described as a newly proved funded TOPUP transaction. Kernel tests actually execute an accepted deposit followed by a root mismatch and inspect reverted world plus retained attempts. Other examples detect wrong selector serialization, offsets, root, alignment/guard precedence, code/funds absence, uint64 overflow, physical carry height and event bytes. Six UTF8 reason-equality assertions use reflexive equality; they are semantic-label checks, not Solidity revert-ABI proofs. The substantive execution/byte/storage tests remain kernel-evaluated.

6. **Actual Solidity evidence:** the independent harness derives its root through four-field reduction, executes genuine SHA in the imported callee, and checks both ETH balances, count/storage, all 32 branch positions and complete event topic/data ABI. Deterministic cases cover every carry height, minimum/maximum uint64-gwei amount and final admissible count. Guard/revert and later-failure rollback cases provide complementary finite execution evidence. Arbitrary branch fixtures and forced balances are correctly disclosed as not necessarily reachable states.

## Limits required for downstream claims

The README and receipt preserve the material boundaries accurately. This new per-call consumer is **not connected to PR267's Lido withdrawal, funded `sourcePushLoop`, parent admission or the registered `TopupTx`**. Those files and their dual journals are unchanged. The new list loop accepts arbitrary payload/value pairs and does not derive aggregate Lido funding/conservation. Do not credit concurrent unaccepted batch/funded work.

The source interpretation uses opaque total SHA and typed digest/octet values. SHA mathematics, gas/precompile failure, generated ABI/memory/compiler/EVM correspondence and deployed code/address identity remain open. Lean event data preserves all 192 field bytes but does not prove Solidity LOG ABI; semantic UTF8 rejection labels are not `Error(string)` bytes. Unsupported selectors/targets and the omitted constructor/getters are outside the delivered callee surface. Natural-number balances do not establish uint256 asset bounds or reachable ledger states. Arbitrary old branch contents do not prove a reachable deposit-tree invariant. The generic target/code/funds premise does not discharge open `A-TOPUP-BEACON-ADDRESS`.

No project writes, new build, commit, push or merge were performed by this review. Only this independent report was written outside the project.
