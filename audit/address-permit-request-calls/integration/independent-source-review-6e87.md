# Independent exact ADDRESS permit-wrapper source review

Verdict: **CLEAN for actual permit transport composed with the entire accepted typed batch effects**, exact `6e87eb6e7d6465936d3fab76c3fc7fab5ec51194`, tree `337b1dbf8925d312cee291d54977eeaddd057426`. No blocking proof, source-composition or source/assembly correspondence discrepancy found. The sole raw-output whitespace exception is disclosed below. This is not a formal permit authentication, whole compiled entry, memory/gas or general Ethereum theorem. STOP.

Reviewed clean `/tmp/lido-address-permit-batches` with the author explicitly stopped. I authored TOPUP increments, including frozen2efb timing/history, but none of this ADDRESS increment or its inherited ADDRESS implementation. The author of this candidate is a different agent. The sole parent is `274d8cc9aa543aa4aafda84937689676e0839cfe`; the complete recursive tree/object/mode comparison preserves every parent entry and adds exactly51 files, three Lean modules and the dossier. All50 packet hashes and three configuration hashes match. No existing source, theorem domain, dependency, provider, wiring or site changes.

## Complete source and actual public consumption

Read all260 lines of the three new Lean modules, including all four source proofs, four public proofs,19 kernel examples, two public instances and the full native diagnostic program. Read the complete inherited AddressRequestBatches source/public and AddressWrappedTransferCalls source/public, including Transcript, canonical dispatch, the entire331 JoinedEffect and its actual consumption. Revisited callWithCalldata, Live.run/transfer, pinned request/owner/quote/enqueue paths. Read the complete retained independent274d review and checked all52 inherited batch receipt hashes and all50 inherited331 source packet hashes; the complete parent tree is unchanged. Earlier item/token correspondence is reused after identity, rather than claiming every one of1,257 imported bodies was newly reread.

PermitInput supplies full-width value/deadline/r/s and UInt8 v. The actual generated payload is selector0xd505accf followed by seven32-byte words: requesting ctx.sender, queue ctx.self, value, deadline, v.toNat, r, s. The owner in permit is not the optional withdrawal recipient. Both address values are inherently canonical uint160; v's typed domain is exactlyuint8, without a fit hypothesis. calldata_length proves228 bytes; kernel examples check the selector and every field's location, including v255 and full-word positions. Malformed outer calldata remains outside the typed input boundary.

permitCall passes those bytes to the existing actual callWithCalldata with caller queue self, token target selected by each public wrapper, and value0. Its code precheck is executed before the external call. The helper's balance guard cannot reject zero value over Nat balances; there is no funding assumption. Rejected bytes and nested attempts are retained, and the actual success World is returned. All successful bytes are ignored by program, including empty, short or noncanonical bytes. No bool decoder or minimum successful returndata length is inserted for a void method.

PermitEffect derives the exact generated request, positive code size, actual external success/successWithTrace reply on the same transferred World, arbitrary return bytes, full returned World and actual attempt journal. Ordinary success derives empty nested trace; traced success retains its actual nested trace. These are observations of the executed call, not independent payload facts or an unconstrained successful stage.

program invokes batch called.world only after actual permit success and concatenates permit attempts before the same batch attempts. runPermit applies root rollback around this entire execution. joined_success obtains both actual permit success and actual downstream batch success by inversion. Its generic batch-success implication is discharged in the two public theorems by actual_steth_request_batch and actual_wrapped_request_batch. No permit-success, allowance, signature, batch-stage, frame, nonalias, fit or nonempty premise remains in the public statements.

StETHEffect and WrappedEffect preserve the **entire** previous public conclusion: Resumed at the permit-returned World, the full ordered Transcript on that World through the final World/journal, and ids.length=amounts.length. Each wrapped transcript item still consumes the complete331 JoinedEffect, including actual canonical transferFrom, ordered qualified balance/allowance effects, actual unwrap/conversion/burn/transfer, returned amount bounds, actual quote, physical enqueue, events and journals. The new permit wrapper neither substitutes a weaker item predicate nor drops the inherited length conclusion.

The permit implementation may change pause storage and arbitrary other state. The subsequent physical pause check reads its actual returned World, rather than the pre-permit World. Both pause-direction kernel cases cover this. The batch still checks pause only once and uses the fixed Context for owner selection, as in274d. There is no invented transaction-wide timestamp/storage frame. Token configuration, queue's designated root storage lens and the inherited item entry timestamp semantics remain explicit boundaries.

Both public failure theorems restore the full original World from **before permit**, for any failure. This includes permit writes/logs and any previously successful batch items. Merely relying on batch rollback would restore only the post-permit World; the actual outer Live.run here provides the stronger result. Attempt journals remain diagnostic observations and are not claimed erased by rollback. Empty batches still perform permit and the physical pause check.

## Pinned source and complete relevant legacy paths

Read pinned WithdrawalQueue interfaces/constructor, PermitInput, both complete WithPermit wrappers, both public batch bodies, internal request paths and amount guards; read the consumed IERC20Permit interface, full pinned WstETH0.6.12 and applicable OZ3.4.0 ERC20Permit body. Core pin is `17005714f151e5502c559932319a3f2f74ac2436`. The queue directly calls STETH.permit or WSTETH.permit before invoking the corresponding public batch. The actual permit implementation is deliberately external in Lean; its signature/domain/nonces are not formal conclusions.

Inspected both full permit wrapper assembly paths, their selected dispatch/outer input path, shared v decoder and seven-word encoder, following the jumps into the inherited batch. The full13,650-line retained legacy artifact remains available and identical to the prior reviewed artifact. Unrelated finalization/access-control bodies are not newly claimed as permit proof work.

- Wrapped body tag145/lines2188 onward loads the immutable WSTETH target, caller as owner, ADDRESS as spender, and all five permit fields. StETH body tag189/lines3234 onward performs the corresponding STETH call.
- Shared tag374 masks the two addresses to160bits, stores full value/deadline/r/s and masked v at seven consecutive word positions, and returns an end pointer224 bytes past the selector payload start. With selector this is228 bytes. Its v source decoder tag372 checks a complete word and equality with its low8 bits; the typed UInt8 domain matches canonical accepted input.
- CALL uses value0, input pointer/size from the generated encoder and no output decoder. EXTCODESIZE precedes CALL. Failure copies/bubbles exact returndata; success pops the result and immediately invokes the batch. Journal bytes in Lean are observational external return bytes, not a claim Solidity copied successful returndata into memory.
- Shared outer tag144 checks the static head, dynamic array offset/extent and canonical address/struct layout before entry. These malformed-ABI behaviors remain explicitly outside the typed model, as do result-array allocation/output ABI and gas. No successful allocator premise is smuggled into the public statement.
- The inherited batch pause, owner, amount/transfer/unwrap/quote ordering and physical enqueue/packing/OZ fresh-read sequence remain byte-identical and covered by the retained274d/331 review. The new wrappers add an actual earlier call and an outer rollback, without altering those bodies.

Exact correspondence checks pass39 reused inputs,27 pinned Git/npm source bodies, integrity-checked OZ3.4.0/4.4.1 archives and metadata source counts11token/18queue/20test. Selector hashes and physical resume slot are independently recomputed by the inspected checker. The token artifact is the exact previously accepted unmodified WstETH constructor artifact. Queue/tests use actual solc0.8.9+e5eed63a legacy optimizer200 London; token0.6.12+27d51765/200/Istanbul is reused. Fixture profiles are not claimed as deployed production bytecode identity.

The current test compiler artifact equals its archive byte-for-byte. An independent whole-current-queue-artifact equality probe initially failed: current out includes the optional assembly field and changed sourceMaps after inspect. The corrected comparison verifies that **only** this optional field and the two sourceMaps differ; creation/runtime bytecode objects, metadata and every other shared field agree. Current assembly plus its documented newline equals the retained raw assembly exactly, which also equals the inherited batch artifact. This agrees with the dossier's expressly limited identity claim; it is not a source-correspondence blocker, nor is whole-artifact equality credited.

## Tests, ordinary checks and evidence reuse

Read both validators before use. Ran them with in-memory Path.write_text interception: exactly four candidate manifest writes became byte-for-byte comparisons, all other checkout writes were forbidden, Python bytecode caching was disabled, and only external temporary scratch files for the independent ordinary Lean query were permitted. Fresh independent checks pass1,257 sources,11 package Git pins, actual selected local olean provider identities, exact unchanged old source bodies and runtime identities. Nine actual environment axiom sets exactly match the archive and contain only propext/Classical.choice/Quot.sound. The generic source call/length/compose scopes use propext. No new axiom, admitted proof, unsafe implementation or native_decide appears in the accepted modules.

Verified every recorded final source/olean/ilean/trace/setup hash, empty normal options/plugins and nonsynthetic traces. The successful final test log reports1,274 jobs and19 kernel examples plus the two public instances. Active source/public compilation is correctly retained in development-tests.log before a later test syntax failure; corrected final tests compile normally. Historical failed proofs and the documented accidental repeated failure provide no proof credit. No normal artifact identity is retrospectively assigned to a failed attempt. I reused the validated final normal artifacts; no source re-elaboration, full build or cache mutation was needed in this read-only review.

Read the four native groups and temporary FFI library runner/log. The accepting permit external checks the exact request and writes15 to the actual qualified owner1/spender99 allowance slot. The two7/8 wrapped items consume it to0; the no-permit case fails. A later second item failure restores pre-permit allowance/supply/logs, and paused empty batch restores permit effects. These are actual executable model diagnostics using the inherited physical dispatch, not positive kernel theorem instances. The final native log is tied to the final compiled test source; I did not rerun FFI.

Read both complete fixture contracts, copied BatchTest and all nine PermitTest cases. The actual inherited public WithPermit methods execute in the fixture. Direct STETH uses the explicit PermitSt double: it records the seven arguments, checks caller/spender/228-byte size, deliberately does not authenticate signatures, and can return arbitrary void bytes or reject. The1,024-run fuzz case varies value/deadline/v/r/s and return lengths0–64. Pause/no-code/rejection priority and late rollback assertions are present. Reverted counters alone show rollback, not absence of prior attempted calls; source/assembly order supplies the latter correspondence.

Three wrapped cases deploy the exact actual WstETH artifact, use its DOMAIN_SEPARATOR and current nonce, sign the correct Permit typed hash with the reproducible fixture-only key1, and execute either two-item allowance consumption, late-item rollback or paused-empty rollback. Seeded token balances and STETH conversions/transfers remain fixture inputs. This is finite real-signature execution evidence, not universal or formal EIP2612 authentication. The log shows nine selected PermitTest tests PASS including1,024 fuzz cases. Copied BatchTest is compiled as a dependency but not counted as newly executed. No Forge or compiler run occurred during review.

Unfiltered candidate diff-check fails **only** `BatchHarness.asm:13650`, its raw blank EOF. Excluding exactly that raw compiler output passes. Raw bytes match the actual retained assembly plus its documented newline. No blanket unqualified diff-check PASS is asserted. Older raw/experimental artifacts remain unchanged and do not acquire new proof credit.

## Disposition

CLEAN at exact6e87 for the stated typed permit-transport/batch composition. Permit body/signature/domain/nonce correctness, initial deployment/configuration/storage provenance, arbitrary STETH behavior, full outer ABI/memory/allocator/gas, complete error/LOG byte semantics and general transaction environment continuity remain explicit boundaries. No new source correctness issue or hidden necessary hypothesis was found.

Candidate remains clean and frozen. No candidate/source/cache/receipt changes, recompilation, Forge/FFI/compiler run, push, merge, site action or publication. Only this external review/evidence is written. Global All/Trust integration remains root's separate task. STOP.
