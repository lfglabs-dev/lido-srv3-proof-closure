# Independent exact ADDRESS permit integration review

**CLEAN** for frozen `1382ffae4373e610fbc5e150e7875fb906f88237`, tree `30f60358f230335424cd4cf1c5b0dd58eac3b4aa`, within the actual permit-transport/complete typed-batch scope. No blocking integration, provider, consumer or evidence discrepancy found. STOP.

Reviewed clean `/tmp/lido-address-permit-integrated`, root writer explicitly stopped. I authored none of the ADDRESS permit or inherited ADDRESS implementation. I did author the TOPUP timing/history substrate now in main335; this report does **not** independently review my own timing implementation. Its separate complete source2efb, integration6fd1 and final5392 CLEAN reviews by the reserved independent reviewer were read and reused after exact identity checks. That provenance is explicit rather than silently relabeled independent.

## Exact union and preservation

Union `6ca10c3de0e67ea90b7c115d711f077a1239d2e4` includes source `6e87eb6e7d6465936d3fab76c3fc7fab5ec51194` and accepted main335 `ece595799ac83a3b35e1ab1e620dbf01ae67812b`. Complete recursive object/mode comparison preserves every main335 entry except the two deliberately extended aggregators. All51 permit source-candidate additions are exactly their reviewed6e87 objects. Integration versus6ca10c changes only AllGuarantees/Trust and adds nine files under the permit integration dossier: two wiring files plus nine archives, no executor/public-body/dependency change.

AllGuarantees prepends the permit public import. Trust prepends the permit regression import and appends six queries: both public success theorems, both public rollback theorems, call_success and the paused rollback instance. The full existing timing, ADDRESS batching and older blocks are retained. Existing Source/Guarantees submodule globs and Tests submodule glob provide the new modules without duplicate/new providers or Lake configuration edits. All original theorem domains remain unchanged.

The full source6e87 report is byte-for-byte my retained external complete review. I reused that full Lean/Solidity/legacy-assembly review after checking all three new source bytes, the entire51-file source packet and every one of its50 hashes. Three source configuration hashes also match. All eight integration receipt hashes match actual bytes. All1,257 recorded source identities and11 package Git pins match the integrated environment. The nine integration ordinary scopes are byte-identical to the already independently recomputed source scopes and contain only propext/Classical.choice/Quot.sound.

The three timing source modules equal source2efb exactly. The separate timing source/integration reports equal their reviewer's external copies; all current timing integration receipt hashes match. The final5392 archive review confirms the exact review/receipt-only successor. Their qualifications and authorship distinctions remain in force. Accepted main335's other objects, including the physical-getter documentation correction and batch/timing archives, are preserved.

## Actual compiled providers and check reuse

Inspected the actual current AllGuarantees and Trust setup importArts. AllGuarantees selects the new permit source/public oleans; Trust selects those and the regression olean. Every selected path resolves to this integration checkout's actual normal `.lake/build/lib/lean` path and matches the three hashes recorded in integration identities.json. Those three oleans also equal the reviewed source candidate's final ordinary artifact hashes. Setup options/plugins are empty, traces are nonsynthetic, and no skipKernelTC is present.

The retained integration log actively compiles permit source1.7s, public1.6s, tests2.1s, AllGuarantees2.4s and Trust2.2s and completes **1,757 jobs successfully**. Its six appended Trust outputs match the actual current source requests and their ordinary foundation-only scopes. The global receipt reports **29 exact existing axioms**, distinguishing23 test/mutant native-decision axioms, three existing production exceptions and three foundations. This is not a claim that global Trust is entirely foundation-only. Its native claims/dependencies were recomputed by root as documented.

The build/global/scoped checks ran on preliminary union `7be1bdf7f5977a58579cf9aeb4c6d4dc7bccaf8f`, with then-uncommitted final timing-plus-permit aggregator content. Subsequent committed deltas contain only batch/timing review archives/receipts and the two aggregators. No other Lean body, Lake file, manifest or toolchain differs from that build union. I checked this complete input delta, rather than assuming a commit-date change was harmless.

To verify the material uncommitted-wiring provenance, I independently computed the **current** aggregator text hashes with the installed Lake.computeTextFileHash in an external scratch query, without compiling the audited modules or writing caches. They exactly equal the source input hashes stored in the actual normal build traces:

- AllGuarantees: `8f06d9ea4922c9fe`.
- Trust: `210241901aa87ace`.

Thus the built aggregator bytes are the final current bytes; later archive merges do not invalidate this build receipt. The one read-only hash query is an identity check, not another All/Trust build or trust rerun. I read the integration adapter, which compares candidate manifests and sends its two output summaries to external scratch paths, but did not rerun unchanged validators/axiom probes. The checked source identities, provider hashes, trace source hashes and exact deltas justify reusing the retained1757/global29/scoped9 results.

## Consumer and source scope retained

The full6e87 review established actual228-byte permit calldata with requesting sender as owner, queue self as spender, canonical uint8 v and full value/deadline/r/s. The selected STETH/WSTETH zero-value CALL checks target code and bubbles transport failure; all successful void-return bytes are ignored. The actual returned World feeds the physical pause check and the complete ordered typed batch. Public JoinedEffect retains actual permit reply/journal followed by the entire previous batch conclusion: Resumed on the returned World, full per-item effects, same original amounts and actual IDs/world chain/attempts, and equal lengths.

Wrapped items still execute the accepted physical transferFrom/unwrap implementations. Permit-returned allowance/storage is consumed by that same execution. No permit-success, successful batch stage, allowance/funding, fit, nonalias or frame premise is added. Outer failure restores the original pre-permit World, including permit and earlier item writes/logs; attempts remain observations. Empty batches still execute permit and the downstream physical pause check. Permit may change pause storage; its actual World is used without an environment frame.

The three public/source/test bodies and all retained Solidity/compiler evidence are unchanged, so the detailed source6e87 correspondence applies without another compiler or runtime execution. The normal source19 kernel examples/two public boundary instances, four native diagnostic groups and nine selected Solidity tests including1,024 fuzz cases retain exactly their original evidence classification. Three finite Solidity cases use actual WstETH permit signatures with the explicit fixture key1; formal permit signature/domain/nonce/body authentication remains external. STETH doubles and seeded storage are explicit. No new Solidity, native or cryptographic proof credit arises from integration.

Queue0.8.9 legacy optimizer200 London and reused token0.6.12/200/Istanbul remain fixture profiles, not deployed production identity. Exact bytecode objects, metadata and complete assembly identities are retained without claiming whole JSON equality where optional assembly/sourceMap differs. The omitted outer ABI, result-array/memory allocation, gas, complete LOG/revert bytes, configured-address/deployment/storage provenance and general transaction environment continuity remain explicit. No site/publication or whole-guarantee closure is certified here.

Integration delta diff-check passes. The unchanged source packet's raw BatchHarness.asm terminal blank line at13650 remains its sole new-source whitespace exception, previously matched to actual assembly stdout plus the documented newline; this review does not replace it with a blanket full-history PASS.

## Disposition

Exact1382 is CLEAN for the stated integration. Candidate remains clean and frozen. Only this external report is written; no candidate/source/cache/receipt mutation, build, validator/trust/Forge/FFI/compiler rerun, merge, push or site action occurred. The external scratch Lake hash computation only checked current build-input identities. Root retains merge/publication responsibility. STOP.
