# Integrated ADDRESS permit transport and full request batches

Source `6e87eb6e7d6465936d3fab76c3fc7fab5ec51194` passed independent complete source/Solidity review. Final union `6ca10c3de0e67ea90b7c115d711f077a1239d2e4` includes main335 `ece595799ac83a3b35e1ab1e620dbf01ae67812b`. Root adds AllGuarantees/Trust imports, six public/source/test queries and this archive. No source body or old theorem domain changes.

The actual228-byte permit request uses the requesting sender and queue spender, canonical uint8 v and full value/deadline/r/s. Its zero-value CALL performs the source code check and ignores all successful bytes, then passes the actual returned World into the full accepted batch theorem. The public consumer retains entry Resumed, every complete per-item effect, the same IDs/Worlds/attempts and equal lengths. Actual WstETH transferFrom consumes the returned allowance state. Whole wrapper failure restores the World from before permit, including permit and earlier item effects. No permit/batch-stage success, funding, fit, signature, nonalias or frame premise is added.

Fresh normal AllGuarantees/Trust build passes1757 jobs, actively compiling all three new permit modules and both aggregators. Fresh global checking retains29 existing disclosed axioms. Fresh comparison validation covers1257 source identities,11 pins and9 ordinary scoped closures; all50 source and3 configuration hashes match. identities.json records actual normal olean hashes. The adapter compares candidate manifests without mutation, permitting only external scratch output for dependency checks.

Checks ran on union `7be1bdf7f5977a58579cf9aeb4c6d4dc7bccaf8f` with exactly the current complete timing-plus-permit wiring. That initial union included timing source2efb and ADDRESS integration860c. Subsequent merges add the independently accepted batch and timing review archives/receipts and committed timing wiring already present during the build. Root preserved the exact final aggregator bytes while merging those changes. No source/dependency/current-wiring input changed; therefore no repeated build/global/scoped execution is claimed. The current union retains all accepted main335 content.

The full independent source report is retained verbatim. Source1274-job normal checks,19 kernel examples/two public boundary instances,4 actual-permit native groups and9 selected real Solidity tests including1024 fuzz retain their stated scope. Three Solidity cases use actual WstETH signatures with fixture key1; formal signature/domain/nonce authentication remains external. STETH doubles, token storage seeding and inherited typed-array allocation/gas boundaries stay explicit. Exact creation/runtime bytecode objects, metadata and assembly are checked without claiming whole-artifact equality across optional assembly/sourceMap differences. No root compiler, Forge or native rerun is credited.

Deployment/configuration/physical initial storage, permit body, arbitrary STETH implementations, full outer ABI/result allocation/memory/gas, complete LOG/error bytes and transaction-wide environment continuity remain outside the added theorem. Successful-return bytes are journal observations, not a claim Solidity copies or decodes them. The batch's physical pause uses the actual permit-returned World, even for an empty batch. All original batch effects and domains remain intact. Source raw BatchHarness.asm:13650 retains its sole new EOF exception; newly authored integration files pass diff-check. Candidate stops for exact integration review.
