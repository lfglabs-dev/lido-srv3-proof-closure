# Checked fee split integration

The independently reviewed a3fe6041 source is united with accepted mainf8ea5f9d (including TOPUP318). All four source bodies are unchanged from that candidate. The only executable-source-file change relative to main is the already reviewed 112-line proof-only insertion into ReportFeeMint; removing exactly that block recovers every prior byte. No old executor, theorem or public premise is changed.

Root registers ReportFeeCheckedSplit and its regression in the existing single AccountAddressChecks provider. AllGuarantees imports PAccount1CheckedFeeSplit. Trust imports the regression and adds four active queries for the public joint consumer, exact checked split, checked-product origin and committed conjunction. The ten kernel regressions are compiled by this actual import, not merely named in documentation.

The normal combined AllGuarantees/Trust build passes1697 jobs and freshly builds the new registered modules and their changed ACCOUNT dependants. The fresh trust gate recomputes dependencies and re-evaluates native claims: exactly29 existing globally disclosed axioms. This is not a globally foundations-only claim. All17 scoped dependency sets were also freshly recomputed from the actual registered test environment and equal the independently reviewed foundations-only source packet.

This extra scoped query is necessary because the candidate manually compiled two files with audit.trio.account-address module identities, whereas normal Lake registration gives ReportFeeCheckedSplit and Tests.Verity.ReportFeeCheckedSplitTest their intended short provider names. The two dependent/new olean identities and public olean change; the modified ReportFeeMint olean stays identical. Source bodies, proofs and public names stay identical. The old four olean hashes describe the candidate only; scoped-axioms.json records the four current hashes. No byte-identical olean reuse is claimed for those three rebuilt artifacts.

The actual report/getter/checked-fee/mint execution yields one Success with the existing mint effects and derived casts, aligned recipient/ID/share lists and the exact module-floor plus treasury partition of the same fee minted. Zero rows remain zero without source division; zero mint skips distribution. Later fee payouts and full-report operations remain outside. The historical Nat fee helper is not silently identified with this checked result. All previous layout/snapshot/physical-total-share versus abstract-account-map/compiler/Verity boundaries retain their scope.

Independent source review checked four normal-kernel modules,10 regressions (including a composed public instance),27 scoped source identities/11 pins, five complete pinned Solidity bodies,17 axiom sets and14 packet hashes. Executables/compiler inputs are unchanged; no fresh Solidity runtime execution is credited. The source dossier records historical candidate status; this directory supplies combined integration evidence. A separate exact frozen integration review is required before merge.
