# Independent exact source review — SSZ declared siblings 3bdd

Verdict: **CLEAN for complete declared indexed-word authentication in the selected compiled CL entry. STOP.** No blocking proof/source/composition discrepancy found. This is source acceptance only; integration is separate.

## Identity and independence

Reviewed clean `/tmp/lido-ssz-declared-siblings`, HEAD `3bdd333b3a0c9e34dd128c17427d51610a60f1b9`, tree `fa42c7b299251ee21a7046bd75e14f26e33691a1`, base346 `4207bb0578b0a6821cd1d78b1f3a4bb08f277905`. All 5,622 old tree entries, including modes and object identities, remain unchanged; exactly22 files are added. Receipt SHA256 is `09155d523ab422613fc7cd446c5181681db307820907ba1d6796e77b2d45e846`; all21 covered hashes match. The entire additive diff passes whitespace checking without exceptions.

I authored none of this source. Read all three new Lean files including proofs and test specialization, README, source map, assessment, validators and receipt logic; the full consumed SszCompiledClEntry and SszProofCommitted bodies; the relevant wrapper and independent Branch/depth definitions; and the complete typed branch bridge. Read the complete retained source51c1 and integration70b8 independent reports. Their broader dependency/source correspondence is reused under exact identities, not silently recertified as newly authored work. Independently compared every original51c1 receipt body to that exact reviewed source commit, in addition to the validator's accepted346 comparisons.

## Universal cursor argument

The new arithmetic statement is genuinely modular: arbitrary uint256 offset, count>=2, stride extent32*count<2^256, and the actual first-step unsigned comparison imply every intervening declared comparison. With a fixed modulus, omega proves this linear modular fact; no alignment, host-buffer extent or nonwrapping condition is supplied. The count extent is derived later from the existing executed uint64 count guard.

`advance_succ` and `end_eq` connect the actual UInt256 addition/shift endpoint to indexed32*j loads. `all_steps` inducts on the same proofWords interpreter. `cursor_dichotomy` splits on the actual first increment: immediate termination gives exactly one word; accepted first increment gives the entire independent List.range-count map. Initial wrap is admitted. A wrap later in the declared progression makes the first comparison fail instead; equality with the endpoint stops. The theorem is not a finite-case approximation or an assumed list-length equality.

`wrapper_depth` consumes actual generic sourceWrapper success, whose result is appendIndex11 of the selected constructor-dependent neighbor. Its lower bound is at least11 and its index is uint248, giving depth3..247 without specializing the constructor or adding fit. `complete_of_branch` consumes the independent Branch already derived from the successful execution: branch_depth identifies consumed length with that actual index depth. A singleton cannot satisfy depth>=3, so the complete-list alternative follows. Its length is the declared count, hence count=depth and3<=count<=247. Counts0/1 are not incorrectly included in the whole-entry success conclusion.

`penultimate` uses the same modular offset and the proved in-range count-2 index; it identifies the actual slot/proposer calldataload with that entry in the complete declared list. No canonical serializer or contiguous host slice is substituted for the engine's rawWord function.

## Complete public consumption

The new public theorem invokes `actual_compiled_cl_entry_branch` once on the identical whole-run success and unchanged inherited ShaWidth. Its complete prior declaration/conclusion is preserved verbatim as the prefix of the stronger conclusion (apart from the theorem name), as independently checked by the validator. All witnesses are retained, not reconstructed independently: header, branch, slot, proposer, actual returned data, produced gi, key slice and fields.

The original root request/timestamp, actual one-call journal, success/code/response facts, reply width, configuration-selected index, key width/fields, consumed-list Branch and unchanged executionEnv survive. The proof adds equality with the complete indexed list, count/depth bounds, penultimate slot/proposer identity and the Branch on that list. The typed validator-tree branch follows by the existing word/digest bijection on this same list/root/leaf. It does not introduce another execution, successful-stage hypothesis, root/index equality, frame, canonicality or byte-fit premise. The new helpers therefore supply facts actually consumed by a public strengthening, not isolated arithmetic credit.

## Solidity and IR correspondence

Read full pinned CLValidatorVerifier, the relevant SSZ proof loop, and the complete retained567-line IR with comments stripped for an executable-order read. Pinned core is17005714f151e5502c559932319a3f2f74ac2436. Source and IR establish slot/proposer comparison before root lookup; the header state-root index11; fork-selected neighbor then concatenation; uint248 packing; and the actual do-while proof cursor with endpoint add(offset,shl5(count)),32-byte loads, shift-before-hash, unsigned continuation, final index1 and root equality. The ABI tail decoder keeps signed offset/extent comparisons and an unsigned uint64 count guard. The new proof accurately reasons over those unchanged operations.

The full old execution's memory-built root payload, actual reply copying/decoding, computed generalized index and validator leaf, and source-call semantics retain their independently reviewed51c1/70b8 correspondence. The new theorem changes none of that executable transcription. It removes the prior complete-declared-list uncertainty for successful selected entries by proof; it does not claim a new deployed-bytecode or canonical-calldata equivalence.

## Validation and evidence degree

Read both scripts before invoking default comparison with Python bytecode writes disabled. `validate.py` passes1217 actual selected source providers,11 package pins,33 ordinary environment scopes, three normal module identities and the entire330 conclusion check. This includes the inherited beforeRoot/rootCall/afterRoot scopes. All queried scopes contain only ordinary foundations (propext/Classical.choice/Quot.sound as applicable). The isolated dependency probe loads the environment as data; new code introduces no macro/elaborator/unsafe/native-decision escape. Independently checked current traces contain no error/sorry, alongside validator checks for normal options/plugins and nonsynthetic/kernel-check status.

The retained final test build completes1234 jobs. Current source/public artifacts are recorded and match; their earlier successful active builds and the failed cursor/test attempts are preserved. Some development-named logs are successful intermediate snapshots, others failures; none replaces the final artifact identities. The concrete rawWord kernel-decide attempts failed at existing opaque ffi.ByteArray.zeroes. Final regressions instead prove symbolic load/list equalities and direct vector byte properties. There are16 regression theorems, including maximal uint64 arithmetic/dichotomy without constructing a huge list.

The 2180-byte calldata and32-byte root literals match the retained inputs exactly. The tests' code owner99, timestamp123, configuration, gas, deliberately nonempty starting memory and static external match the old diagnostic. The validator independently recomputes the slot/proposer digest with Python hashlib and checks the actual penultimate bytes. The public_retained_vector declaration is a specialization retaining the old whole-run success hypothesis and ShaWidth, not a kernel proof of that success. Four unchanged whole-entry IO/FFI diagnostics (valid vector, changed root, changed proof byte and timestamp mismatch) are reused at their original degree. No fresh FFI/SHA/Forge/compiler execution or whole-success kernel claim is made.

Seven current inherited oleans match accepted330 integration hashes. Full old receipts/reviews, seven compiler inputs, materialized Mac replay, complete IR and runtime sources match. Compiler profile remains solc0.8.25 viaIR/optimizer200/Cancun. The validator checks both nested runtime repository heads independently of the eleven Lean pins: keccak256 fc8504750a5c2174a1874094dd05e6a0d8797753 and sha2 565f65009bdd98267361b17d50cddd7c9beb3e6c. The six checked runtime body hashes are:

- ffi.c:0ca099540b8f46888e9ae18afffb2a825ef8ef0edc0d9a239c819c32cf82a020
- ffi.lean:c0b6b455399a476fd3c5be3fd3e1df5788a112dd92b520397512d073b6317562
- sha3.c:9499df301d240fdd296fb53aae554d2615f875156e39174390dc0b7dd2a25fea
- sha3.h:ba0f5cb513018ae84959293f82a2f1b2c027457c6564e8e1b8397fa439bcce6c
- sha-256.c:7a74437adc78576b8faff060f9573ba88a6da798914f45d263c75b149add3d27
- sha-256.h:c2173d83813a0c29fcc3345ce489766efeadee6a52ca927ecd0f917a120df9fb

## Limits retained

Authentication is over every declared indexed word loaded by the actual modular/padded engine operation. It is not an in-bounds contiguous host slice or canonical ABI representation, a hash-injectivity proof, or standard-SHA correctness of the foreign binary. ShaWidth remains explicit. Selected semantic execution, constructor configuration, typed external-call/Live.World interpretation, deployed account identity, EIP-4788 history/authenticity, opcode gas/termination, general bytecode equivalence and failure-world rollback boundaries remain unchanged. This is necessary success, not an unconditional successful run.

No global All/Trust result is claimed; those files and Lake wiring are unchanged. The existing TOPUP typed row verifier is not replaced or strengthened by an unexecuted standalone harness adapter. The archived assessment correctly leaves that later in-place calldata/frame composition separate.

Only this external report was written. No candidate/cache/Git/site mutation, redundant build or runtime rerun occurred. **CLEAN — SOURCE ONLY. STOP.**
