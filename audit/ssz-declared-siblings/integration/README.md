# Complete declared SSZ siblings integration

Reviewed source 3bdd333b3a0c9e34dd128c17427d51610a60f1b9 is composed with accepted main348 43f0e7abcdf64671582351b8cae389134de4326a in union b3df7baa69843f8c603cfc3262319d3b8a5045a9. All prior source, theorem, hypothesis, configuration and evidence objects are preserved. All/Trust add the public/test imports and four ordinary queries. The full independent source/Solidity review is retained verbatim and CLEAN; exact integration review is required before merge.

The public theorem consumes the same actual compiled CL-entry success and inherited ShaWidth. It retains the entire 330 public conclusion on the same witnesses, adding equality of the actually consumed proofWords with every ABI-declared indexed word, count equal to produced index depth between 3 and 247, the penultimate slot/proposer identity, and both word and typed validator-tree Branch conclusions on the complete list. The generic modular cursor dichotomy and actual wrapper depth eliminate the singleton alternative. No alignment, nonwrapping, canonical calldata, count/fit, frame or independently successful stage is assumed.

Normal AllGuarantees/Trust passes 1798 jobs, actively compiling the three new modules and both aggregators. All five normal source/setup/trace/olean identities are retained. Fresh default global trust recomputes the same 29 disclosed axioms, separately from 33 ordinary foundational scopes of the increment and its inherited phase theorems. Strict comparison checks 1217 actual source providers, 11 package pins, 21 source receipt hashes and the entire prior conclusion. The wrapper redirects only path-dependent normal records to an external temporary file and excludes the two expressly additive aggregators from the old346 whole-tree comparison; all other comparisons remain strict. Current configurations match accepted348. Full main348 preservation and actual additive wiring are separately reviewed.

Sixteen kernel regressions cover modular cursor cases and direct bytes of the exact retained vector. The public vector specialization retains whole-run success and ShaWidth as before: archived IO/FFI diagnostic success is not relabeled kernel evaluation. Failed attempts to reduce opaque ffi.ByteArray.zeroes remain failed development evidence. Seven inherited normal oleans, seven complete compiler inputs, full 567-line IR, retained source/integration reviews, four actual whole-entry FFI diagnostics and six runtime source/header bodies with separate nested repository pins are reused by exact identity. No compiler, Forge, SHA or FFI execution was repeated by integration.

The result is complete indexed-word authentication using the actual modular/padded load function. Canonical ABI/contiguous host slicing, standalone-to-TOPUP in-place frame/calldata composition, SHA-width/foreign primitives, deployed identity, constructor/configuration, declared execution semantics, consensus/history authenticity, gas/termination, general bytecode and failure-world boundaries retain their existing scope. No new premise is hidden under these boundaries. ALLOC/RESERVE are unchanged. Site426 remains on its independently reviewed main346 pin until the separate publication pass.

The source and integration delta passes diff --check without exceptions. Integration does not claim a new runtime execution or an unconditional successful entry.
