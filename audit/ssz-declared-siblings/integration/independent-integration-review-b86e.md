# Independent exact integration review — SSZ b86e

Verdict: **CLEAN — exact integration, with the source review's boundaries retained. STOP.** No blocking finding. This is not site or deployment approval.

Reviewed clean `/tmp/lido-ssz-declared-siblings-integrated`, HEAD `b86e8d9b45990131e87d44bc85827ca19400a095`, tree `48b8fbe24ca95726cf1c96441bbb4e497177c495`. Parent union `b3df7baa69843f8c603cfc3262319d3b8a5045a9` has exactly accepted348 `43f0e7abcdf64671582351b8cae389134de4326a` and reviewed source `3bdd333b3a0c9e34dd128c17427d51610a60f1b9` as parents. I authored neither source nor integration.

## Exact preservation and public composition

Complete recursive Git object-map comparison establishes that the union is precisely main348 plus the22 source additions. Every prior main object survives unchanged. The integration changes only AllGuarantees and Trust by additive imports and four queries and adds exactly eight archives. All source3bdd proofs, tests, dossiers, inherited330 code, compiler/FFI evidence and accepted intervening DEPOSIT/TOPUP changes are preserved. The complete main348-to-candidate diff passes whitespace checking without exceptions.

Read the full integration README, wrapper, receipt, identities and logs, the complete public consumer again and the exact wiring diff. AllGuarantees imports PSsz1DeclaredSiblings; Trust imports the regression module and queries the public theorem, cursor dichotomy, complete-of-branch and penultimate facts. Existing queries/imports are retained. The source report is byte-for-byte my external3bdd report. All seven integration receipt hashes and21 source receipt hashes match.

The complete source review applies by exact identity. The public proof still consumes only actual compiled-entry success and inherited ShaWidth. It preserves the entire330 consequence verbatim on the same witnesses, then derives complete indexed-list equality, declared count=actual produced-index depth3..247, penultimate slot/proposer membership and both word/typed validator-tree Branch conclusions on that same list and returned root. The actual wrapper depth excludes the singleton alternative of the universal modular cursor dichotomy. No alignment, nonwrapping, canonicality, frame, fit or independent successful-stage premise is introduced by integration. This is actual public consumption, not registration of an unused arithmetic claim.

## Current providers and evidence

Independently checked all five current source/setup/trace/olean identities: three new modules and two aggregators. Actual selected local import artifact bytes agree with the local normal providers. Setups have empty options/plugins; traces are nonsynthetic, without error/sorry/kernel-skip diagnostics. Current configuration hashes match accepted348 exactly.

The retained normal build actively compiles all five modules at jobs1794–1798 and finishes1798 jobs. Its final four query reports are foundational. The fresh default global checker receipt records recomputed dependencies and reevaluated native claims with29 exact disclosed axioms:23 test/mutant-only native-decision axioms, three retained production exceptions and foundations. This is distinct from the increment's33 ordinary foundational scopes, not a claim of globally foundational-only Trust.

Inspected the wrapper before execution. Its two changes are narrowly justified: redirect the path-dependent new normal identity record externally, and exempt the two expressly additive aggregators from the old346 full-tree comparison. All other comparisons stay strict, and current348 configurations are checked separately. Full main348 preservation and the precise aggregator diff were independently checked here, closing the latter exemption.

For review I ran the unchanged source validator with those same two adjustments, except the normal record was compared in memory against the archived three actual module identities instead of written externally. It passes1217 exact providers,11 Lean pins,33 ordinary scopes, the entire old conclusion, seven identical old normal oleans and complete compiler/FFI evidence reuse. No source record was rewritten. Existing source-only normal records remain historical; current integration identities are separate.

The accepted seven compiler inputs,567-line IR, old51c1/70b8 reviews and source packets, four whole-entry FFI diagnostics and six runtime bodies with their separate nested repository pins remain exact. Their full source/IR correspondence and C-support identities are documented in the retained3bdd review. No compiler, Forge, SHA, FFI or unchanged build was repeated by this reviewer. The final build/global runs are reused under verified identities; the33 ordinary comparison was independently executed.

## Limits and conclusion

The16 regression theorems retain their kernel status for symbolic cursor/list and direct-byte properties. The retained-vector public specialization still requires whole-run success and ShaWidth; archived IO/FFI success is not promoted into a kernel execution theorem. Failed opaque-zero-padding reduction attempts remain failed development evidence.

Complete authentication concerns the actual modular/padded indexed loads. It does not establish canonical ABI or contiguous host slicing, foreign-binary SHA correctness, deployed constructor/callee identity, EIP-4788 history authenticity, full gas/termination, general bytecode equivalence or new failure-world rollback. The standalone CL harness is not silently substituted into TOPUP's in-place row/frame execution. ALLOC/RESERVE are unchanged. All source boundaries remain explicitly attached in the integration README and verbatim source review.

Only this external report was written. Candidate/cache/Git/site state was not mutated. **CLEAN — STOP.** Any subsequent archive-only commit requires its own exact integrity gate.
