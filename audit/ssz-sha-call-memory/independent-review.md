# Independent exact-candidate review: SSZ SHA CALL memory

Verdict: **CLEAN for the bounded direct-engine SHA-call memory result**, candidate `23728167b21c2b0768c9d59cabd4c1dfb55eb6c1`, parent `8236977681bf6c701cba77162a95031e458e411a`. No source or proof finding requiring a change. This is not closure of SSZ-1, complete compiled BLS/ABI execution, cryptographic/FFI correctness or deployed runtime acceptance.

I did not author the three new Lean files or the root-authored Solidity harness. The earlier scratch result and the SHA harness had separate prior reviews; reuse below is supported by byte identity. Review was read-only except this report and evidence under /tmp. No Lean/Forge rebuild, Git mutation, project edit, commit, push or merge was performed. Concurrent TOPUP files are excluded.

## Exact objects and evidence

The candidate has exactly15 added SSZ files and the stated parent. Every candidate blob matches the full working file inspected. Independently checked43 outer input/evidence hashes and1109 recorded source-closure entries against exact Git objects using candidate root configuration, the committed package revisions and core submodule pin17005714f151e5502c559932319a3f2f74ac2436. All match, yielding1129 distinct Git file comparisons after overlaps. Six accepted local SSZ imports and four root build/configuration files are unchanged from the parent. The full source closure includes local modules as well as external package sources; this count is not a full Lean-core closure or a fresh audit of every dependency.

Executed the read-only check_receipt.py:1165 recorded file/package-pin/source comparisons, zero errors. Seven selected Lean core byte/array/UInt/platform files match recorded hashes; toolchain remains Lean4.31.0. Verity remains e977aaad6e1a9e92e0132d41b3d33a14135a4d46, EvmYul remains f7e4ee0dc8f8d5265ce822a937ab5be771f182e9. Exact inputs and all15 candidate file hashes are recorded in /tmp/lido-ssz-sha-call-memory-independent-evidence.json.

Read the complete SszShaCallBytes, SszShaCallMemory and SszShaCallMemoryMutants modules, both accepted scratch modules, full README/checker/receipts/source-check/axiom/validation material, relevant complete actual EVM.call/Theta/precompile/gas/memory functions, actual STATICCALL routing, FFI.sha256 wrapper and pinned BLS/SSZ source operations. No sorry, admit, custom axiom declaration or native_decide/bv_decide shortcut was found in the new files.

There are14 public source theorems,10 kernel regression examples and nine explicit axiom queries. The queried dependencies contain only propext, Classical.choice and Quot.sound; mload_wrap uses propext alone. Axioms.log is byte-for-byte the nine query lines present in validation.log.

**Validation scope:** the final recorded lake build command succeeds with1126 jobs and explicitly reports the test module as **Replayed**, along with unchanged dependency warnings. This attests an exact targeted cache replay, not that the three new files actively recompiled during that final command. Earlier active source/test compilation is not independently reconstructed from this committed log. The recorded source/configuration identities and normal successful Lake replay support this bounded validation without another build. Downstream prose should preserve this distinction; no fresh reviewer build, warning-free dependency tree or whole-repository build is claimed.

Additional historical logs were supplied during review and read in full: /tmp/lido-sha-source-next.log records SszShaCallBytes Built1.2s and SszShaCallMemory Built1.6s,1125 jobs; /tmp/lido-sha-tests-prototype.log records the tests Built7.8s,1126 jobs, with the same nine axiom queries. These support the compilation chronology but contain no contemporaneous source hashes themselves. Author-reported absence of intervening edits is distinguished from the independently verified final source hashes and successful Lake replay. Archiving those logs preserves useful history; it must not turn them into independently source-bound historical receipts. No extra build is required for the bounded verdict.

## Proof substance and actual engine correspondence

**Independent byte lemmas.** SszShaCallBytes derives in-range reads, full32-byte writes, padded prefix readback and pointwise frame from the existing ByteArray definitions. It uses the immutable actual reply array as source, so input/output overlap does not introduce a circular expected-memory premise. write32 fixes complete output memory to reply ++ old.extract32 old.size, including short old arrays. read32_prefix/frame32 establish real observations rather than assuming them.

**Actual SHA routing and gas.** SszShaCallMemory.sha_selected selects the existing precompile2 independent of account code. theta_sha unfolds the actual EVM.Theta and Xi_SHA256, whose64-byte fee reduces to60+12*2=84. shaOutput is the exact opaque ffi.sha256 input input.size.toUSize used by ffi.SHA256; it is not the unrelated higher-level SHA helper or a replacement interpreter. The real wrapper returns that opaque output. Sufficient callee gas derives the successful flag and g-84 returned gas. The theorem intentionally existentially retains the engine-computed account/substate outputs and its empty createdAccounts result; it makes no whole-world preservation claim.

**Real CALL writer.** callSha supplies the actual zero value/value-prime, recipient/code address2, input0/64, output0/32 and false permission. call_sha_bytes unfolds EVM.call and routes through Theta with sufficient fuel and depth<1024. Zero value discharges the engine funds guard even for absent accounts. Admission concerns computed Ccallgas, not merely the requested word. gas_fits derives the cap representation bound from the zero-value branch and the requested UInt256 bound; no arbitrary no-wrap premise is imported.

The actual caller reads memory with readWithPadding, receives the actual opaque output, uses min(outSize, returndata.size), writes through the existing writeBytes/ByteArray.write, retains full returnData and updates activeWords with the existing M function. The output-size32 condition makes the copy exactly32. The result is success1, exact returndata, the complete output-array normal form and max(old activeWords,2). No success result, output buffer, final read or SHA root is a premise. Generic gasCost is arbitrary, so no physically valid gas subtraction follows from this generic helper alone; documentation explicitly retains that limitation.

**Scratch composition.** prepared lifts the accepted mstore32zero then calldatacopy48 into the real EVM state. scratch_call_bytes derives the64-byte input from the actual raw calldata extent, then composes it through CALL to exact reply readback. The final byte frame at every address>=64 follows from the output writer frame and the old scratch frame. It admits arbitrary initial dirty/short memory and arbitrary represented activeWords. The input-size condition in the generic theorem is derived here, not supplied as an expected result.

**Real caller and source gas operands.** blsCall specializes source to UInt256.ofNat(codeOwner.val), matching the inspected existing STATICCALL dispatch, and requests the prepared gasAvailable word with the actual Ccall fee. bls_call_success retains explicit fee admission hpaid and proves the fee fits UInt256, along with success1, returndata.size32 and the actual mload value. It does not execute opcode stack decoding, X gas/memory-cost admission or compiler output. The preceding scratch primitives do not themselves debit real opcode gas; no full BLS gas-sufficiency result is inferred.

**mload representation boundary.** mload32 proves the actual lookupMemory condition using positive activeWords and activeWords<2^251, so activeWords*32 does not wrap its UInt256 extent. scratch_call_mload derives positivity from the two-word input window and threads the initial width bound through max. The actual machine.mload returned word is UInt256.ofNat(fromByteArrayBigEndian actualReply). The separately proved mload_wrap exposes the counterexample at2^251 words: extent multiplication wraps to0 and lookup returns0 even when physical byte-array contents are nonzero. This bound belongs to word interpretation, not byte copying. The theorem does not claim that such an initial state is reachable under gas-charged EVM execution.

## Regression and Solidity checks

The10 Lean examples genuinely reduce existing engine operations. Four identity-precompile calls cover return lengths0/31/32/33, checking full returndata and capped caller output; they do not substitute an identity callback for the SHA theorem. Another actual SHA path rejects at83 gas without evaluating successful opaque SHA output. Fuel0/1 and depth1024 regressions exercise real failure/admission branches. Two final examples pair the actual wrapped-extent mload result with an independently nonzero32-byte interpretation. No native checker is used. These are kernel boundary regressions, not10 separately mutated SHA implementations or proof that the opaque FFI always returns32 bytes.

The four-file nested Solidity dossier is byte-identical to the earlier independently reviewed harness/receipt (receipt SHA25651c573d8af05e9a7937cc1a38ead4f85c6b07297537b970e969f6c9d90349d99). Its six source/configuration and three artifact identities were retained. The prior review at /tmp/lido-ssz-sha-call-harness-prereview.md therefore applies unchanged: four solc0.8.25 tests, two1024-run fuzz properties, no reviewer rerun. Actual BLS/SSZ functions receive explicit synthetic address2 replies/failures; GI2 leaves the original left word under partial overwrite. BLS requires success plus exact32-byte returndata, while SSZ checks only the flag. A distinct fixture calls the real identity precompile4 to test output copying/frame, and one deterministic test uses real SHA for pair/pubkey. Synthetic short successful address2 replies are not standard-SHA behavior, an observed deployed vulnerability or an FFI proof.

## Retained boundaries and disposition

Opaque FFI output length32 is an honest explicit condition, and neither cryptographic correctness nor conversion of the output to the separate typed ShaReply/hash-tree semantics is established. Solidity ABI/length-guard execution, stack/public-step/X and full memory-expansion charging, caller failure/revert ABI, whole-world frame, sibling proof scratch/loads, fls/index decoding, root lookup and deployment/consensus provenance remain open. The actual engine's createdAccounts reset is documented, not hidden behind a frame assumption.

The new source and proof claims match these bounds. The narrow direct-engine call/copy/readback/mload increment is suitable for integration; no complete SSZ guarantee acceptance follows. Preserve the explicit final-cache-replay description in integration/site validation text.
