# Independent exact integration review — DEPOSIT a3c0

Verdict: **CLEAN — exact integration. STOP.** No blocking finding; source boundaries remain unchanged. This does not approve site publication or deployment.

Reviewed clean `/tmp/lido-deposit-admission-errors-integrated`, HEAD `a3c0ede3af44bd8890981157aca49a178d35cf80`, tree `75c4497c3a21f0a87ae273b4145c713c8ade1778`, parent union `7c301481a34754c52f0fba9648a7e71fa292f59a`. That union has the exact parents accepted346 `4207bb0578b0a6821cd1d78b1f3a4bb08f277905` and reviewed source `a279b5661f5b1f6c588bacd2918dd2e0cd14cb2d`. I did not author source or integration.

## Identity and wiring

Compared complete Git trees: all 5,591 prior source-branch blobs and all 5,622 accepted346 blobs remain identical except the two explicitly additive aggregators. The union adds exactly the source's 19 files to accepted346. Integration adds one public import to AllGuarantees, one regression import and five ordinary axiom queries to Trust, plus eight tracked archives. No declaration, proof, hypothesis, source receipt, compiler input, package pin or build configuration was modified. All seven integration receipt hashes match, and the archived independent a279 report is byte-for-byte identical to my external report. The complete source/integration delta passes `git diff --check`, with no exceptions.

Re-read the complete public proof and actual wiring. AllGuarantees imports `PDeposit1AdmissionErrors`; Trust imports the actual regression module and queries the new public conjunction, gate origin, success projection, local rejection and original rollback. Historical imports and queries remain. The public theorem has no propositional input premise: its conditional conclusions describe the actual executor, rather than requiring independent successful stages.

The complete a279 source review remains applicable by exact source and dependency identity. Success retains the entire prior328 result, both journals and full Effects. The actual local tag is connected to the decoded DSM lookup/static journal, all earlier successful physical guards, the first failed guard, outward exact bytes, empty module attempts and original World. Universal outward-error rollback remains. There is no detached classification, duplicate runtime lookup, global reason remapping, new frame/initialization/fit premise or substituted subset of inherited effects.

## Normal artifacts and validation

Read the integration README, receipt, identity records, wrapper and underlying validator before using the comparison logic. The wrapper changes only handling of the path-dependent normal identity record, redirecting it externally; all other strict source/axiom/compiler/reuse comparisons remain. It separately checks all three configurations against accepted346. For this review I redirected that one record to an in-memory equality assertion against the archived first three normal identities, avoiding external diagnostic writes. No candidate/cache writes or build/compiler/Forge/native reruns were performed.

Independently matched all five current source/setup/trace/olean identities: the three new modules and both aggregators. Checked normal options/plugins, nonsynthetic traces, absence of error/sorry/kernel-skip diagnostics, and actual aggregator import artifact providers against the local artifacts. The retained build actively compiled all five modules at jobs1787–1791 and completed 1791 normal jobs. The stored global result records a fresh default environment recomputation and native-claim reevaluation: 29 exact disclosed axioms (23 test/mutant-only native-decision axioms, three existing production exceptions and foundations). This global count is distinct from the new increment's 25 ordinary foundational-only scopes.

The independent ordinary comparison passes 1317 actual source identities, 11 Lean package pins, all 25 ordinary closures, three current normal module identities, 99 prior receipt hashes and both 28-input compiler packets. All 18 source receipt hashes also match the unchanged reviewed source dossier. The accepted build and global evidence are reused under the verified exact identities, not presented as a new reviewer build or a new global run.

An initial per-file Git comparison was deliberately interrupted for efficiency and replaced with complete `git ls-tree` object-map equality, which completed successfully. It was a read-only comparison, not a failed build or proof check.

## Evidence scope preserved

The source's 11 kernel regressions, four execution/transport theorems and three consumed public instances retain their reviewed status. Fifteen ordinary Solidity cases and four 1024-run fuzz cases are exact inherited evidence, not fresh runs. The fixtures execute inherited admission helpers; full compiled deposit IR supplies broader source correspondence, not a whole-suffix runtime test. The old verbose no-code diagnostic remains FAIL alongside unchanged-artifact ordinary-mode PASS, with no attribution of instrumentation cause or unconditional all-mode byte claim.

The adapter still omits getDepositableEther and the SRLib allocation prelude across which credential/module-address preservation is not established. Selected allocation, maxEB/immutable/context identity, independent phase cursors and linked-library identity remain supplied boundaries. A DELEGATECALL is not granted a frame by a view annotation. Full ABI entry/memory/gas, deployed bytecode, cryptographic interpretation, natural ledger and complete merged nested/static trace qualifications remain. The four local encodings do not establish exact ABI for all downstream faults. Accepted ALLOC/RESERVE claims are unchanged.

Only this external report was written. **CLEAN — STOP**, subject to a separate exact gate for any subsequent archive-only commit.
