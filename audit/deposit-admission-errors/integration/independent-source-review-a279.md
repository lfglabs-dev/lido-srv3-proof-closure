# Independent exact source review — DEPOSIT admission errors a279

Verdict: **CLEAN — SOURCE ONLY. STOP.** No blocking finding. This is independent source/composition review, not integration, site publication, deployment or an unconditional EVM equivalence approval.

## Exact identity and independence

Reviewed `/tmp/lido-deposit-admission-errors`, clean HEAD `a279b5661f5b1f6c588bacd2918dd2e0cd14cb2d`, tree `b93b02a13ba6ea9a23add64db3aeef6cc2be0991`, additive to accepted345 `b3b63586ecd72467dadc2e5ccdca220d820f15fb`. Exactly 19 additions: three Lean files and the new dossier. Every preexisting tracked blob, including All/Trust, Lake configuration, inherited proofs and evidence, is unchanged. Independently compared all 18 receipt SHA256 hashes and three configuration hashes; the receipt covers the exact added-file set except itself. `git diff --check` passes without raw whitespace exceptions for this delta.

I did not author this source or its integration. I read the complete new program/proofs, public consumer, regression module, README, validator, receipt writer and receipt; the consumed DSM/physical-admission programs, Effects and public proof; both retained Solidity fixtures/tests; and the complete independent source/integration reviews 39f4/7305 and 73c6/ffc2. Those inherited reviews are qualified reuse, not retroactive review of this new source. All 49 physical-admission and 50 DSM receipt bodies were independently compared against their exact reviewed source commits 39f48452bd5bdfff2478fffabe4b53efb20ad21e and 73c6360efdcd5b25a7188b4ed3de292001128f1d, respectively, as well as the validator's accepted345 comparisons.

## Actual consumed composition

`PDeposit1.actual_dsm_call_admission_bytes_suffix` has no propositional input premises. Its conjunction concerns the result of the actual new executor. Success implies no local failure, equality of the entire successful result to the old DSM executor (including World and both journals), and the entire unchanged `DepositDsmCall.Effects`. The proof reconstructs the old successful execution equation from that result equality and invokes the complete prior public consumer. It does not replace the old effects with a selected subset or assume a successful intermediate stage.

The runtime makes one DSM lookup, branches on that returned result, uses its decoded DSM to overwrite the supplied DSM, evaluates the physical local gate, and invokes the unchanged ModulePhysicalMetadata continuation once when admitted. Equality to old execution is proof transport, not an executable duplicate lookup or continuation. The continuation retains its own withdrawal/beacon execution; this does not assert there is only one external call in the entire suffix.

The local tag is not detached evidence. On an actual local tag, the public theorem derives the exact successful lookup and its actual static journal, the decoded DSM, all preceding successful local guards, the first failing guard, outward `.error (.bubbled (errorBytes e))`, original World, and empty module-attempt journal. The tag is generated by precisely that runtime branch. It is not a classification of every downstream error or a converse claim that identical bytes from another callee must have local origin. Lookup/decoder failures and downstream failures retain their original semantics and no local tag. Every outward failure restores the original entry World.

The positive nonempty regression transports through actual accepted328 execution and consumes the full new public theorem. The late example reaches the positive module and metadata continuation before withdrawal rejection, then proves original rollback. The local public instance consumes actual unregistered origin with competing invalid status. Helper transport lemmas have lookup/gate equations, but those helper hypotheses are not exposed as public input assumptions.

## Solidity/compiler correspondence

Checked pinned core `17005714f151e5502c559932319a3f2f74ac2436`, actual StakingRouter.deposit and auth, SRUtils membership, and retained full DsmHarness IR at the complete deposit entry and exact guard/encoder definitions. Actual order is DSM lookup, caller authorization, registered membership, enum validity, inactive status, then credential capture and the later allocation/module path. Membership is nonzero at Keccak(id, routerRoot+2); status is bits224–231. Values >=3 panic before the status1/2 inactive error. Type bits232–239 remain distinct, including type255.

The four encodings agree with actual compiled code: NotAuthorized `ea8e4eb5` (4 bytes); StakingModuleUnregistered `d41d6282` (4, IR shl225 6a0eb141); enum Panic `4e487b71` plus uint256(0x21) (36); StakingModuleNotActive `645cc9f6` (4, IR shl225 322e64fb). Guard priority in `Origin` includes each earlier successful condition. No global reason-string remapping is introduced; the inherited zero-maxEB division fault remains a semantic downstream fault.

The retained fixtures exercise actual inherited helpers and the admission fragment. They do not execute the entire deposit suffix. Full compiled inherited deposit IR supplies correspondence evidence for the broader function; its mere presence is not credited as a runtime end-to-end test. Existing independent review of the unchanged continuation supplies the bounded source correspondence for its module-returned World, physical metadata, captured credentials, zero-key branch and withdrawal/beacon suffix.

## Validation and evidence

Read the complete validator before executing its default comparison with `PYTHONDONTWRITEBYTECODE=1`. It passed: 1317 actual selected source identities, 11 Lean package pins, 25 ordinary transitive foundational-only scopes, three normal module identities, 99 retained receipt hashes, both 28-source compiler closures, 15 retained normal Solidity cases and four 1024-run fuzz cases. Temporary ordinary Lean queries were used; no candidate recording, build, compiler, Forge or native execution was repeated.

The validator checks selected setup/import providers and source bodies, exact current setup/trace/olean/source hashes, empty options/plugins and nonsynthetic traces without kernel-skip/error/sorry. All 25 queried theorem closures are foundational only (propext/Classical.choice/Quot.sound as applicable). This is a scoped statement; source-only review claims no new global All/Trust registration or global axiom count.

The final retained test build completed 1334 normal jobs. Source/public were actively built successfully in the preceding retained attempt, whose test transport metavariables failed; the final test correction built the test and reused unchanged verified source/public artifacts. The earlier 1329-job source build predates the transport addition and is not treated as final proof evidence. Development proof-shape, match and test failures remain explicitly failed. Tests contain 11 kernel regressions, four transport/execution theorems and three public instances, including all 256 status bytes. Synthetic test hashes do not establish cryptographic Keccak correctness.

Both retained compiler packets, exact metadata closures, full IR, vendored core and OZ archive contents were checked by the default validator. No fresh Solidity result is claimed. The old DSM `-vvvv` no-code diagnostic remains FAIL; the immediate unchanged-artifact ordinary `-vv` check remains PASS. Exact empty returndata is credited only within the retained ordinary-mode evidence. Neither instrumentation causality nor unconditional behavior across modes is inferred.

## Limits retained

This is the bounded328 adapter. The actual source captures credentials/module address before getDepositableEther and allocation and reads cap afterward. Those omitted calls and preservation across their prelude are not proved here: selected allocation, maxEB/immutable/context identity, phase memory cursors and linked-library identity retain their inherited boundaries. The SRLib DELEGATECALL is not converted into a frame guarantee by a view annotation. No new initialization, authorization, fit, successful-stage, nonalias or hash-injectivity premise appears in the public theorem.

Opaque cryptographic interpretation, natural ledger semantics, full ABI entry/memory/copy/gas, deployment/production bytecode equivalence, complete merged nested/static trace and downstream event/error-byte qualifications remain. Accepted ALLOC/RESERVE claims are unchanged. This source review authorizes no site claim expansion beyond these conditions and requires separate exact integration review.

Only this external report was written. Candidate and caches were not mutated. **CLEAN — STOP.**
