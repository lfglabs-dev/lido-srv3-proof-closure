# DEPOSIT exact local admission errors

Additive source candidate on accepted345 `b3b63586ecd72467dadc2e5ccdca220d820f15fb`, core `17005714f151e5502c559932319a3f2f74ac2436`. Only the three new Lean files and this dossier change. Source authoring is not independent review; a separate reviewer and root integration are required.

`PDeposit1.actual_dsm_call_admission_bytes_suffix` is the consumed all-outcome public theorem. For success of the actual new executor it retains the **entire unchanged328 `DepositDsmCall.Effects`**, exact old successful result (World and both journals), and absence of a local rejection. For each actually recorded local rejection it derives the decoded DSM lookup/trace, exact first-failing physical guard, exact bytes, original World and no module attempt. For every failure it proves whole original-World rollback. There are no propositional input premises, independent successful-stage premises, initialization, fit, frame, hash-injectivity/nonalias or WC-type assumptions. The conditional success/error properties are conclusions about the executor it actually ran.

The runtime definition performs one existing DSM lookup, then the local physical gate, then one unchanged ModulePhysicalMetadata.program continuation. It overwrites the supplied DSM as328 did; it replaces supplied active/WC using the same physical derived context. Successful equality with the old executor is a proof equation, not an executable second lookup or second module call. The `localFailure` tag records only this gate's branch and decoded DSM. No generic downstream reason-string mapping exists. Locator failures, decoder failures, arbitrary module/withdrawal errors and the inherited zero-maxEB panic keep their original behavior. This increment does not claim exact bytes for every later semantic error.

## Four local errors and their source order

Pinned StakingRouter deposit943–950 calls `_checkAppAuth(_getDepositSecurityModule())`, membership, enum/status, then WC capture. `_checkAppAuth`1177–1179 and SRUtils membership45–47 supply the actual custom errors. Physical membership is nonzero at Keccak(id,routerRoot+2), not lastModuleId or a guessed array bound. Status is bits224–231 of the config word; enum values>=3 panic before the inactive test. Only status0 admits. Credential type is the separate bits232–239 and every uint8 remains accepted by this getter.

| First failing local guard | Exact outward bytes |
| --- | --- |
| caller != decoded DSM | `ea8e4eb5`, 4 bytes, NotAuthorized() |
| membership position ==0 | `d41d6282`, 4 bytes, StakingModuleUnregistered() |
| status >=3 | `4e487b71` followed by uint256(0x21), 36 bytes |
| status1 or2 | `645cc9f6`, 4 bytes, StakingModuleNotActive() |

`Origin` includes every earlier successful guard, so it states error priority, not merely an error label. The public LocalRejection includes the same actually executed lookup's decoded DSM and static journal. Source and compiler selectors were recomputed independently with Python Crypto Keccak.

Full accepted DsmHarness IR is retained unchanged at `audit/deposit-dsm-call/solidity/DsmHarness.ir` (5064 lines): complete fun_deposit3323 onward, actual authorization4232–4244, membership4119 onward. The enum encoder is `4e487b71`/0x21/36 bytes; inactive `shl(225,0x322e64fb)` equals `645cc9f6`; membership `shl(225,0x6a0eb141)` equals `d41d6282`. The complete5030-line AdmissionHarness IR, full vendored source and compiler artifacts are likewise retained from326. No new Solidity fixture or compiler profile is substituted.

## Full continuation and boundaries

The new successful result is exactly328's successful result, not a selected subset of its commitments. That includes the actual module raw reply/returned World, physical metadata, zero-key path or Lido withdrawal/beacon suffix, captured credentials, balances and separate static/module observations. The late-failure example performs the actual positive module and metadata before withdrawal rejection, then restores the original World and retains inherited attempts. New errors are emitted only at the local admission branch; no module attempt is synthesized on that path.

This remains the bounded328 adapter. Actual source captures WC/module address before getDepositableEther and allocation, and reads cap afterward. Those calls and preservation across their omitted prelude remain outside: `i.selected`, maxEB/immutable/context identity, independent memory cursors and linked-library identity remain the same supplied boundaries. Actual allocation invokes SRLib DELEGATECALL; this lot does not pretend that a view annotation proves a read-only generic callback. ALLOC1/2 and RESERVE1 are unchanged. Full ABI entry decoding, memory/copy/gas, deployment, cryptographic interpretation, natural ETH ledger, full nested/static trace and downstream event/error-byte qualifications remain attached. No caller-role/pause prefix is invented for this entrypoint.

## Validation and reuse

Normal final `lake build LidoSRv3.Tests.DepositAdmissionErrorsRegression` passes1334 jobs. Source/public were actively compiled successfully in the immediately preceding test-build attempt, whose test trace metavariable failed; the final test-only correction actively builds the final test and validates those unchanged source/public artifacts. Both logs are retained. The earlier source-only1329-job success preceded addition of the transport lemma and is not final-module evidence. Current normal identities record all three actual setup/trace/olean/source hashes, empty options/plugins, nonsynthetic traces and no kernel-skip/error/sorry. There are1317 actual source identities,11 package pins and25 ordinary transitive theorem scopes, all foundational only. No global All/Trust count is claimed.

The tests contain11 direct kernel regressions (including the complete256-status sweep), four positive/late transport and execution theorems, and three consumed public instances: nonempty full328 success, late original rollback, and local error origin. They cover competing DSM/auth/membership/enum/status failures, exact4/4/36/4 lengths/bytes, type255 admission, absent module attempts, short DSM response and unchanged downstream zero-divisor error. Positive/late instances transport through the actual accepted328 nonempty execution. Test hashes deliberately use accepted synthetic/colliding layouts and are not new Keccak correctness evidence.

No fresh Forge/compiler/native run is needed for these four encoders: the complete existing326 suite directly tests exact auth/membership errors, every256 enum/status and credential type, physical membership and high unrelated config fields, with two1024-run fuzz cases. The existing328 suite tests actual DSM request/decoder/auth/membership ordering, forbidden static mutation, all32 short lengths and canonical/dirty/trailing responses, with two1024-run fuzz cases. All15 retained normal-mode tests and four1024-run fuzz tests are **reused evidence**, not new test runs. Both28-source compiler metadata closures (27 common dependencies plus each harness), current core Git bodies, actual vendored OZ5.2.0 archive bytes, full IR and all99 old receipt hashes are checked against accepted345. The two source-independent reviews39f4/73c6 and integration reviews7305/ffc2 are retained and identity-recorded; this new author does not present them as a review of the new code.

The existing328 `-vvvv` no-code test remains **FAIL**, with its original trace and immediate unchanged-artifact `-vv` PASS both preserved. This lot credits exact empty returndata only to the retained normal mode; full IR/trace supports call order. Attribution to Foundry is not a newly established diagnosis. No unconditional all-modes exactbytes claim or new no-code result is made.

`validation/validate.py` defaults to read-only comparisons plus temporary ordinary Lean axiom queries, without builds/compiler/Forge/native execution; `--write` records only this new dossier. It checks actual selected imports and old local bytes at345, current normal artifacts, all new named theorem closures, exact retained runtime/source/IR identities and local selector/order correspondence. Older validators were read but not executed because they write their frozen receipts. `write-receipt.py` records this new dossier only. Reviewers should inspect wrappers first and reuse valid artifacts without unnecessary rebuilds.

Development failures are explicitly excluded from PASS: initial proof-shape/superfluous-tactic and reserved-name mistakes; a transport match simplification error; then an unspecified lookup trace in test transport. Original logs remain unchanged. They contain replayed diagnostics from old sources; inherited warning text is not a new source change. Raw whitespace findings, if any, are listed precisely in the receipt. No old file, cache rewrite outside normal authorized builds, All/Trust/lakefile, site, merge, push or deployment is included.
