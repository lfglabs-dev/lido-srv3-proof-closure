# Physical consolidation quota in the actual settlement

Source base: main342 `1cd11a4719ce9dec72f0c7dcffde5bcd7848f28d`. Core Solidity: `17005714f151e5502c559932319a3f2f74ac2436`. This is an additive source candidate requiring independent review and later root integration; it does not change All/Trust, Lake configuration, prior proofs, site claims or guarantee status.

The useful result is one execution from the physical consolidation quota through the actual fee quote, actual vault CALL, decoded width-valid source/target inbox requests and refund. The public consumer preserves the **entire unchanged GatewaySettlement.Success and SettlementRequests.Effects** on the actual post-quota World. Its only success premise is the new whole execution's success. Quota conditions and prior success are derived by inversion. Root failure restores the complete original already-payable-credited suffix World, including the quota write, while retaining attempted-call traces.

## Exact source boundary and consumed state

The modeled suffix begins just before `_consumeConsolidationRequestLimit` at Gateway source209. Source189–199's pure balance/value/group/count checks are replayed as in the accepted settlement model; this does not execute or move quota ahead of intervening DSM/Lido preconditions201, locator203 or witness verification205–207. Role/pause and the earlier payable credit remain outside. The subsequent existing settlement executor replays the same pure checks on a World whose only prefix change is the physical quota storage word; this introduces no second external call or detached settlement.

The storage owner is the actual executing `ctx.self`, distinct from the vault's supplied configured `gateway` identity. The latter is still checked by the actual vault body, and the inherited successful effects derive equality. There is no new address-equality premise, and a kernel regression distinguishes these roles before vault authorization.

Quota is loaded from `dbb01bb6dca1179d47b58b17828e02dd61fb4c11ad515bab6e3e9ce25440d797`, the actual Keccak of `lido.ConsolidationGateway.maxConsolidationRequestLimit`. Five uint32 fields occupy offsets0/32/64/96/128: maximum, previous remaining, previous timestamp, duration and items. `packed_fields` proves all five decoded field identities and preservation of the upper96 bits without a fit premise. A successful enabled transition derives remaining≤maximum, remaining<2^32, available<2^32, timestamp<2^32 and count+remaining=available. The exact checked timestamp-update equation is retained. Field bounds come from physical decoding, not initialized-storage assumptions.

| Source operation | Executed behavior |
|---|---|
| isLimitSet | Maximum0 returns without arithmetic or any write, even on otherwise invalid raw state. |
| calculateCurrentLimit | Checked uint256 timestamp−previousTimestamp first. If elapsed<duration or items0, returns previous. Otherwise division checks zero, restored product and previous+restored are checked uint256, then min with maximum. |
| Gateway count guard | Available<count gives exact68-byte `d0e5bff5` plus count/available. Count comes from the actual supplied source-key groups and has a derived uint256 bound. |
| updatePrevLimit | Maximum<remaining gives exact4-byte `3261c792`, then checked timestamp subtraction and zero-checked division. Frames truncate to uint32 BEFORE checked uint32 multiplication by duration, then previousTimestamp+passedTime is checked uint32. |
| physical store | The same original loaded word supplies unchanged max/duration/items and upper96 bits. There is no intervening external call between load and store. |
| quote/vault/inbox/refund | Each consumes the actual modified or subsequently returned World; outer and value-credited inner fee reads remain distinct. |
| callback/rollback | No final quota frame is asserted. An accepted refund callback can overwrite quota; every covered failure restores the original suffix World. |

Arbitrary bad raw state is covered by execution, including previous>maximum, backwards timestamps, zero duration, uint256 replenishment overflow and uint32 update overflow. No nonzero duration, timestamp fit, initialization, successful stage, nonalias, balance frame or cryptographic premise was added. Internal arithmetic helpers use Nat; the public runner supplies Word timestamp and a derived bounded source count.

## Compiler and runtime evidence

`solidity/build-info.json` retains the actual complete compiler input/output from the one necessary fresh Forge compilation:22 pinned core/cached OZ source bodies plus the two new harness/test files,24 total. `PhysicalQuotaHarness.ir` contains the full2456-line optimized IR. All four fresh artifacts are retained. Profile: solc0.8.25+b61c2a91, viaIR, optimizer200, Cancun. Core bodies are exact Git objects; OZ5.2.0 bodies are compared with the retained cached npm tarball and its SHA512 pack integrity. This is not a deployed production bytecode identity claim.

Read the full RateLimit and Gateway bodies, getter, `_getTimestamp`, storage helpers and relevant complete retained compiler paths. IR1699–1780 executes actual inherited quota: disabled leave, current calculation, count error, checked subtraction, maximum guard, timestamp subtraction, division/cast/product/add and full packed SSTORE. IR2061–2076 decodes five low32 fields; IR2089–2129 executes replenishment. The actual full Gateway entry reaches quota at1158 only after its earlier preconditions/locator/witness code, then quotes at1159. The separate typed fixture entry also invokes that same unmodified internal quota before its quote. Memory allocation in these compiler paths remains an explicit boundary.

The new PhysicalQuotaHarness inherits actual Gateway/RateLimit, `_getTimestamp`, pair producer, fee/refund helpers and balance modifier without overriding them. Its explicit `settle` entry reuses the earlier typed count/settlement fragment with quota inserted at the documented suffix position; it bypasses the omitted stateful prefix and accepts a supplied vault. Its unrestricted raw setter is fixture instrumentation, including the callback mutation case. It is not presented as full gateway admission.

The actual accepted two-source SettlementVaultHarness0.8.9 cached artifact is reused from `/tmp/lido-consol-root/audit/consolidation-settlement/solidity/out/SettlementVaultHarness.sol/SettlementVaultHarness.json`, with both exact accepted source bodies, metadata Keccak and artifact hash checked. Profile remains solc0.8.9+e5eed63a, optimizer200, London. No legacy compiler rerun. Vault authorization, decoder, width checks and actual inbox loop remain the accepted fixture/source boundary of prior320. Full original reviews remain at `audit/consolidation-settlement/independent-review-e7ea.md` and `audit/consolidation-settlement-requests/integration/independent-source-review-27f2.md`, unchanged by this increment.

Twelve fresh Solidity tests PASS. The1024 fuzz runs exercise bounded initialized-state replenishment with arbitrary upper96 bits; explicit deterministic cases separately cover disabled bad state, backwards time, both zero-duration paths, count-before-update priority, inconsistent previous limit, uint256 product/add overflow, uint32 product/add overflow, frames-cast-before-product and cap behavior. The same-execution tests require both quotes/inbox calls and refund to observe the actual updated quota; verify exact96-byte source+target payload; restore quota, inbox/refund effects and transaction balances on late failure; and allow an accepted callback to change final quota. These tests do not claim arbitrary full-state fuzz coverage, full ABI equivalence or deployed consensus execution.

## Lean validation and reuse

The final targeted normal build completes1269 jobs. All three new modules have actual normal setup/trace/olean identities with no synthetic trace or skipKernelTC. The final log replays source/public and actively builds tests. Their exact source/public normal artifacts were compiled in the preceding attempt whose test-only record syntax failed; that failed whole build is not credited as a PASS. No global All/Trust build is claimed at this source stage.

Sixteen finite kernel regression theorems and two public instances are retained. The nonempty public instance consumes the full new theorem and both old certificates for two actual decoded inbox requests followed by refund. The late-refund public instance derives whole original-World rollback. Unlike the prior TOPUP entry increment, the nonempty success here is kernel-evaluated, not native-only evidence. No FFI or native decision theorem shortcut is used.

`validation/validate.py` defaults to strict comparison. `--write` is the author-only recording mode for this new dossier. It checks actual regression import providers, old local bodies at base, package bodies at11 Git pins, all three normal source/setup/trace/olean records, every named regression plus source/public proof's ordinary axiom closure, complete prior consumer identities, compiler input/output/artifact metadata, exact rate selectors/slot/IR ordering and cached legacy evidence. Temporary ordinary Lean dependency probes are the only execution; no build/compiler/Forge occurs in this validator. The recorded result covers1252 actual source identities,11 pins and30 ordinary scopes. New theorem scopes allow only propext, Classical.choice and Quot.sound. The global repository's disclosed exceptions are not attributed to these scoped results.

Development logs preserve proof-shape, namespace ambiguity, packing heartbeat, reply-pattern and test-record syntax failures. A validation attempt during a failed test build correctly failed because its olean was absent. An initial IR-validator range selected the constructor helper instead of the later runtime helper; its failed attempt is also retained and the range is now bounded after the runtime calculate function. All are named development failures, not accepted proof evidence. Earlier successful test snapshots are retained as superseded when owner/bound conclusions changed. The final normal identities and successful ordinary closure are the accepted records.

## Remaining limits

The rollback snapshot already includes payable credit; the theorem does not separately model caller debit or pre-payable transaction rollback. Full role/pause/DSM/locator/witness execution, deployed address-to-code dispatch, storage initialization/provenance, arbitrary malformed outer ABI/array-allocation behavior, complete memory/copy/alias/gas, LOG ABI and consensus processing remain outside. Refund errors and request events retain their inherited semantic representations. Existing arbitrary external/callback Worlds and aliases remain supported; no net-recipient-credit theorem or final quota-preservation promise is added. There is no change to the public eleven-guarantee facade or any site claim.

## Raw artifact whitespace

`git diff --check` has exactly10 raw-evidence findings: compiler IR `solidity/PhysicalQuotaHarness.ir` EOF2456; failed raw log `validation/development-public-word-name.log` lines136,142; failed raw log `validation/development-test-word-name.log` lines168,342,348,373,379,403,422. These original compiler/error bytes are retained verbatim. Excluding only those three named raw artifacts, the complete added diff passes. No proof, validator, README or receipt whitespace exception is claimed.
