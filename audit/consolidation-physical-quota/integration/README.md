# Physical consolidation quota integration

Source82051ab0a552734712b8f33e3f96f0074bf9ee39 passed the retained independent full source/Solidity/consumer review. Unionb38eefd62eeae02a85bc44ac66749e62ab32f952 combines it with accepted main344d353e85438b3456604a76ee417f9359627751240. All three new Lean modules and the complete source dossier remain unchanged. All/Trust prepend public/test imports and Trust adds six queries; prior providers and queries are preserved. Configuration equals accepted344, including its two registered accounting-call roots.

The actual executing gateway quota word supplies five uint32 fields. The successful transition derives arithmetic bounds and writes the packed formula preserving upper96 bits, without adding initialization, valid-state, timestamp-fit or nonzero-duration hypotheses. The public result consumes this exact intermediate World in the complete accepted GatewaySettlement.Success and SettlementRequests.Effects: fee reads, vault attempt, actual decoded inbox requests, refund and final balance assertion remain composed. Every covered failure restores the original already-payable-credited suffix World. The separate packed_fields lemma derives decoded field identities; the public certificate consumes the complete packing equation, rather than advertising those identities as explicit conjuncts. Accepted callbacks may change final quota, so no final quota frame is claimed.

Fresh normal AllGuarantees/Trust passes1785 jobs, actively compiling the three new modules and both aggregators. All five normal source/setup/trace/olean identities are retained; aggregator providers match. Fresh global trust reports29 existing disclosed axioms, distinct from the30 ordinary foundation-only scopes of this increment. The integration wrapper compares1252 actual source bodies,11 pins,42 source receipt hashes and all deterministic source records. Only path-dependent normal module identities are redirected to an external temporary output; all three current configuration bodies are compared to accepted344. No source receipt or source-stage normal artifact is rewritten.

Retained source evidence includes16 kernel regressions and two public instances, including nonempty composed success and late original-World rollback. Twelve Solidity tests pass, including1024 bounded initialized-state fuzz runs and separate malformed/extreme arithmetic cases. The24 exact fresh compiler inputs, four artifacts and full2456-line IR retain identity; the two-source legacy vault artifact is reused exactly. Source review checks the actual quota source and full helper/entry IR, not just compiler success. No compiler, Forge or native run is repeated by integration.

The modeled suffix starts at quota after omitted stateful admission; inherited pure balance/value/group/count checks are replayed. Complete role/pause/DSM/locator/witness admission, pre-payable transaction entry, deployed code/configuration identity, general malformed ABI, memory/copy/alias/gas, LOG ABI and consensus remain outside this increment with their existing scope. The next independent physical-entry writer uses a separate checkout and cannot mutate this frozen candidate. Site426 remains at its last reviewed343 proof pin and does not yet credit this increment.

Exactly ten source raw-whitespace exceptions remain those in the unchanged source receipt: PhysicalQuotaHarness.ir EOF2456, development-public-word-name.log136/142 and development-test-word-name.log168/342/348/373/379/403/422. This integration delta passes diff --check without exceptions. Independent review of this exact integrated commit is required before merge.
