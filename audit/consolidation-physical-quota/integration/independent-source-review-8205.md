# Independent source review: physical consolidation quota 8205

Verdict: **CLEAN for the declared bounded source increment; STOP.** No blocking correctness, source-correspondence, composition or evidence finding identified. This verdict does not accept a future normal All/Trust integration or site publication.

Exact reviewed checkout: `/tmp/lido-consolidation-physical-quota`, clean HEAD `82051ab0a552734712b8f33e3f96f0074bf9ee39`, tree `0e3f64ee478014428ef69d4cff1981302f577639`, parent/base `1cd11a4719ce9dec72f0c7dcffde5bcd7848f28d` (main342). Pinned core Solidity: `17005714f151e5502c559932319a3f2f74ac2436`.

## Independence and exact scope

I am the independent reviewer `/root/direct_steth_exact_review`; I did not author these sources, proofs, fixtures, evidence or integration. The source author was stopped. Root is the separate integrator. I made no candidate, cache, Git, site or browser mutation, and ran no build, compiler, Forge, native suite, network publication or external contact. Only this external report was written. Review used source reads, Git/blob/hash comparisons and the inspected validator's ordinary temporary Lean dependency probe.

I read all three new Lean modules, including every definition, proof, public statement and regression: `audit/trio/consolidation/PhysicalQuotaSettlement.lean`, `LidoSRv3/Audit/Guarantees/PConsolidationEth1PhysicalQuota.lean`, and `LidoSRv3/Tests/TrioConsolidation/PhysicalQuotaSettlement.lean`. I read the entire new README, receipts, correspondence map, validator and recording scripts, Solidity harness/test/runner, retained compilation identities and relevant logs. I checked the actual pinned RateLimit source and Gateway quota, entry ordering, timestamp, fee, refund and balance-modifier paths against the complete retained compiler artifact and pertinent complete optimized-IR helper bodies.

The exact delta is 43 additions, with no old tracked object changed. All 42 receipt file hashes, working bytes and committed blobs match; all three recorded build-configuration hashes match. All/Trust, Lake and prior public sources remain unchanged. Final clean HEAD/tree was independently reconfirmed.

## Solidity correspondence and physical transition

The executor loads the actual executing `ctx.self`'s quota slot at Keccak `dbb01bb6dca1179d47b58b17828e02dd61fb4c11ad515bab6e3e9ce25440d797`. It decodes five uint32 fields at bit offsets 0, 32, 64, 96 and 128 and preserves upper96 bits in the write formula. This storage owner is not assumed equal to the configured gateway argument used by the actual vault authorization. The inherited successful vault effects derive that equality; the distinct-target regression shows quota can fail at the physical owner before authorization.

The disabled maximum-zero path returns without arithmetic or a write, including malformed other fields. Enabled current-limit calculation performs checked uint256 timestamp subtraction before the early return for elapsed<duration or items=0. Only the replenishing path divides, checks uint256 multiplication and addition, then caps at maximum. The count guard precedes the update division and carries the actual count/available error words with selector d0e5bff5. Maximum<remaining is checked before update division with selector 3261c792. Update frames truncate to uint32 before checked uint32 multiplication; previousTimestamp addition is also checked uint32. The model retains division-by-zero and arithmetic panic distinctions. There is no invented nonzero-duration, timestamp-fit, initialization or well-formed-state premise. Physical decoding supplies the field bounds; successful execution derives remaining, availability, timestamp and count bounds.

These conclusions are supported by the full inherited consume IR1699–1780, storage decode IR2061–2076 and current calculation IR2089–2129, not just compilation success. The final SSTORE preserves the upper96 bits of its fresh slot read. There is no intervening external call or storage write between the initial load and that store, so the modeled original-word formula agrees within the stated memory boundary. Actual full Gateway entry reaches quota at IR1158 after its stateful preconditions/locator/witness work, then quotes at1159. `_getTimestamp` is the actual block timestamp and is not overridden in the fixture.

The prefix boundary is accurately stated: the modeled suffix begins at quota; old pure balance/value/group/count guards are replayed. It does not execute or move quota before omitted role/pause/DSM/Lido/locator/witness admission. Replaying those pure guards in the inherited executor on a quota-only modified World introduces no additional external call.

## Complete public composition

The public theorem has only the whole new execution's success premise. Inversion derives prepare/count/transition success and the physical intermediate QuotaEffects. The exact postQuota World feeds the complete unchanged `GatewaySettlement.Success` and `SettlementRequests.Effects`; neither is replaced by a reduced certificate. The new result equals the actual inherited result wrapper, preserving its returned World and entire trace.

I read the consumed Success structure and complete Effects definition/proof/public consumer. The outer fee read observes the post-quota World; the inner read observes the actual vault-value-credited World. Decoded source/target pairs, width checks, actual inbox loop requests, vault attempt, refund from the loop's returned World and final gateway balance assertion remain connected in one execution. No detached counter, stage-success premise, fee equality, ABI-roundtrip, callback frame or final quota frame is supplied. Accepted callbacks may overwrite quota, as explicitly tested. Every covered failure restores the original already-payable-credited suffix World, including quota and subsequent callback effects; diagnostic attempts remain observable.

A precision point raised during integration planning was checked: `packed_fields` separately proves the five decoded identities and upper96 preservation, but is not invoked by the public proof. The public certificate consumes the full `q.raw = packed ...` formula and actual postQuota World with derived bounds, rather than an explicit six-conjunct decoded-fields conclusion. The current README accurately attributes the separate lemma and does not claim otherwise. This is not a blocker. A future claim explicitly advertising those decoded identities as public conjuncts should use a derived corollary or describe the current statement precisely; it must not turn the intermediate property into a final callback frame.

## Independent reuse and validation

I read the complete retained independent reviews `audit/consolidation-settlement/independent-review-e7ea.md` and `audit/consolidation-settlement-requests/integration/independent-source-review-27f2.md`. Actual GatewaySettlement bytes equal accepted commit `e7ea041c216365acab8114aaac8d6eca109aae84`; SettlementRequests and its public consumer equal accepted commit `27f2394bd9afb416ffc6ed753d57f04d0f082228`. Thus their earlier correspondence evidence is reused under exact body identity, with their original boundaries and independent provenance, rather than silently self-certifying inherited work.

After reading its code, I ran `PYTHONDONTWRITEBYTECODE=1 python3 audit/consolidation-physical-quota/validation/validate.py` in default comparison mode. Observed exit0: **1252 actual sources, 11 Git pins, 30 ordinary foundation-only scopes, 3 normal module identities, 24 fresh compiler inputs/full IR/4 artifacts, 2 legacy cached inputs, 12 Solidity tests/1024 fuzz; entire prior consumer unchanged**. The first probe's completion output became unavailable across context/tool-session turnover; the subsequent observed comparison is the credited result. Neither call built or compiled anything. One manual Git comparison initially used ambiguous short `e7ea`; it failed before the comparison and was corrected to the complete accepted commit ID. No failed command is credited as a pass.

The validator resolves actual import providers, checks local/base/package body identities and selected artifacts, compares source/setup/trace/olean hashes, rejects synthetic traces and skipKernelTC, and queries actual ordinary declaration dependencies. All 30 scoped closures contain only propext, Classical.choice and Quot.sound. This is a scoped result, not a claim that the entire repository lacks its disclosed exceptions.

The retained final targeted normal build passes1269 jobs and actively builds the final tests. Exact source/public normal artifacts were actively compiled in the preceding attempt that subsequently failed on test-record syntax. That failed whole build is explicitly not credited as successful. Current normal identities and the final test success support reuse; no source-stage global All/Trust pass is claimed. Sixteen finite kernel regressions and two public instances include nonempty two-request success and complete late-refund rollback. They use no native decision shortcut. Arithmetic regressions cover disabled malformed state, backward time, zero duration, guard priority, inconsistent previous state, both uint256 overflows, both uint32 update overflows, frame truncation, cap and upper96 preservation.

## Compiler/runtime degree and limits

Retained build-info contains 24 exact inputs: 22 pinned core/cached OZ bodies and two new fixture files. All four fresh artifacts, their metadata and bytecode/compiler-output relationship and the full2456-line optimized IR match. The profile is solc0.8.25+b61c2a91, viaIR, optimizer200, Cancun. OZ5.2.0 source bodies match the retained tarball and npm integrity. The legacy SettlementVaultHarness artifact is byte-identical to its documented cached origin, with solc0.8.9+e5eed63a, optimizer200, London and two exact accepted metadata source hashes. No legacy compilation was repeated.

The fixture inherits unmodified Gateway/RateLimit, timestamp, pair, fee/refund and balance helpers. Its explicit settle entry is a typed suffix fixture bypassing omitted stateful admission, not a full gateway-entry proof. The actual accepted vault fixture executes authorization, decoding, width checks and inbox calls. Twelve retained Solidity cases pass;1024 fuzz runs cover bounded initialized replenishment with arbitrary upper96 bits, while deterministic tests separately cover malformed/extreme arithmetic. Both fee reads/inbox calls and the refund check actual updated quota, exact96-byte payload is checked, late failure restores transaction effects and balances, and a successful callback can replace final quota. This does not establish arbitrary full-state fuzz coverage.

Remaining exclusions are unchanged and material: pre-payable caller debit/transaction rollback, complete role/pause/DSM/locator/witness admission, deployed address-to-code identity, storage initialization provenance, arbitrary malformed outer ABI/allocation, complete EVM memory/copy/alias/gas, LOG ABI and consensus processing. Refund errors/events retain inherited semantic representations. No net-recipient-credit or final quota-preservation theorem is claimed.

Exactly ten raw `git diff --check` findings match the README and receipt: IR EOF2456; development-public-word-name.log136/142; development-test-word-name.log168/342/348/373/379/403/422. Filtering only these three raw artifacts makes the complete delta pass. No proof, validator or documentation exception is needed.

**Final disposition: CLEAN at exact82051ab0a552734712b8f33e3f96f0074bf9ee39 for this bounded source increment. STOP. Separate integration and publication review remain separate gates.**
