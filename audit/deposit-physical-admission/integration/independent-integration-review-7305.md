# Independent exact DEPOSIT integration review

Verdict: **CLEAN** for frozen integration `73055005e3126742dfb949247ab76859e9dd50e6`, within the explicitly bounded physical-admission adapter scope. No blocking integration defect found. This does not certify full deployed DEPOSIT entrypoint equivalence. STOP.

## Exact union and delta

Reviewed `/tmp/lido-deposit-admission-integrated` read-only. HEAD matches the requested frozen integration, and its worktree is clean. Git confirms union `55f209e44f68e65708a155f88431119a28965331` has parents `4c41703d47a0a4c44d9210cc00a581c709bd6b10` and accepted ADDRESS main `9b9a757fe4e4b748decc452a2e30cb322ac28772`. The former has parents source `39f48452bd5bdfff2478fffabe4b53efb20ad21e` and ACCOUNT main `d6bbf39ad153ee979e2357fa5df2830c17f353f2`.

The union versus accepted main contains exactly the 50 source-candidate additions. Integration versus union changes only AllGuarantees, Trust and seven integration archive files. AllGuarantees adds the public physical-admission import; Trust adds the regression import and four fully qualified queries for success, rollback, selected fields and selected-octet head. No executor, input domain, theorem statement, existing provider, package pin or old guarantee was changed by this integration delta. `git diff --check` for integration versus union passes; this does not erase the inherited raw IR EOF exception.

## Proof/provider preservation

I previously read the complete new source/public/test files and pertinent executed dependency definitions, compared physical constants and behavior with pinned Solidity and full inherited deposit IR, and freshly re-elaborated the exact new Lean files. Those findings remain valid here by checked identities, rather than a redundant rebuild.

Every one of the 1,311 recorded imported/new source SHA256 values matches the integrated tree. Every consumed local source was additionally compared directly to its Git object at source39f4. All eleven package checkout revisions match their recorded pins. All 49 original candidate receipt hashes match. The integration preserves the public result on the same old Commitment, module-returned World, metadata update and suffix journal; it adds derived authorization, membership, status and exact selected-WC word/octet provenance. No hcfg, internal stage-success, hash-injectivity, nonalias or extra funding premise has appeared.

The three normal source/public/test olean hashes match identities.json and receipt.json. I inspected the actual AllGuarantees/Trust setup importArts: they select the normal integrated-worktree physical-admission oleans, and the selected bytes match the checked normal artifacts. This verifies the registered provider, rather than relying on an unrelated old worktree cache having a similarly named module. The source and public provider are selected by AllGuarantees; all three are selected by Trust.

The retained independent source report is byte-for-byte identical to my external39f4 report. All six integration receipt SHA256 entries match their files; receipt.json excludes its own hash as expected. No provenance substitution or altered conclusion was found.

## Validation evidence and scope

The retained normal combined build log shows AllGuarantees freshly built at job1721 (2.8seconds), Trust freshly built at job1722 (2.7seconds), and successful completion of all1,722jobs. Its final physical-admission queries correspond to the actual four added Trust lines. The global checker receipt explicitly reports29 exact axioms:23 test/mutant native-decision axioms, three production exceptions and the three foundations, with dependencies recomputed and native claims re-evaluated. Neither this report nor the integration README relabels the global environment foundations-only.

The six scoped results contain only propext/Classical.choice/Quot.sound; rollback scopes contain only propext/Quot.sound. The integration accurately says these were recomputed in the normal registered DEPOSIT test environment before the later ADDRESS union, then retained after the1,311sources and three normal oleans were confirmed unchanged. I checked those final identities myself. I did not rerun the global build/checker, the unchanged scoped checks or Forge merely to reproduce identical receipts.

The exact49source hashes also preserve the previously reviewed full source/IR/layout and Solidity fixture. Six source-run Solidity tests, two1,024-run fuzz tests and all256status/type cases remain reused author evidence; no fresh integration Forge execution or universal source correspondence is claimed. The raw AdmissionHarness.ir single blank EOF remains an explicitly retained compiler-output exception, not an unconditional full-diff formatting PASS.

## Claims and limits

The new README accurately describes an additive admission adapter around accepted321. It preserves the pre-resolved DSM and omitted getDepositableEther/ALLOC boundaries, selected allocation and phase-memory cursor inputs, constructor maxEB identity, arbitrary external/world environment, linked-library deployment identity, opaque cryptography, gas/memory/copy and semantic ABI/log/ledger limitations. It does not conflate arbitrary constructor maxEB with fixed32ETH beacon calls; the fixture7/11 observation is retained. Actual source read sequencing and lack of a general omitted-prelude preservation proof remain explicit. Linked SRLib/BeaconChainDepositor bodies are modeled without proving their deployed DELEGATECALL identities.

The unchanged Trust header already enumerates the three production exceptions and exact test-only list; the integration adds no claim contradicting that boundary. The README's foundations-only statement is expressly scoped to six DEPOSIT checks. No website, external publication or full-entrypoint claim is introduced.

No checkout mutation, source edit, rebuild, paid call, merge, deploy or external message was performed. The only write is this external review report. Final disposition: CLEAN for this exact frozen integration and stated scope. STOP.
