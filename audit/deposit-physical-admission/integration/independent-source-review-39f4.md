# Independent DEPOSIT physical admission review

Verdict: **CLEAN for the explicitly bounded additive physical-admission consumer.** This is not certification of the complete deployed `deposit` entrypoint, Solidity/EVM equivalence, or deployment/library identity. No candidate changes were made. STOP.

Reviewed candidate `39f48452bd5bdfff2478fffabe4b53efb20ad21e`, exact base `10e2b693d00cbbeab65f60cd8964ced87297336a`, pinned core `17005714f151e5502c559932319a3f2f74ac2436`. Candidate was clean before and after verification. All 50 delta files are additions; the receipt covers the other 49 files and its own hash is necessarily excluded. The complete three new Lean files were read, including all proofs, executable examples and public positive/rejection instances. The executable inherited definitions consumed by this consumer were followed through ModulePhysicalMetadata, ModuleCall, PhysicalMetadata, LiveBeacon, SuffixCommitment, router context/root/length helpers, physical credentials/address helpers, Live execution/withdrawal, low-level CALL, beacon loop and beacon callee. This is not a claim that every proof body in all 1311 imported source files was manually read.

## Physical source correspondence

The pinned StakingRouter deposit body and its admission, credential and metadata helpers were checked with SRStorage, SRUtils, SRTypes, EnumerableSet, WithdrawalCredentials, BeaconChainDepositor and the inherited Lido withdrawal/beacon source bodies. The full inherited `fun_deposit` compiler IR was read, not just the harness admission fragment.

- The namespace is exactly `lido.StakingRouter.routerStorage`; ERC-7201 uses the Keccak of ABI-encoded `(keccak(namespace)-1)`, with low eight bits masked off. The resulting inherited literal is `0x5648d366b9f342bdcc64be95cdcf5f05da808509be70eaa548a8795901d5d000`. The Solidity fixture independently recomputes the formula.
- `RouterState.moduleStates` is root+0; `moduleIds` occupies root+1/root+2; accounting root+3; raw withdrawal credential root+4. UintSet wraps a Set with a dynamic values array followed by `_positions`. Membership therefore reads the uint256 value at Keccak of exactly two big-endian 32-byte words `(id,root+2)`, and is nonzero membership. Neither `lastModuleId` nor an invented array bound is involved.
- The module config preimage is `(id,root)`. Module address consumes its low 160 bits; four uint16 fields follow, then status bits 224–231 and type bits 232–239. All word projections are physical reads from the supplied router account in the same entry World.
- Enum status decoding is load-bearing: the source enum has three members, and actual compiler IR checks `<3`, panicking 0x21 otherwise. Values 1 and 2 produce StakingModuleNotActive; only 0 passes. The new runner performs authorization, membership, invalid enum, inactive status in precisely that order after the explicitly resolved DSM boundary.
- Raw WC selection preserves low 248 bits and replaces the full high byte with the uint8 type. It correctly admits every possible uint8, including 0 and 255: the getter does not call the separate type-validity setter guard. `selected_fields`, `selectedOctets_head` and length cover numeric selection and big-endian encoding. The effect ties the complete encoding to the credentials of the same module/suffix Commitment.
- Module deposits reside at wrapping `(keccak(id,root)+1)`. The uint64 cap occupies bits 128–191; metadata timestamp and block writes replace bits 0–63 and 64–127, preserving upper 128 bits. The source updates timestamp, then block, then emits the event before the zero-key return or withdrawal.
- I independently compared all seven inherited Live Lido slot literals (locator, buffer/post-report, next-report/nonce, seed, reserve, target, active) with pinned Lido.sol/Pausable.sol; all match. The target slot is not read by this withdrawal path but was checked as well. The beacon source has branch slots 0–31, count slot 32 and depth/cap 32 / `2^32-1`, matching the consumed beacon model. Thus the earlier ADDRESS wrong-literal failure is not reproduced in these DEPOSIT slots.

## Composition and theorem domain

The public success theorem has only the new runner success equation as its propositional premise. It derives authorization, membership and status, then obtains the entire old PDeposit1.actual_module_call_metadata_suffix consequence on the derived context. Its proof retains `hrest` intact and adds credential identity/packing/encoding facts to the same existential Commitment. There is no hcfg, supplied internal stage success, injective hash, slot separation, root-match, alias exclusion or extra funding premise.

The derived context replaces only supplied active/WC fields. The success regression deliberately supplies false/none, so it exercises this replacement. Old source files, inputs, byte decoder, module target computation and public theorem are unchanged. The arbitrary module may return a wholly changed World: that returned world is passed to physical metadata, followed by withdrawal and beacon execution. Entry-selected WC remains captured even if the module changes the router configuration. The journal is the actual module journal concatenated with the suffix journal. Zero keys still retain module/metadata/event effects without suffix calls. A failure at any stage restores the entire entry World using the outer Live.run while preserving attempted-call observations.

The inherited decoder retains its uint256 projection, signed head guard, offset/length checks, pointer-dependent uint64 allocation bounds and sequential dual-bytes decoding. The raw bytes come from the actual modeled CALL, and its address/count/payload are the values computed by this runner. The public result derives coded-module origin and return size bounds; it does not restrict calldata or returndata to canonical/nonalias layouts. Existing ordinary-no-code/precompile limitations remain explicit in ModuleCall/LowLevel.

## Boundaries that must remain attached

Actual source resolves DSM through STATICCALL before authorization; this adapter receives the resolved address. It captures WC and module address before `LIDO.getDepositableEther()` and ALLOC, whereas the cap is read after allocation. The actual IR confirms this ordering. The adapter omits those calls, allocation computation/returned state, and the phase-memory origin, taking `i.selected` and `i.returnBuffer` as inputs. It therefore does not prove preservation of captured values across an arbitrary omitted prelude or establish a whole-entrypoint execution equation. Source getDepositableEther is STATICCALL; linked library allocation is a separate runtime/deployment boundary.

Constructor maxEB is any nonzero supplied immutable, not universally 32 ether; the actual harness uses 7 and 11. Arbitrary model maxEB retains the old zero-divisor panic rather than silently assuming constructor identity. The beacon helper itself uses 32 ether. Old success/ledger properties are retained, without inventing an unconditional equality between the two constants.

The model executes Lido withdrawal internally in its supplied context and uses a fixed source-shaped beacon dispatcher; arbitrary external interpreters, context/address bindings and deployed code identity remain boundaries. Actual IR also contains linked SRLib and BeaconChainDepositor DELEGATECALLs and their code checks; the source-shaped model inlines their bodies. This review does not turn those links into proved deployed identities. Memory copies, gas, runtime/deployment, opaque Keccak/SHA, event/revert byte ABI and natural-number ETH-ledger abstractions remain inherited limitations. Module-returned environment fields are used by metadata; an arbitrary external interpretation is not proved to preserve immutable EVM timestamp/block fields. New error reasons are semantic identifiers, not a newly proved Solidity revert-data encoder.

## Independent validation performed

1. Exact candidate HEAD and clean tree checked; all 49 receipt SHA256 values matched. Vendored core Solidity source bytes were independently compared to Git objects at the exact core pin.
2. OpenZeppelin 5.2.0 cached archive independently matched recorded SHA1 and SHA512 integrity; every vendored OZ source matched its archive entry. Stored compiler storageLayout equals compiler-artifact storageLayout. Recorded source/build artifact identities checked where present.
3. Reviewed validate.py before use. Ran it with the two receipt writes replaced by byte-equality assertions, and Python bytecode writes disabled. It passed **1311 imported/new source identities, 11 package pins, six fresh active ordinary axiom scopes**, including unchanged old sources and selected local cache bytes. No candidate receipt was rewritten.
4. Fresh direct `lake env lean` re-elaboration of each new source, public facade and test file, with no output artifact arguments, all exited 0. The tests actively re-elaborated the 16 kernel examples and positive/public rollback instances. The six axiom outputs contained only propext, Classical.choice and Quot.sound; rollback scopes contained only propext/Quot.sound. This is fresh new-file checking against identity-validated inherited caches, not a full dependency rebuild.
5. Reviewed complete actual admission fixture and its six passing author tests: two 1024-run fuzz tests and every one of 256 status/type bytes. These receipts are reused after identity checks, not represented as independently rerun Forge results. They exercise inherited admission helpers and the exact status condition; they do not execute the full Lido allocation/module/beacon deposit transaction.
6. Reviewed full deposit IR plus admission/root/membership/credential helper IR at the recorded solc 0.8.25, viaIR, optimizer 200, Cancun profile. The IR file is evidence from the author, hash-validated here; no independent full compiler rebuild is claimed.
7. `git diff --check` returns 2 solely for AdmissionHarness.ir line 5030, the preserved raw compiler blank EOF. This is an explicit byte-preservation exception, **not an unconditional diff-check PASS**.

No blocking defect identified within the declared adapter scope. No merge, deploy, external message, paid call or candidate mutation performed. STOP.
