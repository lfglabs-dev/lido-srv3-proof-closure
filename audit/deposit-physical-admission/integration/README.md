# Physical DEPOSIT admission consumer integration

Source `39f48452bd5bdfff2478fffabe4b53efb20ad21e` is united with ACCOUNT324 and ADDRESS325 at union `55f209e44f68e65708a155f88431119a28965331`, accepted main `9b9a757fe4e4b748decc452a2e30cb322ac28772`. The three new Lean bodies, all49candidate hashes and1,311imported source identities are unchanged. Only AllGuarantees/Trust wiring adds the public physical-admission consumer, its regression module and four axiom queries. Existing execution definitions, domains, providers and eleven dependency pins are unchanged.

The success consumer executes authorization and physical membership/status checks, derives the selected withdrawal-credential word and full32bytes from the actual router configuration/raw storage, and invokes the accepted321module/metadata/withdrawal/beacon program on that same input World. It retains that public theorem’s entire consequence and same Commitment, adding the derived physical facts. No supplied active/WC, stage-success, nonalias, funding or hash-injectivity premise replaces this proof. Canonical root/preimages/offsets, uint8enum Panic21 before inactive rejection, source read order, all uint8credential types, actual consumed encoding and later module-returned world were independently checked against complete relevant pinned Solidity and full inherited deposit IR.

This additive result is an admission adapter around the accepted phase consumer. It does not execute the omitted DSM resolution/getDepositableEther/ALLOC prelude or prove preservation across an arbitrary omitted prelude. Selected allocation, earlier memory cursor, constructor maxEB identity and existing context/runtime/deployment identities remain explicit. maxEB is not unconditionally32ETH; the fixture uses7and11. Linked source libraries are modeled by their bodies, without proving deployed DELEGATECALL code identity. The existing arbitrary-callee metadata environment, scalar memory/copy, natural ETH ledger, crypto/gas/ABI and full-entrypoint exclusions are preserved. The public claim does not silently acquire full deployed deposit equivalence.

Final combined normal AllGuarantees/Trust passes1,722jobs. Fresh global checking recomputes dependencies and re-evaluates native claims, retaining29disclosed axioms, not globally foundations-only. Six scoped checks were freshly recomputed in the normal registered DEPOSIT test environment and are foundations-only. The subsequent ADDRESS union leaves all1,311DEPOSIT source identities and three normal olean hashes byte-identical, so those scoped checks remain valid. identities.json records the normal artifacts without assuming cross-worktree olean identity.

The independent source review freshly re-elaborated all three exact source/public/test files, checked16kernel examples and two public instances,49candidate hashes,11pins,27actual core/OZ imports and full pertinent IR/storage layout, including independent OZ archive provenance and consumed inherited Lido literals. Six source-run Solidity tests (two×1,024fuzz plus all256status/type values) are reused by identity, not represented as fresh root Forge execution. They exercise actual inherited admission helpers and the exact status fragment, not the complete deposit transaction. The full deposit body is separately present in the reviewed IR at solc0.8.25viaIR optimizer200Cancun; this is a fixture profile, not production bytecode identity. Raw AdmissionHarness.ir retains its single compiler-stdout blank EOF as an explicit formatting exception. Other inherited raw-output exceptions are unchanged.

Source writer stopped before its independent review. The source review is retained verbatim. A separate independent review of this exact frozen integration is required before merge. No website or external publication changes are included.
