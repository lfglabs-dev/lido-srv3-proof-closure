# Independent exact DEPOSIT DSM source review

Verdict: **CLEAN for the explicitly bounded additive DSM-call consumer** at `73c6360efdcd5b25a7188b4ed3de292001128f1d`. The archived tracing-mode Solidity test remains FAIL and receives no unconditional exact-returndata credit. This is not full deployed DEPOSIT equivalence, integration approval or publication approval. STOP.

Reviewed `/tmp/lido-deposit-dsm-call` read-only, exact base `9383467fe19c621cc5c37ea54e1d5585b9ba49ee`, pinned core `17005714f151e5502c559932319a3f2f74ac2436`. The complete delta is 51 additions: three Lean files and the source dossier. No shared executor, AllGuarantees, Trust, package or old guarantee source changes. Worktree clean before and after checks. All three new Lean files, their proofs/tests and public statements were read completely; actual consumed admission/commitment/allocator/static-call definitions and relevant pinned Solidity/full deposit IR were followed. No candidate correction was made.

## Actual composition and theorem meaning

`program` executes lookup once, using router `liveCtx.sender`, supplied locator, zero value and the four selector bytes. The actual readonly external observes the entry Live.World and cannot return a replacement writable world. A rejected reply bubbles its bytes; forbidden state change is an actual failure with empty rejection bytes. On decoded success, `resolvedContext` overwrites the inherited DSM field with this address. The same entry world then enters actual DepositPhysicalAdmission.program; it does not consume an assumed lookup or instantiate an unrelated executor merely for the conclusion.

The public success theorem has exactly the new whole-execution success equation as its propositional premise. Inversion derives the actual locator response, canonical decoded address, static observation and `ctx.caller = dsm`. It reconstructs the successful old admission execution on the identical after-world/journal, invokes the accepted326 public theorem and retains the entire old Effects value. Thus the same old Commitment still contains the actual module request, raw reply, module-returned World, decoder/preparation, physical metadata and withdrawal/beacon suffix. These facts are conclusions, not supplied internal-stage premises. The separate `execute_of_lookup` equation is used for executable positive transport, not passed as a new public assumption.

The old externally supplied DSM=999 is demonstrably overwritten by actual reply7 in the regression. False/none old active/WC fields are likewise replaced by physical admission. The positive test transports through actual accepted326 positive execution; it does not manufacture suffix success. Late rejection occurs after module effects and physical metadata, then outer execute restores the complete original World. Static observations survive rollback separately from the unchanged module/suffix journal. This is not a unified or complete EVM nested trace claim.

## Pinned source, guards and memory scalar correspondence

Read pinned StakingRouter.deposit in full, constructor/immutable setup, actual `_getDepositSecurityModule`, `_checkAppAuth`, module membership and credential helpers, relevant SRStorage/SRTypes/SRUtils/locator interface and consumed inherited context/low-level definitions. Read the complete fresh `fun_deposit` IR, including omitted getter/allocation calls, module decoder, linked-library calls and final balance assertion, plus the actual DSM getter, allocator, canonical-address decoder and admission helper IR.

The selector independently recomputes with `cast sig depositSecurityModule()` to **0x472c1776**. Actual IR stores `shl(225,0x23960bbb)`, the same four bytes, and calls STATICCALL with input length4/output length32 against the masked locator immutable. There is no EXTCODESIZE precheck in this getter. Authentication runs only after the returned address decoder, before membership and status. The new consumer correctly uses the inherited low-level static semantics rather than the distinct older 0.8.9 typed helper that rejects code-less targets before attempting a call.

On static failure, actual IR copies and bubbles returndata before decoding or authorization. On success it chooses min(32,returndatasize), performs finalize_allocation first, then signed head-size and canonical high-bit validation. `decodeAddress` executes the same wrapping round32 allocator before the short/canonical tests. For copied size0..32, successful allocator guards imply cursor and next cursor below2^64, no pointer wrap and a small nonnegative difference. Thus the signed `slt(sub(dataEnd,headStart),32)` reduces to the implemented copied<32 check; this is not an extra input premise. Canonical160-bit words are accepted including zero, trailing bytes ignored, dirty high96 bits rejected rather than truncated. Successful lookup derives code presence under the explicitly ordinary-no-code interpretation; it does not assume it.

The DSM cursor and later module returnBuffer are independent explicit phase inputs (regression128 and512). This scalar allocator call is actually executed, but no successor-pointer equality, pointer provenance, full returndata copying, alias behavior or whole memory simulation is claimed. The host length is projected to uint256 consistently with the inherited byte decoder; no arbitrary host-list-length fact is silently asserted to be EVM realizable.

Membership is nonzero at Keccak(id,routerRoot+2); module configuration is Keccak(id,routerRoot), statusbits224–231 with enum values>=3 panicking0x21 before inactive status, typebits232–239, raw WC at root+4. Selected WC preserves low248 and replaces the whole high byte. Full source/IR confirms captured WC/module address before getDepositableEther/ALLOC and cap afterward. Accepted326 retains this full word/octet identity in the same suffix commitment; no new physical-slot, alias or stage assumption appears here.

## The no-code tracing discrepancy

Read all of forge.log, no-code-trace.log, no-code-check.log, the complete actual inherited harness and all nine tests. The original -vv suite passes nine tests, including two1024-run fuzz tests, every short length0..31, zero/canonical/trailing/dirty responses, selector/caller checks, physical credentials and actual forbidden SSTORE. The additional **-vvvv run fails** `testNoCodeSuccessThenDecoderRejection` at its conjunction asserting failure plus empty returned bytes. Its trace records the locator STATICCALL returning `[Stop]`, then the router revert labeled `call to non-contract address`, then the test's own failure. It is not a passing exact-byte test and is not merely ignored terminal decoration: the outer assertion genuinely fails in that mode.

The immediately subsequent -vv rerun explicitly says compilation skipped/no files changed and passes the same assertion. Together with full getter IR showing the successful no-code call followed by an explicit empty decoder revert, this supports a bounded interpretation: ordinary-no-code call order and source decoder outcome are justified by IR plus the observed call/revert trace; exact empty returndata has the retained **normal-mode** test evidence only. Attribution to Foundry's diagnostic instrumentation is an inference from the verbosity-dependent unchanged-artifact behavior, not a proved diagnosis of Foundry internals. No all-modes exactbytes result, raw traced empty returndata or independently replayed transaction is credited.

This discrepancy therefore does not demonstrate a mismatch in the new Lean executor or inspected compiler getter, within the declared source-model boundary. The README and receipt preserve the FAIL, both commands, differing outcomes and this limited attribution. No stronger successful-runtime promise is needed for the public theorem. A future byte-exact tracing claim would require resolving the framework behavior with separate evidence. There is no blanket waiver of a green/failed contradiction.

## Independent checks performed

- All50 receipt SHA256 values match. All51 changed paths are additions. All12 recorded normal source/public/test artifacts (olean, ilean, trace, setup) match build-identities.json.
- Inspected validate.py and its checker use before execution. Ran it with only the two receipt writes replaced in memory by exact-byte equality assertions, with Python bytecode writing disabled. Freshly checked **1314 imported/new source identities, eleven package pins and six active axiom closures**. Actual selected local cached sources/oleans agree with this checkout; every consumed old local source matches9383. No frozen receipt was rewritten.
- Fresh `lake env lean` checks of source, public facade and tests all exit0, with no output-artifact arguments. All20 executable kernel examples, positive transport/execution and three public instances are actively elaborated. The six displayed closures use only propext/Quot.sound, with Classical.choice additionally present on the full success and positive instance. No new native_decide, sorry, unsafe implementation or project axiom appears. This is focused re-elaboration against identity-validated caches, not a fresh entire dependency build.
- The retained author normal build ends at1331 jobs with current tests built in3.1s. Earlier development/parser failures remain historical diagnostics, not the final validation result. No global AllGuarantees/Trust gate is claimed for this unintegrated source lot.
- All27 vendored Solidity dependencies exactly equal accepted326 copies. Core-origin files additionally match actual Git blobs at the core pin, using the real core checkout. All28 compiler metadata source Keccak values were independently recomputed with cast and match the artifact. Compiler metadata explicitly records solc0.8.25, viaIR, optimizer200, Cancun. The retained full IR equals artifact irOptimized plus its original stdout newline. The harness inherits the actual router getter/auth/storage helpers, but does not run the entire deposit transaction. No fresh compiler or Forge execution by this reviewer is claimed.

## Preserved boundaries and raw-output finding

Constructor locator identity, external caller/context/contract bindings, selected allocation, maxEB and independent memory-phase origins remain supplied. This increment removes the supplied DSM address on its own path; it does not prove omitted getDepositableEther/ALLOC execution or preservation across that prelude. Locator may be an arbitrary typed address under the declared environment; deployed constructor/code identity is not inferred. The low-level no-code arm excludes precompile dispatch as already declared. Nested arbitrary locator subcalls are not recorded by this StaticCall.External interface. Cryptographic interpretation, natural ETH ledger, semantic errors/logs, runtime/gas and linked-library deployment boundaries remain. No arbitrary constructor maxEB=32ETH, full module-effect ledger or full source/EVM rollback theorem is asserted.

One additional packet precision was reported promptly to root: unfiltered `git diff --check` returns2 for **four raw-output locations**, not solely IR EOF. DsmHarness.ir line5064 is blank EOF; development.log lines1308,1310,1312 retain trailing spaces from original Lean error diagnostics. README records the development errors but receipt's diff_exception lists only IR. This review explicitly preserves and discloses the additional three raw diagnostic whitespace findings; integration documentation should enumerate them. They are byte-preserved historical log data, not source or proof defects, and no unconditional diff-check PASS is credited.

No source, logs, oleans or receipts were edited; no paid resource, network, merge, publication, site or deployment action was taken. The only persistent write by this reviewer is this external report. **CLEAN for exact73c6's bounded source/proof claim, with the tracing FAIL and full raw-output exceptions retained as above. STOP.**
