# Independent exact DEPOSIT DSM integration review

Verdict: **CLEAN** for `ffc2b7c76251bbe52a51d258d6f9861d4061cd78`, within the source review's explicitly bounded scope. The traced no-code test remains FAIL; no all-modes byte-equivalence claim is approved. STOP.

Reviewed `/tmp/lido-deposit-dsm-integrated` read-only. HEAD is exactffc2, its parent is reviewed source73c6360efdcd5b25a7188b4ed3de292001128f1d, and that source parent is accepted main9383467fe19c621cc5c37ea54e1d5585b9ba49ee. Worktree clean. Integration changes exactly two wiring files and adds seven dossier files. The source candidate itself only added its three Lean files and dossier, so all old source executors, theorem domains and accepted main claims remain preserved.

AllGuarantees adds only the PDeposit1DsmCall public import. Trust adds only the DepositDsmCall regression import and four queries: public success, public rollback, lookup_origin and success_effects. No alternative provider, weakened conclusion, new premise or altered executable is introduced. The registered public theorem is the exact source-reviewed consumer: actual locator STATICCALL and scalar decoder produce the DSM used for authorization, then the entire326 physical-admission/module/metadata/withdrawal/beacon Effects on the same World and old Commitment. Whole-run success is its sole propositional premise; static observations remain separate from the old suffix journal. Earlier source boundaries and old guarantees remain intact.

Independently checked all50 original receipt hashes, all1314 imported/new source SHA256 entries and eleven current package pins. All three normal olean hashes match both the integration identities/receipt and source-reviewed artifacts. Inspected actual AllGuarantees and Trust setup importArts: both choose the source/public normal artifacts from this integrated worktree; Trust additionally selects the normal regression artifact. Selected bytes match the recorded three hashes. Thus this acceptance refers to the real registered provider and dependency closure, not a differently located similarly named cache.

All six integration receipt hashes match their files. The archived independent73c6 report is byte-for-byte identical to my external report. Scoped-axioms.json contains the same six reviewed closure names, only propext/Quot.sound, with Classical.choice additionally in success and positive-instance closures. The retained combined build finishes successfully at1728 jobs; its four new Trust query outputs match the actual wiring. Fresh root global-check stdout records exactly29 axioms, recomputed dependencies and re-evaluated native claims, distinguishing23 test/mutant native axioms, three production exceptions and foundations. The global environment is not mislabeled foundations-only.

Read the complete integration README, identities, receipt, scoped results and pertinent build/query output. Source review's full new-source/pinned-Solidity/IR review and fresh three-module kernel re-elaboration remain valid by the checked unchanged identities. The root's fresh global/scoped checks are retained evidence; I did not rerun unchanged compiler, Forge, kernel or global builds merely to duplicate them. The recomputed source manifest was retained rather than duplicated, and its actual entries were independently checked here.

The README correctly preserves constructor locator and context/deployment bindings, independent DSM/module memory cursors, omitted getDepositableEther/ALLOC and preservation, selected allocation/maxEB, external/no-code/precompile, cryptographic, linked-library, gas, natural-ledger and semantic error/log boundaries. It neither asserts full memory continuity nor treats arbitrary maxEB as32ETH. The same actual decoded DSM replaces the old context value; there is no supplied successful-stage, frame, funding or credential premise hiding a missing composition.

All nine unchanged normal-mode Solidity tests and their two1024-run fuzz tests/32short-length sweep remain reused source evidence. The additional -vvvv no-code diagnostic is explicitly still failed, while exact empty returndata has only the normal-mode assertion evidence. Full getter IR and observed nested Stop/caller-revert order support the modeled ordinary-no-code decoder path; attribution to instrumentation remains an inference, not an established Foundry diagnosis. No fresh integration Forge or full deposit-transaction execution is credited.

Integration documentation explicitly repairs the source receipt's incomplete whitespace enumeration: raw DsmHarness.ir EOF at5064 and original development.log trailing spaces at1308/1310/1312 remain preserved. Integration-only `git diff --check` passes. That does not erase the four inherited raw-output findings or become an unconditional whole-source diff-check PASS.

No source, log, receipt, olean or checkout mutation; no rebuild, merge, site edit, deployment, paid resource or external contact. Only this external report was written. **CLEAN for exactffc2 and the preserved bounded claim. STOP.**
