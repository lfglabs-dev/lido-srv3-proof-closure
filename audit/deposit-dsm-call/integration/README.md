# DSM call integration

Source `73c6360efdcd5b25a7188b4ed3de292001128f1d` extends integrated main `9383467fe19c621cc5c37ea54e1d5585b9ba49ee`. All three new Lean files and all 50 source-receipt hashes are unchanged. Existing executors, public domains, package pins and prior evidence are preserved. AllGuarantees imports the public consumer; Trust imports the regressions and adds four actual dependency queries.

The successful public consumer binds the actual locator STATICCALL response to canonical DSM decoding, the caller authorization and the complete accepted326 physical admission/module/metadata/withdrawal/beacon Effects, on the same worlds and suffix journal. The previously supplied DSM field is overwritten by the actual decoded response. The scalar allocator executes before the address length/canonicality checks. Failure restores the whole initial World, including late module and metadata effects. Static observations are retained separately from the suffix journal; no unified complete EVM trace is asserted.

Locator constructor identity, context/deployment bindings, omitted getDepositableEther/ALLOC execution and preservation, selected allocation/maxEB and independent phase-memory origins remain outside this increment. The DSM cursor and later module return buffer are independent inputs; no whole-memory continuity is claimed. Existing ordinary-no-code/precompile, external-call, cryptographic, natural-ledger, semantic error/log, linked-library and gas boundaries retain their prior scope. No new stage, frame, funding, credential or successful-call premise is introduced.

Normal combined AllGuarantees/Trust passes 1,728 jobs. Fresh global checking recomputes the actual dependency closures and re-evaluates native claims, retaining exactly the same 29 disclosed axioms; the global environment is not foundations-only. Six fresh scoped checks use only ordinary foundational axioms. Root independently verifies 1,314 imported/new source identities, eleven pins, all 50 source-receipt hashes and three normal olean hashes. The recomputed source manifest is byte-identical to the retained source manifest. The exact independent source review also freshly elaborates all three modules and reads full source/Solidity/IR composition.

Nine unchanged normal-mode Solidity tests, including two 1,024-run fuzz tests and all 32 short lengths, are reused by source/artifact identity. The actual inherited router helper fixture uses solc0.8.25 viaIR optimizer200 Cancun and does not execute the entire deposit transaction. No fresh integration Forge run is credited. The retained additional no-code -vvvv diagnostic **fails**, whereas the same unchanged-artifact -vv assertion passes. The full getter IR and observed call order support the modeled ordinary-no-code decoder path; exact empty returndata receives only the normal-mode test credit. Attribution to instrumentation remains an inference, not a proved Foundry diagnosis or all-modes byte-equivalence result.

The source receipt's historical diff_exception field lists only raw DsmHarness.ir EOF. Independent review and this integration explicitly correct that incomplete enumeration: unfiltered diff-check also flags original development.log lines 1308, 1310 and 1312, which preserve trailing spaces in earlier Lean error output. All four raw-output findings remain byte-preserved; the filtered check excludes precisely those two files and passes. No unconditional diff-check PASS is claimed.

The source author stopped before independent review. Root freezes this integration before its separate exact review. No site edit, deployment or publication is included; the validated site79ce snapshot does not yet credit this increment.
