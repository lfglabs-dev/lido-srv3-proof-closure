# ACCOUNT302 integration

Source candidate `965835661f8db5caba9263b0d62e94106b1f07cf` was frozen clean with its writer stopped before the retained independent full-source and pinned-Solidity review. The review covers all unaccepted ancestry from the common ancestor, not just the final increment.

Root combined it with integrated main `64f73f101187992db5b9b446852c3063a5a6d5b7` in union `32853f90f6dac15f1854bb43a1a61579314e41eb`. The seven ACCOUNT Lean files changed by the frozen candidate are unchanged in the union. The redundant accountAddress path dependency and manifest entry were removed: the existing root AccountAddressChecks library is the sole provider, now including both mint modules and their kernel regressions. All external dependency pins retain main's exact manifest. AllGuarantees already directly imports the registered PAccount1 module; Trust now imports the composed regression and queries the two public consumers and representative same-world/auth/rollback regressions.

The shared historical README now bounds the mint source extent to Accounting403–406, identifies the abstract account map, and limits its old unchecked-product warning to the historical helper. No theorem or public claim was weakened.

The source dossier's scoped checks are retained by exact artifact identity. Integration validates the full public facade and Trust environment separately; its receipt records commands and logs. Independent review of the exact frozen integration commit remains the separate merge gate. This dossier does not extend the source theorem to full handleOracleReport, physical account-map keys, locator CALL, deployed component separation or compiled Solidity/EVM equivalence.
