# Independent ACCOUNT302 exact integration review

**CLEAN at `86ee843a2c755bc044b6eed47fe332a4138d4d6e`** in `/tmp/lido-account302-integrated`. Exact frozen HEAD and clean checkout confirmed; root source writer stopped. This reviewer authored no ACCOUNT source or integration changes. The separate TOPUP no-code lot authored by this reviewer is not included here and supplies no evidence to this verdict.

## Source review and supported claim

Read the complete retained independent source/Solidity review of `965835661f8db5caba9263b0d62e94106b1f07cf` by `/root/topup_root_call_batch`, which covers the full unaccepted ACCOUNT ancestry. Read the source dossier and integration dossier, public success/failure declarations, actual ReportFeeMint input/world/execution/Success/MintBranch definitions, root-facing wrapper, and the StETH mint/storage/conversion implementation relevant to integration. The earlier full independent pinned-source review is reused by exact source identity; this integration review does not misrepresent a compilation check as a new whole-source equivalence proof.

The public consumer establishes the defined report/getter/checked-fee/mint composition. The same report-written router is consumed by the getter; the actual getter distribution enters checked arithmetic; its computed fee quantity enters mint with the independently supplied Accounting address as caller and recipient. Positive success derives authorization from execution, preserves the high128 external-share half and updates the low128 total-share word, updates the abstract account map pointwise, and computes event conversion on the post-mint state. Zero fee skips mint/auth/events. Failure restores the entire modeled composite world.

The scope is explicit and useful: supplied report/calculation snapshot, layout/registered IDs, locator result/call context and separate router/StETH components remain boundaries. Per-account shares remain an abstract map. Full handleOracleReport, intervening source operations, physical account-map keccak, actual locator CALL, deployed component separation, later _distributeFee/reportRewardsMinted and full compiled EVM equivalence are not claimed. Integration introduces no success-stage, amount, equality or frame premise. It does not credit the old callback projection as part of this composed executor.

The historical README correction is faithful: source extent is Accounting403–406, the account map is explicitly abstract, and the old unchecked-product limitation is restricted to ReportWriteFee.calculateProtocolFees rather than falsely applied to the new checked consumer. Broader historical OPEN text remains scoped historical context, not a new closure claim.

## Exact union and provider migration

Verified merge union `32853f90f6dac15f1854bb43a1a61579314e41eb` has source9658 and accepted main `64f73f101187992db5b9b446852c3063a5a6d5b7` as parents. Compared complete Git object/mode maps against their common ancestor. All non-overlapping main and candidate changes survive. The only overlapping file is lakefile.lean; read its complete relevant union/final differences. Main's TrioDepositCommitted declaration is preserved. Final lakefile differs from main only by adding the two mint modules and two test roots to the existing AccountAddressChecks library.

The final commit changes only four pre-existing paths after the union: lakefile, manifest, Trust and the historical README. The remaining additions are the six declared integration audit artifacts. All seven source-reviewed ACCOUNT Lean files are byte-identical to9658 and match their independently recorded SHA256 values.

The redundant accountAddress path requirement and manifest entry are removed. This is a single-provider correction, not a dependency upgrade: final lake-manifest.json is byte-for-byte identical to main64f, retaining all11 external pins. Existing AccountAddressChecks supplies ReportFeeMint, StETHMintShares, the report/getter substrate and regressions from `audit/trio/account-address`.

Inspected actual Lake setup import artifacts for root public PAccount1, Trust, ReportFeeMint and StETHMintShares. Their ACCOUNT imports resolve to this integration checkout's `.lake/build/lib/lean` artifacts from the single root provider, not a stale isolated-package provider. AllGuarantees already directly imports registered PAccount1. Trust newly imports Tests.Verity.ReportFeeMintTest and queries the two actual public consumers plus same-world/auth/rollback regressions. No axiom allowance or public guarantee identifier changes.

## Validation and artifact identities

Independently ran the read-only receipt identity checker against the exact pinned core: **27 artifact hashes, 8 pinned source/Git-body identities, 10 scoped axiom sets, 8 kernel regressions and 3 slot identities PASS**. This reuses the exact underlying validation; it is not a fresh Solidity test run or new keccak theorem.

Independently recomputed all five integration receipt hashes and all seven source identity hashes, also comparing the seven bodies to the exact9658 Git blobs. Read the retained complete **1685-job AllGuarantees/Trust PASS** and fresh **29-exact-axiom PASS**. The Trust gate explicitly recomputes environment dependencies and re-evaluates native-decision claims. Build output identifies the actual source provider and the new public/regression declarations. The unchanged source-scoped foundation-only checks and compiler-profile correspondence remain those independently reviewed at9658.

The source dossier's old duplicate-provider build failure is explicitly diagnostic. It is not credited as passing. The final integration log supplies the successful aggregate validation that the source review required. No redundant build, source rewrite or additional premise was used by this reviewer.

No concrete source-composition, provider, main-union, facade, Trust, identity or documentation blocker found for exact86ee. Root may integrate this proof candidate under the user's proof-repository authorization. Site updates must retain the bounded claim and receive their own exact publication review; this verdict does not authorize merging426, deployment or Lido communication. Only this local report was written; reviewer stopped.
