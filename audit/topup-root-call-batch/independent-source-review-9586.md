# Independent TOPUP actual-root/module composition review

**CLEAN for exact source candidate `958617bb6faed5f3de12563f028c2dbd41378b01`**, clean and writer-stopped in `/tmp/lido-topup-root-call-batch`, additive over `ea5774a82facf0d474e68058c4645ebf4380d403`. This source verdict is for the stated typed covered-phase claim. Final main-union/facade/Trust integration remains a separate exact-head gate. No source edits, builds, push or merge were performed by this reviewer.

## Scope and complete changed-source review

Read all four new Lean files completely: TopupGatewayRootCalls, TopupBatchRootCalls, PTopup2RootCalls and TopupBatchRootCallsRegression. The complete branch delta contains those four files plus the new audit dossier only. Earlier public consumers and executable definitions are unchanged. Read the dossier, validation scripts, axiom sets and retained checks. Followed the imported value path through the existing gateway row/configuration, SSZ root/leaf/branch, module call/decode, router continuation and arithmetic implementations; reused earlier independent source reviews where those exact sources are unchanged.

The public `PTopup2.actual_root_module_batch_bound` consumes success of the newly executed combined phase, with no arbitrary RootOracle and no supplied verifier, root, leaf, intermediate-stage, no-wrap, target or callee-frame proposition. Its `Success` relation is simultaneous: the same ordered rows produce authenticated field/limit pairs, the same pubkeys and limits become moduleInput, the actual module call returns raw data which decode to the bounded allocations, and the actual committed module/continuation world and attempts are retained. This closes a useful connection rather than adding an isolated theorem or putting an unused witness into a result.

## Pinned Solidity correspondence

Checked against core `17005714f151e5502c559932319a3f2f74ac2436` TopUpGateway.sol:163–175 and 204–232, CLValidatorVerifier.sol:44–106 and StakingRouter.sol:690–758, plus the already reviewed BLS/SSZ and physical storage/call helpers.

- Length admission is executed with the actual typed row/key/operator lengths and physically decoded uint64 max-validator count. `environment` replaces the supplied loop-only cfg with the actual gateway storage read. The count/target/minimum packing matches the first-word bits 0–63 and 160–223 and second-word bits 0–63. Typed rows intentionally align witness/index/pending arrays; this does not claim a raw calldata decoder.
- Pubkey width and strict index order precede activation; activation executes the uint256 divisor-zero guard and explicit uint64 epoch conversion. Activation precedes verifier execution. Each admitted row executes `SszRootCall.run` with **gateway as caller**, preserving internal inheritance semantics rather than pretending a separate verifier address called BEACON_ROOTS.
- The same row proof is consumed by the slot check and final validator branch. Slot checking precedes the actual fixed-address timestamp STATICCALL. The actual returned bytes pass through failure/empty/short-data guards and first-word decoding; wrapper index and actual leaf are evaluated before the proof fold. Authentication derives the independently constructed eight-field tree using the same interpreted SHA outputs. No globally successful SHA adapter or return-width premise is introduced. The helper's width guard and proof loop's success-only guard retain their existing distinction.
- Root execution is read-only under the existing static interpreter. Every row therefore uses the same world; snapshot configuration reads remain valid across these root calls. The timestamp/root request is executed for each row, not cached as a fabricated result. Successful traces are obtained from those executions. Failure before verification has no root attempt; verifier/headroom failures preserve the attempted prefix and skip the module.
- Headroom uses the very witness effective balance, exit/slash fields and caller-supplied pending word associated with that row. Its checked addition remains checked even inside the caller's unchecked block. Wei multiplication and accumulator modulo are the actual definitions; success plus physical uint64 target/count derives exactness, rather than assuming it. The copied pubkeys and produced limit list are exactly those passed to the existing module payload encoder.
- Module address is the existing physical packed mapping read; caller is router (`routerContext ctx`), value is zero, and payload encodes the actual target, pubkeys, key indices, operator IDs and generated limits. The actual raw reply is decoded by the existing byte decoder. Per-item alignment precedes limit indexing, excess length faults at the actual index, and the guarded unchecked sum is proved equal to the mathematical sum before applying the packed router cap. Shorter returned arrays retain the inherited successful domain.
- The router target is computed from an arbitrary preceding allocation value and the physical uint64 block cap, with min and gwei rounding. No bounded-allocation premise is added. The arbitrary module-returned world feeds the unchanged withdrawal/beacon continuation. `Success` retains the exact committed world and attempts of that execution. Root and module attempts remain distinct existing types, with an explicitly ordered sum view; no complete opcode/nested SHA trace is claimed.
- Failure uses the existing module root rollback wrapper; root-phase failures return the initial read-only world. `failure_restores` proves complete modeled entry-world restoration, including earlier provisional module/continuation effects. Diagnostic attempted calls remain observable in the declared model, rather than being confused with committed logs.

## Necessary boundaries and publication wording

This is a typed **covered value phase**, not a full TopUpGateway/StakingRouter entry simulation. The preceding role/pause/timing/root-age/locator/credential checks, router admission and allocation computation, actual gateway-to-router outer ABI CALL and final gateway history update are outside it. Expected credentials, immutable-like divisor/index configuration, module allocation and external interpretations are explicit inputs. The physically read configuration and block-cap facts are derived within that phase; no reachability claim about all deployment/configuration states is needed for its conditional bound.

The SHA interface remains the previously accepted digest/scratch interpretation, not compiled memory execution or cryptographic authentication of an arbitrary external reply. This result does not prove full declared ABI-list consumption, EIP-4788 storage authenticity, canonical consensus membership, fresh roots, full gas/compiler semantics or whole-batch ETH conservation. These exclusions are not missing links inside the revised claim.

The inherited module CALL primitive rejects a no-code target before making an attempt, while the inspected typed Solidity path rejects its empty return during decoding. Success-domain transport is unaffected, but **failure/no-code attempt traces must not be advertised as Solidity-identical**. The existing TopupModuleCall source documents this distinction. The new result truthfully describes modeled diagnostic phases and modeled rollback; a publication statement should retain that qualification.

The symbol `Authenticated` denotes the proved relation to the actual interpreted reply and the independent tree, not unconditional consensus authentication. README makes that scope explicit. No semantic or proof defect requiring a candidate correction was found.

## Relevant checks independently verified

- Recomputed all **14 packet hashes** in the exact candidate receipt.
- Recomputed all **1282 source-body hashes**, compared every body to the candidate's Git object or its exact package revision, and checked all **11 package HEAD pins**. The full branch delta confirms inherited local source bodies remain unchanged from the accepted base.
- Read the retained **1299-job target build PASS**. All new executable regressions use `decide +kernel`, not FFI/native receipt axioms. There are **11 kernel regressions plus a public consumer instantiation**. The partial SHA interpreter admits the full modeled batch while failing unused inputs; the two-row case demands exact 32/31 Gwei-generated limits in the actual module calldata and checks root/root/module phase order. Wrong caller, root rejection/forbidden state change, early activation/slot failure, duplicate indices, pending overflow after verification and above-cap rollback are exercised at their stated scopes.
- Read the fresh recomputed **18 scoped axiom sets** and validator procedure; all contain only `propext`, `Classical.choice`, `Quot.sound`. No new axiom, admission placeholder or hidden runtime test axiom is introduced in the four changed sources. These sets are retained exact validated evidence; this review did not rerun the environment query unnecessarily.
- Independently recomputed all **45 inherited Solidity source/fixture identities** and all three inherited receipt hashes, including pinned core Git-body comparisons. The dossier correctly credits identity reuse, not a fresh Solidity execution or a compiled cross-contract composition proof.

The candidate is a useful strict increment over the published TOPUP cap floor: the actual per-row root calls and independent validator tree are now consumed by the same generated-array/module-reply cap execution. Source review is CLEAN. Root should finish the main union, facade/Trust checks and independent exact integration confirmation before integrating this proof increment or publishing it as integrated evidence. Only this local review artifact was written; reviewer stopped.
