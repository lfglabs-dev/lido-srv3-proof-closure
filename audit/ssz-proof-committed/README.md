# Actual successful proof loop and raw consumer

This increment connects the unchanged `SszWitnessAbi.run` to an independent Merkle branch from its existing typed validator leaf to its original calldata root. It composes accepted PR296's actual key/SHA/leaf result with the successful proof verifier on that same returned state. The resulting branch is over the words actually read by the source cursor, not a payload added to an otherwise unrelated result.

`prepared_memory` and `prepared_read` prove the two scratch stores for arbitrary calldata and uint256 offsets; the sibling is the actual `calldataload` result. `step_call_digest` derives depth and precompile-gas admission from the actual nonzero CALL flag. `step_success_shape` and `step_success_effects` invert the real source guard and mload result, transport the independent one-item fold, and derive caller-environment and memory-extent preservation from the actual transitions. No per-call gas, depth, supplied scratch layout, calldata-size or independently supplied successful hash is assumed.

`proofWords` is a calldata-only word view with the source's uint256 increment and unsigned continuation. `loop_success_fold` relates successful iteration to the existing independent `sourceFold`; `verify_success_branch` additionally consumes the real final index and root checks. `verify_success_depth` derives a nonempty branch with exactly the decoded index's depth, at most 247 consumed words. `run_success_branch` consumes the actual raw ABI/SHA/typed-leaf/verifier execution, retaining original fields, credentials, root, raw GIndex and signed slices. Final environment and scratch extent are derived; no whole-World conservation is claimed.

## Remaining assumptions and required work

The initial `activeWords < 2^251` invariant remains INTERNAL required work. Later memory extents are derived by the new transition/loop proofs. The original actual-initialization and preceding compiled-transition obligation is not replaced by a new premise or declared external.

`ShaWidth` reuses the opaque SHA output-width condition already explicit in the accepted proof-loop/typed bridges. It is required here because pinned SSZ checks only the CALL flag, whereas BLS separately checks returndata size. The code does not import BLS's size guard into SSZ. Root inspected the pinned EvmYul `EvmYul/FFI/ffi.lean` opaque declaration and `EvmYul/FFI/ffi.c` lines 8 and 15–21: the C wrapper specifies 32 output bytes and allocates/pushes that fixed output. This is source inspection supporting the external FFI specification boundary, not a formal C refinement or execution of that native binary. SHA implementation and cryptographic correctness remain external trust, with inherited finite Solidity SHA tests retained by exact source/evidence identity. No fresh Forge or C execution is claimed.

The unsigned cursor may terminate before the supplied round bound on arbitrary wrapping offsets. `proofWords_length_le` proves the safe upper bound; the regressions include a four-round cursor that consumes one word and a two-round cursor that wraps to calldata offset zero. These are primitive cursor fixtures, not compiled-ABI admission or reachable Solidity counterexamples. Equality between the consumed words and the ABI-declared complete proof list must still be derived for the actual admitted/reachable entry. That obligation is not closed by renaming the consumed list.

Complete raw entry/slot/proposer/root/GIndex prefix and EIP-4788 lookup, compiler allocations/free-pointer checks/spills, surrounding opcode gas, exact error ABI and compiled World/rollback remain required internal work. The inherited Theta created-accounts reset still prevents a full World frame. Authentic consensus and deployed configuration remain external context. All eight complete guarantees remain OPEN; ALLOC-1, ALLOC-2 and RESERVE-1 retain their accepted scope.

## Validation

Ten public source theorems compile, with no private proof helper. Three named kernel-checked cursor regressions and five kernel examples cover ordinary cursors, two wrapping behaviors, the consumed-count distinction, the actual zero-count verifier rejection, the actual extra-item guard and zero interpreter fuel. These tests do not evaluate opaque SHA. Fourteen axiom queries cover thirteen distinct declarations; only propext, Classical.choice and Quot.sound occur. Source build12 actively elaborated the final source in 1.7 seconds (1136 jobs); target build14 actively checked the final tests in 1.2 seconds (1137 jobs). Earlier failed attempts are retained as diagnostics without proof credit.

The accepted PR296 sources and dependencies are unchanged at integrated main8125d79f (PR298 changes only guards/metadata). Their validations and the older Solidity/compiler evidence are reused by identity. The UX2 source-tree index is regenerated for the two new Lean files and checked, so this increment does not reintroduce stale export metadata. Independent exact-source review is required before integration or site credit.
