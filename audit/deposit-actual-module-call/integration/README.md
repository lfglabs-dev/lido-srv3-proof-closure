# Actual DEPOSIT module CALL integration

The independently reviewed source80f391ee is united with accepted main3b818d14 (CONSOL320, ACCOUNT319 and TOPUP318). All four new source bodies and their115 imported local source identities are unchanged. Root registers ModuleCall, ModulePhysicalMetadata and its regression module in the existing single TrioDepositCommitted provider; AllGuarantees imports PDeposit1ModuleCalls and Trust imports its tests plus four active public/source queries. No previous executor, theorem or dependency pin is changed.

The normal combined AllGuarantees/Trust build passes1,704 jobs. The fresh global gate reports29 existing disclosed axioms, with dependencies recomputed and native claims re-evaluated; this is not globally foundations-only. The complete26 newly named theorem sets (15 production,11 regression) were also freshly recomputed on the actual registered test environment: foundations only. Their exact sets and four current olean hashes are retained. Normal-Lake artifacts differ from the earlier manual candidate olean bytes; no byte-identical artifact reuse is claimed for those four files. The actual source bodies, declarations and ordinary kernel checks agree.

Source review independently checked the full actual module CALL, scalar allocator/signed and unsigned decoder guards, checked count/value preparation, physical metadata on the module-returned world, and real withdrawal/beacon suffix. There is one consumed execution and no supplied stage success, count bound or callee frame. The public result derives a Commitment, exact callee reply/world, copied-range bounds, metadata fields and zero or positive suffix effects. Root failure restores the whole incoming world including module and earlier beacon effects; diagnostic attempts remain visible.

The source packet includes23 artifact hashes,115 local source/cache identities,11 pins,six pinned Solidity identities and seven compiler inputs. Four independent normal-kernel rechecks also reproduce the two runtime checks;26 named theorems and16 unnamed kernel examples are distinct from those runtime checks. A fresh independent Lean export reproduces64 vectors. Eight Forge tests with256 seeded fuzz runs and64 vector comparisons are retained from the reviewed source candidate, using matching solc0.8.25/optimizer200/viaIR/Cancun inputs and IR cache. Root did not rerun or credit a new Solidity execution.

The added callsite begins with a typed selected allocation and a supplied uint256 CALL-buffer memory cursor. Earlier allocation/admission/config/credentials provenance and cursor origin are not established; neither are memory-copy opcodes, full LOG/revert ABI or deployed entrypoint correspondence. The scalar allocation and bounds checks are executed and their successful small-range bound is derived, not assumed under gas/compiler labels. Arbitrary module effects are propagated without an assumed ledger/frame. The separate accepted locator-bound metadata/withdrawal ledger keeps its existing scope. No accepted ALLOC/RESERVE guarantee is changed.

The original source dossier is a historical candidate record. This directory records actual integration validation. A separate independent frozen integration review is required before merge.
