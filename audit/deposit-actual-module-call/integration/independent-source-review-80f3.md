# Independent DEPOSIT actual module CALL source review

Verdict: **CLEAN** for exact frozen commit `80f391eedea92824327edefffe3f75635e1d271e` in `/tmp/lido-deposit-actual-module-call`. The writer was explicitly stopped. I authored none of this DEPOSIT increment. The checkout remained clean and exact through completion; this review modified no candidate file.

## Reviewed scope and preservation

I read the four new Lean sources completely: ModuleCall, ModulePhysicalMetadata, PDeposit1ModuleCalls and ModulePhysicalMetadataTest. I traced the actual imported low-level CALL, module-address/storage mapping, physical metadata, LiveBeacon suffix and its necessary-success commitment, underlying Live world/bind/rollback/withdrawal execution, and concrete beacon dispatch. The earlier prepared-prefix executors remain distinct from the new actual-call variant.

Every existing tracked path from accepted base `f8ea5f9d3cffc165f5d84dd96444836c806827eb` is unchanged; the candidate adds only the four Lean files and its audit dossier. `git diff --check` passes. All 115 recorded local source identities and corresponding recorded cached olean identities match; every old source in that closure equals its base bytes. All 11 actual package heads match the packet. These are scoped imported-source/cache identities, not a clean dependency rebuild or a full-root Trust result.

## Consumed execution and public theorem

The physical module address is selected from the low 160 bits of the actual config word; the module mapping consumes the two encoded words through the existing explicit Keccak parameter. The cap comes from bits 128–191 of the deposits word at the wrapped mapping slot plus one. After the contextual authorization/active/credentials checks, division by zero is rejected, the target is `min(cap, selected/maxEB)`, and a zero target rejects before any module attempt. The address and target used by the CALL are captured from the entry world and survive arbitrary module changes.

The real request uses the router as caller, the selected module address, zero value, selector `0xbee41b58`, the computed target, and the actual depositCalldata bytes with their dynamic head/length/padding. The low-level transport derives either ordinary-no-code empty acceptance or the arbitrary coded interpreter's actual reply and world. Empty acceptance fails in the decoder. Precompile dispatch remains explicitly outside that inherited ordinary-no-code arm.

The decoder consumes that exact raw reply. Preparation checks pubkey alignment, the returned count against the actual target, and checked count-times-maxEB, in source order. It does not invent an early signature guard. The resulting prepared object carries the returned pubkeys/signatures and actual count/value into metadata and the existing live suffix.

The arbitrary module-returned world is passed verbatim to PhysicalMetadata.update. Two physical read/modify/write operations set low uint64 timestamp and the next uint64 block number while preserving the returned world's upper 128 bits. The semantic router event is appended before either the zero-key return or withdrawal. It uses the actual checked deposit value. The positive suffix executes the existing Lido withdrawal, then batch-length checks, the concrete beacon CALL loop and final live router-balance assertion. Its successful subexecutions are derived, not supplied as theorem premises.

The public `actual_module_call_metadata_suffix` consumes one root success and returns a Commitment for exactly that run, including call/decode/prepare/suffix equations, actual callee origin, copied-range bounds, metadata fields and chronological journal. The zero branch commits the module effects plus metadata/event and has no suffix calls. The positive branch contains the existing same-world SuffixCommitment. No framing, stage success, funding, cap, SHA-totality or synthetic reply premise is added. Root failure restores the whole entry World while preserving observed module/nested/suffix attempts, including late beacon failures.

## Pinned Solidity and scalar decoder correspondence

I checked the complete relevant source bodies at core `17005714f151e5502c559932319a3f2f74ac2436`: StakingRouter.deposit and metadata helper, SRLib metadata update, SRStorage mapping/root, SRTypes packed layouts, IStakingModule.obtainDepositData and BeaconChainDepositor's positive helper. I also followed the unchanged Lido withdrawal and beacon-dispatch boundaries. The six retained Solidity source identities match both the working files and pinned Git blobs.

I read the entire 1,058-line retained harness IR. Its no-code behavior, selector/payload, call/rejection forwarding, copied returndata allocation, dual-byte decoder and physical fields agree with the new model's declared projection. The repository compiler profile is solc 0.8.25, optimizer 200, viaIR true, Cancun; it matches harness metadata and foundry.toml. The complete IR and metadata also equal the actual existing Forge artifact, and all seven compiler inputs match both recorded SHA-256 and freshly computed Keccak identities.

The scalar decoder order is exact: initial raw-return allocation; signed head-size check; first offset uint64 check; signed absolute length-word bound; first length uint64/allocator checks; unsigned payload extent; then the second offset and bytes. Round-up and pointer addition use uint256 wrapping. The allocator checks both the uint64 ceiling and wrap below the old pointer. Near-modulus sizes that round to zero are still rejected by the signed head guard. Small copied-range bounds are proved from the executed guards, with no new raw-size admission premise. Aliased heads, unaligned/reversed tails, omitted final padding and trailing bytes are retained.

Input.returnBuffer is expressly the supplied memory cursor at the CALL-buffer phase. Its provenance from earlier execution is not proved, and memory-copy correctness, opcode gas/expansion and the later suffix's memory abstraction remain boundaries. Scalar projection tests do not claim enormous concrete EVM allocations. The harness is a bounded source-shaped callsite, not compiled StakingRouter entrypoint/deployed-bytecode correspondence. Earlier getDepositableEther/ALLOC, registration/admission/config/credentials provenance and immutable identities remain outside this public claim. No global conservation across the arbitrary module, full log/revert ABI theorem, or EVM transaction-journal proof is inferred.

## Independent checks and retained evidence

All 23 packet artifact hashes pass the inspected read-only checker. I independently rechecked all four Lean files with ordinary `lake env lean` and no output artifact, obtaining byte-identical retained outputs. This also reran the two explicit runtime checks. There are 11 named regression theorems and 16 unnamed kernel examples, including actual two-key withdrawal/beacon success reaching the public consumer, changed-world propagation, cached address/cap/payload, zero-key commit, malformed/over-target/alignment rejection, noncanonical decoding, allocator/signed boundaries and no-code/bubbled failures. The late runtime check observes one committed beacon iteration in the unrolled program before the second failure, then full root restoration with both attempted beacon calls retained.

I independently recomputed the 16 printed dependency sets from the built test environment and matched their logs. I then expanded coverage to all 26 new named theorems: 15 production and 11 regression. Every set contains only `propext`, `Quot.sound` and/or `Classical.choice`. There is no new axiom, sorry, native reduction axiom or kernel-skipping option.

A fresh read-only Lean export reproduces all 64 retained vectors. Independent canonical encoding checks pass, and deterministic reconstruction matches every byte of the generated Solidity vector library. The retained Forge receipt records eight passing tests, including 256 seeded fuzz runs and all 64 Lean-vector comparisons. Its hand-built malformed/noncanonical and exact bubble tests are meaningful independent checks; scalar-memory tests are clearly labeled projections. I reused that expensive Solidity validation after verifying cache/source/compiler identities and did not rerun or credit a fresh Solidity test/build.

No full AllGuarantees/Trust build is claimed for this source-only candidate. Root must register the new modules in the existing DEPOSIT provider, import the public facade/regressions and perform its usual integration validation. No source finding blocks that integration. Review stopped after writing this external report.
