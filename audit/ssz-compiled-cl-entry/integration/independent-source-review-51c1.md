# Independent exact SSZ compiled CL-entry source review

Verdict: **CLEAN for the selected compiled CL-entry execution and its explicit boundaries** at `51c118b36cb28f3bf5e2ade031b5c55428e2707e`, tree `eddb186d05a144be0e6b7c12c7494574b16f2b53`. No blocking source/proof/composition discrepancy found. This is not a universal deployed-bytecode, EIP-4788 authenticity, complete gas, complete error-ABI or failure-world rollback theorem. STOP.

Reviewed `/tmp/lido-ssz-compiled-entry-repair` read-only, with exact parent9723377c3a936c522ec10e88dadcab7e1a7b25b0 and accepted base2c2c72a91cd68a43de0777912772d12cc48a285d. HEAD/tree identities and clean worktree match the frozen request. Every change versus accepted base is an addition; no prior executor, public domain, package source, AllGuarantees/Trust wiring or site is changed. This reviewer authored none of the seven new Lean files.

Read all **2,611 lines of the seven new Lean files**, including proofs and regressions, and the necessary executed/transitive definitions: fresh/prologue/store/load/allocate, typed root request/low-level static interpretation, ABI tails/field guards, pair/pubkey primitives, typed leaf connection, proof cursor/loop and necessary-success branch transport. Checked the inherited ShaWidth definition and its use by PSsz1ActualMemory. Read pinned CLValidatorVerifier and GIndex source, witness structures and the complete relevant BLS sha256Pair/pubkeyRoot and SSZ verifyProof/endian paths. Read the full retained567-line IR, including constructor, selected entry, helper functions and other dispatcher cases; a comment-stripped read preserved all executable IR. This is not a claim to manually reread every body in the entire1,215-source imported closure.

## One actual execution, with produced operands

The new `run fuel cfg external world context` is one execution chain on the existing EvmYul EVM.State. BeforeRoot calls the actual fresh-entry prologue, ABI guards, proof-tail decoder, slot/proposer uint64 reads and pair SHA; the pair output is compared with the actual indexed calldata sibling. RootCall builds the timestamp ABI buffer with machine stores, reads its length and input back from that memory, performs the single typed readonly BEACON_ROOTS call, and places its response into that machine's returndata. AfterRoot copies/allocates/decodes those bytes, computes the generalized index, allocates and hashes the validator's eight fields, and calls the existing proof loop with those exact produced index/leaf/root values.

The public branch theorem assumes only this whole-run success equation plus **the unchanged inherited ShaWidth**. No root equality, index equality, successful intermediate stage, initial frame, calldata layout, reply-size or successful hash prefix is supplied. Helper lemmas do have local Stored/bound/success inputs, but the public proof derives all of them from the preceding executed phases; none escapes into the public premise list.

The proof identifies the two decoded slot reads and the two decoded proof tails through preservation of the same execution environment, rather than substituting unrelated inputs. The root is the first word actually copied and loaded from this call's returned bytes. The index is the typed sourceWrapper result of the actual slot and validator-index word. The leaf is the independent typed validator tree of the consumed pubkey octets, decoded fields and expected-credential word164. The branch's siblings are exactly the calldata words consumed by the unsigned cursor loop, with its wrapping/continuation behavior retained. The tree theorem transports this same branch through the existing digest/word bijection; it does not rerun the external call or assume a second proof execution.

## Source/IR correspondence matrix

| Phase | Executed behavior and independent review conclusion |
|---|---|
| Entry and ABI | Existing fresh state agrees with the accepted engine entry initialization; arbitrary incoming memory is discarded by that entry operation, not constrained by a premise. Prologue writes free pointer128 at64. Selected selector0x2e77b4ba, nonpayable guard, six-word head, redundant96-byte head guard, witness offset word100 uint64 guard and eight-word extent match IR. All ABI errors here share the modeled ABI error; no byte-exact error claim is inferred. |
| Slot check | Actual proof tail uses the signed relative/extent guards without invented alignment/canonical-layout premises. Slot word36 and proposer68 are checked uint64 and little-endian encoded before the actual pair SHA. Only after SHA, length-2 is checked for underflow0x11, the indexed access for bounds0x32, and the loaded sibling compared for InvalidSlot. Thus malformed slot proof precedes root lookup, and SHA failure is not reordered behind subtraction. |
| Timestamp root request | Timestamp word4 is guarded uint64 after slot verification. Stores at160 and128 construct the32-byte ABI payload with its length word; finalize advances128→192. The actual memory-read bytes, not a parallel reported payload, feed STATICCALL to0x000F3df6D732807Ef1319fB7B8bB8522d0Beac02, caller=machine code owner, value0, no selector. The call has no EXTCODESIZE precheck. The existing ordinary-no-code interpretation returns success-empty, subsequently rejected. |
| Reply handling | Machine returndata is copied by the existing memory operation. Empty returndata selects pointer96; its zero length is derived from fresh-memory padding, not assumed. Nonempty return length gets the uint64 check, rounded byte allocation plus length word, wrapping-pointer/uint64 guards, free-pointer store, length store and full returndata copy. Copy/allocation occurs before RootNotFound. `!success` short-circuits the length load; successful zero length is RootNotFound; nonempty short reply hits signed ABI decoder rejection; successful ≥32-byte replies load the first word and permit trailing bytes. |
| Generalized index | Second slot read, pivot fork selection, packed index/power masks, width=1<<power, zero-width guard, checked remainder+offset, range guard, checked index+offset and uint248 pack follow actual IR. Concat executes both literal Solady fls routines, checked depth arithmetic, depth≤248 and final pack. No overflow is replaced by unbounded arithmetic without proof of its guarded range. |
| Validator memory and leaf | Leaf array is allocated at the reply-dependent free pointer, not fixed128. Pubkey tail and exact48-byte guard precede scratch zero/copy/SHA, then its digest is stored at that pointer. Expected credentials come from word164. Effective balance is read before slashed, then eligibility, activation, exit and withdrawable epochs; uint64 and bool canonical guards and offsets match IR. Seven actual pair calls consume loaded cells in the8→4→2→1 order with intermediate array allocations. |
| Proof loop | The produced packed index is shifted right8 by the actual verifier; the first copied reply word remains the root operand. It rejects empty proof, shifts index before each pair and checks extra item, loads each real calldata sibling, uses actual SHA output, advances the wrapping cursor and follows unsigned continuation. Final index1 and leaf=root checks produce the independent nonempty Branch conclusion. No claim equates these consumed words with the entire ABI-declared list in every noncanonical/wrapping case. |

## Derived memory facts and arithmetic

BeforeRoot derives memory size96, activeWords3 and Stored free128 after the slot SHA, whose scratch writes remain in0..63. RootCall's actual store at160 pads the96..127 gap with zeros, then the length and free-pointer stores preserve it. The resulting payload reads exactly the zero-extended timestamp word; free192 and Stored zero at96 are outputs of those operations.

Reply success derives size≥32, size≤2^64−1, and `F = 192 + ceil(size/32)*32 + 32 ≤ 2^64−1`. Returndatacopy is from the machine's actual response buffer into224; its first word is proved identical to typed firstWord of the same bytes. The reply proof derives freeF, bounded active words, environment preservation and F≤memory.size+31. The subsequent allocator supplies its own F+256 bound. These relations discharge the generic frame lemmas' gap and pointer requirements internally.

SszCompiledFrame generalizes existing concrete memory operations rather than introducing a replacement memory interpreter. Preserved-cell proofs reason about actual byte-array writes and loaded values, disjoint allocated locations and scratch writes. Their bounded padding requirements are justified by the advancing allocations and actual prior memory extent. Pair stores preserve free-pointer and unconsumed cells; the later128- and64-byte allocations get their bounds from their own successful guards. No new global memory-shape or alias assumption is present. The retained fixed-frame PSsz1ActualMemory result remains unchanged.

The literal fls magic/table constants match GIndex.sol and IR. The kernel finite table lemma covers every nonzero byte; the mathematical proof connects all256-bit inputs to log2, with zero sentinel256. Typed Configuration pack words are the explicit constructor boundary; pinned harness words are2819 and0x96000000000028. For the aligned pinned base, maximal offset reaches IndexOutOfRange without first-add overflow; a separate nonaligned-base regression correctly reaches Panic11. The correction fixes a false expected outcome and does not change or weaken the executor.

## Trust, identities and independent checks

1. Inspected validate.py before running. In this frozen successor its default invocation already compares archived JSON; only --write mutates receipts. Ran the default with Python bytecode writing disabled. It freshly passes **1,215 actual imported/new source identities, eleven exact package pins and31 named ordinary axiom scopes**, checks selected local normal artifact bytes, all old local sources against accepted base, seven normal module setups/traces and seven pinned compiler inputs. No receipt was rewritten.
2. Independently checked all45 original packet SHA256 entries. Normal artifact/setup/trace identities are included in the validated record; no plugin, synthetic trace, skipKernelTC or nondefault setup option is accepted. The complete public file and Fls/GIndex/Frame bodies are unchanged from972; only Reply/ClEntry repairs and regression correction affect the main seven-file successor.
3. Fresh no-output `lake env lean` re-elaboration of **all seven modules** exits0, including both public consumers and14 named regressions. This is fresh ordinary checking against validated caches, not a fresh entire-dependency rebuild. The six local phases and public proofs contain no sorry/admit/native_decide/project axiom/unsafe substitution. maxRecDepth4096 is used for nested-state proof normalization; no maxHeartbeats or kernel-check bypass is introduced.
4. The inherited `SszProofCommitted.ShaWidth` is exactly `∀ left right : UInt256, shaOutput(fixedBE32 left ++ fixedBE32 right).size=32`, already present in PSsz1ActualMemory. It is not strengthened to a universal successful call, injected expected digest, independent stage match or a new resource premise. The31 fresh scopes contain only propext/Classical.choice/Quot.sound. This local statement is not a claim that future global Trust is foundations-only.
5. The retained warm normal build reports1,232 successful jobs, including replayed final artifacts. Historical Reply/parser failures and error-recovery sorryAx outputs remain rejected development history; they are not used as successful kernel receipts. Current fresh re-elaboration and identity checks are the acceptance evidence.
6. The seven materialized compiler bodies, original URL input set/settings, SHA256/Keccak identities and actual pinned Git bodies match. The root's retained **Mac solc0.8.25** output reproduces the entire original IR plus documented newline, SHA256 `aba7b84e915d3279916e7d18150f047e553c8f712b1e30ea96235e11998679e3`. Profile is viaIR/optimizer200/Cancun/ipfs metadata. The earlier Linux binary was not rerun; neither this review nor a byte-identical IR receipt turns the selected semantic transcription into a proved whole-bytecode compiler equivalence.
7. Read the complete independent hashlib/ABI generator, diagnostic consumer and temporary-FFI runner/log. Recomputed the vector in memory with its writes replaced by equality assertions: exact2,180-byte calldata,50-sibling path, typed index, leaf and returned root match retained bytes/JSON. The real slot/proposer sibling is at position48; proof fold reaches index1. No Lean-computed digest was reused to manufacture this reference vector.
8. The four retained whole-entry FFI diagnostics exercise successful independent vector consumption, changed root, changed proof byte and timestamp mismatch against an exact-request finite external. Initial memory is deliberately nonempty. Runtime FFI input hashes and repositories are checked; the runner builds a temporary native library without modifying package sources. These are IO diagnostics, not whole-success kernel instances or theorem axioms. I reused their unchanged passing execution receipt, rather than claiming a fresh diagnostic run. No new Forge execution is credited; the unchanged nine-test inherited harness receipt remains historical fixture evidence only.
9. Unrestricted diff-check reports exactly the four disclosed raw-output files: IR EOF and cl-1/cl-2/reply-1 development-log whitespace. A check excluding precisely those four paths passes. The raw bytes are retained; no unconditional whole-diff formatting PASS is asserted.

## Scope that must remain attached

The selected entry uses existing EVM.State memory/SHA primitives and the accepted typed StaticCall.External over Live.World for BEACON_ROOTS. The timestamp payload is tied to the machine, but deployed EIP-4788 storage/history behavior, root freshness and cross-model deployment/account identities remain interpretations. No stronger hidden relation between a supplied root/index and the machine is assumed, because root and index are actually produced. Configuration represents constructor words; general configuration theorems do not claim the fixed harness was deployed with every such configuration.

The theorem is necessary success, not total termination or completeness for arbitrary fuel. Outer opcode gas, full compiled-bytecode correspondence, complete custom-error/revert byte encodings and a new failure-world rollback result remain outside. The SHA output-width condition is inherited and explicit. Arbitrary calldata is handled with the source's signed guards and wrapping cursors; the branch states the consumed-word list, not an unproved whole-declared-list identity. These limits are already stated in source/public/README/receipt and are not newly added excuses for a missing operand or memory join.

No candidate edits, source/cache output/receipt changes, full rebuild, paid service, merge, site publication or deployment was performed. Only this external report was written. **CLEAN for exact51c1 and its stated bounded compiled-entry/public-branch scope. STOP.**
