# Selected compiled CL-entry integration

Reviewed source `51c118b36cb28f3bf5e2ade031b5c55428e2707e`, successor of preserved initial `9723377c3a936c522ec10e88dadcab7e1a7b25b0`, is united with main `f193ebf96beef3f5e1e1568fc9f2b906c0192299` through ADDRESS329 at `6dc13bc0c7a63c5dec158c93f4f1de2b0e331758`. The seven new Lean files and all 45 source-receipt hashes are unchanged. Prior proofs, public domains and all intervening ACCOUNT, DEPOSIT, TOPUP, ADDRESS and SSZ evidence survive unchanged. AllGuarantees imports the new public facade; Trust imports its regressions and adds six dependency queries.

One selected compiled semantic execution now produces every operand of its final branch: fresh frame, checked slot/proposer sibling, timestamp bytes actually stored then read from machine memory for STATICCALL, actual returndata allocation/copy/first-word decoding, guarded constructor-configured generalized index, reply-dependent validator leaf allocation and proof-loop consumption of those produced values. The public tree form identifies the independent eight-field container of the same witness/credential octets. Helper frame, pointer, byte-width and stage premises are discharged from actual prior transitions; no initial-memory shape, successful prefix or root/index equality premise is supplied.

The public success implication retains exactly the inherited SszProofCommitted.ShaWidth used by the old compiled-memory theorem. The older typed partial-SHA result is unchanged and receives no new universal-width premise. StaticCall.External over Live.World, constructor Configuration words and the opaque SHA engine remain explicit interpretations. EIP-4788 storage/history authenticity, root freshness, deployed contract/context identity, full declared ABI proof-list equality, complete error ABI, gas/termination, general compiled-bytecode correspondence and a new failure-world rollback result remain outside. The result concerns the selected harness reaching the unmodified pinned CL body, not universal deployed-bytecode equivalence.

Normal combined AllGuarantees/Trust passes 1,739 jobs. Root freshly recomputes global dependencies and re-evaluates native claims, retaining exactly the same 29 disclosed axioms; global Trust is not foundations-only. Thirty-one fresh normal scoped queries use only ordinary foundations. The validator rechecks 1,215 imported/new source identities, eleven pins, seven normal setups/traces/artifacts and seven pinned compiler inputs. Source/scoped/compiler/runtime manifests are byte-identical to the source records; the integration records actual seven artifact hashes separately, allowing legitimate setup-path differences. All 45 source-receipt hashes match.

Independent exact source review read every new Lean body and the complete 567-line IR, followed all necessary consumed definitions and pinned Solidity paths, freshly re-elaborated all seven modules and checked the entire request/reply/index/leaf/branch composition. The reviewed 14 kernel regressions and four whole-entry native diagnostics are retained by exact identities. The independent 2,180-byte calldata/50-sibling vector uses hashlib rather than a Lean-derived reference; corruption tests change root, a proof byte and timestamp. Native whole successes are diagnostics, not kernel-evaluated instances. The original noncompilable972 and failed repair logs remain rejected development history.

The retained root replay on existing Mac solc0.8.25 reproduces the complete IR plus documented newline, SHA256 aba7b84e915d3279916e7d18150f047e553c8f712b1e30ea96235e11998679e3, from seven exact pinned inputs using viaIR/optimizer200/Cancun. This replay was performed once and then reused by identity; the older Linux binary was not rerun. No new integration compiler, Forge or FFI execution is credited. The nine old root-call Solidity cases remain unchanged historical fixture evidence rather than a fresh compiled-composition test.

Four source raw-output whitespace exceptions are preserved: the compiler IR EOF and development cl-1/cl-2/reply-1 logs. Main-to-integration diff-check with precisely those four files excluded passes. Earlier integrated raw-output exceptions are not erased. No unconditional whole-source formatting PASS is claimed.

The remote source writer stopped before explicit transfer; the native repair writer stopped before full independent source review. Root freezes this integration before a separate exact review. No site edit, deployment or external publication is included. Site79ce remains the separately validated earlier publication snapshot until its next reviewed update.
