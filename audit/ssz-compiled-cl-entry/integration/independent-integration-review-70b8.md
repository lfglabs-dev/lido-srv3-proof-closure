# Independent exact SSZ compiled CL-entry integration review

Verdict: **CLEAN for the selected compiled CL-entry composition and its explicit boundaries**, exact commit `70b8e9a702029bad48648e2678934416f57b97b8`, tree `4adcf742a71c04659c6f7400790d0709381069ec`. No integration blocker found. STOP.

Reviewed `/tmp/lido-ssz-compiled-entry-integrated` read-only while root was stopped. HEAD is the requested commit and worktree is clean. This reviewer authored none of the SSZ sources or integration changes.

## Exact union and retained review

Parent `6dc13bc0c7a63c5dec158c93f4f1de2b0e331758` has exactly source `51c118b36cb28f3bf5e2ade031b5c55428e2707e` and accepted main through ADDRESS329 `f193ebf96beef3f5e1e1568fc9f2b906c0192299` as parents. Independently compared recursive Git tree entries, including modes and object identities: the union is exactly all main objects plus source-only additions. Every main object is preserved. There is no old-source replacement, dropped intervening proof, changed public domain or missing evidence.

The integration changes exactly two old files: one public import in AllGuarantees, one regression import plus six axiom queries in Trust. It adds exactly eight files under the integration dossier. All seven SSZ Lean bodies and the entire original SSZ dossier remain object-identical to independently reviewed51c1. The archived source review is byte-for-byte my external review, SHA256 `7cf93d7fafa08c71429f139c673e14b2b00c877e791d60f216ae4ab3583fffa4`. I read that full report and the complete integration README, receipts, identities and scope records. The earlier full-source/567-line-IR/Solidity review is reused solely for these identical sources and validated dependency identities; no new full-source review or compiler execution is invented for integration.

## Public wiring and actual meaning

Read the complete public facade again and the entire wiring diff. AllGuarantees imports PSsz1CompiledClEntry; Trust imports SszCompiledClEntryRegression and queries both public branch/tree theorems plus beforeRoot_success, rootCall_success, afterRoot_success and run_success. The normal AllGuarantees and Trust setup importArts point to the actual integrated public/regression oleans, and their bytes match the recorded normal local artifacts. No alternative provider, unused adapter or replacement declaration is substituted.

The public branch theorem directly applies run_success to the same successful run and unchanged inherited SszProofCommitted.ShaWidth. That single execution runs fresh entry/prologue, actual ABI/slot sibling guards, memory-built timestamp payload and one BEACON_ROOTS STATICCALL, actual reply allocation/copy/first-word decoding, constructor-configured generalized index, reply-dependent eight-field validator allocation/hashing and proof loop. The loop consumes the root, index and leaf actually produced, and the branch records its actual consumed calldata words. The tree theorem transports this same branch and same returned bytes through the existing digest bijection. It does not execute a second call. No supplied root/index equality, initial memory shape, successful stage, reply size or helper frame premise enters either public theorem. The nonzero-code conclusion is derived from successful nonempty response under the accepted external-call interpretation; it does not add an EXTCODESIZE precheck to the source execution.

Source review already checked the internal free128→192→reply-dependent allocation bounds, mutable byte-array writes, scratch preservation, signed ABI guards, uint64/bool widths, fls constants and overflow/error order against the pinned code and full retained IR. All corresponding bodies and necessary imported source identities remain unchanged. The old typed partial-SHA and fixed-frame results retain their previous statements and hypotheses.

## Independent checks and reused execution evidence

- Verified all seven integration receipt hashes, including the verbatim review, and all45 source packet SHA256 entries. The receipt's identity data agrees with the separate identity record.
- Inspected the source validator, then executed it in memory with two controlled changes: build-identities compares the integration record instead of the old path-sensitive source record; scoped dependencies reuse the archived31-scope map, with exact queried-name equality and foundations checks. Nothing was written. All other source/scoped/compiler/runtime manifests compare exactly against the source records. Fresh identity assertions pass for1,215 sources, eleven package pins, seven normal source/olean/setup/trace sets, seven pinned compiler inputs and exact retained IR. Normal setups have empty options/plugins, nonsynthetic traces and no skipKernelTC.
- The integration31-scope map is exactly the source axiom map and contains only propext, Classical.choice and Quot.sound. These are reused root-fresh queries, not a claim that this reviewer reran31 queries. The existing full source review included fresh no-output elaboration of all seven modules and14 kernel regressions.
- Inspected the retained1,739-job normal AllGuarantees/Trust build log: successful final status, no build-error status, and the six new public/phase query results are ordinary foundations. Its exact bytes are receipt-hashed. The global checker log records fresh dependency recomputation and native claim re-evaluation with exactly29 disclosed axioms: foundations,23 test/mutant native decisions and three production exceptions. Global Trust is not foundations-only. This review reuses those root executions after checking identities rather than repeating the global build or checker.
- Seven pinned compiler bodies/settings and actual Git/Keccak identities validate. The retained Mac solc0.8.25 replay reproduces the complete IR plus documented newline, SHA256 `aba7b84e915d3279916e7d18150f047e553c8f712b1e30ea96235e11998679e3`, viaIR/optimizer200/Cancun. No integration or reviewer compiler run is credited.
- Four unchanged native whole-entry diagnostics and their independent hashlib-generated2,180-byte/50-sibling vector remain the previously reviewed diagnostic evidence. They are not kernel-evaluated whole-success instances. Nine inherited Solidity fixture cases remain historical evidence. No fresh Forge or FFI execution occurred in this integration review.
- Integration-only diff-check passes. Main-to-integration unfiltered diff-check reports exactly the disclosed compiler IR EOF and cl-1/cl-2/reply-1 development-log whitespace. Excluding precisely those four paths passes. Raw failed development history, including initial972 noncompilability/error-recovery outputs, remains preserved and receives no successful proof credit.

## Boundaries and conclusion

The success theorem concerns the selected semantic execution on EVM.State, using the accepted typed StaticCall.External over Live.World and opaque SHA engine. Constructor configuration words, deployed context/callee identity and EIP-4788 storage/history authenticity remain explicit interpretations. The inherited SHA output-width premise is unchanged. Root freshness, complete declared ABI proof-list equality for all wrapping layouts, full compiled-bytecode correspondence, complete error-byte encoding, opcode gas/termination and a new failure-world rollback theorem remain outside. The dossier states these boundaries accurately; integration introduces no stronger hidden premise to bridge a missing consumer.

Exact70b8 preserves the full reviewed source composition and every prior main object while registering the actual public consumer and regressions. **CLEAN within the stated scope.** No candidate source/cache/receipt edits, redundant build, Forge/FFI/compiler execution, merge, site change, push or deployment was performed. Only this external report was written. STOP.
