# Independent exact ADDRESS request-batches source review

Verdict: **CLEAN for physical entry pause admission and ordered typed-array batching of the complete accepted per-item effects**, exact `274d8cc9aa543aa4aafda84937689676e0839cfe`, tree `d7e3edc2d361dac016f901e20b112222329aef43`. No blocking source/proof/composition/correspondence discrepancy found. This is not a compiled whole-entry, gas, universal token-conservation or general Ethereum theorem. STOP.

Reviewed clean `/tmp/lido-address-request-batches` with author stopped. Sole parent is accepted331 main `ae30a086154dc78196034657953b9a09cc75b48b`. This reviewer authored none of the new batch sources or the inherited331 source. The author of the new lot is distinct from this reviewer. The complete recursive tree comparison preserves every parent object/mode and adds exactly53 files: three Lean modules and the new dossier. All52 candidate hashes and three configuration hashes match. No old theorem, domain, implementation, dependency configuration or global wiring changes.

## Complete source and consumer review

Read all325 lines of the new Source, public Guarantees and Tests modules, including every proof,14 kernel examples, two public boundary instances and five native diagnostic groups. Read the complete inherited AddressWrappedTransferCalls implementation/JoinedEffect and both retained independent331 source/integration reports. Revisited the actual stETH request, owner resolution, physical enqueue, effects and relevant Live/ABI/call definitions. Prior wrapped-token/caller reviews are reused after exact identity, not presumed merely because imported. This is not a claim to reread every body among1,255 imported sources.

The loop is a real execution over the current World. For each original Word amount it runs the selected step, then passes that step's returned world to the recursive tail; it returns the actual ID followed by the tail IDs and concatenates actual first/tail attempts in order. Failure propagates the attempted journal. `runBatch` wraps the entire entry execution in existing root rollback, restoring the complete batch-entry world on any failure. Inner item rollback alone would not restore earlier items; the outer run does so.

Transcript is an inductive relation whose cons constructor ties an original amount and returned ID to the complete chosen item effect, its exact before/middle worlds and attempts, and a tail beginning at that same middle world. It does not merely record IDs or assume a successful external stage. `loop_success` derives each constructor by inversion of the actual executions. Its generic item implication is completely discharged in stETH_batch_success and wrapped_batch_success by the existing **public** per-item theorems. The two new public success theorems have only their whole-run success equation and return entry Resumed, the complete Transcript and equal input/output lengths. No item-success, nonempty, funding, allowance-fit, amount/share-fit, initial frame or nonalias premise escapes.

`stETHStep` invokes the actual accepted stETH request runner, with amount guards before its transferFrom CALL, then actual shares quote and physical enqueue. `wrappedStep` invokes AddressWrappedTransferCalls.runRequest from331, and wrappedEffect is exactly its JoinedEffect. That effect retains the entire329 JoinedEffect as a conjunct, itself retaining the complete wrapped caller RequestEffect. Thus each batch item includes actual first WSTETH transferFrom, canonical original amount, returned transfer world, actual unwrap conversion/burn/transfer, captured conversion result, bounds, shares quote, physical enqueue, events and journals on the same worlds. The old canonical adapters remain consumed; nothing is replaced with a parallel reported payload or supplied successful token reply.

Both public failure theorems use the same full batch runner and restore the incoming World. The theorem does not claim that failed attempts disappear. IDs are in item order and each complete old effect retains its local ID arithmetic/width; there is no unjustified global strict monotonicity across arbitrary callbacks that can change queue storage.

## Admission, owner and environment semantics

Entry performs the actual full-word pause-slot read once, before owner resolution and before even the empty loop. Resume slot is the independently recomputed Keccak of `lido.PausableUntil.resumeSinceTimestamp`, `0xe8b012900cb200ee5dfc3b895a32791b67d12891b09f117814f167a237783a02`. Pause is timestamp < storedResume; equality resumes. The queue uses the accepted designated root ContractState lens. Source Resumed is the negated guard in ordinary Word-value arithmetic, derived from success.

Owner is selected at entry using fixed Context. The old item helpers still evaluate their pure resolver; owner_idempotent proves a second application cannot change the already selected owner, including zero sender. That auxiliary theorem is not needed by the generic public induction because the complete old effect already expresses its own resolver; this does not create a residual premise or change the owner between items. The README accurately distinguishes entry selection from pure helper reevaluation.

Callbacks can write the pause slot after item1 and item2 still runs: the loop has no pause recheck. The actual native and Solidity cases cover both stETH and wrapped paths. They do not assume callbacks preserve storage or balances. The old item model captures that item's entry timestamp; a returned model World may change environment fields before the next item. The dossier explicitly does not assert a single transaction-wide timestamp or EVM environment-continuity theorem. The fixed Context/owner and chronological World chain remain proved without adding a hidden callback frame. These limits are an explicit inherited model boundary, not an unmentioned premise disguising compiled execution.

## Pinned Solidity and complete relevant legacy assembly paths

Read pinned WithdrawalQueue constructor/initialization context, complete public requestWithdrawals/requestWithdrawalsWstETH bodies, internal stETH/wrapped requests and amount guards, complete PausableUntil and UnstructuredStorage, pertinent complete queue enqueue and EnumerableSet insertion code. Exact core pin is17005714f151e5502c559932319a3f2f74ac2436. Reviewed the complete new BatchHarness, inherited WrappedRequestHarness and Batches tests.

The retained full13,650-line legacy assembly is byte-identical to the current BatchHarness compiler artifact's assembly plus its documented newline. I inspected the complete relevant batch bodies, pause/isPaused, internal calls, enqueue/set insertion, error/checked-arithmetic helpers and selected dispatcher/array decoder/output encoder paths, following their jump targets. I do not claim that unrelated permit/finalization/access-control functions in that large artifact were each reread as new batch proof obligations.

- Public selectors independently recompute to requestWithdrawals0xd6681042 and wrapped0x19aa6257. Both dispatch through the same actual array/address decoder. The new Lean result is explicitly for decoded typed inputs, not arbitrary ABI acceptance.
- Wrapped body tag87 and stETH tag227 call _checkResumed/tag283 before low160 owner fallback and new-array allocation. isPaused/tag192 loads the exact data-pool resume slot and compares TIMESTAMP. ResumedExpected selector0x14378398 agrees with the generated shifted constant. The Lean fault uses the established semantic category; it is not a raw custom-error byte theorem.
- Both result allocations reject length >2^64−1 with Panic0x41/tag287, write the length at the free pointer, advance by32+32*length and zero-fill using CALLDATACOPY. The public calldata decoder also checks dynamic offset/length/extent and canonical address. These operations are outside the typed List executor, explicitly rather than through a successful-allocation premise.
- Loops initialize uint256 index0, compare it unsigned to length, perform source input/result bounds checks with Panic0x32/tag295, store the actual returned ID at that result index and increment through checked tag300, with Panic0x11 on max. Successful compiled allocation bounds preclude index overflow in ordinary iteration. Arbitrary finite Lean lists do not therefore imply compiled acceptance of impossible allocation sizes; output allocation/encoding/exhaustion and gas remain excluded.
- stETH loads the item and executes low100/high1000*10^18 admission before the actual internal CALL. Wrapped calls transferFrom and unwrap with the original Word, then applies those bounds to the actual decoded unwrap word. The source CALL/CALL/STATICCALL order, targets, sender/recipient/value0 payloads, code checks, failure bubbling and return decoders match the inherited consumed models. The caller bool is canonical0/1 with its value discarded; quote/unwrap consume the first word with minimum32 bytes.
- The same enqueue checks cumulative shares then cumulative stETH as uint128, then uint256 ID, writes last ID, reads report timestamp at the later point, writes packed amounts/metadata and executes source-order owner insertion. High metadata bits, fresh length reread, wrapping set helpers and WithdrawalRequested then Transfer remain the old effect. The new batch does not substitute final aliased-slot rereads or token-conservation formulas.

The omitted allocator/ABI boundary is concrete and material, but accurately stated in the source module, public facade, README and receipt. The useful new claim is the physical entry pause guard plus actual ordered composition/rollback of already accepted full item effects. No former claim was weakened or renamed to pretend this covers full compiled request entrypoints.

## Independent checks and exact evidence reuse

Read both validators before use. Executed them with an in-memory Path.write_text interception: the four candidate manifest writes became exact-byte equality assertions, all other checkout writes were forbidden, external scratch queries were allowed and Python bytecode caching disabled. Fresh independent checks pass1,255 imported/new sources,11 exact package Git pins, actual selected local olean providers, unchanged old source bodies, runtime inputs and **nine ordinary scoped axiom closures**. All scopes contain only propext/Classical.choice/Quot.sound; generic loop/admission/owner proofs use propext. No admitted proof, unsafe substitution, native_decide or new axiom appears in the accepted new modules.

Fresh no-output `lake env lean` re-elaboration of each of the three modules passes, including14 kernel examples and the two public instances (empty stETH success, paused wrapped rollback). Positive Keccak-backed batches are not mislabeled as kernel instances. Independently checked all three normal source/olean/ilean/trace sets, empty setup options/plugins, nonsynthetic traces and absence of skipKernelTC. This is focused ordinary checking with validated caches, not a full dependency rebuild.

Fresh correspondence checks pass35 reused input entries,27 actual pinned Git/npm bodies, integrity-checked OZ3.4.0/4.4.1 archives, metadata source counts11token/18queue/19tests, resume-slot/selector recomputation and exact archived token identity. Both archived queue/test artifacts equal the current compiled output files; full assembly equality is independently checked. The inherited331 Source/public/tests equal sourcea9c6 exactly and all50 of its source receipt hashes remain valid. Its complete independent source/integration review therefore applies to the inherited implementation.

Reused author's final normal1,272-job successful logs after identity checks, not historical failed attempts. Read five native diagnostic bodies and their temporary-dylib runner/log: actual stETH transcript, actual wrapped transcript, pause-callback worlds, late stETH rollback/journal and late wrapped rollback/journal pass. They use official existing Keccak FFI and remain diagnostics, not universal positive kernel proofs. No new FFI execution occurred during this review.

Read all eleven real Solidity tests and the successful build/test log, including1,024 fuzz runs. The inherited actual public batch functions run against the real archived unmodified WstETH0.6.12 constructor artifact; STETH, pause setter and storage seeding are explicit fixture inputs. Empty/paused/equality, owner/IDs/order, actual two-item token execution, late allowance/amount failure with full rollback and pause callbacks on both paths are exercised. Assertions that counters are zero after revert establish rollback, not by themselves the absence of prior attempted calls; source/assembly order supplies that correspondence. Token0.6.12/200/Istanbul is reused; queue/tests0.8.9/200/London compile in the author receipt, not production deployed-bytecode identity. No redundant Forge or compiler execution was performed.

Experimental IR inspection remains a recorded compiler ICE `Invalid stack item name: slot` with empty retained stdout. It is not the chosen legacy profile and receives no IR credit. Historical Lean, fixture and checker failures remain separated from final passing evidence. Unfiltered diff-check reports exactly BatchHarness.asm:13650 blank EOF; excluding only that raw file passes. Earlier raw artifacts are preserved, not assigned a blanket formatting PASS.

## Final limits and disposition

Initial queue/token deployment and storage/allowance provenance, configured-address relations, queue root lens, STETH implementations, permit entrypoints, allocator/ABI/memory/gas execution, complete error/LOG bytes, general compiler/EVM/cryptographic correspondence and transaction-wide environment continuity remain explicit boundaries. No final token balance or arbitrary-callback conservation theorem is added. Global All/Trust integration and any publication remain separate work.

The complete actual item effects, original amounts, returned IDs/worlds, chronological attempts, entry-only physical pause, owner choice and whole-batch rollback support **CLEAN at exact274d within the stated typed-batch scope**. Candidate remained clean; no candidate/source/cache/receipt edits, full rebuild, Forge/FFI/compiler run, merge, site action or external publication occurred. Only external report/evidence were written. STOP.
