# Independent exact ADDRESS batch integration review

**CLEAN for the bounded typed-batch integration**, exact `860c88b82fa3b3f23ad0619a78dca6b1dedbcae6`, tree `017a2f696d266fda981b70ef8ba4166e46f74da3`. No source, composition, wiring, correspondence or evidence discrepancy found. Checkout and writer remained stopped/clean; no candidate/cache writes, build, Forge, compiler or native diagnostic execution occurred in this review.

## Independence and exact preservation

I authored neither the new AddressRequestBatches lot nor this integration. I previously authored some inherited request/wrapped-token/transfer bodies. Their independent acceptance is supplied by the retained prior reviews, not by treating my present rereading of my own bodies as independent source review. In particular, `audit/address-wrapped-transfer-call/integration/independent-source-review-a9c6.md` and `independent-integration-review-13c6.md` retain that provenance. The full new batch source/Solidity review by a different reviewer is archived verbatim as `audit/address-request-batches/integration/independent-source-review-274d.md`, byte-identical to `continuous-address-batches-274d-independent-review.md`, SHA256 `31571f85d874d5db85ae61324249c7bce093bd3228eb8e97c5362e2c70535dd5`. I read that report completely and checked source identity before reusing its source-review evidence.

Source `274d8cc9aa543aa4aafda84937689676e0839cfe` adds exactly 53 paths. Every one is byte-identical in final union `9b8946a15a61125fb8f3d0f7fa4d22ed52c2184e` and candidate 860c. The union preserves every existing object from main333 `e3e833bbeaadccf1cf311ee55a775d0d1fa45905`; only these 53 source/audit additions differ from main. The candidate then changes only AllGuarantees/Trust and adds nine integration files. No old theorem, effect definition, input domain, provider or dependency is weakened or replaced.

All **52 source receipt hashes**, **eight integration receipt hashes** and **three configuration hashes** independently match the exact candidate Git blobs and current bytes. There are nine integration files, of which eight are hashed by the ninth, the receipt; this is not nine self-referential receipt hashes. The entire original source review is preserved. The integration delta passes `git diff --check`; the full main-to-candidate delta has exactly the documented raw `BatchHarness.asm:13650` EOF blank exception.

## Complete consumer matrix

Read the complete three new Lean modules, every proof and test/diagnostic definition, the complete integration/source descriptions, and the consumed old request/transfer/unwrap/enqueue effect chain. Reviewed the inherited public connections and Live loop/rollback semantics. The following checks concern the actual executed composition, not compilation alone.

| Public obligation | Executed producer and consumed conclusion |
|---|---|
| Initial physical pause admission | `entry` reads the full resume slot once before owner resolution and even before the empty loop. Success derives stored resume ≤ entry timestamp. Equal timestamp is resumed. No second pause check occurs after a callback. |
| Ordered actual stETH requests | `stETHStep` invokes the accepted request runner on each original Word value. The generic induction's item implication is discharged with `PAddress1.actual_request_withdrawal_enqueue`. Each Transcript node contains the full RequestEffect: admitted original amount, actual transfer reply/world, quote on that returned world, truncating share cast, physical checked enqueue, ID/events and journal. |
| Ordered actual wrapped requests | `wrappedStep` invokes the accepted transferFrom/unwrap runner with the same original Word. Its effect is exactly the entire `AddressWrappedTransferCalls.JoinedEffect`, retaining its older JoinedEffect and RequestEffect conjunctions. Thus actual first transferFrom, same transferred world, unwrap conversion/burn/transfer/decoded amount, later amount bounds, static shares quote and physical enqueue are not replaced with supplied payloads or an ID-only relation. |
| Same worlds, IDs and attempts across the batch | `loop` executes the tail on the first item's actual returned World. `Transcript.cons` joins that identical middle World and concatenates first/tail journals. `loop_success` derives the constructor by inversion of actual outcome branches. The returned IDs and original amounts remain ordered, and transcript induction proves equal lengths. |
| Full batch failure rollback | `runBatch` uses the existing root `run` around the whole entry/loop. Any failure restores the complete initial batch World, including effects of prior successful items. Journals remain observable. Per-item rollback alone is not incorrectly substituted for this outer rollback. |

Both new public success statements have only whole-run success as their propositional premise. The generic implication over successful steps is discharged internally by old public theorems; it is not an exported stage-success assumption. No nonempty, balance/funding/allowance fit, amount/share fit, nonalias, callback-frame or item-success premise was added. Both failure theorems refer to the same complete runner.

Owner fallback uses fixed Context at entry; the inherited pure resolver is idempotent, including zero sender. Its repeated evaluation does not change owner selection. Local IDs retain each inherited checked physical enqueue result; the theorem does not claim globally strictly increasing IDs through arbitrary callbacks. Each inherited item captures its own entry timestamp, and the batch explicitly does not prove an immutable transaction-wide environment across arbitrary returned model Worlds.

## Solidity and assembly correspondence

Read the actual pinned WithdrawalQueue public batch bodies (129–160), internal stETH/wrapped requests and amount guards, complete PausableUntil, the new BatchHarness, and all eleven Solidity tests. Revisited the complete relevant legacy assembly batch bodies at 1379–1544 and 4086–4268, isPaused at 3353 onward and pause guard at 5828 onward. The broader inherited compiler/caller/enqueue function review is reused from the complete independent 274d review after exact identity, rather than claiming that every unrelated body in the 13,650-line artifact was reread here.

The source and assembly check pause first, resolve zero owner to caller, allocate result memory, iterate original items, execute the selected internal request and store the actual returned ID at the corresponding result index. The stETH path checks the original amount before its transfer CALL; the wrapped path uses the actual unwrap result for bounds and the shares quote. The pause guard compares TIMESTAMP with the exact stored full-word slot and is not reentered by the loop. The assembly retains the explicit result allocator and uint256 index/bounds checks.

The typed List model deliberately omits outer ABI decoding, memory allocation/output encoding and gas. In particular, the real allocator rejects lengths above 2^64−1 before iteration, while a finite Lean List is not such a compiled allocation proof. This material boundary is stated consistently in source/public documentation, README and receipt; it is not hidden behind a successful allocator hypothesis or promoted to arbitrary compiled-input acceptance. Fault/event names remain the inherited semantic categories, not new universal raw error/LOG ABI correspondence.

Independent byte checks verified **35 reused input entries**, **27 actual pinned Git/npm bodies**, token/queue/test metadata source counts **11/18/19**, the exact archived WstETH constructor artifact, complete assembly equality with its compiler artifact plus the documented newline, and the recomputed pause slot and three selectors. Core pin remains `17005714f151e5502c559932319a3f2f74ac2436`. Queue/tests use legacy solc0.8.9/optimizer200/London; token0.6.12/optimizer200/Istanbul is reused. These are fixture profiles, not deployed-bytecode proofs.

The eleven-test source/log uses inherited real public batch functions and the actual archived token constructor, with explicit STETH doubles, storage seeding and pause hook. The single fuzz test ran 1,024 cases. Late-failure checks attest full rollback; zero counters after revert are not presented alone as evidence that no earlier call was attempted. Five native positive/rollback diagnostic groups remain distinct from fourteen kernel examples and two public boundary instances (empty success and paused rollback). No new native or Solidity execution is claimed here. The experimental IR compiler ICE remains unsuccessful historical tooling evidence, with no IR success credit.

## Integration wiring, normal build and scopes

AllGuarantees adds the actual public module. Trust adds the actual regression module and six active queries: both batch success and failure theorems, generic loop composition and the empty public instance. Existing imports/queries are unchanged. Both current aggregator normal setups include those modules. Existing Lake source, guarantee and test providers cover the three new paths; `lakefile.lean`, manifest, toolchain and trust checker are identical at source, accepted main and candidate.

The archived normal build succeeds at **1,751 jobs**, actively compiling Source (1.6 s), public (1.5 s), tests (1.9 s), AllGuarantees (2.3 s) and Trust (2.1 s). The global check retains **29 disclosed axioms**: the existing native-test/production exceptions plus foundations, not a foundations-only global inventory. All **nine scoped closures** for this new batch are ordinary and match the source scoped record exactly; the generic loop proof uses propext.

The root's fresh build/global/scoped checks ran with current wiring on union `4b034905a8e2e372b9c1275074e5ec9cdb077397`. Its entire tracked delta to final union 9b8946 is the accepted TOPUP333 integration review and corresponding receipt update. No build input changed, so those executions are reused without an additional run claim.

My independent current identity checks covered **1,255 imported/new source hashes**, **eleven package pins**, all **nineteen selected local import source/olean providers**, three new normal olean hashes, ordinary setup options/plugins and nonsynthetic source/test/aggregator traces without skipKernelTC. Selected shared paths resolve to the same source bytes. The nine scoped records and all logged checks retain exact input identity. Both validator implementations were read; neither writing validator nor its scratch-output wrapper needed to be rerun because current source/artifact equality and the root's fresh scoped record suffice.

## Disposition and boundaries

No new finding. The source review and this exact integration support physical entry pause admission plus ordered typed-array composition of complete accepted per-item effects and full root rollback. Deployment/storage/allowance provenance, configured-address and designated queue-root lens, external STETH behavior, outer ABI/result allocation/memory/gas, general compiler/EVM/cryptographic correspondence, raw error/LOG bytes, universal callback conservation and transaction-wide environment continuity remain explicit. Permit work is separate and receives no credit.

Evidence: `/tmp/lido-address-batches-860c-independent-evidence.json`. **CLEAN at exact 860c; reviewer STOP.** Any final report-only successor requires only its exact documentary delta check.
