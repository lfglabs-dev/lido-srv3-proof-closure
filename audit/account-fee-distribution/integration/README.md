# Actual checked fee mint and distribution integration

Source `6c1f367a7c9c22af7bad2dae073b8dbf277ea02b` is united with accepted main `2c2c72a91cd68a43de0777912772d12cc48a285d`. The four new Lean bodies and all 31 imported source identities are unchanged. Root registers FeeDistribution, ReportFeeDistribution and the regression module in the existing single AccountAddressChecks provider. AllGuarantees imports PAccount1FeeDistribution; Trust imports its nine regressions and queries both public results, derived mint funding and the sequential payment ledger. No prior execution definition, theorem or dependency pin changes.

Normal combined AllGuarantees/Trust passes 1,708 jobs. A fresh global gate recomputes dependencies and re-evaluates native claims: 29 existing disclosed axioms, not globally foundations-only. A separate fresh query on the actual registered test environment covers all 27 newly named theorems (18 production and nine regression): foundations only. All 21 source-packet sets agree exactly. Four normal-Lake olean hashes are retained; they differ from the earlier manually produced candidate oleans. No byte-identical olean reuse is claimed for these four modules.

The public consumer runs the same accepted report/getter/checked-fee/mint execution and consumes its exact checked split in sequential typed StETH transfers. Actual mint derives Accounting funding. Positive module payments occur in source order; zero allocations skip; positive treasury resolves on the actual post-module state and consumes that returned recipient. Transfer guards, debit followed by fresh recipient read, checked addition, post-state conversion and paired events are derived from execution. The paid sum equals the same minted fee, and the inductive pointwise ledger proves every final account balance equals its initial balance plus actual payments received, including duplicates and Accounting self-recipients. Physical packed storage and post-report router are preserved. Covered errors restore the complete incoming report world; attempted typed recipient/amount pairs are diagnostic, not EVM traces or committed logs.

The new TreasuryRead boundary remains expressly supplied and typed read-only. Its result is consumed, but no formal locator STATICCALL/ABI/deployment correctness is claimed. This is separate from the older supplied locatorAccounting context, not hidden under a compiler/Verity label. StETH transfers are typed source-corresponding executions; the account-share map remains abstract. Earlier supplied report context and later reportRewardsMinted, observer/rebase/full-entry operations remain outside. No new stage-success, fee-equality, array-alignment, funding, distinctness, global balance or callee-frame hypothesis is introduced.

Independent source review checks the full new Lean bodies and pinned source, actual inherited StETH, Accounting distribution fragment and retained compiler assembly. Its four independent ordinary kernel rechecks and expanded 27-theorem audit are retained. All 58 source artifact hashes, 31 source identities and 11 pins pass root identity checks. Eight fresh source-candidate Forge tests are reused, not rerun during integration. The retained 29 original vendored dependencies, 32 compiler inputs and three compiled artifacts were independently verified. The solc0.4.24/0.8.9 optimizer200 Byzantium fixture is explicitly distinct from production profiles; it checks real StETH CALLs and treasury STATICCALL/order but is not a full deployed Accounting/Lido/report test.

Raw-output exception: `audit/account-fee-distribution/solidity/DistributionHarness.asm` preserves compiler-inspection stdout exactly, including its extra blank line at EOF (line1609). The compiled artifact assembly plus stdout newline is independently verified. Unqualified source diff-check exits2 for that sole raw output; all source/nonraw changes pass. This harmless evidence-format exception is retained explicitly rather than modifying an accepted source packet or claiming a blanket whitespace PASS.

The original dossier remains a historical source-candidate record; this directory records the actual integrated checks. An independent review of the frozen integration commit is required before merge. Nothing here changes or publishes the website; ACCOUNT distribution is outside the current reviewed site9bc2 snapshot until a later reviewed publication pass.
