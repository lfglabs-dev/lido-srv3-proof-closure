# Independent ACCOUNT fee-distribution integration review

Verdict: **CLEAN, with the explicitly retained raw compiler-stdout EOF whitespace exception below.** No substantive source, proof, provider, preservation, receipt, or scope defect found. Read-only review; no project edits, build repetition, push, merge, or site action.

Exact frozen commit: `369f25ced0a7be966c2cf79de9a7c0cf93f51d01`, tree `652c059f66d88843441ab1e2df433ca8e763aa27`, checkout `/tmp/lido-account-distribution-integrated`. HEAD and clean status were checked before and after review. Root writer was explicitly stopped. This reviewer authored none of this ACCOUNT increment or integration.

The complete recursive trees establish that union `b34fa406f5a9feaf475975fd963463111c058a79` contains source `6c1f367a7c9c22af7bad2dae073b8dbf277ea02b` and accepted main `2c2c72a91cd68a43de0777912772d12cc48a285d`. Every pre-existing main tree entry is preserved outside the three explicitly reviewed wiring files. All 58 source-packet paths retain their source-candidate Git objects and SHA-256 identities. The frozen integration adds exactly six integration audit files and changes exactly lakefile, AllGuarantees, and Trust relative to the union. There are no new executable bodies beyond the four source-candidate Lean files. All 31 scoped input-source hashes and 11 actual package heads were checked; all five integration receipt hashes match. The archived source review is byte-for-byte the external independent full-source/Solidity review of 6c1f.

The single existing AccountAddressChecks provider registers FeeDistribution, ReportFeeDistribution, and Tests.Verity.ReportFeeDistributionTest from audit/trio/account-address. No duplicate account-address provider or dependency is added. AllGuarantees imports the new public guarantee; Trust imports the actual nine-regression module and queries both public results, minted_budget, and PaymentChain.ledger. Actual generated setup files resolve the short module names to the integrated source and integrated oleans, including the existing report/mint/split/cast prerequisites. AllGuarantees and Trust setups include the new public module, and Trust includes the new test module. The four normal-Lake traces name the correct source paths and have synthetic=false, empty options, no plugins, and no skipKernelTC. Their four current olean hashes match integration/scoped-axioms.json and each differs from its historical manually compiled candidate artifact, exactly as disclosed. No artifact identity is inferred from source identity.

The retained normal AllGuarantees/Trust build passes 1,708 jobs. I parsed its complete Trust axiom output using the unchanged repository checker: all currently printed theorem names are covered and the union equals exactly the 29 disclosed global axioms (foundations plus the existing 26 native exceptions). The retained fresh global-gate result explicitly recomputes dependencies and re-evaluates native claims. I independently recomputed all 27 newly named theorem dependency sets through the checker's controlled Lean.collectAxioms probe against the actual registered Tests.Verity.ReportFeeDistributionTest environment: exact agreement with the integration packet and foundations only for all 18 production and nine regression results. All 21 historical printed source sets equal the corresponding registered sets. This fresh read-only probe used temporary external files; it did not build or rewrite audit output.

The full-source 6c1f review remains applicable by exact identity. The public consumer executes the same accepted report/post-state getter/checked fee/mint, retains the intermediate checked split and cast certificate, and consumes its actual amounts in sequential typed transferShares executions. Mint-derived funding and the exact checked partition discharge payment alignment and budget obligations; there is no supplied funding, separation, distinctness, stage-success, or global-balance premise. Debit followed by fresh recipient read handles Accounting self-recipients and duplicates. Guard/conversion/event order, zero module skips, actual post-module treasury resolution, zero-treasury skip, and full incoming report-world rollback are retained. The payment-chain ledger proves each final abstract share-map value equals its initial value plus actual incoming payments, while total payment equals the same minted fee; packed physical storage and the post-report router are preserved.

Pinned Solidity correspondence and inherited tests are reused from the complete 6c1f review: exact core 17005714f151e5502c559932319a3f2f74ac2436 Accounting distribution and StETH/Lido conversion/transfer bodies, 29 original vendored dependencies, 32 compiler inputs, three compiled artifacts, and eight fresh source-candidate Forge regressions. No fresh Solidity execution is claimed here. The solc 0.4.24/0.8.9 optimizer-200 Byzantium fixture remains explicitly distinct from production compiler profiles. Actual fixture CALL/STATICCALL assembly checks do not turn the formal TreasuryRead parameter into a proved locator ABI implementation. TreasuryRead is an explicit typed read-only boundary; transfer execution is typed source correspondence; per-account share storage is an abstract mapping. Earlier report context and later reportRewardsMinted/observer/rebase/full-entry operations remain outside this theorem. Integration README accurately preserves these boundaries and makes no site-publication claim.

Raw-output exception: unqualified `git diff --check` against accepted main exits 2 solely for `audit/account-fee-distribution/solidity/DistributionHarness.asm:1609: new blank line at EOF`. I independently rechecked that the 38,062-byte file is exactly retained compiled-DistributionHarness.json assembly plus the stdout-printing newline (SHA-256 `0d24d4b47e0b0bc79f142cff59f293bf191476f157240746ee8fe6b1f7fc4ba9`). Source/nonraw diff-check excluding only this file passes; the entire integration-only diff-check passes. README and receipt disclose the exception, and no blanket whitespace PASS is asserted. Preserving these original stdout bytes is an evidence-format exception, not a proof or source-correspondence blocker.

The reviewed candidate is suitable for root's archive/integration flow within these exact boundaries. Reviewer stopped after writing this external report; the candidate remains unchanged and clean.
