# Independent ACCOUNT fee distribution source review

Verdict: **CLEAN with explicit raw-output whitespace exception** for exact frozen commit `6c1f367a7c9c22af7bad2dae073b8dbf277ea02b` in `/tmp/lido-account-fee-distribution`. The author was explicitly stopped. I authored none of this increment. The checkout remained clean and exact through completion; this review modified no candidate source, receipt or build artifact.

## Source scope and execution

I read all four new Lean files completely: FeeDistribution, ReportFeeDistribution, PAccount1FeeDistribution and ReportFeeDistributionTest. I followed their actual accepted ReportFeeMint/checked-split/cast certificate and StETHMintShares helper, reusing the earlier independent source reviews only after checking unchanged identities. Every existing tracked file from accepted base `241c2ce1e78860b8451a843949b71f60b8c2df25` is preserved. Only the four new Lean files and their audit directory are added. There are no registration, manifest, aggregate or Trust changes. The final `git diff --check` reports one extra blank line at the end of the generated assembly file; see the explicit exception below.

ReportFeeDistribution.execute actually runs the existing report/getter/checked-fee/mint executor once, retains its exact FeeResult, and supplies that result and the actual minted StETH state to distribution. The sender is the independently supplied Accounting address that the existing successful positive mint authenticated. Zero minted fees skip the distribution. Each positive module amount is transferred in recipient-array order, consuming the previous transfer's state; zero module amounts are skipped. Treasury resolution occurs only after the module chain, only for positive treasury shares, on the actual returned intermediate state. The returned treasury address is then consumed by the last transfer.

TransferShares preserves StETH's order: typed Nat admission; sender nonzero; recipient nonzero; recipient not StETH; active status; sender funding; debit; fresh recipient read and checked addition; post-state conversion; Transfer followed by TransferShares. The recipient fresh read is load-bearing for sender=recipient. A self-transfer restores the original share balance instead of creating shares. The debit SafeMath subtraction cannot fail after the executed funding check; recipient addition retains the uint256 overflow surface. No recipient callback exists in this source path or is invented by the model.

The unchanged pooledEthByShares helper enforces the strict amount < UINT128_MAX guard and reads the post-transfer packed state through Lido's numerator/denominator overrides. It retains raw uint256 multiplication and subtraction, a checked nonzero divisor, and the bounded packed internal-ether sum. These packed reads are unchanged by transfers. In particular, the internal-ether sum consists of four uint128 fields and cannot overflow uint256; its mathematical bound prevents an invented competing overflow from changing observable conversion-error order. Conversion failure rolls the transfer's debit/credit back. The paired event values are the actual conversion result and actual transferred shares, not a recomputed fee approximation.

## Public certificate and pointwise ledger

The public theorem has only success of the complete covered executor as its proof premise. It returns the previous #319 report/mint/split/cast certificate at the actual minted intermediate world, derives Accounting's available mint budget, preserves the post-report router and post-mint packed storage, and exposes the exact ordered module and conditional treasury transfers with their actual intermediate states, effects and events.

PaymentChain carries executed transfer equalities as well as independently phrased TransferEffect facts. Each constructor is derived by inverting the actual transfer result. Its pointwise ledger is proved by induction over those sequential transfers, including self and duplicate recipients. The positive-payments sum uses recipient/amount alignment derived from the consumed #319 certificate, not a caller-supplied length hypothesis. Thus filtering zero payments and pairing the lists loses no allocation.

The same checked fee partition establishes paid(trace) = minted fee shares. Combining that equality with the actual mint's pointwise increase cancels Accounting's mint/debit term and proves, for every account, final shares = initial shares + received payments. Existing balances are retained; duplicate and Accounting-self credits are counted correctly. There is no distinctness, global funding, stage success, amount equality, fee partition or successful resolver hypothesis supplied by the public caller. The nine regressions include a successful composed public instance, so the result is not vacuous.

Any covered error restores the complete pre-report World, including report writes, packed mint, mapping changes and earlier successful payments. Reverted outcomes expose no committed event list. The retained attempted recipient/amount list is explicitly a diagnostic typed journal; it is not claimed to be an EVM call trace.

## Solidity, fixture and boundaries

I checked the relevant complete pinned source bodies: Accounting403–407/470–488, inherited StETH transferShares/_transferShares/getPooledEthByShares/event helpers, Pausable, SafeMath and the Lido rate overrides. The vendored full 590-line StETH is unchanged source, not a rewritten transfer implementation. The Accounting _distributeFee function in the harness is byte-identical to the complete pinned function, including whitespace and ordering. Core is exactly `17005714f151e5502c559932319a3f2f74ac2436`; all five retained complete Solidity file identities match pinned Git blobs.

The source declares the new TreasuryRead boundary clearly: it is a supplied typed read-only external resolver on the actual post-module StETH state. It does not prove a deployed locator, actual STATICCALL/ABI decoder, revert bytes or whole-world locator implementation. Likewise the transfer model is the covered typed StETH execution, not a new external-call ABI/deployment equivalence proof. The physical packed words remain distinct from the accepted abstract Nat-keyed account-share map; no formal physical mapping-keccak theorem is added. Earlier report context, immutable identity and later reportRewardsMinted/observer/rebase calls remain outside. These conditions are visible in the public docstring, README and receipt, rather than hidden under a generic compiler/gas label.

The new runtime harness uses real external StETH CALLs and a real treasury STATICCALL. I followed the generated distribution assembly through the recipient loop, selector/argument construction, checked call returns, conditional treasury read and final transfer. The entire retained legacy assembly equals the compiled DistributionHarness artifact. The runtime locator checks actual post-module balances. StethHarness only exposes setup/mint and virtual rate choices; it does not claim Lido mint authorization or calculate protocol fees. Its mapping setup is checked through runtime sharesOf and is expressly fixture-only.

Both fixture compilers use optimizer200 and Byzantium: solc0.4.24 and solc0.8.9. I checked the actual production hardhat profile: its targets are Constantinople for 0.4.24 and Istanbul for 0.8.9. The README/receipt expressly deny production-bytecode/profile equivalence, so no fixture execution is promoted to that claim. The retained eight fresh Forge tests cover exact order/amounts/eight events, aliases, late-overflow root rollback, both zero skips, sender-zero precedence, conversion rollback and raw multiplication wrap. The unusual gas report is not used as evidence of gas behavior.

## Independent validation

All 58 packet hashes, 31 scoped source identities, 11 actual package revisions and four candidate olean hashes pass. Each inherited local source equals its accepted-base Git body. I independently checked the actual prerequisite setup import paths, compared their source and cached olean bytes with current local providers, and verified the package source bytes against pinned revisions. This supports the identified cache reuse; it is not a clean dependency rebuild.

I rechecked all four new Lean files with ordinary `lake env lean`, without writing output artifacts. All passed, including the nine kernel regressions and composed public instance. An independent environment query reproduces all 21 packet axiom sets exactly. I expanded the query to all 27 new named theorems (18 production, nine regression), including the otherwise unprinted ledger/chain helpers: every closure uses only propext, Quot.sound and/or Classical.choice. No new axiom, sorry, native reduction axiom or kernel-skipping option is introduced.

I reconstructed the full StETH import closure and verified all 29 original vendored source files. The Aragon4.4.0 and OpenZeppelin Solidity2.0.0 versions match the pinned core package file; independently downloaded original tarballs match the recorded SHA-256/integrity strings and every copied package byte. All 32 actual compiler-input Keccak/SHA identities pass. All three retained compiled artifacts match the actual existing Forge cache and their recorded compiler/settings/hash identities. No Solidity validator that rewrites receipts was executed; no Forge build/test was repeated or credited as fresh by this review.

The source candidate's 29 prerequisite jobs plus four sequential normal checks are accurately distinguished from a future root aggregate build. Root must register the new modules in the existing ACCOUNT provider and validate the integrated AllGuarantees/Trust consumers; no full-root Trust verdict is implied here. No actionable source, proof or scope finding remains. Root explicitly retains the raw-output formatting exception described below and will disclose it in integration documentation. Review stopped after writing this external report; root owns integration, publication and merge.

## Explicit raw-output whitespace exception

The unqualified `git diff --check 241c2ce1e78860b8451a843949b71f60b8c2df25 HEAD` exits 2 and reports only `audit/account-fee-distribution/solidity/DistributionHarness.asm:1609: new blank line at EOF.` Root explicitly instructed that the exact raw compiler-inspection stdout be preserved, with this exception disclosed in integration documentation. This is not a source/proof/receipt correctness blocker and no blanket diff-check PASS is claimed.

I verified byte-for-byte that the retained raw file equals the compiled artifact's complete assembly string plus the single newline added by stdout printing. The compiled snapshot also equals the actual Forge cache. A diff check excluding exactly this raw assembly stdout file passes for every source and nonraw artifact. No receipt falsely asserts a successful unqualified diff check. The candidate remains unchanged and no writer restart, trimming or receipt rewrite is required.
