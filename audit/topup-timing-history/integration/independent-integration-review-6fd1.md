# Independent exact integration review — TOPUP timing/history

**CLEAN** for frozen integration `6fd115a71b0405cfc3233d28f4875ac8d93cbe3f`, tree `09d3f8424abfbdea92371f13a69e4e54defd802a`, within the bounded typed timing/history consumer scope. No blocking integration, proof-provider, source-correspondence or evidence issue found. This does not certify whole deployed TOPUP execution or the later website/publication result.

## Exact union and registration

The clean checkout `/tmp/lido-topup-timing-history-integrated` has the requested HEAD/tree. Union `343441117899e40cd76c54f906e7124ec52bddcc` joins preliminary union `b60f5f2b5164d4d096d57550d3a06653cb655cf8` and accepted main334 `52a4483b7e7030f0a8c76b056da8de8d3a2ec85f`. The preliminary union contains source `2efb510497b38da2782b9ffc80e6c68d3c3f784c` and ADDRESS integration `860c88b82fa3b3f23ad0619a78dca6b1dedbcae6`.

I checked that final union versus main334 contains exactly the original 18 source-candidate additions, with every added file byte-identical to source2efb. Integration versus union changes only two wiring files and nine integration archive files. AllGuarantees adds the public timing/history import at the beginning. Trust adds the regression import and five fully qualified success, rollback, execution-projection, packed-fields and public-instance queries. There is no executor, theorem-domain, old-provider or dependency change in this integration delta. Integration `git diff --check` passes; inherited raw artifact exceptions are not erased by that result.

The physical-getter README and receipt are byte-identical to accepted correction44d5. The older source-parent documentation P3 is therefore resolved in this actual integrated tree; no exhaustive uint8 regression claim is credited.

## Same reviewed source and actual providers

I previously read all three new Lean files completely, the complete inherited public consumer, executable root/getter/module/memory/effect composition, the complete pinned TopUpGateway source and full relevant compiler paths. I reread the retained full source report here and verified it is byte-for-byte my external2efb report, SHA256 `fbb6a1dc1417ff7e98994086fc8f1cf7a466aa88f54f7c528201c7c248fe0945`. Its detailed source correspondence, evidence provenance and qualifications apply unchanged after the checked identities. I authored none of the implementation or integration; my prior source review is reused as prior review, not described as a second independent author.

All 1,352 recorded source SHA256 identities and eleven package revisions match the integrated checkout. All seventeen source receipt hashes and eight integration receipt hashes match both exact HEAD Git objects and working bytes. Each of the three normal source/public/test olean, setup and trace identities matches the separate integration build-identities record. The normal oleans also retain source2efb identities; setup/trace paths legitimately differ. Their options/plugins are ordinary, their traces nonsynthetic, and no skipKernelTC is present.

I inspected current AllGuarantees and Trust setup importArts. AllGuarantees selects the source and public timing/history artifacts; Trust selects all three. Each selected path resolves to the actual integrated checkout’s normal `.lake/build/lib/lean` provider, and its bytes equal the checked artifact hashes. AllGuarantees/Trust themselves have ordinary options/plugins and nonsynthetic traces. This verifies the registered provider rather than accepting a similarly named external cache.

The public consumer thus still retains the entire 3674 intermediate conjunction, using unconditional execute_projection and the same actually executed loop Output. History branches on witness-produced total limits, including positive limits with zero allocations, and consumes the returned intermediate World. Packed timestamp/block truncation, preservation of all other packed bits and full semantic event timestamp are unchanged. Physical prior effects remain properties of that intermediate World before the history update. Full input-World rollback and all three attempt journals remain connected to the same actual execution. No stage-success, frame, fit, stronger cryptographic, future-time or monotonicity premise is introduced.

## Build and trust evidence

I read the integration validator adapter before deciding to reuse its retained results. It changes only the destination of path-dependent build-identities, preserving comparison checks for the other manifests and checking all source receipt hashes. I did not rerun the unchanged validator, dependency probes, builds, compiler, Forge or FFI.

The retained initial log accurately records failure of AllGuarantees because an import was placed after the namespace. It actively builds source2.8s, public1.5s, tests4.4s and Trust2.1s. This failed combined command is not credited as a complete successful build. The final log replays those unchanged artifacts, actively builds corrected AllGuarantees2.1s, and completes all 1,754 jobs successfully. The current import is at the beginning, matching that correction.

The retained global check reports 29 exact axioms: 23 test/mutant native-decision axioms, three existing production exceptions (Phase-3 capacity, consolidation flow, SSZ digest), and three foundations, with dependencies recomputed and native claims re-evaluated. It is not described as globally foundation-only. The retained scoped comparison reports 28 ordinary foundational closures, 1,352 sources, eleven pins, 22 exact reused compiler inputs and three normal modules.

These checks ran on preliminary unionb60f with corrected final wiring. Its complete tracked delta to final union343441 consists only of the accepted ADDRESS independent integration review and its receipt. No proof, dependency or wiring input changed. Reuse at the final frozen tree is therefore justified by identity; it is not reported as a second build or global-check execution.

The original 16 kernel regressions, public success/rollback instances, seven actual-SHA native IO diagnostics and unchanged compiler/Forge evidence retain the source review’s distinctions. There is no new timing/history Solidity runtime test or new compiler execution in this integration.

## Retained scope

The actual outer role/pause/locator/deployment and gateway-to-router CALL/return allocator remain outside the model. Full memory/copy/aliasing, ABI/LOG/revert bytes, gas, linked/deployed identities, physical hash and typed cryptographic/external-world boundaries remain explicit. An arbitrary returned World may contain changed metadata; this does not assert real EVM callbacks can change transaction TIMESTAMP/NUMBER. The ledger remains a separate locator-bound result. No whole-guarantee or deployed-equivalence claim follows from this acceptance.

No candidate or cache mutations, merge, push, publication or paid calls were performed. The only write is this external report. Exact integration accepted for its stated scope. STOP.
