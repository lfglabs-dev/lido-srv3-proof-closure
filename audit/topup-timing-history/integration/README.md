# Integrated TOPUP timing and actual-limit history

Source `2efb510497b38da2782b9ffc80e6c68d3c3f784c` independently passed complete source/Solidity review. Union `343441117899e40cd76c54f906e7124ec52bddcc` includes accepted main334 `52a4483b7e7030f0a8c76b056da8de8d3a2ec85f`, including the earlier physical-getter44d5 wording correction. No source body changes. Root adds AllGuarantees/Trust imports and five public/source/test queries.

The same real loop produces the retained Output consumed by final history. The public result preserves the entire preceding physical-credential/root/module/memory conjunction at its actual intermediate World, derives physical temporal admission, and relates final history to that same World and all three journals. History branches on witness-produced total limits, not returned allocations. Its fresh post-module packed word preserves all other bits; timestamp/block fields truncate to uint32 while the semantic event carries the full returned-world timestamp. Complete failure restores the original World, without stage-success, fit, frame, future-time or monotonicity premises.

The initial integration build actively compiled the three new modules and Trust but failed AllGuarantees because root appended its import after the namespace. The retained initial-import-failure.log is failed development evidence. Root moved that import to the beginning, then the normal build passed1754 jobs with unchanged source modules reused and AllGuarantees actively compiled. The fresh global gate passes29 exact existing disclosed axioms. Fresh comparison validation passes1352 source identities,11 pins,28 ordinary foundation-only closures,22 reused exact compiler inputs and3 normal modules; all17 source receipt hashes match. Path-dependent setup/trace identities are recorded separately in build-identities.json, while all other source manifests compare without mutation.

Build/global/scoped checks ran on union `b60f5f2b5164d4d096d57550d3a06653cb655cf8` with the final corrected wiring. Its only tracked delta to343441 is the already accepted ADDRESS334 integration report and receipt. All proof/dependency/wiring inputs are identical. These checks are therefore reused without a second execution claim. The three source normal outputs were actively compiled in the retained initial build; the final passing build is not misrepresented as freshly recompiling all of them.

The full independent source review is retained verbatim with provenance and source3674/44d5 distinction. Source1369-job build,16 kernel regressions/public success and late rollback instances,7 actual-SHA FFI diagnostics and complete retained2519-line Gateway IR keep their reviewed scope. No root compiler, Forge or native diagnostic rerun is credited. Inherited seven Solidity tests remain evidence only for their original fixture properties; there are no new timing/history Solidity tests.

Outer role/pause/locator, initial deployment/setters, actual gateway-to-router CALL and its return allocator, complete memory/ABI/gas and LOG/revert bytes retain explicit scope. Typed external Worlds are not constrained to preserve transaction metadata; final history reads the actual returned values, without claiming that real EVM callbacks can alter TIMESTAMP/NUMBER. The old physical effects are asserted before the added history write, not silently restated on the final World. The ledger remains a separate locator-bound result. Newly added source/integration files pass diff-check; unchanged inherited raw artifacts keep their prior exceptions. Candidate stops for exact integration review.
