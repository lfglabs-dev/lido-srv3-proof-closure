# Independent exact source review — TOPUP timing/history

**CLEAN for the bounded additive source/proof increment** at `2efb510497b38da2782b9ffc80e6c68d3c3f784c`, tree `f6efc459215947dcd7503301b699dd5bab6d13ba`, parent `367412569a2dd85972ab8bdab8304d261fa7b4ec`. No blocking correctness, source-correspondence, public-composition or evidence finding identified. This is not acceptance of whole deployed TOPUP execution or the later integration/site result.

Review was read-only in `/tmp/lido-topup-timing-history`. I authored none of this increment or its inherited substrate in this review session. No source, audit artifact, Git state or build cache was intentionally changed; the only retained write is this external report. No rebuild, compiler, Forge or native diagnostic was repeated. No paid external model/API was used.

## Executed source and public result

I read all three new Lean modules in full, including every proof and regression, the complete new README, receipt, validator and diagnostic programs, and the retained validation identities/logs. I read the complete 3674 public theorem and physical getter source, f008 credential source/public consumer, the memory public consumer and executable memory/root modules, and the complete Success, Effects and PositiveEffects statements and their proofs. I followed the actual produced arrays, raw module reply, scalar decoder, continuation World, physical count/balance effects and journals. This is not a claim to have manually read every proof in all 1,352 imported sources.

The full pinned `contracts/0.8.25/TopUpGateway.sol` at core `17005714f151e5502c559932319a3f2f74ac2436` was read. The retained complete 2,519-line GatewayWitnessHarness IR was used directly: I followed the complete topUp entry path from ABI/role/pause through lengths, timing, locator/getter, witness/root/SHA loop, outer router CALL and final history, plus complete relevant arithmetic, limit-evaluation, allocation, uint64-read and hashing helpers. Inspection did not rely only on the dossier excerpts or substring assertions.

| Obligation | Assessment |
| --- | --- |
| Physical admission and priority | `stored` reads the gateway-qualified namespace word. Timestamp/block occupy bits64/96 with uint32 width; distance/age occupy bits128/144 with uint16 width. `run` performs typed lengths and physical count first, then gates, then actual lookup/root/module. Repeated length checks are pure on identical initial configuration. |
| Checked block distance | Source323–326 and IR2152–2170 short-circuit on zero lastBlock. Otherwise `checked_sub_uint256(number(),value)` panics on underflow before comparison. `gates` matches this, including zero configured distance and equality acceptance. |
| Checked root age | IR450–479 adds masked uint64 child and uint16 age, rejects overflow past uint64, then compares against full TIMESTAMP, then rejects child <= lastTimestamp. The model preserves exactly this priority and width. Future child timestamps are not independently rejected. No setter-reachability or monotonicity premise is added. |
| Actual retained stage | `execute` executes the physical getter dispatcher, resolves exactly its decoded word, runs the actual root loop once and passes that same checked result into `TopupBatchMemory.finish`. It retains that actual Output in Stage.produced. `execute_projection` is unconditional, covering both success and errors. The program does not invoke a second root interpreter to reconstruct the total. |
| Full prior consumer | `IntermediateEffects` is byte-for-byte the entire old 3674 conclusion, independently checked by the comparison validator. The proof applies that old theorem to success derived through the unconditional projection. The prior World and both downstream journals remain attached to the exact intermediate run; all three final attempt journals equal those intermediate journals. No old conjunct is dropped. |
| Exact history selector | History consumes the retained Output.total, which the inherited Success relation connects to the actual witness-produced limit sum with a derived no-wrap bound. It does not consume allocation sum. Success proves a produced Output exists, so the generic absent-output fallback cannot supply a false successful zero total. |
| Fresh post-module physical write | Source233–235/340–345 and IR1114–1125 branch on totalLimits, freshly SLOAD the gateway word, clear bits64–127, insert truncated TIMESTAMP/NUMBER, then emit LastTopUpChanged with full TIMESTAMP. `update` uses the actual intermediate World for both metadata and preserved word. Its full World equation, log append and packed-fields theorem retain bits0–63 and128–255, including unused high bits. |
| Failure and observations | Every new error restores the complete original input World. Length/timing failures have no attempts; downstream errors retain actual getter/root/module observations. The new final history cannot fail within the stated semantic model. Public rollback is universal, including arithmetic/timing/phase errors, without successful-prefix premises. |

The exact old effects apply before the new history write. Arbitrary callback Worlds may change gateway state, block number and timestamp; the model reads those returned values. This is intentionally not a proof that real EVM callbacks can change immutable transaction metadata. No frame or equality between admission metadata and final metadata is silently assumed. Likewise, entry-selected credential identity is not asserted to equal a later physical reread after arbitrary module effects.

## Validation and provenance

I checked clean HEAD/tree before and after, all 17 receipt SHA256 entries against exact Git objects and filesystem bytes, exactly 18 additions, and `git diff --check`. The three Lean source hashes are respectively `6d2e520be376d948b740d24be934ca3bd47f7de90a4bc3b0ee40bb3a28684888`, `e8c9b517e4fa1df0db33e51dd0b0cc6beaaf769561489ccb75b71deb39b6b250`, and `ecef71d9bb55f9f0b6b43af3c5d9bf6ac0c576b9dd164dd95cd1a71f4e8e56ad` (source/public/tests).

After inspecting the validator and its controlled Lean dependency probe, I ran `PYTHONDONTWRITEBYTECODE=1 python3 audit/topup-timing-history/validation/validate.py` in comparison-only mode. Captured completion exited 0: **PASS 1352 sources, 11 pins, 28 ordinary foundation-only scopes, 22 exact reused compiler inputs, 3 normal modules**. An earlier identical comparison was launched without retaining its asynchronous session identifier; I do not count its uncaptured completion as additional evidence. Neither invocation rebuilds modules or runs compiler/FFI diagnostics.

The validator matches old local bodies to the exact parent, package bodies to pinned Git, selected normal local artifacts and all three setup/trace/olean records. Fresh controlled environment inspection recomputes the 28 named axiom closures; only propext, Classical.choice and Quot.sound occur. No new axiom, sorry, native_decide, unsafe declaration or skipped kernel mode is introduced by the new sources. The retained normal build actively Built source2.3s/public1.6s/tests4.3s and completed all 1,369 jobs; these are reused author build results, not a new independent rebuild.

There are 16 kernel regression theorems, a concrete complete public success instance and a universal-rollback theorem instantiated on a late failure. Their structural SHA fixture is not a cryptographic test. The important behavioral distinctions are exercised: positive produced limits with zero allocations still write; nonempty zero limits skip history; post-module packed-word changes/high bits are preserved; stored uint32 fields truncate while event timestamp remains full width; late aggregate-cap failure retains attempts and rolls state back.

Seven retained author native FFI IO diagnostics use the unchanged independently generated f008 three-row tree/proofs. I read the diagnostic bodies and loader and checked their runtime/vector identities through the validator. Positive physical [1 ETH,0,2 ETH], zero allocations with positive limits, changed credentials/root, temporal priority, aggregate-cap failure and age overflow are exercised. These are executable diagnostics, not new kernel axioms or EVM/consensus execution claims. No native execution was rerun here.

The 22 compiler source bodies match the exact core Git pin, accepted OZ vendoring and unchanged inherited harness, their original fixture SHA256 and compiler metadata Keccak. Complete input/output/IR bytes retain parent identity. The profile is solc0.8.25+b61c2a91, viaIR, optimizer200, Cancun, not production bytecode identity. The gateway namespace and history event topic are independently recalculated by the reviewed validator. Inherited seven Forge tests/three1,024-run fuzz properties receive only their original historical scope; none is represented as a fresh timing/history runtime test.

Prior independent evidence was read and identified:

- `continuous-topup-credential-f008-independent-review.md`: SHA256 `110e08a79f2086181c9198751e991f0426323e6b0d521ca3b8972eb6ff094d75`.
- `continuous-topup-physical-3674-independent-review.md`: SHA256 `b59eae006c2cf2a88f13c6c9635b11db16fe52d8399bcc5075376a64a5b9a81b`.
- `continuous-topup-physical-44d5-independent-review.md`: SHA256 `bfdb38a1b83570c0f12d46aacd284709fe6e0ab7c7b4a7adff1f6d7f3cc5e148`.

The 3674 reviewer disclosed authorship of inherited DepositPhysicalAdmission. I do not relabel that review as independent review of their own substrate. Its retained independent source39f4/integration7305 reports were read and their SHA256 checked (`c37efb46ff225791363c6f3275591418d7937ee039a1b06555beb31b5f3099af`, `e40e6599c894916fa30cf6a43801eae2f28c42aa892ad07fa064b6f257530779`). Complete DepositPhysicalAdmission and TopupRouterCredentials bodies match those accepted Git objects and the current parent. The 3674 regression-wording P3 was corrected by exact successor44d5. This intentionally older-parent candidate still inherits the old wording; it is not new timing evidence or an exhaustive uint8 test claim. Root integration must retain the already accepted current-main documentation correction.

## Boundaries retained

The result remains a typed covered phase. Outer role/pause, locator resolution, supplied gateway/router role binding, full outer ABI, deployment/setter reachability, actual gateway-to-router topUp CALL and its return allocator are not newly executed. Complete memory allocation/copy/aliasing, LOG/revert byte encoding and gas remain outside the new theorem. Existing physical hash/selected dispatch, SHA/root-response, withdrawal/beacon, arbitrary external World and source-shaped linked-body boundaries remain. No stage-success, frame, fit, funding, stronger cryptographic or time-monotonicity hypothesis appears.

Global All/Trust, integration against current main, publication/site consistency and whole-guarantee closure remain root responsibilities. No finding requires source mutation in this exact candidate. STOP.
