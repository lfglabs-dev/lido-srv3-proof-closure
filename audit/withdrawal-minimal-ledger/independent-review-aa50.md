# Independent review: minimal withdrawal ledger composition

**CLEAN for the stated source-execution scope.** Reviewed exact candidate `aa50b41a6f07d1321b752ba0108fcccacb1ca0c5`, base `b45cf453834a628439c9899feba760a77d3b984d`, in `/tmp/lido-topup-pipeline-ledger`. Solidity source HEAD independently confirmed as `17005714f151e5502c559932319a3f2f74ac2436`. No blocking proof, source-correspondence, hidden-premise, or integration defect found. This review is independent of the implementation; no repository files were changed and no build was rerun.

## Exact public results

- `PTopup1.actual_continuation_locator_conserves` consumes success of the actual post-module `TopupRouterContinuation.execute`. It retains precisely the physical locator relation and Lido/router and router/beacon inequalities. It derives the guarded total, target bound, pointwise Lido-to-beacon ledger, and restored router balance. Positive success derives nonempty pubkeys and equality with the mathematical allocation sum. Zero success deliberately does not infer that an unchecked sum did not wrap. This is a continuation result, not conservation across the preceding arbitrary module CALL.
- `PDeposit1.actual_physical_metadata_conserves` consumes one actual `PhysicalMetadata.execute`, joining preparation, the executed packed metadata writes/event, withdrawal on that updated world, and the concrete beacon loop. It derives the pointwise Lido-to-beacon ledger for actual key count times 32 ETH and restored router balance; nonempty success derives the configured 32-ETH value. No independently supplied successful withdrawal, beacon result, funding, frame-preservation certificate, or intermediate-world agreement is introduced.

## Source and proof audit

Read all six added Lean files, both public facades, integration changes, regression definitions, dossier and validator; traced the relevant unchanged executors and consuming lemmas through PhysicalMetadata/LiveBeacon, WithdrawalParent/Spend/CallFlow, Pipeline/Locator/Queue/Oracle/Router/StaticCall, and TopupRouterContinuation/Committed/BeaconCommitted. Checked the pinned Solidity bodies: Lido.sol buffer allocation and lines 790–885; StakingRouter.sol receiver, topUp continuation 721–758, deposit 942–996 and metadata helper 1054 onward; SRLib.sol 896–901; AccountingOracle.getCurrentFrame/_getSlotTimestamp and BaseOracle._getCurrentRefSlot with its actual stored consensus pointer.

1. The eight removed `Pipeline.Bound` fields are the configured consensus-pointer equality, three getter-role inequalities and four configured code-presence assumptions. Their removal is justified for the necessary ledger result, not by assuming these conditions elsewhere. The physical locator relation remains explicit. Unknown selectors delegate through the layered dispatchers, while the actual locator, queue, oracle and receiver selectors are distinct; address aliases do not divert these particular requests to the arbitrary mutable fallback. Successful router lookup binds the actual receiver address.
2. Queue and oracle getter calls preserve the entire incoming world in the actual dispatch paths, including errors. Nested consensus reads may use the actual physical pointer and arbitrary `StaticCall.External` reply: this interface has no writable-world output. Thus consensus-value correctness is not required for this balance conclusion. Missing actual code can reject; its absence is not reinterpreted as successful source execution.
3. The payable receiver path is the configured router implementation with its authorization and event. Successful actual CALL derives funding and its transfer; there is no supplied balance/log frame for an arbitrary callee. Buffer/counter writes preserve balances and transport the locator relation before subsequent getters.
4. Metadata writes occur before zero-key return and before withdrawal, as in the pinned source. `update_other_slot` covers both account-zero storage lenses and all permitted aliases, using only the already required Lido/router inequality. It proves preservation at every other-account key, so no new slot-disjointness or oracle/router inequality is hidden. Zero-key success retains the updated world; failure uses existing root restoration.
5. Positive TOPUP guards and word bounds, concrete beacon CALL balances, and the final router assertion derive the exact mathematical debit. DEPOSIT derives the word fit from actual preparation and the amount equality from actual withdrawal, beacon debit and final assertion. The pointwise equations also cover Lido=beacon; only the two stated role inequalities are used. Root failure restoration remains that of the unchanged executors, including rolled-back metadata and logs, with attempted-call trace retained under the model semantics.
6. AllGuarantees, Trust and lake integration add the intended modules/public checks without deleting older claims or changing executable source models. No new unsupported production axiom appears in the retained exact dependency sets.

## Validation evidence and limits

Independently recomputed all 16 receipt SHA-256 entries and all 1,316 manifest source-body hashes. Independently compared the 1,310 inherited/package bodies against base git objects or the eleven exact package pins; all matched. The remaining six bodies are the reviewed new Lean files. Exact worktree HEAD and cleanliness were checked. Read retained successful 1,641-job facade/Trust build, 29-exact-axiom trust check, and source/ten-theorem dependency receipts. These are retained implementation checks, not fresh reviewer compilation. The new theorem dependency records contain only the established foundations; no new native-decision or domain axiom is introduced into these consumers.

The four new kernel regressions cover aliased getters with mismatched configured consensus pointer, proof that removed premises are false, missing-code locator rejection, and an actual nested callback observing updated metadata before rejection and complete modeled root restoration. The alias fixture is a model-domain witness, not evidence that several distinct deployed bytecodes coexist at one address. No fresh Solidity transaction test is claimed by this increment.

Preserve the declared deployment bindings, typed Live/Verity/StaticCall semantics, prepared allocation/module prefix, whole-entry ABI/error/LOG encoding, SHA/compiler/gas and consensus boundaries. In particular the TOPUP locator binding is supplied at continuation entry; it is not established across the preceding arbitrary module CALL. The DEPOSIT result composes actual physical metadata with the suffix, but does not replace the modeled/prepared module prefix with a full deployed module execution theorem.

Nonblocking documentation correction: README names `SRLib.setLastDepositTimeAndBlock`; the pinned Solidity function is `SRLib._updateModuleLastDepositState` (896–901). The referenced writes and their order are correct; this naming typo does not affect the proof or verdict.

Reviewer stopped. Only this review artifact was written.
