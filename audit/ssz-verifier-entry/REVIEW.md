# Independent review — SSZ verifier entry

Verdict: **CLEAN for this bounded typed-source increment**, exact candidate `74a79dc6f3c8c4e1f9b6c0296a4b504532a9a832`, parent `c22ce0acbe53d9fae3adb2eef2d4695cba8f13fd`. This verdict does not close SSZ-1 or certify deployed execution/consensus authenticity. No blocking or editorial finding.

## Exact identity and validation

The commit adds exactly nine expected source/test/harness/audit files. Independently compared all **22 receipt SHA-256 values against Git objects**, resolving six core files through the exact submodule object `17005714f151e5502c559932319a3f2f74ac2436`; all match. Current corresponding files match these objects. The four accepted SSZ dependencies and four toolchain/build configuration inputs are byte-identical to the parent. `git diff --check` passes. Replayed the read-only `check_source.py`: its JSON matches the committed receipt exactly. Its six source identities and 35 ordered anchors are transcription evidence, not semantic equivalence.

Read the complete new Lean source, tests, Solidity harness and dossier, the relevant full accepted dependency chain, and pinned CL entry/root/leaf/slot implementation plus its BLS, SSZ and GIndex callees. No project mutation or new Lean/Forge build was performed. Existing exact-source logs attest the targeted 790-job build, 13 theorems, 35 kernel regression examples, and seven axiom queries. Queried dependencies are only `propext`, `Quot.sound`, and (for the four entry/consumer queries) `Classical.choice`; no native proof axiom, `sorry`, or added axiom was found. This is targeted compilation with accepted dependency reuse, not a whole-repository rebuild.

The exact Solidity log attests four passing tests with solc 0.8.25, including two fuzz properties at 1,024 runs each. The harness inherits the actual pinned verifier, independently constructs the eight-field leaf and ordered proof root, and mocks the exact EIP-4788 address/timestamp request. Root response widths and failure ordering are exercised. These synthetic roots are not authenticated consensus state; the suite does not force SHA precompile failures or short SHA returndata.

## Substantive correspondence

- `Source/SszVerifierEntry.lean:13–35,74–80`: timestamp calldata is the zero-extended big-endian 32-byte ABI word, without selector. Root decoding consumes the actual returned bytes. Call failure or empty data takes priority; 1–31 bytes fail decoding; at least 32 bytes decode the first word and allow trailing data. The universal word/suffix theorem and actual Solidity width tests cover this distinction.
- `Source/SszVerifierEntry.lean:65–105` matches pinned `CLValidatorVerifier.sol:44–108`: slot pair SHA precedes checked proof-length/index access; root lookup precedes generalized-index computation, then leaf, then proof fold. The same proof, slot, credentials and precompile flow through the stages. BLS keeps both success and exact 32-byte-return checks; `foldHash` intentionally keeps SSZ's success-only guard. No nonexistent fold size guard was introduced.
- `Source/SszVerifierEntry.lean:186–257`: the necessary/sufficient theorem relates actual stage computations to the independently defined ordered `Branch`. Its returned-byte corollary fixes the branch root to `firstWord` of the exact oracle response, rather than a supplied root-match Boolean. Actual success derives pubkey length 48 and proof length between 2 and 247, rather than imposing an artificial input-list restriction.
- `Source/SszVerifierEntry.lean:263–295`: the final consumer derives leaf and the same 50-element header proof using the accepted independent validator/header tree. It does not assume a leaf-digest equality or final root-match flag. The structural placement, numerical state path, successful pinned index calculation and exact oracle response are explicit premises. In particular `hresponse` supplies successful bytes encoding the computed header root at the exact request; this is an honest external trust premise, not root authentication.
- `Tests/SszVerifierEntryMutants.lean` contains meaningful rejection/ordering witnesses: wrong timestamp/address, returned root mismatch, missing/extra sibling, bad index/pubkey, 0/1 proof lengths, failed and short SHA replies, and root response boundaries. Toy hashes in kernel regressions are disclosed; the actual Solidity suite supplies complementary SHA execution evidence.

## Boundaries to retain in site claims

`audit/ssz-verifier-entry/README.md:111–136` states the material limits accurately. This delivers a composed typed entry interpretation, not compiler/EVM refinement. Raw calldata and memory, returndata copying, exact ABI revert bytes, staticcall behavior, gas/history-dependent precompile schedules and SHA mathematics remain outside the proof. The abstract precompile is shared and deterministic; the semantic success consumer uses a total standard adapter. In particular an abstract output word under a short reply does not establish actual SSZ scratch-memory behavior.

Canonical state path/list layout, structural placement and fork provenance are not derived from protocol reachability. GIndex fls remains the accepted `Nat.log2` interpretation without universal Yul refinement. EIP-4788 storage/timestamp provenance, deployed address/code identity and consensus anchoring are not proved. The existing registered transaction-model correspondence is not established by this new typed wrapper. Preserve these qualifications when recording progress; no guarantee closure is supported.
