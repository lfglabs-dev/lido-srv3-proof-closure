# Actual TOPUP physical entry admission

Source base main340 `53871e66833bd45190eb83dffc15439ceafa9c42`. Three new Lean modules and this dossier only; no old source/domain, dependency, All/Trust registration or publication changes. Independent exact source review and separate integration are required.

## Public consumer

`actual_physical_entry_locator_timing_credential_root_module_memory_history` executes physical TopUpGateway `onlyRole(TOP_UP_ROLE)` then `whenResumed` before the complete accepted338 locator/credentials/root/module/history runner. Its conclusion contains the entire previous public conjunction byte-for-byte, preceded by derived physical admission and equality of the actual new result to `ofPrior` of that same executed suffix. The suffix option preserves every actual prior result/observation; the new final World is its actual World. No extra successful stage or detached certificate is supplied. The new failure consumer restores the whole incoming World for admission and all inherited late failures.

The external gateway caller is an explicit typed call-context input, distinct from the old router/Lido continuation Context. The actual admission reads gateway-qualified storage at that caller's nested role key. The actual locator still derives the router used by the physical credentials getter and module path. Role and pause are not imposed as caller premises, and older public claims remain available on their original domains.

## Exact source and compiler paths

Pinned core `17005714f151e5502c559932319a3f2f74ac2436`: full TopUpGateway.topUp, AccessControlUpgradeable hasRole/_checkRole, ContextUpgradeable._msgSender, PausableUntil._checkResumed/isPaused and UnstructuredStorage. Full inherited GatewayWitnessHarness2519-line optimized IR and22 exact input bodies/metadata are reused by immutable byte identity from `audit/topup-credential-call/solidity/` (solc0.8.25, viaIR, optimizer200, Cancun). The harness overrides none of these functions; constructor grants/configuration are explicit fixture preparation.

Actual topUp selector1141aa5a entry IR337–370 first performs the outer nonpayable/head/offset/extent checks, then physical role admission; those earlier full ABI checks remain outside this typed claim. The role is keccak256(TOP_UP_ROLE)=5e4bd437d29fad01c10cdcfff414f0d6b0e84b96d2dade88d780d45b5630696b. AccessControl's namespace is02dd7bc7dec4dceedda775e58dd541e08a116c6c53815c0bd028192f7b626800. Two exact64-byte preimages are consumed: role/base, then cleaned caller/inner Keccak. `roleSlot_bytes` proves the corresponding nested hash equation. No collision or namespace-separation premise is used.

OZ5.2 admission is `(SLOAD(key) & 255) != 0`: the LOW BYTE, not the entire word and not canonical1. Raw2 is admitted; high bits alone are denied. This differs intentionally from legacy Lido's full-word flag. Failure is exact68-byte e2517d3f + address-word caller + role-word. The caller type supplies160-bit canonicality, without another caller equality hypothesis.

After role admission, full IR1864–1876 checks TIMESTAMP < SLOAD(e8b012900cb200ee5dfc3b895a32791b67d12891b09f117814f167a237783a02), the existing PausableUntil key. Failure is exact4-byte14378398 (compiled shl2270286f073). Equality resumes; even an infinite-pause word admits timestamp=max256 in this unrestricted model exactly as the comparison specifies. No bound on timestamp or canonical role word is added. Both guards precede typed array lengths/count, physical temporal guards, locator and all later calls. No new storage write, log, counter or external call is added by admission.

## Evidence and limits

Normal source/public build passes1358 jobs and the later test build1375. Eleven finite kernel regressions cover raw role bytes, full pause word, priority, equality/max boundaries, exact error payload and no suffix before admission. One public kernel rollback instance consumes the new public failure theorem; positive nonempty success is native/Solidity execution evidence, not a kernel batch instance. Fresh validation checks1358 source identities,11 package pins and18 ordinary transitive scopes, all foundations only; this is not a new global All/Trust result. Actual normal setup/trace/olean identities are recorded. The full previous public conclusion is checked byte-identical in the new consumer.

Four cast-derived nested mapping vectors separately verify both64-byte preimages and are checked against actual Lean FFI evaluation. Four native execution groups use the retained independent f008 SHA tree with three50-sibling proofs, actual root and locator replies, physical getter and module continuation: full nonempty success/history, high-bit-only role denial, physical pause denial and late module failure rollback. They add no theorem axioms. Source and diagnostic driver validation have separate scope.

Eight fresh Solidity cases include1024 arbitrary256-bit role/resume fuzz inputs. Actual inherited topUp executes the role/pause gates, exact error bytes, ordering before malformed typed lengths, caller-qualified mapping, full successful SHA/router/history suffix and timestamp equality. The fixture uses a recorder router, independent SHA tree and explicitly seeded storage; it does not prove physical module behavior or initialization.23 runtime inputs and both actual artifacts/metadata are checked. Existing2519-line IR checks are reuse, not a repeated compiler run; only the new test project is freshly compiled. No gas claim is inferred from fixture numbers.

Caller/gateway/deployment identity and proxy initialization provenance, complete malformed ABI/memory/allocator/gas/event-byte refinement, arbitrary callback frame/transaction environment continuity, omitted outer gateway-to-router topUp CALL and router admission remain inherited boundaries. This increment closes the actual gateway role/resume checks within the executed typed phase, not those other properties. Exact role and pause reads do not assert storage separation from subsequent writes under hypothetical hash collisions.

Development logs preserve failed attempts: missing Environment/address namespace imports, a simp-reduced reflexive goal requiring True rather than rfl, a diagnostic variable named reserved token slot, and an unqualified diagnostic Audit namespace. Each was diagnosed and changed; temporary sorryAx messages in these FAILED logs receive no proof credit. Successful normal scopes and outputs are separate. Raw log bytes are retained. No source/compiler/Forge failure is labeled passing.
