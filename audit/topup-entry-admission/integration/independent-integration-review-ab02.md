# Independent exact TOPUP entry admission integration review

**CLEAN** at `ab02833c654d5a5baefe2f3eec1f17773e6ad01b`, tree `8e49a9dacc0949eebf926080ba4b7c42c2644116`, sole parent union `c7a4f4a32533c75f15a4c865033ef15419f118fb`. No blocking integration, source-identity, public-consumption or evidence finding. The integrator was stopped and `/tmp/lido-topup-entry-admission-integrated` clean. This accepts the bounded integrated increment, not full TOPUP closure or site publication.

## Exact union and independent review reuse

Compared complete recursive Git trees. The union is exactly accepted main341 `7d00a45b01e1a88d193fe3917d2a3ec772586488` plus the 33 additive source-bdeb files. Main341's tree is exactly the independently reviewed ADDRESS transfer final `cb9c71edb2d10f4da7d29dffc7c2397a13e44752`. There is no merge alteration of either source or the accepted main bodies. My earlier full transfer integration/final reviews remain the provenance for that inherited contribution; the new TOPUP source does not modify it.

All three new Lean bodies, the full source dossier, and all three configurations remain byte-identical to `bdeb3d23b20dfeafb32c0e893dc260432183da08`. The archived complete source report is verbatim my external `continuous-topup-entry-bdeb-independent-review.md`, SHA256 `09d45120aff8fec233ed5fbf751e83d09c9a99ffcab775347d3219aaab0fdf5c`. I authored neither this source increment nor the preceding locator increment. Reuse of my complete source/Solidity/compiler/public-hypothesis review is therefore justified by exact identity. Its inherited locator/physical-admission authorship qualifications and independent-review chain are preserved.

The sole integration delta comprises two wiring files and eight new archive files. AllGuarantees prepends the public import; Trust prepends the test import and adds five axiom queries for complete success, failure, exact role-slot bytes, gate admission and public rollback. Read the complete changed wiring, wrapper, README, identities and receipt. Old imports, public facade/IDs/layers, hypotheses, queries and trust policy remain unchanged. No stronger assumption or alternate public theorem is substituted.

## Actual composition preserved

The source review covers the actual low-byte OZ role load at the qualified gateway storage, both 64-byte nested Keccak preimages, the explicit gateway caller distinct from router/Lido Context sender, and subsequent full-word resume-time comparison. Role denial precedes pause and the retained typed phase, with exact 68-byte unauthorized and four-byte resumed errors. Equality resumes, including the explicit maximum-word model case.

The public theorem still derives admission and prior success from the complete new outcome, then retains the **entire 338 conjunction verbatim** plus exact equality to `ofPrior` of the actual locator runner. That result preserves actual selected-router/cursor consumption, physical getter, root/module observations, intermediate World and final history. History continues to depend on produced witness limits, not allocation totals. Universal failure restores the complete original World. No role, resumed, fit, frame, nonalias, Keccak-injectivity or successful-stage premise was introduced by source or integration. The supplied allocation remains distinct from decoded module allocations.

Complete source correspondence remains core `17005714f151e5502c559932319a3f2f74ac2436`, unchanged actual Gateway/OZ/Pausable source and 2,519-line retained IR, including the previously reviewed full downstream paths. This integration review reuses that complete correspondence by identity; it does not pretend to rerun or reread all compiler paths. Outer ABI/callvalue checks precede the typed gates and remain excluded, as do deployment/role-grant provenance, gateway-to-router CALL/dispatch/admission, prior allocation, complete memory/gas and transaction-wide environment continuity. Supplied locator identity/body and inherited external/hash/root interpretations retain their boundaries.

## Build, providers and validation

The hash-bound 5,999-line normal All/Trust build log completes **1,775 jobs** without a failure marker. It actively builds all three new modules and both aggregators: source 4.6s, public 1.5s, tests 1.9s, AllGuarantees 2.5s and Trust 2.2s. These are retained integrator build results, not a rebuild by this reviewer. Existing inherited warnings are not represented as a warning-free run.

Verified all three current source/setup/trace/olean records against the integration identities. Their oleans are identical to the reviewed source artifacts; setup/trace differences are path-dependent normal build differences. Both current aggregator setups select the exact new source/public providers, and Trust selects the exact test provider. Their traces are nonsynthetic, setup options/plugins are empty, and there is no skipKernelTC option. The recorded commands target this integrated checkout and its actual setups.

After reading the source validator and integration wrapper, independently reran the comparison-only validation with an in-memory interception of only `build-identities.json`: the proposed record was compared directly with archived integration `normal` identities. Every other proposed dossier record remained in the original strict comparison mode. No candidate/cache output and no wrapper temporary output was written. Completion exited 0: **PASS 1358 sources, 11 pins, 18 ordinary scopes, 22 exact reused compiler inputs; full old public conclusion, both mapping preimages and physical guard lowering**. This recomputes the scoped dependencies against the actual selected environment without rebuilding modules.

The retained separate global check reports **29 exact disclosed axioms**: 23 test/mutant native-decision axioms, three production exceptions and foundations. This is not the same claim as the **18 new ordinary scopes**, which contain only propext, Classical.choice and Quot.sound. The integration did not silently replace the global policy with the scoped result or vice versa. No new global check was rerun by this reviewer.

All **32 source receipt hashes**, **seven integration receipt hashes**, and **three configuration hashes** match current bytes. Exact post-union changes are only the two wiring edits and eight archive additions. `git diff --check HEAD^ HEAD` passes; source/configuration identities and current providers agree.

## Evidence limits and disposition

The unchanged source evidence has eleven finite kernel regressions and one public rollback instance. Nonempty complete success is supplied by the four native execution groups, with actual SHA and four independent nested mapping vectors, not by a new successful public kernel evaluation. Eight fresh source-stage Solidity tests include 1,024 raw-role/resume fuzz runs on the unmodified Gateway with recorder router; 23 runtime inputs/two artifacts and 22 compiler inputs retain the exact earlier validation. Native/Forge/compiler evidence is retained, not rerun or promoted to a universal proof. Failed development attempts remain explicitly excluded from accepted results.

No source, cache, Git, site or dossier mutation, build, compiler, Forge or FFI execution was performed. Only this external report was written. **CLEAN at exact ab02833c654d5a5baefe2f3eec1f17773e6ad01b; review complete and STOP.**
