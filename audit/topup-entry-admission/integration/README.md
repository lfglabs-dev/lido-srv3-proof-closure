# Physical TOPUP entry admission integration

Frozen source bdeb3d23b20dfeafb32c0e893dc260432183da08 has passed the retained independent complete source/Solidity/consumer review. Physical caller role (nested Keccak, low byte), full-word resume time, exact error bytes and order are consumed before the entire prior locator/timing/credential/root/module/memory/history result. Actual success derives both admission conditions; failure restores the original World. No new frame, fit, nonalias or successful-stage premise is added.

Checked union c7a4f4a32533c75f15a4c865033ef15419f118fb combines this source with accepted main341 7d00a45b01e1a88d193fe3917d2a3ec772586488. Only integration wiring prepends the public/test imports and adds five Trust queries. All three configurations remain identical to the reviewed source.

Normal AllGuarantees/Trust passes1775 jobs, actively compiling all three new modules and both aggregators. Fresh global check reports29 existing disclosed axioms, distinct from18 fresh ordinary scopes containing only foundations. Read-only validation checks1358 actual source identities,11 pins,32 source receipt hashes,22 exact reused compiler inputs and the entire preserved public conjunction. Actual normal artifacts are recorded here; only the path-dependent proposed build identity is redirected to an external temporary file. Every other proposed dossier output is compared without writes. External temporary ordinary dependency probes are permitted.

The retained source evidence includes11 finite kernel regressions and one public rollback instance, four native groups with nonempty success and late failure plus four nested mapping vectors, and eight Solidity tests including1024 raw-role/resume fuzz cases. Positive nonempty success is runtime evidence, not a new kernel-evaluated successful public instance. The runtime23 inputs/two artifacts and source22 compiler inputs/full2519-line IR retain the source review's exact validation. No compiler, Forge or native replay is claimed by this integration. Development failures remain disclosed, never credited as PASS.

Outer ABI/callvalue precedes these typed gates and remains excluded. Deployment/role-grant provenance, supplied immutable locator, gateway-to-router CALL/dispatch/admission, prior router allocation, complete memory/gas and transaction-wide environment continuity retain their existing boundaries. This increment does not claim full TOPUP closure. Separate exact integration review remains required before merge. Site426 is unchanged.
