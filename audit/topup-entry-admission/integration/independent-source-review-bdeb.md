# Independent exact TOPUP entry admission source review

**CLEAN for the bounded additive source/proof increment** at `bdeb3d23b20dfeafb32c0e893dc260432183da08`, tree `2c225d7c040d076ee8f51338775d5c92be6c26e4`, sole parent accepted main340 `53871e66833bd45190eb83dffc15439ceafa9c42`. No blocking source, proof, public-consumption, correspondence or evidence finding. Integration, global wiring and publication are subsequent work.

The author/root was stopped and `/tmp/lido-topup-entry-admission` clean. I authored neither this increment nor the preceding locator increment. I read all three new Lean modules and proofs, all tests and diagnostic code, README, receipts, validators and runners, complete relevant pinned Solidity bodies, and the compiler admission path. This is a semantic review, not a compile-only verdict. Only this external report was written.

## Actual source, state and complete public consumption

The runner reads qualified storage at `e.gateway` in the original `e.before`, using the explicit gateway transaction caller. This caller is deliberately distinct from the supplied Context sender subsequently replaced by the locator-selected router for the physical getter and Lido continuation. Neither context is silently identified with the other.

The role namespace is `02dd7bc7dec4dceedda775e58dd541e08a116c6c53815c0bd028192f7b626800`; role is the Keccak of TOP_UP_ROLE. `roleSlot_bytes` exposes both exact 64-byte preimages: role then namespace, followed by caller then the inner digest. The mapping is actual Keccak, not an unconstrained slot oracle. Admission tests only the loaded word modulo 256. Raw low byte 2 grants access; high bits alone do not. There is no canonical-one requirement or full-word boolean substitution and no collision/injectivity premise.

Only after that role check does the runner read the complete uint256 resume word at `e8b012900cb200ee5dfc3b895a32791b67d12891b09f117814f167a237783a02`. It rejects exactly when timestamp is less than that word; equality succeeds. A maximum resume word rejects before maximum timestamp and succeeds at maximum timestamp in this word model, exactly as the source comparison does. The model does not invent a separate infinite-pause sentinel branch.

Failure bytes agree with source and IR: 68 bytes consisting of selector `e2517d3f`, canonical caller word and role word; or four bytes `14378398` for ResumedExpected. Role failure precedes pause, typed lengths, temporal guards and locator lookup. Admission failure returns the complete original World and no suffix. No event or attempted external call is introduced by these storage-only gates.

On admission success, the runner invokes the unchanged locator runner with exactly the same environment, Context, external/hash interpretations, cursors, rows, keys/operators, module ID, deposit identity and allocation input. `ofPrior` retains its actual World and the entire actual prior result in `some prior`; only the error tag is wrapped. The public theorem includes exact equality to this result and the **entire previous 338 public conjunction verbatim**. That includes actual locator request/raw reply/decoder, selected router, actual next scalar cursor, exact timing result, ordered locator/credential/root/module observations and complete TimingEffects. The new theorem derives prior success from the actual new outcome before applying the previous theorem. There is no separately supplied successful stage or reconstructed execution.

The sole new success premise is success of the complete new runner. Role and resume conditions are conclusions, not new caller obligations. No new frame, balance, fit, target equality, nonzero address, hash injectivity or cryptographic premise appears. All previous public domains and bodies remain unchanged. The universal failure theorem restores `e.before` for every covered admission or later phase error; later suffix attempts remain available through the retained result. The native late-failure checks sample this property; the public theorem supplies whole-World equality.

The inherited full result still places physical continuation effects at the intermediate World before final packed history. History depends on actual witness-produced limits, not allocation sum. Initial allocation remains a supplied prior-router input distinct from module-decoded allocations. Neither this wrapper nor its new evidence closes that boundary.

## Pinned Solidity and compiler correspondence

Read complete pinned TopUpGateway, AccessControlUpgradeable, ContextUpgradeable, PausableUntil and UnstructuredStorage bodies. Core is `17005714f151e5502c559932319a3f2f74ac2436`. TopUpGateway topUp uses onlyRole(TOP_UP_ROLE) before whenResumed. The unmodified hasRole storage layout, _checkRole overloads and _msgSender returning msg.sender match the explicit caller and nested mappings. PausableUntil reads uint256 directly through SLOAD.

Inspected the complete new admission/transition path in the retained 2,519-line GatewayWitnessHarness IR, including the surrounding outer ABI checks, both hashes, mask, error encodings, resume helper, typed lengths, temporal guards, locator and credential transition. IR337–370 uses CALLER, two 64-byte Keccaks, `and(sload(...), 0xff)`, then the 68-byte error or `_checkResumed`. IR1864–1876 compares TIMESTAMP with the full resume SLOAD and emits the four-byte error. There is no overriding sender/role behavior in the actual inherited harness. Outer callvalue and calldata-head checks occur before the gates and are explicitly excluded from the typed runner; this is not a claim that role rejection precedes malformed outer ABI rejection.

The complete previously reviewed remaining root/module/history IR and source paths are reused by exact identity, not rereported as newly read in full here. The comparison validator confirmed all 22 retained compiler inputs, output and full IR against accepted base/core/vendor bodies and original metadata. Full IR SHA256 is `4170a026329677bca075096ede2b500f76d0ec7a481d793086d3793fa88358b0`. Profile remains solc0.8.25+b61c2a91, viaIR, optimizer200, Cancun; production deployed-bytecode identity is not asserted.

The retained independent locator source review was read in full and remains SHA256 `1bb422a614df27c343f87446afae5e25ddbb232e895729558129058c48638c03`; locator integration review is `43a4750d1583ee6b9bdce9d86a834c544eb68c6e9a61b6ef6cffd1a914cca3b4`. Earlier accepted 6c33/961f/7134 locator and my 2efb/6fd1 timing reviews provide the unchanged complete downstream review. The locator reviewer disclosed prior DepositPhysicalAdmission authorship and relied on independent source39f4/integration7305 reports for that component; this review preserves that provenance rather than treating author reinspection as independent evidence.

## Checks and evidence qualifications

After reading the validators, I ran only their read-only modes with PYTHONDONTWRITEBYTECODE=1. Default `validation/validate.py` completed exit 0: **PASS 1358 sources, 11 pins, 18 ordinary scopes, 22 exact reused compiler inputs; full old public conclusion, both mapping preimages and physical guard lowering**. It compares actual selected imports and current normal source/setup/trace/olean records, accepted old local bodies and exact package pins, and recomputes ordinary named theorem dependencies. Current scopes contain only propext, Classical.choice and Quot.sound. New source has no admitted proof, native theorem shortcut or stronger axiom. The `--write` mode and the native/Solidity writer runners were not executed.

The retained successful public build actively builds source and public modules and completes 1,358 jobs; the test build actively builds the new regression module and completes 1,375 jobs. Current normal setup/trace/olean identities match the records. These are the author's retained build results, not new builds by this reviewer. Eleven kernel regressions cover low-byte semantics, full-width pause, equality/maximum timestamps, error bytes and ordering; one public rollback instance consumes the actual rejected runner. There is **no nonempty successful new public kernel instance**. Uniform test storage intentionally avoids kernel Keccak evaluation.

The separately retained native diagnostic executes actual nested Keccak and actual SHA through the existing FFI. Four groups cover a nonempty three-row locator/credential/root/module/history success, role denial, pause denial and late rollback, plus four independent nested mapping vectors. I read its full code and command log. The three independent SHA branches have 50 siblings each; their roots/proofs/generator remain exact accepted-base artifacts. These executable diagnostics are not theorem axioms or a full EVM/consensus proof.

`check-runtime-evidence.py` completed exit 0, validating 23 exact runtime inputs, both actual compiled artifacts and metadata, eight selected tests including 1,024 arbitrary uint256 role/resume fuzz runs, and the retained SHA evidence. Read the complete new Solidity fixture and the complete inherited Gateway harness/recorder/independent sparse-tree fixture. Gateway/verifier operations are unmodified; role grant/config setters initialize a standalone harness. Router remains an explicit recorder and anchoring is mocked. The successful low-byte and equality cases enter the real gateway suffix; fuzz tests distinguish role, pause and typed-length rejection. Reverted recorder counters alone are not used to prove ordering: pinned source/IR supplies that ordering. The new selected test suite does not recredit all inherited tests.

Five development logs preserve missing namespace/identifier, reflexive-equality-vs-True and native diagnostic failures. Their errors and temporary sorryAx reports are **failed attempts**, not accepted results. The later normal builds and fresh ordinary closure check contain no such admission. Existing inherited linter warnings are not misrepresented as a warning-free build.

Independently checked exact clean HEAD/tree/sole parent, 33 additions only, all 32 receipt hashes and three configuration hashes. Every previously tracked object is unchanged. `git diff --check HEAD^ HEAD` passes. No compiler, Forge, native FFI, module build or global aggregation was rerun, and no candidate/cache file was written.

## Scope and disposition

New coverage is actual physical gateway role/resume admission before the entire previously accepted typed phase, with exact caller/error semantics and universal original-World rollback. Deployment/proxy initialization, role grant provenance, supplied immutable locator identity/body, outer ABI/callvalue handling, gateway-to-router CALL/dispatch/admission, prior router allocation, complete memory/alias/gas and transaction-wide environment continuity remain outside. Existing external/hash/root interpretations and arbitrary module/callback effects retain their established boundaries. No site or whole-guarantee closure follows from this source-only increment.

**CLEAN at exact bdeb3d23b20dfeafb32c0e893dc260432183da08. Review complete; STOP.**
