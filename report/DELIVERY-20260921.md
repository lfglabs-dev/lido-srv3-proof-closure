# Lido delivery checkpoint — 2026-09-21

Status: **not ready for final acceptance**. This checkpoint preserves the current
proof work and corrects its published scope. It is not a successful combined
validation receipt or an independent audit.

## Preserved inputs

- PR #919: `2b5ab7f58ae216e6a3a0168d367a7012832dcbd5`.
- PR #918 OfficialSemantics repair: `3532635db268ed555dac29bdb6c991b2d9a1d659`,
  merged into this local finalization branch.
- Solidity: `17005714f151e5502c559932319a3f2f74ac2436`.
- Verity: `600a2f7a9b07f7ea532451e7d58b106347a6817a`; Lean 4.31.0.

The two read-only review missions were archived before cancellation was requested:
`5cf6718c-c6aa-49fb-91d1-aa289e309036` and
`732499f2-2f13-4f48-8246-e05ac7715387`. Their reports are partial and do not
constitute acceptance of this successor. Both identified disclosure defects;
neither supplied a clean independent review of the combined candidate.

## Corrections retained

- Register the existing authentic-beacon-root premise as `A-EIP4788-AUTHENTIC`
  on SSZ and both consolidation rows, including the reader-facing catalog and
  README. This exposes a retained hypothesis; it does not discharge it.
- Narrow the address-renaming claim: balances and code are fixed, callee
  equivariance is assumed, and no concrete `LiveRenaming` instance or general
  existence theorem establishes non-vacuity. Other full entrypoint bodies remain
  deferred.
- Disclose `d.consensusHash = o.reportHash` in oracle-entry refinement. Distinguish
  the `processingStarted` result flag from an emitted EVM log.
- Do not claim exact Solidity ABI revert bytes from the lazy vault model.
- Restore all seven OfficialSemantics names to the explicit trust inventory.
  They describe a synthetic compiler/denotation experiment, not Lido source
  correspondence.
- Repair the malformed proof indentation in `SszCompiledBeforeRootClass`.

## Remaining acceptance work

The initial combined build failed in three modules. The SSZ syntax repair is
retained. Two unchanged compiler-success witnesses remain blocked:

- `ConsolidationFee.function_spec_compiles`;
- `DepositRollback.checked_prefix_compiles`.

At this dependency pin, `decide +kernel` cannot reduce their compilation
propositions. Exploratory traversal-equation/`cbv` replacements reached Lean
resource limits and were not retained. No theorem was weakened and no native
exception was introduced to obtain a green result.

After repairing these witnesses, run the combined production/test build,
`make prove`, `make test`, and the full transitive trust checker on the same
candidate. Refresh the exact-tree receipt and obtain independent review of the
resulting commit. Predecessor receipts cannot satisfy this requirement.

The targeted build of `OfficialSemantics` and `SszCompiledBeforeRootClass` passed
(1,210 jobs including cached dependencies). This is not a combined repository build.

Local checks already passed for the disclosure changes: proof-escape inventory,
import DAG, report theorem inventory, reproduction targets, public-claim surfaces,
assumption presentation, main-guarantee mutation tests, pinned source checks
(24 existing warnings), and strict source annotations (1,174 citations, no false
citations or quote mismatches). Metadata generation/checking binds these edits;
it does not prove compilation success.

## Deferred by the owner

Do not start new missions for these during delivery finalization:

1. Invariants of every deployed module replacing `A-SUPPORTED-MODULES`.
2. Non-reentrance proofs for every external contract body.
3. Live address equivariance for `transferFrom`, `requestWithdrawals`, `unwrap`.
4. General correspondence to deployed bytecode, SHA-256 implementation and
   authentic beacon history.

These remain visible hypotheses or open proof boundaries. The website must
publish the actual pinned candidate and its pending validation, without
representing the deferred work or historical receipts as current proof closure.
