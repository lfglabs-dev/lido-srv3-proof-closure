# Lido SRv3 validation and proof scope

The report covers eleven guarantees at Solidity commit
`17005714f151e5502c559932319a3f2f74ac2436`, using Verity
`600a2f7a9b07f7ea532451e7d58b106347a6817a` and Lean 4.31.0.
The checked proof input tree is
`2ab1d336ce40df6fabf25b934aaff548fcab83ae`.

## Validation results

All three commands completed successfully for the checked inputs:

| Command | Result | Execution log |
| --- | --- | --- |
| `make prove` | PASS — 1,932 jobs | [proof build](../audit/receipts/delivery-20260921/prove.log.gz) |
| `make test` | PASS — 80 differential/signing-key tests, regression builds and all validation gates | [test suite](../audit/receipts/delivery-20260921/test.log.gz) |
| `lake build LidoSRv3 LidoSRv3Audit LidoSRv3Test AccountAddressChecks` | PASS — 2,171 jobs | [library build](../audit/receipts/delivery-20260921/libraries.log.gz) |

The [validation receipt](../audit/receipts/delivery-20260921/validation.json)
records the tested commit, proof input tree, environment and SHA-256 of each
compressed log. The logs are compressed only; no output is omitted. The
packaged commit may add only receipts and this report after validation; the
website checks that no other tracked inputs changed. Compare the proof input
tree as well as the commit IDs; a packaging commit does not claim a new build.

## Reproduction

Run `make prove`, `make test`, and
`lake build LidoSRv3 LidoSRv3Audit LidoSRv3Test AccountAddressChecks`
from this checkout with the pinned dependencies, Python 3.10+ and Bash 4+.
On macOS, put supported Python and Bash installations ahead of the system
versions on PATH. The local test run used `LEAN_NUM_THREADS=2` and
`RAYON_NUM_THREADS=2` to limit concurrent Lean processes. `make test` includes the
transitive trust check, source differential tests, metadata checks, immutable
fixture checks and regression builds. Per-guarantee build commands appear on
the website and in the registry.

## Compiler checks

`AllocCapacity.oneModule_compiles_to_official_ir`,
`ConsolidationFee.function_spec_compiles`, and
`DepositRollback.checked_prefix_compiles` establish compiler acceptance of
their unchanged specifications. Their proofs compose input validation,
function-body compilation and constructor compilation. Input validation uses
fully transparent reflexivity; all resulting proof terms are checked by the
Lean kernel. These declarations depend only on `propext`, `Classical.choice`
and `Quot.sound`. No new native proof exception is used.

The signing-key memory regressions use proved array/list equivalences to
check the original overlap, framing and wraparound assertions without costly
byte-array reduction. The trust audit discovers production theorems from all
compiled modules, loading standalone command-line drivers separately and
checking that repeated declarations retain the same types and dependencies.

Compiler acceptance does not establish correspondence with deployed bytecode.
The exact Lean statements and each card's assumptions define the delivered
scope.

## Trust scope

The dependency audit checks 1,259 explicit disclosures and 16,645 compiled
claims, including unprinted supporting results. Every checked claim uses only
`propext`, `Classical.choice` and `Quot.sound`; no native proof exception is used.
Two auxiliary mapping-base separation lemmas take explicit non-collision
premises. Slot-derivation identities do not prove Keccak injectivity.

## Proof boundaries

- Supported-module counter and type invariants are assumed through
  `A-SUPPORTED-MODULES`; they are not proved for every deployed module.
- External-body storage preservation relies on `A-NO-REENTRY`.
- Live address renaming is conditional for `claimWithdrawalsTo`; a concrete
  state-renaming instance and the full bodies of `transferFrom`,
  `requestWithdrawals` and `unwrap` remain outside the proved scope.
- General deployed-bytecode correspondence, SHA-256 implementation correctness
  and authentic beacon history remain explicit assumptions or open boundaries.

Independent agents reviewed predecessor proof revision 938b8bba and reported
the allocation-bound and disclosure issues addressed here. They could not
access the private website repository. Independent review of this corrected
proof/site pair is separate from executable validation and is not complete. The report does not claim independent
audit acceptance.

## Delivery revisions

PR #921 (proofs) and PR #445 (website) are merged predecessors. The correction
branches are `fix/delivery-audit-20260922` in this repository and
`fix/lido-delivery-audit-20260922` in `lfglabs-dev/lfg_marketing`. The website’s
identity snapshot names the exact proof revision it displays. Review that
pair together; historical dossiers apply only to their own revisions.

The allocation arithmetic now bounds maxEBType2 by 2^128-1 wei, admitting
the pinned mainnet value of 2048 ether. A regression rejects the former
uint64 bound and checks a nonempty type-2 example under the revised premise.
The remaining count, total and module-history premises are still assumed.

Deployment identity checks use recorded fixtures. Live RPC re-verification
is not part of this delivery run. The known ConsolidationGateway source
mismatch in PausableUntil.sol (IPausableUntil import/inheritance) remains
unresolved; see PROVENANCE.md. Fixture identity checks do not prove this
source difference harmless or establish general bytecode correspondence.
