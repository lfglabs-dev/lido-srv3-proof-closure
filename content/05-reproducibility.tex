\section{Reproducibility and handoff}
\label{sec:reproducibility}

\subsection*{Reviewer command}

A reviewer does not need to trust the PDF alone. The handoff includes the proof
sources and commands that rebuild the selected executable target set and check
that the PDF's assumptions match the machine-readable trust-boundary report.

\smallskip
\noindent The rebuild is the reviewer's receipt. If it succeeds, the reviewer
learns that the target IDs in the PDF exist, the executable checks pass, and the
listed assumptions match the machine-readable trust report. If it fails, the PDF
claim is not ready to rely on.

\begin{lstlisting}[language=shell]
git clone git@github.com:lfglabs-dev/lido-srv3-proof-closure.git
cd lido-srv3-proof-closure
make bootstrap
make test
make prove
make report
\end{lstlisting}

\noindent These commands check that the source fixtures are present, verify every matrix row
has a matching Verity/Lean artifact in \path{proofs/}, emit
\path{proofs/logs/proof-report.json}, and rebuild the PDF.

\subsection*{Minimum provenance fields}

\noindent
\begin{tabularx}{\linewidth}{@{}p{0.32\linewidth}X@{}}
  \toprule
  \thead{Field} & \thead{Repository content} \\
  \midrule
  PR and source revision & PR \#1811 URL, base branch, audited commit hash,
  and review timestamp. Report package target:
  \path{af095e48bbc1c3841c2c9936219c8461af01056b}. \\
  \rowsep
  Proof toolchain & Lean v4.31.0/Lake project importing pinned Verity reference
  \path{04729a9de9099e065dd09283e4f733a5fd4c2a16}; proof command
  \texttt{lake build LidoSRv3}. \\
  \rowsep
  Target namespace & Selected proof target files in \path{proofs/}, target names
  in \path{archive/legacy-p1-p15/verity-targets/srv3-proof-targets.json}, and the command that fails
  on a failed model check. \\
  \rowsep
  Trust allowlist & Machine-readable assumption IDs, out-of-model boundaries,
  and target-to-assumption mapping in \path{verity/targets/trust-boundary.json}. \\
  \rowsep
  Artifact integrity & Generated PDF in
  \path{dist/lido-srv3-formal-methods-report.pdf} and proof output in
  \path{proofs/logs/proof-report.json}. The proof report's
  \texttt{build.verified\_source\_tree} is the deterministic Git tree ID
  \path{0b495e5a67e90cd2ad0dae3e2db9feebd911043a}: a virtual tree of
  \path{LidoSRv3/}, \path{LidoSRv3.lean}, \path{lakefile.lean}, and
  \path{lake-manifest.json}, and \path{lean-toolchain}. These inputs must be
  clean, and no untracked (including ignored) path may exist below
  \path{LidoSRv3/}; hence Lake's source view is exactly the hashed tree. The
  successful build log records this tree ID and report generation recomputes and
  requires an exact match. The logged version of the Lean executable is derived
  during the build and validated against \path{lean-toolchain}. The tree
  deliberately excludes generated logs and PDF artifacts, avoiding a
  self-referential final commit identifier. \\
  \bottomrule
\end{tabularx}

\subsection*{Source anchors}

\noindent The matrix uses broad PR \#1811 surfaces to stay readable. The exact
source map is \path{verity/targets/source-map.json}, and the Certora prior-work
to PR \#1811 property and assumption map is
\path{archive/legacy-p1-p15/verity-targets/certora-pr1811-map.md}. Anchors for the target
source revision include:

\begin{itemize}[leftmargin=1.4em,itemsep=2pt,topsep=2pt]
  \item Buffer and reserve accounting:
  \path{Lido.sol::_getBufferedEtherAllocation},
  \path{getDepositableEther}, and \path{withdrawDepositableEther}.
  \item Deposit allocation:
  \path{StakingRouter.sol::deposit} and
  \path{SRLib.sol::_getDepositAllocations}.
  \item Reports and rewards:
  \path{StakingRouter.sol::reportValidatorBalancesByStakingModule},
  \path{getStakingRewardsDistribution}, and
  \path{SRLib.sol::_reportRewardsMinted}.
  \item Oracle validation:
  \path{AccountingOracle.sol::_checkStakingRouterModuleBalances}.
\end{itemize}

\subsection*{Handoff contents}

\begin{itemize}[leftmargin=1.4em,itemsep=2pt,topsep=2pt]
  \item \textbf{PDF:} \path{dist/lido-srv3-formal-methods-report.pdf}. This
  report package for Lido reviewers.
  \item \textbf{Lockfile:} \path{proofs/LOCKFILE.md}. Pinned Lido and Verity
  references.
  \item \textbf{Trust report:} \path{verity/targets/trust-boundary.json}.
  Machine-readable assumption and boundary register.
  \item \textbf{Certora map:} \path{archive/legacy-p1-p15/verity-targets/certora-pr1811-map.md}.
  Certora prior-work to PR \#1811 property and assumption map with disciplined
  coverage status per surface.
  \item \textbf{Proof targets:} \path{proofs/}. One target file for each selected
  P0 property.
  \item \textbf{Model:} \path{LidoSRv3/Legacy/Model.lean}. SRv3 economic state machine.
  \item \textbf{Theorems:} \path{LidoSRv3/Legacy/SpecProofs.lean}. Checked P0 proof
  targets.
  \item \textbf{Reference fixtures:} \path{fixtures/solidity-reference/}. Copied
  Lido references used as source-facing fixtures; never executed.
\end{itemize}

\subsection*{Integrity checks}

\noindent
\begin{tabularx}{\linewidth}{@{}p{0.30\linewidth}>{\raggedright\arraybackslash}X@{}}
  \toprule
  \thead{Check} & \thead{Expected result} \\
  \midrule
  \texttt{make test} &
  Source-facing Solidity fixtures are present and no legacy proof artifacts remain. \\
  \rowsep
  \texttt{make prove} &
  Verity/Lean theorem checking passes SRV3-P1 through SRV3-P6 and writes
  \path{proofs/logs/proof-report.json}. \\
  \rowsep
  \texttt{make report} &
  Rebuilds \path{dist/lido-srv3-formal-methods-report.pdf}. \\
  \rowsep
  \texttt{proofs/LOCKFILE.md} &
  Contains Lido PR \#1811 commit
  \path{af095e48bbc1c3841c2c9936219c8461af01056b} and Verity commit
  \path{04729a9de9099e065dd09283e4f733a5fd4c2a16}. \\
  \bottomrule
\end{tabularx}
