\section{What the proof closes}
\label{sec:proof-closure}

\newcommand{\theoremid}[2]{\texttt{\footnotesize #1}\newline\texttt{\footnotesize #2}}
\newcommand{\proofhandle}[3]{\texttt{\footnotesize #1}\newline\theoremid{#2}{#3}}

\noindent A local proof target is closed in this package when the executable
model check passes, the model and source anchors are listed, and the remaining
premises are named as assumptions. This section shows the six accounting claims
in three forms: a system map, a plain-language story, and artifact rows for the
evidence package.

\subsection*{Modeled architecture}

\noindent The proof model follows the accounting path where PR \#1811 concentrates
fund-safety risk. In plain terms, the model follows ETH as it moves from the
Lido buffer, through the router, into staking-module deposits, and then into
later balance reports and reward calculations. Each proof asks whether that flow
preserves the accounting rule it is supposed to preserve. External contracts are
summarized only at the interfaces used by those transitions.

\begin{center}
\begin{tikzpicture}[
  node distance=8mm and 7mm,
  box/.style={draw=vrule, fill=vsurface, line width=0.35pt, minimum height=11mm,
    text width=28mm, align=center, font=\sffamily\footnotesize},
  proof/.style={draw=sevok, fill=sevoktint, line width=0.35pt, minimum height=9mm,
    text width=23mm, align=center, font=\sffamily\scriptsize},
  arrow/.style={-{Latex[length=2.2mm]}, draw=vaccentdeep, line width=0.45pt}
]
  \node[box] (buffer) {Lido buffer\\reserve split};
  \node[box, right=of buffer] (reserve) {Depositable ETH\\withdrawal exclusion};
  \node[box, right=of reserve] (alloc) {Router pull\\module allocation};
  \node[box, below=of alloc] (balances) {Accepted module\\balances};
  \node[box, left=of balances] (rewards) {Rewards, fees,\\recipient alignment};
  \node[box, left=of rewards] (router) {Status gates\\module state};

  \draw[arrow] (buffer) -- (reserve);
  \draw[arrow] (reserve) -- (alloc);
  \draw[arrow] (alloc) -- (balances);
  \draw[arrow] (balances) -- (rewards);
  \draw[arrow] (rewards) -- (router);
  \draw[arrow] (router) -- (reserve);

  \node[proof, above=7mm of reserve] (p1) {P1 reserve};
  \node[proof, above=7mm of alloc] (p2) {P2 deposits};
  \node[proof, below=7mm of balances] (p3) {P3--P4 reports};
  \node[proof, below=7mm of rewards] (p4) {P5--P6 rewards};

  \draw[arrow] (p1) -- (reserve);
  \draw[arrow] (p2) -- (alloc);
  \draw[arrow] (p3) -- (balances);
  \draw[arrow] (p4) -- (rewards);
\end{tikzpicture}
\end{center}

\subsection*{From intuition to target}

\noindent Read the six properties as one accounting story. P1 protects ETH
reserved for withdrawals. P2 checks that initial deposits pull exactly the ETH
they should. P3 checks that the router's total balance equals the module balances
it accepted. P4 makes module fee weights depend on the accepted report, not an
old or partial one. P5 bounds reward and fee movement. P6 blocks non-active or
paused modules from deposit allocation and stopped modules from module-side
reward or fee allocation.

\smallskip
\noindent Each invariant below has two layers. The first sentence says why the
property matters in ordinary accounting language. The formula then states the
same idea in a form that can be checked over all modeled inputs. You can read
the formulas like receipts: they name the money bucket before and after a move,
then check that the totals still match. The symbols below only stand for modules,
balances, reserves, and reports already described in the ledger story.

\smallskip
\noindent Let \(M\) be the finite set of staking modules in router order. For a
module \(m \in M\), let \(\bal_m\) be its accepted validator balance in gwei,
\(\modstatus_m\) its module status, and \(\mathsf{moduleRecipient}_m\) its module
reward recipient in the modeled reward step. Let \(B_R\) be the router validator balance, \(T\) the buffered ETH,
\(R_D\) the stored deposit reserve, and \(R_W\) the withdrawal-reserved buffer.
The executable checks cover the named fixtures and the bounded enumeration in
\path{verity/src/verity_srv3/prove.py}. Each accepted report must still cover the
current router module set in router order, with unique module IDs, matching array
lengths, and the freshness and range premises named in the target file.

\medskip
\noindent\textbf{P1: reserve separation.}
\[
  d = \min(T,R_D)
  \qquad
  w = \min(T-d,R_W)
  \qquad
  u = T-d-w
\]
\[
  \mathsf{depositable}(T) = d + u
\]
\[
  \forall a.\quad
  \mathsf{spendDeposit}(a) \Rightarrow
  a \leq \mathsf{depositable}(T)
\]
\[
  \mathsf{spendDeposit}(a) \Rightarrow
  \mathsf{usesWithdrawalReserve}(a,T,R_D,R_W)=0
\]

\noindent Plain reading: some ETH is saved for withdrawals, and deposits are not
allowed to spend that saved amount. Deposit spend is bounded by the pre-spend
deposit reserve plus unreserved allocation and does not consume the pre-spend
effective withdrawal-reserved bucket. The executable target deliberately avoids a
raw post-state equality for \(R_W\); any post-state reserve fact belongs to the
model's explicit reserve-recomputation rule.

\medskip
\noindent\textbf{P2: exact router pull.}
\[
  \mathsf{pulledWei}
    = 32\,\uniteth \cdot \mathsf{actualDeposits}
  \qquad
  \mathsf{routerEthAfter} = \mathsf{routerEthBefore}
\]
\[
  \mathsf{actualDeposits}
    = \sum_{m \in M} \mathsf{allocatedDeposits}_m
\]

\noindent Plain reading: across the modeled atomic initial-deposit transition,
the router pulls exactly one 32 ETH unit per validator and immediately forwards
it to the beacon deposit sink. Top-ups, if included in a later lane, are a
separate gwei-aligned allocation target and are not instances of this 32 ETH
exact-pull statement.

\medskip
\noindent\textbf{P3: module-balance conservation.}
\[
  \mathsf{acceptedReport}(M,\vec{b})
  \Rightarrow
  B_R' = \sum_{m \in M} \bal_m'
  = \sum_{i=0}^{|M|-1} \vec{b}_i
\]

\noindent Plain reading: the router total must equal the sum of the module rows
after an accepted report. The proof is over arbitrary finite module arrays, with module-ID
uniqueness and report alignment named as assumptions rather than hidden bounds.

\medskip
\noindent\textbf{P4: report-before-reward consistency.}
\[
  \mathsf{rewardStep}(s,s')
  \Rightarrow
  \exists r.\,
  \mathsf{acceptedAt}(r,s)
  \land
  \mathsf{rewardBase}(s)=\mathsf{balances}(r)
\]

\noindent Plain reading: accepted module balances determine the module
fee-distribution weights used by the modeled reward step. Total reward magnitude
and protocol fee inputs are treated as accepted report or vault-context inputs
subject to the listed range and freshness assumptions. In simpler terms, rewards
should be split using the report the system just accepted, not a previous
snapshot and not a half-updated table.

\medskip
\noindent\textbf{P5: bounded rewards and fee alignment.}
\[
  0 \leq \mathsf{moduleFee}_m
  \leq \mathsf{totalReward}\cdot \mathsf{moduleFeeBps}_m / 10{,}000
\]
\[
  \mathsf{moduleRecipient}_m'=\mathsf{moduleRecipient}_m
  \land
  \mathsf{rewardRecipient}(\mathsf{moduleFee}_m)
    = \mathsf{moduleRecipient}_m
\]

\noindent Plain reading: fee amounts cannot exceed the configured basis-point
bound, and each amount remains paired with the recipient for the module that
earned it.

\medskip
\noindent\textbf{P6: status gating.}
\[
  \modstatus_m \neq \stActive \lor \mathsf{depositsPaused}_m
  \Rightarrow
  \mathsf{allocatedDeposits}_m = 0
\]
\[
  \modstatus_m = \stStopped
  \Rightarrow
  \mathsf{moduleReward}_m = 0
\]

\noindent Plain reading: a stopped or paused module is not just marked differently
in storage; that status changes the allowed flow. Deposit-paused or non-active
modules receive zero modeled deposit allocation. Stopped modules receive zero
module-side fee or reward allocation. Any residual protocol-fee treatment is
stated separately from module recipient payment.

\subsection*{Report package traceability register}

\noindent The next table is for auditability. The artifact path is the local file
a reviewer can inspect or run. The assumption IDs say which facts the check is
allowed to borrow from outside the model. The report does not ask the reader to
trust prose alone; every claim has this chain.

\noindent
\begin{xltabular}{\linewidth}{@{}p{0.12\linewidth}p{0.30\linewidth}p{0.20\linewidth}>{\raggedright\arraybackslash}X@{}}
  \toprule
  \thead{ID} & \thead{Executable artifact} & \thead{Assumptions} & \thead{Claim} \\
  \midrule
  \endhead
  SRV3-P1 &
  \path{proofs/reserve-separation.md} &
  A-EXT-01;\newline A-ARITH-05 &
  Deposit spend cannot consume withdrawal-reserved ETH. \\
  \rowsep
  SRV3-P2 &
  \path{proofs/deposit-conservation.md} &
  A-DEP-02;\newline A-ID-04;\newline A-ARITH-05 &
  Pulled wei equals 32 ETH times actual deposits over all allocated modules. \\
  \rowsep
  SRV3-P3 &
  \path{proofs/module-balance-conservation.md} &
  A-ORC-03;\newline A-ID-04;\newline A-ARITH-05 &
  Accepted report application preserves router balance as the module sum. \\
  \rowsep
  SRV3-P4 &
  \path{proofs/report-before-reward-consistency.md} &
  A-ORC-03;\newline A-ID-04 &
  Module fee-distribution weights read the latest accepted module-balance state. \\
  \rowsep
  SRV3-P5 &
  \path{proofs/reward-correctness.md} &
  A-ORC-03;\newline A-ARITH-05 &
  Fee amounts are bounded and recipient/module arrays stay aligned. \\
  \rowsep
  SRV3-P6 &
  \path{proofs/status-gating.md} &
  A-EXT-01;\newline A-ID-04 &
  Non-active or deposit-paused modules receive no modeled deposit allocation;
  stopped modules receive no module-side reward or fee allocation. \\
  \bottomrule
\end{xltabular}

\subsection*{Evidence architecture}

\noindent
\begin{xltabular}{\linewidth}{@{}p{0.21\linewidth}p{0.23\linewidth}X@{}}
  \toprule
  \thead{Layer} & \thead{Artifact} & \thead{Role} \\
  \midrule
  \endhead
  Solidity-facing summaries & \path{verity/src/verity_srv3/model.py} &
  Preserve the relevant array, loop, reserve, and call structure while
  summarizing external contracts through explicit interfaces. \\
  \rowsep
  Economic model & \path{verity/src/verity_srv3/model.py} &
  Defines modules, reserves, Wei/Gwei arithmetic, status flags, accepted reports,
  rewards, and fee state. \\
  \rowsep
  Proof obligations & \path{verity/src/verity_srv3/properties.py} &
  States each target over the focused transition model, with uniqueness,
  ordering, alignment, and freshness premises named in target files. \\
  \rowsep
  Trust-boundary report & \path{verity/targets/trust-boundary.json} &
  Machine-readable list of assumptions, target IDs, source links,
  retained Certora assumptions, and out-of-model claims. \\
  \rowsep
  Rebuild gate & \texttt{make test; make prove} &
  Rechecks all selected model tests and proof targets and emits
  \path{proofs/logs/proof-report.json}. \\
  \bottomrule
\end{xltabular}
